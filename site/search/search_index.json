{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"OpenNeuro \u00b7 openneuro.org Live \u2014 datasets \u2014 participants Python \u00b7 OpenNeuro \u00b7 BIDS Qortex <ul> <li><code>can_train(\"trial_type\")</code> \u2014 manifest verdict before any download</li> <li><code>minimum(\"first-batch\")</code> \u2014 exact files and byte count for a concrete ML goal</li> <li><code>doctor(recipe)</code> \u2014 15-point BIDS readiness check: events, companions, splits</li> <li>Selective download by subject \u00b7 task \u00b7 modality \u00b7 suffix \u2014 resumable, size-limited</li> <li>Parquet \u00b7 Zarr \u00b7 HDF5 \u00b7 WebDataset \u00b7 HuggingFace \u00b7 TFRecord \u2014 subject-level splits, leakage verified</li> <li>MRI \u00b7 fMRI \u00b7 DWI \u00b7 PET \u00b7 MEG \u00b7 EEG \u00b7 iEEG \u00b7 fNIRS \u2014 uniform inspect / convert API</li> </ul> Quickstart Concepts API reference session.py <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n# 88 subjects \u00b7 MRI + EEG \u00b7 156 files \u00b7 4.8 GB\n# no download yet\n\nds.can_train(\"trial_type\")\n# True (2 classes \u00b7 240 windows \u00b7 86 subjects)\n\nds.minimum(\"first-batch\")\n# 2 subjects \u00b7 4 files \u00b7 0.81 GB\n\nds.download(subjects=[\"01\", \"02\"], datatypes=[\"func\"])\n# \u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588\u2588 100% 831 MB\n\nart = ds.convert(\nformat=\"parquet\",\nwindow=dict(mode=\"event_aligned\", tmin=-0.2, tmax=0.8),\nlabel_col=\"trial_type\",\n)\n# train 180 \u00b7 val 30 \u00b7 test 30 samples\n\nX, y = art.sklearn(split=\"train\")\n# X: (180, 64, 256) y: (180,)</code></pre>"},{"location":"#key-interfaces","title":"Key interfaces","text":"<p>Three phases. Each is independent \u2014 inspect without downloading, download without converting, convert in any format.</p> <p>Before download \u2014 remote manifest only</p> Call Returns <code>Dataset(\"ds_id\")</code> Manifest: subjects, files, entities \u2014 no data transferred <code>ds.can_train(target_col)</code> <code>True/False</code> + blocking reasons <code>ds.minimum(goal)</code> File list + byte count for <code>\"first-batch\"</code>, <code>\"label-check\"</code>, <code>\"validation\"</code> <code>ds.label_landscape()</code> Per-class trial counts and subject coverage across all events.tsv <code>ds.doctor()</code> Full readiness report \u2014 events, companions, sizes, split feasibility <code>DatasetQuery().modality(\"eeg\").has_events().min_subjects(30).fetch()</code> Filtered catalog results <code>client.search_datasets_rich(modality=\"MRI\", sort_by=\"downloads\")</code> Live API results with engagement <p>After selective download</p> Call Returns <code>ds.download(subjects, datatypes, suffixes, max_size_gb)</code> Resumable selective transfer <code>ds.doctor(recipe=\"eeg-classification\")</code> Modality-specific checks post-download <code>ds.visual_audit(output_dir)</code> Center-slice thumbnails \u2014 no full volume loaded <code>ds.get_validation_issues(tag)</code> BIDS validator errors and warnings <p>Convert to artifact</p> Call Returns <code>ds.convert(format, window, label_col, split)</code> Artifact with subject-level train/val/test split <code>art.sklearn(split)</code> <code>(X, y)</code> numpy arrays <code>art.torch(split)</code> PyTorch <code>Dataset</code> <code>art.huggingface(split)</code> HuggingFace <code>Dataset</code> <code>art.braindecode(split)</code> BrainDecode <code>BaseConcatDataset</code>"},{"location":"#implementation-status","title":"Implementation status","text":"Area Status Manifest inspection Remote file tree, typed Manifest, BIDS entities extracted Selective download Subject \u00b7 session \u00b7 task \u00b7 modality \u00b7 suffix \u00b7 size filters; automatic resume BIDS validation Wraps official validator; cached, normalized JSON output Catalog search Local DuckDB + live API; <code>DatasetQuery</code> fluent builder with 10 filters Readiness checks <code>doctor</code> \u00b7 <code>minimum</code> \u00b7 <code>can-train</code> \u00b7 <code>first-batch</code> \u00b7 <code>leakage-check</code> \u00b7 <code>content-status</code> Conversion Parquet \u00b7 Zarr \u00b7 HDF5 \u00b7 WebDataset \u00b7 HuggingFace \u00b7 TFRecord ML adapters PyTorch \u00b7 Lightning \u00b7 scikit-learn \u00b7 HuggingFace \u00b7 BrainDecode \u00b7 Dask Visual QC \u2014 NIfTI Ortho \u00b7 lightbox \u00b7 fMRI QC \u00b7 DWI QC \u00b7 PET overlay \u00b7 one center slice per file Visual QC \u2014 EEG/MEG Butterfly \u00b7 PSD \u00b7 spectrogram \u00b7 topomap \u00b7 epoched previews via MNE Visual QC \u2014 DICOM Series browser with PHI protection Surface rendering GIFTI mesh/scalar/label QC and CIFTI dense-matrix summary with typed surface inspection"},{"location":"#where-to-start","title":"Where to start","text":"Quickstart <p>End-to-end: inspect ds004130, run readiness checks, download two subjects, convert to Parquet, load into PyTorch.</p> Readiness first <p>Why the manifest answers most questions before any file transfer, and how each readiness stage gates the next.</p> Modalities <p>MRI \u00b7 fMRI \u00b7 DWI \u00b7 PET \u00b7 MEG \u00b7 EEG \u00b7 iEEG \u00b7 fNIRS \u2014 what each modality looks like in BIDS and what Qortex can do with it.</p> NeuroAI runtime <p>Source \u2192 model \u2192 output pipelines on local files, DICOM, NWB, XDF, LSL streams, and BrainFlow boards. Compatibility check before weight loading.</p> CLI reference <p>All commands: <code>search</code> \u00b7 <code>inspect</code> \u00b7 <code>doctor</code> \u00b7 <code>download</code> \u00b7 <code>convert</code> \u00b7 <code>visualize</code> \u00b7 <code>neuroai run</code>.</p>"},{"location":"api/","title":"API Reference","text":"<p>Reference is auto-generated from source docstrings via mkdocstrings.</p> Module Entry point What it covers Dataset <code>qortex.Dataset</code> Inspect, download, convert, visualize \u2014 the main facade Visualization <code>qortex.visualize</code> Viewers, overlays, QC figures, audit reports Artifact <code>qortex.Artifact</code> Open and use a converted ML artifact Conversion <code>qortex.convert</code> <code>ConversionPipeline</code>, <code>WindowSpec</code>, <code>SplitSpec</code> Datasets <code>qortex.datasets</code> Keras-style loaders for open neuroscience datasets Neuroclassic <code>qortex.neuroclassic</code> Signal QC, image QC, connectivity, stats, splits CLI <code>qortex &lt;command&gt;</code> All CLI commands with options"},{"location":"api/#openneuroclient","title":"OpenNeuroClient","text":"<p>Direct access to the OpenNeuro GraphQL API. Use when you need rich metadata without the <code>Dataset</code> facade.</p> <pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    info    = client.get_dataset_rich(\"ds008039\")\n    readme  = client.get_readme(\"ds008039\")\n    issues  = client.get_validation_issues(\"ds008039\", \"1.0.0\")\n    snaps   = client.get_snapshots(\"ds008039\")\n    results = client.search_datasets_rich(modality=\"eeg\", sort_by=\"downloads\")\n</code></pre> Method Returns Description <code>get_dataset_rich(id)</code> <code>RichDatasetInfo</code> All metadata for one dataset <code>get_readme(id, tag?)</code> <code>str \\| None</code> README / description text <code>get_validation_issues(id, tag)</code> <code>list[dict]</code> BIDS validation errors and warnings <code>get_snapshot_summary(id, tag)</code> <code>SnapshotSummary</code> Subjects, sessions, demographics <code>get_snapshots(id)</code> <code>list[SnapshotRef]</code> All available versions <code>get_files(id, tag?)</code> <code>(SnapshotRef, list[dict])</code> Full file tree <code>search_datasets_rich(...)</code> <code>list[RichDatasetInfo]</code> Filtered rich search <code>get_dataset(id)</code> <code>DatasetRef</code> Lightweight metadata"},{"location":"api/#datasetquery","title":"DatasetQuery","text":"<p>Fluent search builder for local catalog and live API searches.</p> <pre><code>from qortex.catalog import DatasetQuery\n\nresults = (\n    DatasetQuery()\n    .modality(\"eeg\")\n    .min_subjects(30)\n    .has_events()\n    .containing(\"auditory\")\n    .limit(20)\n    .fetch()        # local catalog (fast, offline)\n)\n\nlive = DatasetQuery().modality(\"eeg\").min_subjects(30).live()  # OpenNeuro API\n</code></pre> <p>See Search &amp; filter for the full filter reference.</p>"},{"location":"api/artifact/","title":"Artifact API","text":"<p>An <code>Artifact</code> is the output of a <code>ConversionPipeline</code> run \u2014 a directory containing split subdirectories and an <code>artifact_manifest.json</code>.</p> <pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nart.summary()\n</code></pre> <p>Open and use a converted Qortex artifact.</p>"},{"location":"api/artifact/#qortex.Artifact.open","title":"open  <code>classmethod</code>","text":"<pre><code>open(path: Path | str) -&gt; 'Artifact'\n</code></pre>"},{"location":"api/artifact/#qortex.Artifact.summary","title":"summary","text":"<pre><code>summary() -&gt; dict[str, Any]\n</code></pre>"},{"location":"api/artifact/#qortex.Artifact.sklearn","title":"sklearn","text":"<pre><code>sklearn(split: str | None = None)\n</code></pre>"},{"location":"api/artifact/#qortex.Artifact.torch","title":"torch","text":"<pre><code>torch(\n    split: str | None = \"train\", *, iterable: bool = False\n)\n</code></pre>"},{"location":"api/artifact/#qortex.Artifact.compare_splits","title":"compare_splits","text":"<pre><code>compare_splits(\n    *,\n    n: int = 8,\n    splits: list[str] | None = None,\n    seed: int | None = 0\n)\n</code></pre> <p>Render n random samples from each split side-by-side for comparison.</p> <p>The returned :class:<code>~qortex.visualize._audit.VisualAuditReport</code> has one entry per sample, with <code>path_label</code> encoded as <code>\"{split}/sample_{i} label={lbl} sub={sub}\"</code> so the gallery is self-explanatory without requiring the user to understand shard layout.</p> <p>Uses the same lazy Parquet reading strategy as <code>visual_audit()</code>: only the minimum rows are scanned per shard.</p>"},{"location":"api/artifact/#qortex.Artifact.compare_splits--parameters","title":"Parameters","text":"<p>n:     Samples to render per split. splits:     Which splits to compare.  Defaults to all available splits from the     manifest (<code>[\"train\", \"val\", \"test\"]</code> filtered by what exists on disk).</p>"},{"location":"api/artifact/#qortex.Artifact.check_leakage","title":"check_leakage","text":"<pre><code>check_leakage() -&gt; dict\n</code></pre> <p>Check for subject-level and source-level leakage across splits.</p> <p>A split is leaky if the same subject ID or source file appears in two or more splits.  This is the primary leakage risk for subject-stratified splits; it does not catch temporal leakage or derivative-source overlap.</p>"},{"location":"api/artifact/#qortex.Artifact.check_leakage--returns","title":"Returns","text":"<p>dict     <code>{\"ok\": bool, \"leaky_subjects\": [...], \"leaky_sources\": [...], \"details\": [...]}</code></p>"},{"location":"api/artifact/#qortex.Artifact.validate_contract","title":"validate_contract","text":"<pre><code>validate_contract() -&gt; dict\n</code></pre> <p>Verify the artifact's integrity and contract against its manifest.</p> <p>Checks: - <code>artifact_manifest.json</code> exists and is parseable (done at open time) - SHA-256 of every listed file matches the stored hash - <code>artifact_contract.json</code> is present and consistent with the manifest - All declared splits have at least one shard on disk - Sample count in manifest matches actual row count in shards</p>"},{"location":"api/artifact/#qortex.Artifact.validate_contract--returns","title":"Returns","text":"<p>dict     <code>{\"ok\": bool, \"errors\": [...], \"warnings\": [...]}</code></p>"},{"location":"api/artifact/#qortex.Artifact.visualize_sample","title":"visualize_sample","text":"<pre><code>visualize_sample(\n    index: int = 0,\n    *,\n    split: str | None = \"train\",\n    mode: str = \"static\"\n)\n</code></pre> <p>Render one converted sample as a visual figure.</p> <p>Reads a single sample from the Parquet artifact without loading the whole split into memory.  Returns a plotly Figure showing:</p> <ul> <li>Signal / time-series data \u2192 butterfly plot (EEG/BOLD)</li> <li>Image data (2D/3D array) \u2192 center-slice orthogonal view</li> </ul>"},{"location":"api/artifact/#qortex.Artifact.visualize_sample--parameters","title":"Parameters","text":"<p>index:     Zero-based sample index within the split. split:     Which split to read from (<code>\"train\"</code>, <code>\"val\"</code>, <code>\"test\"</code>).     <code>None</code> reads from the first available shard. mode:     <code>\"static\"</code> (default) returns a plotly Figure; <code>\"html\"</code> returns     an HTML string.</p>"},{"location":"api/artifact/#qortex.Artifact.visualize_sample--returns","title":"Returns","text":"<p>plotly.graph_objects.Figure  (mode=\"static\") str                          (mode=\"html\")</p>"},{"location":"api/artifact/#qortex.Artifact.visual_audit","title":"visual_audit","text":"<pre><code>visual_audit(\n    *,\n    split: str = \"train\",\n    n: int = 16,\n    seed: int | None = 0\n)\n</code></pre> <p>Render thumbnails for n random samples from the artifact.</p> <p>Returns a :class:<code>~qortex.visualize._audit.VisualAuditReport</code> whose <code>.show()</code> opens an HTML gallery.</p>"},{"location":"api/artifact/#qortex.Artifact.visual_audit--parameters","title":"Parameters","text":"<p>split:     Which split to sample from.  <code>\"all\"</code> scans all shards. n:     Number of samples to render.</p>"},{"location":"api/cli/","title":"CLI Reference","text":"<p>All commands are accessed via <code>qortex &lt;command&gt; [options]</code>.</p>"},{"location":"api/cli/#dataset-commands","title":"Dataset commands","text":""},{"location":"api/cli/#search","title":"search","text":"<p>Search the local catalog for datasets by modality, task, and size.</p> <pre><code>qortex search [--modality MOD] [--task TASK] [--min-subjects N]\n              [--max-size GB] [--query TEXT] [--json] [--csv]\n</code></pre>"},{"location":"api/cli/#inspect","title":"inspect","text":"<p>Fetch and display the dataset manifest summary.</p> <pre><code>qortex inspect DATASET_ID [--snapshot VER] [--local] [--json]\n</code></pre>"},{"location":"api/cli/#metadata","title":"metadata","text":"<p>Print dataset metadata: dataset_description.json, participants.tsv, or a specific sidecar.</p> <pre><code>qortex metadata DATASET_ID [FILE_PATH] [--participants] [--snapshots] [--json]\n</code></pre>"},{"location":"api/cli/#readme","title":"readme","text":"<p>Fetch and print the README / description text for a dataset.</p> <pre><code>qortex readme DATASET_ID [--snapshot VER]\n</code></pre>"},{"location":"api/cli/#validation-issues","title":"validation-issues","text":"<p>Print BIDS validation issues for a snapshot.</p> <pre><code>qortex validation-issues DATASET_ID --snapshot VER [--errors-only] [--json]\n</code></pre>"},{"location":"api/cli/#preview","title":"preview","text":"<p>Show the first N rows of a tabular file.</p> <pre><code>qortex preview DATASET_ID FILE_PATH [--n 10]\n</code></pre>"},{"location":"api/cli/#readiness-commands","title":"Readiness commands","text":""},{"location":"api/cli/#doctor","title":"doctor","text":"<p>Full readiness report: subjects, modalities, events, label coverage, split feasibility.</p> <pre><code>qortex doctor DATASET_ID [--snapshot VER] [--local] [--data-dir DIR]\n              [--recipe NAME] [--json]\n</code></pre>"},{"location":"api/cli/#minimum","title":"minimum","text":"<p>Compute the smallest download for a given goal.</p> <pre><code>qortex minimum DATASET_ID --goal {first-batch,label-check,validation,metadata}\n               [--download] [--data-dir DIR]\n</code></pre>"},{"location":"api/cli/#can-train","title":"can-train","text":"<p>Binary label readiness check.</p> <pre><code>qortex can-train DATASET_ID --label COL [--min-classes N] [--min-per-class N]\n                 [--min-subjects N]\n</code></pre>"},{"location":"api/cli/#first-batch","title":"first-batch","text":"<p>Download minimum subjects and run a full pipeline pass.</p> <pre><code>qortex first-batch DATASET_ID --label COL [--data-dir DIR] [--format FMT]\n</code></pre>"},{"location":"api/cli/#leakage-check","title":"leakage-check","text":"<p>Verify no subject appears in two splits of a converted artifact.</p> <pre><code>qortex leakage-check DATASET_ID --artifact DIR [--level {subject,session}]\n</code></pre>"},{"location":"api/cli/#content-status","title":"content-status","text":"<p>Check local files for LFS pointers and incomplete downloads.</p> <pre><code>qortex content-status DATASET_ID --data-dir DIR\n</code></pre>"},{"location":"api/cli/#make-recipe","title":"make-recipe","text":"<p>Create a readiness recipe file from parameters.</p> <pre><code>qortex make-recipe RECIPE_NAME [--modality MOD] [--label COL]\n                   [--min-subjects N] [--output FILE]\n</code></pre>"},{"location":"api/cli/#run-recipe","title":"run-recipe","text":"<p>Run a recipe file against a dataset.</p> <pre><code>qortex run-recipe RECIPE_FILE --dataset DATASET_ID [--data-dir DIR]\n</code></pre>"},{"location":"api/cli/#download-commands","title":"Download commands","text":""},{"location":"api/cli/#plan","title":"plan","text":"<p>Show what would be downloaded without downloading.</p> <pre><code>qortex plan DATASET_ID [--subjects S [S ...]] [--tasks T [T ...]]\n            [--suffixes SUF [SUF ...]] [--output FILE]\n</code></pre>"},{"location":"api/cli/#download","title":"download","text":"<p>Download files from OpenNeuro.</p> <pre><code>qortex download DATASET_ID [--subjects S [S ...]] [--tasks T [T ...]]\n                [--suffixes SUF [SUF ...]] [--datatypes D [D ...]]\n                [--metadata-only] [--min-goal GOAL]\n                [--data-dir DIR] [--snapshot VER]\n                [--max-size GB] [--concurrency N] [--force]\n</code></pre>"},{"location":"api/cli/#validate","title":"validate","text":"<p>Run BIDS Validator on a local directory.</p> <pre><code>qortex validate DIR [--dataset-id ID]\n</code></pre>"},{"location":"api/cli/#local-index","title":"local-index","text":"<p>Scan a local BIDS directory and build a local manifest.</p> <pre><code>qortex local-index DIR --dataset-id ID\n</code></pre>"},{"location":"api/cli/#conversion-commands","title":"Conversion commands","text":""},{"location":"api/cli/#eda","title":"eda","text":"<p>Exploratory data analysis: signal statistics and label landscape.</p> <pre><code>qortex eda DATASET_ID [--data-dir DIR] [--label COL]\n</code></pre>"},{"location":"api/cli/#convert","title":"convert","text":"<p>Convert a local BIDS dataset to an ML artifact.</p> <pre><code>qortex convert DATASET_ID [--data-dir DIR] --output DIR\n               [--format {parquet,zarr,hdf5,webdataset,huggingface,tfrecord}]\n               [--window SECONDS] [--overlap FRAC]\n               [--event-aligned] [--tmin SECONDS]\n               [--label COL] [--subjects S [S ...]]\n               [--val-frac F] [--test-frac F] [--seed N]\n               [--workers N] [--overwrite]\n</code></pre>"},{"location":"api/cli/#cache-commands","title":"Cache commands","text":""},{"location":"api/cli/#cache-info","title":"cache info","text":"<p>Show total cache size.</p> <pre><code>qortex cache info\n</code></pre>"},{"location":"api/cli/#cache-list","title":"cache list","text":"<p>List cached datasets.</p> <pre><code>qortex cache list [DATASET_ID]\n</code></pre>"},{"location":"api/cli/#cache-remove","title":"cache remove","text":"<p>Remove a dataset from cache.</p> <pre><code>qortex cache remove DATASET_ID [--snapshot VER]\n</code></pre>"},{"location":"api/cli/#cache-clear","title":"cache clear","text":"<p>Remove all cached data.</p> <pre><code>qortex cache clear [--yes]\n</code></pre>"},{"location":"api/cli/#authentication","title":"Authentication","text":""},{"location":"api/cli/#login","title":"login","text":"<p>Store an OpenNeuro API token for private dataset access.</p> <pre><code>qortex login [--token TOKEN]\n</code></pre>"},{"location":"api/cli/#catalog-commands","title":"Catalog commands","text":""},{"location":"api/cli/#catalog-refresh","title":"catalog-refresh","text":"<p>Pull the latest catalog index from OpenNeuro.</p> <pre><code>qortex catalog-refresh\n</code></pre>"},{"location":"api/cli/#catalog-profile","title":"catalog-profile","text":"<p>Show catalog statistics.</p> <pre><code>qortex catalog-profile\n</code></pre>"},{"location":"api/cli/#visualization-commands","title":"Visualization commands","text":""},{"location":"api/cli/#visualize","title":"visualize","text":"<p>Render figures for local files.</p> <pre><code>qortex visualize DATASET_ID [--subject S] [--suffix SUF]\n                 [--data-dir DIR] [--output DIR]\n</code></pre>"},{"location":"api/cli/#visualize-openneuro","title":"visualize-openneuro","text":"<p>Render center-slice thumbnails from OpenNeuro CDN without full download.</p> <pre><code>qortex visualize-openneuro DATASET_ID [--subjects S [S ...]]\n                            [--suffixes SUF [SUF ...]]\n                            [--snapshot VER] [--output DIR]\n                            [--format {html,png}]\n</code></pre>"},{"location":"api/cli/#dicom-browser","title":"dicom-browser","text":"<p>Browse a local DICOM directory.</p> <pre><code>qortex dicom-browser DIR [--output FILE]\n</code></pre>"},{"location":"api/cli/#fmri-qc","title":"fmri-qc","text":"<p>Render the fMRI QC summary for a BOLD file.</p> <pre><code>qortex fmri-qc FILE [--events FILE] [--confounds FILE] [--output FILE]\n</code></pre>"},{"location":"api/cli/#dwi-qc","title":"dwi-qc","text":"<p>Render the DWI QC summary.</p> <pre><code>qortex dwi-qc FILE --bval FILE --bvec FILE [--output FILE]\n</code></pre>"},{"location":"api/cli/#visual-audit","title":"visual-audit","text":"<p>Run a visual audit on a local BIDS directory or manifest.</p> <pre><code>qortex visual-audit DATASET_ID [--data-dir DIR] [--output FILE]\n                    [--mode {local,manifest}]\n</code></pre>"},{"location":"api/cli/#visualize-overlay","title":"visualize-overlay","text":"<p>Overlay a mask or stat map on an anatomical image.</p> <pre><code>qortex visualize-overlay BACKGROUND_FILE --overlay FILE\n                          [--type {mask,labelmap,stat,pet,contour,edges}]\n                          [--alpha F] [--threshold F] [--output FILE]\n</code></pre>"},{"location":"api/cli/#compare-masks","title":"compare-masks","text":"<p>Side-by-side or overlay comparison of two masks.</p> <pre><code>qortex compare-masks BACKGROUND_FILE --mask-a FILE --mask-b FILE\n                     [--labels A B] [--mode {side-by-side,overlay}]\n                     [--output FILE]\n</code></pre>"},{"location":"api/cli/#artifact-visualize","title":"artifact-visualize","text":"<p>Visualize samples from a converted artifact.</p> <pre><code>qortex artifact-visualize ARTIFACT_DIR [--split {train,val,test}]\n                           [--index N] [--compare-splits]\n</code></pre>"},{"location":"api/cli/#dashboard","title":"dashboard","text":"<p>Launch the Qortex Streamlit dashboard.</p> <pre><code>qortex dashboard [--host HOST] [--port PORT]\n</code></pre>"},{"location":"api/cli/#preflight-command","title":"Preflight command","text":""},{"location":"api/cli/#preflight","title":"preflight","text":"<p>Run a goal-aware preflight check before data processing.</p> <pre><code>qortex preflight DATASET_DIR --goal {visualize,convert,train,neuroai-run}\n                 [--modality MOD] [--target COL] [--split-unit {subject,session,run}]\n                 [--pipeline YAML] [--output FILE] [--strict]\n</code></pre>"},{"location":"api/cli/#check-commands","title":"Check commands","text":"<p>Goal-specific data integrity checks. Each outputs a structured report and exits 1 on BLOCK.</p>"},{"location":"api/cli/#check-events","title":"check events","text":"<p>Validate event timing, onset ranges, and BIDS entity matching.</p> <pre><code>qortex check events DATASET_DIR [--modality MOD] [--require-trial-type]\n                    [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-units","title":"check units","text":"<p>Validate declared units against observed signal scale.</p> <pre><code>qortex check units DATASET_DIR [--modality MOD] [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-geometry","title":"check geometry","text":"<p>Validate NIfTI affine, qform/sform, and DWI gradient tables.</p> <pre><code>qortex check geometry DATASET_DIR [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-leakage","title":"check leakage","text":"<p>Validate ML label availability and train/test leakage risk.</p> <pre><code>qortex check leakage DATASET_DIR [--target COL]\n                     [--split-unit {subject,session,run}] [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-structure","title":"check structure","text":"<p>Validate BIDS layout, companion-file closure, and entity consistency.</p> <pre><code>qortex check structure DATASET_DIR [--modality MOD] [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-metadata","title":"check metadata","text":"<p>Cross-check BIDS JSON sidecars against raw file headers.</p> <pre><code>qortex check metadata DATASET_DIR [--modality MOD] [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-dwi-gradients","title":"check dwi-gradients","text":"<p>Validate DWI bval/bvec integrity and volume count consistency.</p> <pre><code>qortex check dwi-gradients DATASET_DIR [--output FILE]\n</code></pre>"},{"location":"api/cli/#check-eeg-channels","title":"check eeg-channels","text":"<p>Validate EEG channel metadata and unit consistency.</p> <pre><code>qortex check eeg-channels DATASET_DIR [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuroai-commands","title":"NeuroAI commands","text":""},{"location":"api/cli/#neuroai-check","title":"neuroai check","text":"<p>Run compatibility check between a source and a model.</p> <pre><code>qortex neuroai check SOURCE_PATH --model MODEL_ID [--provider {huggingface,braindecode,ultralytics,onnx}]\n                  [--task TASK]\n</code></pre>"},{"location":"api/cli/#neuroai-run","title":"neuroai run","text":"<p>Run a NeuroAI inference pipeline from a YAML spec.</p> <pre><code>qortex neuroai run PIPELINE_YAML [--data-dir DIR] [--output DIR]\n                   [--dry-run] [--verbose]\n</code></pre>"},{"location":"api/cli/#neuroai-benchmark","title":"neuroai benchmark","text":"<p>Benchmark a model against a source or BIDS dataset.</p> <pre><code>qortex neuroai benchmark SOURCE_PATH --model MODEL_ID [--n-runs N]\n                          [--provider PROV] [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuroai-replay","title":"neuroai replay","text":"<p>Replay a previously saved inference result.</p> <pre><code>qortex neuroai replay RESULT_PATH [--output DIR]\n</code></pre>"},{"location":"api/cli/#neuroai-inspect-source","title":"neuroai inspect-source","text":"<p>Probe a source file and print its SourceProfile.</p> <pre><code>qortex neuroai inspect-source SOURCE_PATH [--type {auto,edf,nifti,bids,dicom,lsl,brainflow,nwb,xdf}]\n</code></pre>"},{"location":"api/cli/#neuroai-inspect-model","title":"neuroai inspect-model","text":"<p>Inspect a model and print its ModelProfile and InputContract.</p> <pre><code>qortex neuroai inspect-model MODEL_ID [--provider {huggingface,braindecode,ultralytics,onnx}]\n                              [--task TASK]\n</code></pre>"},{"location":"api/cli/#neuroai-suggest-models","title":"neuroai suggest-models","text":"<p>Suggest compatible models for a given source file and task.</p> <pre><code>qortex neuroai suggest-models SOURCE_PATH --task {classification,segmentation,detection,regression,embedding}\n                               [--modality {eeg,mri,ct,image,video}]\n                               [--top-k N] [--provider PROV]\n</code></pre>"},{"location":"api/cli/#neuro-classic-commands","title":"Neuro-classic commands","text":"<p>Classical computational neuroscience methods. Requires <code>pip install 'qortex[neuroclassic]'</code>.</p>"},{"location":"api/cli/#neuro-classic-signal-qc","title":"neuro-classic signal-qc","text":"<p>Run signal QC on all EEG/MEG files (flatline, NaN, saturation, amplitude).</p> <pre><code>qortex neuro-classic signal-qc DATASET_DIR [--modality {eeg,meg}]\n                                [--max-files N] [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuro-classic-image-qc","title":"neuro-classic image-qc","text":"<p>Run image QC on all NIfTI files (NaN, constant, tSNR, volume outliers).</p> <pre><code>qortex neuro-classic image-qc DATASET_DIR [--modality {mri,fmri,ct,dwi,pet}]\n                               [--max-files N] [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuro-classic-eeg-psd","title":"neuro-classic eeg-psd","text":"<p>Run EEG power spectral density and spectral slope summary.</p> <pre><code>qortex neuro-classic eeg-psd DATASET_DIR [--max-files N] [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuro-classic-connectivity","title":"neuro-classic connectivity","text":"<p>Compute connectivity matrix and graph metrics on EEG files.</p> <pre><code>qortex neuro-classic connectivity DATASET_DIR [--method correlation]\n                                  [--threshold F] [--output FILE]\n</code></pre>"},{"location":"api/cli/#neuro-classic-cohort-anomalies","title":"neuro-classic cohort-anomalies","text":"<p>Detect cohort-level amplitude outliers across subjects.</p> <pre><code>qortex neuro-classic cohort-anomalies DATASET_DIR [--output FILE]\n</code></pre>"},{"location":"api/conversion/","title":"Conversion API","text":"<p>The conversion layer reads a local BIDS dataset, applies windowing and splits, and writes an ML-format artifact.</p> <pre><code>from qortex import Dataset\nfrom qortex.convert import ConversionPipeline, WindowSpec, SplitSpec\n\nds = Dataset(\"ds004130\", data_dir=\"data/\")\nart = ds.convert(\n    output_dir=\"artifacts/ds004130/\",\n    format=\"parquet\",\n    window=WindowSpec(duration_s=4.0, overlap=0.5, event_aligned=True),\n    split=SplitSpec(train=0.7, val=0.15, test=0.15, strategy=\"subject\"),\n    label_col=\"trial_type\",\n)\n</code></pre>"},{"location":"api/conversion/#conversionpipeline","title":"ConversionPipeline","text":"<p>Orchestrates the full ETL: load \u2192 window \u2192 split \u2192 write format. Instantiated directly when you need lower-level control, or via <code>ds.convert()</code>.</p> <p>End-to-end ETL: BIDS files \u2192 load \u2192 window \u2192 split \u2192 ML format.</p> <p>The pipeline is deliberately serial (no async) so it can run inside any Jupyter kernel or HPC job scheduler without event-loop conflicts.</p>"},{"location":"api/conversion/#qortex.convert.ConversionPipeline--parameters","title":"Parameters","text":"<p>manifest:     Built by <code>manifest.builder.ManifestBuilder</code>. data_dir:     Root of the locally downloaded BIDS tree. output_dir:     Destination for the converted artifact. output_format:     <code>\"parquet\"</code> | <code>\"zarr\"</code> | <code>\"hdf5\"</code> | <code>\"webdataset\"</code> |     <code>\"huggingface\"</code> | <code>\"tfrecord\"</code>. window_spec:     Fixed-stride or event-aligned window config.  <code>None</code> = full signal. split_spec:     Train / val / test split strategy.  <code>None</code> = no split column set. shard_size:     Samples per output shard for sharded formats. loader_registry:     Override the global loader registry (useful for testing). skip_missing:     If True (default), failed files are logged and counted but do not abort     the pipeline.  Set False to raise on first failure.</p>"},{"location":"api/conversion/#qortex.convert.ConversionPipeline.run","title":"run","text":"<pre><code>run() -&gt; ConversionResult\n</code></pre>"},{"location":"api/conversion/#windowspec","title":"WindowSpec","text":"<p>Controls how raw signals are cut into fixed-length or event-aligned windows.</p> <p>Parameters for a sliding or event-aligned window.</p> <p>Fields</p> Field Type Default Description <code>duration_s</code> <code>float</code> required Window length in seconds <code>overlap</code> <code>float</code> <code>0.0</code> Fraction of window that overlaps with the previous (sliding only) <code>tmin</code> <code>float</code> <code>0.0</code> Seconds before event onset (event-aligned; negative = baseline period) <code>event_aligned</code> <code>bool</code> <code>False</code> If <code>True</code>, cut one window per event; if <code>False</code>, use fixed-stride sliding"},{"location":"api/conversion/#splitspec","title":"SplitSpec","text":"<p>Controls how subjects are assigned to train / val / test splits.</p> <p>Fields</p> Field Type Default Description <code>train</code> <code>float</code> <code>0.7</code> Fraction of subjects in the training split <code>val</code> <code>float</code> <code>0.15</code> Fraction of subjects in the validation split <code>test</code> <code>float</code> <code>0.15</code> Fraction of subjects in the test split <code>seed</code> <code>int</code> <code>42</code> Random seed for reproducibility <code>stratify_by_label</code> <code>bool</code> <code>True</code> Balance class distribution across splits <code>strategy</code> <code>str</code> <code>\"subject\"</code> <code>\"subject\"</code> \u2014 no subject spans two splits; <code>\"random\"</code> \u2014 sample-level random; <code>\"stratified\"</code> \u2014 stratified by label <p>Fractions must sum to 1.0; <code>__post_init__</code> raises <code>ValueError</code> otherwise.</p>"},{"location":"api/conversion/#windowing-functions","title":"Windowing functions","text":"<p>Yield fixed-stride windows from a SampleRecord.</p> <p>Signal in sample.data is expected to be shaped (n_channels, n_timepoints).</p> <p>Yield one window per event onset, aligned to the event.</p> <p>Events.data is a Polars DataFrame with at minimum an 'onset' column (seconds) and optionally 'trial_type' for labels.</p>"},{"location":"api/conversion/#split-functions","title":"Split functions","text":"<p>Dispatch to the configured split strategy.</p> <p>When strategy=\"subject\" and stratify_by_label=True, this promotes to stratified subject-level splitting so class balance is preserved across train/val/test partitions.</p>"},{"location":"api/conversion/#provenance","title":"Provenance","text":"<p>Persist a ProvenanceRecord as JSON alongside the converted artifact.</p> <p>Load a previously saved ProvenanceRecord.</p>"},{"location":"api/conversion/#formatwriter-protocol","title":"FormatWriter protocol","text":"<p>Every output-format writer implements this protocol.</p> <p>               Bases: <code>Protocol</code></p> <p>Write an iterable of SampleRecords to a target directory.</p>"},{"location":"api/conversion/#qortex.convert.FormatWriter.write","title":"write","text":"<pre><code>write(\n    samples: Iterator[SampleRecord],\n    output_dir: Path,\n    *,\n    shard_size: int = 1000,\n    metadata: dict[str, Any] | None = None\n) -&gt; Path\n</code></pre> <p>Write samples to output_dir.  Returns the root path of the output.</p>"},{"location":"api/conversion/#qortex.convert.FormatWriter.estimate_size","title":"estimate_size","text":"<pre><code>estimate_size(\n    n_samples: int, sample_shape: tuple[int, ...]\n) -&gt; int\n</code></pre> <p>Return a rough byte estimate for n_samples of the given shape.</p>"},{"location":"api/conversion/#output-formats","title":"Output formats","text":"Format Extra required Notes <code>\"parquet\"</code> (none) Default. Columnar, fast random access. <code>\"zarr\"</code> <code>qortex[zarr]</code> Chunked array store, good for large volumes. <code>\"hdf5\"</code> <code>qortex[hdf5]</code> Single-file archive. <code>\"webdataset\"</code> (none) Tar shards, optimised for streaming. <code>\"huggingface\"</code> <code>qortex[hf]</code> HuggingFace <code>datasets</code> format. <code>\"tfrecord\"</code> <code>qortex[tf]</code> TensorFlow record format."},{"location":"api/dataset/","title":"Dataset API","text":"<p><code>Dataset</code> is the main entry point for working with an OpenNeuro or local BIDS dataset. Creating an instance makes no network calls \u2014 methods that need the manifest fetch it on first call.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nds = Dataset(\"ds004130\", snapshot=\"1.2.0\")\nds = Dataset(\"ds004130\", data_dir=\"/data/ds004130\")\n</code></pre> <p>High-level facade for working with a single OpenNeuro dataset.</p>"},{"location":"api/dataset/#qortex.Dataset--parameters","title":"Parameters","text":"<p>dataset_id:     OpenNeuro dataset ID, e.g. <code>\"ds004130\"</code>. snapshot:     Snapshot tag to pin to.  <code>None</code> uses the latest published snapshot. token:     Optional API token.  Falls back to env var <code>QORTEX_API_TOKEN</code> or     <code>~/.config/qortex/credentials.json</code>. data_dir:     Override the download destination.  Defaults to     <code>~/.cache/qortex/datasets/{dataset_id}/{snapshot}/</code>.</p>"},{"location":"api/dataset/#qortex.Dataset.manifest","title":"manifest","text":"<pre><code>manifest(*, force_refresh: bool = False) -&gt; Manifest\n</code></pre> <p>Fetch and cache the file manifest for this dataset.</p>"},{"location":"api/dataset/#qortex.Dataset.info","title":"info","text":"<pre><code>info() -&gt; dict[str, Any]\n</code></pre> <p>Return a summary dict of dataset metadata.</p>"},{"location":"api/dataset/#qortex.Dataset.files","title":"files","text":"<pre><code>files(\n    *,\n    subjects: list[str] | None = None,\n    sessions: list[str] | None = None,\n    tasks: list[str] | None = None,\n    modalities: list[str] | None = None,\n    datatypes: list[str] | None = None,\n    extensions: list[str] | None = None,\n    metadata_only: bool = False\n) -&gt; list[FileRecord]\n</code></pre> <p>Return manifest files matching structural filters.</p>"},{"location":"api/dataset/#qortex.Dataset.metadata_files","title":"metadata_files","text":"<pre><code>metadata_files() -&gt; list[FileRecord]\n</code></pre> <p>Return essential metadata and lightweight sidecar/table files.</p>"},{"location":"api/dataset/#qortex.Dataset.inspect","title":"inspect","text":"<pre><code>inspect(*, level: str = 'manifest') -&gt; 'DatasetProfile'\n</code></pre> <p>Fetch metadata and return a DatasetProfile \u2014 no download needed.</p>"},{"location":"api/dataset/#qortex.Dataset.inspect--parameters","title":"Parameters","text":"<p>level:     Inspection depth:</p> <pre><code>``\"summary\"``\n    API-only, completes in &lt;2 s. Returns subjects, sessions,\n    tasks, modalities, size, demographics, and engagement from\n    the OpenNeuro GraphQL API without fetching the file tree.\n    ``profile.manifest`` will be ``None`` at this level.\n``\"manifest\"`` *(default)*\n    Fetches the full recursive file tree, builds a typed\n    ``Manifest``, and computes modality breakdown, companion\n    coverage, ML readiness score, and recommendations.\n``\"deep\"``\n    Manifest level plus concurrent remote events and sidecar\n    analysis. Adds ``LabelLandscape`` and ``SignalBudget`` data\n    to the profile. Takes 10\u201360 s depending on dataset size.\n</code></pre>"},{"location":"api/dataset/#qortex.Dataset.inspect--returns","title":"Returns","text":"<p>DatasetProfile     Inspection report. Call <code>.summary()</code> for a compact view,     <code>.report()</code> for the full modality breakdown.</p>"},{"location":"api/dataset/#qortex.Dataset.inspect--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000117\")</p>"},{"location":"api/dataset/#qortex.Dataset.inspect--fast-api-only-check","title":"Fast API-only check","text":"<p>quick = ds.inspect(level=\"summary\") print(quick.n_subjects, quick.tasks)</p>"},{"location":"api/dataset/#qortex.Dataset.inspect--full-manifest-analysis","title":"Full manifest analysis","text":"<p>profile = ds.inspect() print(profile.ml_readiness.grade)</p>"},{"location":"api/dataset/#qortex.Dataset.inspect--with-remote-events-signal-budget","title":"With remote events + signal budget","text":"<p>deep = ds.inspect(level=\"deep\")</p>"},{"location":"api/dataset/#qortex.Dataset.participants","title":"participants","text":"<pre><code>participants(*, prefer_api: bool = True)\n</code></pre> <p>Return participant demographics as a Polars DataFrame.</p> <p>Tries the OpenNeuro API first (<code>snapshot.summary.subjectMetadata</code>) which gives age, sex, and group without downloading participants.tsv. Falls back to fetching participants.tsv via CDN if the API returns no demographics or the API path fails with a recognised transient error.</p>"},{"location":"api/dataset/#qortex.Dataset.participants--parameters","title":"Parameters","text":"<p>prefer_api:     When True (default), try the API before the remote TSV. Set False     to always read directly from the CDN file.</p>"},{"location":"api/dataset/#qortex.Dataset.participants--returns","title":"Returns","text":"<p>polars.DataFrame     Columns: participant_id, age (int | null), sex (str | null),     group (str | null).  Additional columns from the TSV are included     when the fallback path is taken.  The dataframe carries a     <code>_source</code> attribute (<code>\"api\"</code> or <code>\"participants.tsv\"</code>)     indicating which path was used.</p>"},{"location":"api/dataset/#qortex.Dataset.participants--examples","title":"Examples","text":"<p>df = Dataset(\"ds000117\").participants() df.filter(pl.col(\"sex\") == \"M\")[\"age\"].mean() df.attrs.get(\"_source\")   # \"api\" or \"participants.tsv\"</p>"},{"location":"api/dataset/#qortex.Dataset.events","title":"events","text":"<pre><code>events(\n    subject: str | None = None,\n    session: str | None = None,\n    task: str | None = None,\n    run: str | None = None,\n)\n</code></pre> <p>Fetch a remote events TSV as a Polars DataFrame without downloading.</p> <p>All parameters are optional; when None, the first matching events file in the manifest is used.</p>"},{"location":"api/dataset/#qortex.Dataset.events--parameters","title":"Parameters","text":"<p>subject / session / task / run:     BIDS entity filters.  Partial matches are accepted \u2014 e.g. just     <code>task=\"rest\"</code> will find any events file for the rest task.</p>"},{"location":"api/dataset/#qortex.Dataset.events--returns","title":"Returns","text":"<p>polars.DataFrame     Columns from the events TSV: onset, duration, trial_type, \u2026</p>"},{"location":"api/dataset/#qortex.Dataset.events--examples","title":"Examples","text":"<p>df = Dataset(\"ds000117\").events(subject=\"01\", task=\"facerecognition\") df.head()</p>"},{"location":"api/dataset/#qortex.Dataset.sidecar","title":"sidecar","text":"<pre><code>sidecar(path: str, *, strict: bool = False)\n</code></pre> <p>Fetch and merge BIDS JSON sidecars for a file path, without downloading.</p> <p>Follows BIDS inheritance: most-general (dataset root) sidecar values are overridden by more-specific ones (subject \u2192 session \u2192 file-level). Sidecar fetch failures are logged at WARNING level and recorded in the returned dict under the <code>\"_sidecar_warnings\"</code> key so callers can detect partial merges without crashing.</p>"},{"location":"api/dataset/#qortex.Dataset.sidecar--parameters","title":"Parameters","text":"<p>path:     BIDS-relative path, e.g. <code>\"sub-01/eeg/sub-01_task-rest_eeg.set\"</code>. strict:     When True, raise the first sidecar fetch/parse error rather than     continuing.  Useful for debugging incomplete CDN responses or     unexpected JSON schema changes.</p>"},{"location":"api/dataset/#qortex.Dataset.sidecar--returns","title":"Returns","text":"<p>dict     Merged JSON sidecar key-value pairs.  On partial failure the dict     includes <code>\"_sidecar_warnings\": [...]</code> listing failed sidecar     paths and their error messages.</p>"},{"location":"api/dataset/#qortex.Dataset.sidecar--examples","title":"Examples","text":"<p>meta = Dataset(\"ds004130\").sidecar(\"sub-01/eeg/sub-01_task-rest_eeg.set\") meta[\"SamplingFrequency\"] 256 meta.get(\"_sidecar_warnings\")   # None or list of {\"path\": ..., \"error\": ...}</p>"},{"location":"api/dataset/#qortex.Dataset.nifti_info","title":"nifti_info","text":"<pre><code>nifti_info(path: str)\n</code></pre> <p>Extract NIfTI header info remotely using an HTTP Range request.</p> <p>Fetches only 352 bytes (NIfTI-1) or up to 64 KB (NIfTI-2 / gzip) to determine image shape, voxel sizes, TR, and number of volumes.</p>"},{"location":"api/dataset/#qortex.Dataset.nifti_info--parameters","title":"Parameters","text":"<p>path:     BIDS-relative path to a .nii or .nii.gz file.</p>"},{"location":"api/dataset/#qortex.Dataset.nifti_info--returns","title":"Returns","text":"<p>NIfTIHeader     Shape, voxel sizes in mm, TR in seconds, number of volumes.</p>"},{"location":"api/dataset/#qortex.Dataset.nifti_info--examples","title":"Examples","text":"<p>info = Dataset(\"ds000117\").nifti_info( ...     \"sub-01/func/sub-01_task-facerecognition_bold.nii.gz\" ... ) print(info) 4D fMRI 64\u00d764\u00d733\u00d7208 vox=3.00\u00d73.00\u00d74.05mm TR=2.000s</p>"},{"location":"api/dataset/#qortex.Dataset.preview","title":"preview","text":"<pre><code>preview(\n    path: str,\n    *,\n    local_path: Path | None = None,\n    n_rows: int = 5,\n    max_bytes: int = 64000\n) -&gt; FilePreview\n</code></pre> <p>Preview a local or remote file without downloading the full file.</p>"},{"location":"api/dataset/#qortex.Dataset.first_rows","title":"first_rows","text":"<pre><code>first_rows(\n    path: str,\n    *,\n    n: int = 5,\n    local_path: Path | None = None,\n    max_bytes: int = 64000\n) -&gt; list[dict[str, Any]]\n</code></pre> <p>Return first rows of a TSV/CSV file from local disk or remote URL.</p>"},{"location":"api/dataset/#qortex.Dataset.preview_metadata","title":"preview_metadata","text":"<pre><code>preview_metadata(\n    *,\n    local_path: Path | None = None,\n    n_rows: int = 5,\n    max_files: int | None = None\n) -&gt; list[FilePreview]\n</code></pre> <p>Preview essential metadata and sidecar/table files.</p>"},{"location":"api/dataset/#qortex.Dataset.prefetch_metadata","title":"prefetch_metadata","text":"<pre><code>prefetch_metadata(\n    modalities: list[str] | None = None,\n    *,\n    concurrency: int = 16\n) -&gt; dict[str, Any]\n</code></pre> <p>Prefetch remote headers for all subjects concurrently.</p> <p>Populates the NiftiStreamer/EDFStreamer header cache for all files of the specified modalities, enabling subsequent <code>stream_slice</code> calls to skip the header fetch round-trip.</p>"},{"location":"api/dataset/#qortex.Dataset.prefetch_metadata--parameters","title":"Parameters","text":"<p>modalities:     BIDS suffixes to prefetch headers for.  <code>None</code> = all image files. concurrency:     Number of parallel Range requests to fire simultaneously.</p>"},{"location":"api/dataset/#qortex.Dataset.prefetch_metadata--returns","title":"Returns","text":"<p>dict     <code>{manifest_path: header_object}</code> mapping for all successfully     prefetched files.</p>"},{"location":"api/dataset/#qortex.Dataset.prefetch_metadata--examples","title":"Examples","text":"<p>Dataset(\"ds000001\").prefetch_metadata(modalities=[\"eeg\", \"meg\"])</p>"},{"location":"api/dataset/#qortex.Dataset.stream_header","title":"stream_header","text":"<pre><code>stream_header(\n    *,\n    subject: str,\n    session: str | None = None,\n    modality: str = \"T1w\",\n    run: str | None = None\n)\n</code></pre> <p>Fetch the NIfTI or EDF header for a remote file via byte-range requests.</p> <p>No full file download is required \u2014 only the header bytes are fetched.</p>"},{"location":"api/dataset/#qortex.Dataset.stream_header--parameters","title":"Parameters","text":"<p>subject:     Subject ID (with or without the <code>sub-</code> prefix). session:     Session ID (with or without the <code>ses-</code> prefix).  <code>None</code> = any. modality:     BIDS suffix, e.g. <code>\"T1w\"</code>, <code>\"bold\"</code>, <code>\"eeg\"</code>. run:     Run label, e.g. <code>\"1\"</code>.  <code>None</code> = any.</p>"},{"location":"api/dataset/#qortex.Dataset.stream_header--returns","title":"Returns","text":"<p>NiftiStreamHeader | EDFStreamHeader     Parsed header object.</p>"},{"location":"api/dataset/#qortex.Dataset.stream_header--examples","title":"Examples","text":"<p>hdr = Dataset(\"ds000001\").stream_header(subject=\"01\", modality=\"T1w\") hdr.shape, hdr.voxel_sizes_mm ((256, 256, 176), (1.0, 1.0, 1.0))</p>"},{"location":"api/dataset/#qortex.Dataset.stream_slice","title":"stream_slice","text":"<pre><code>stream_slice(\n    *,\n    subject: str,\n    modality: str = \"bold\",\n    run: str | None = None,\n    session: str | None = None,\n    time_index: int = 0,\n    axis: int = 2,\n    slice_index: int | None = None\n) -&gt; Any\n</code></pre> <p>Stream a 2D slice or 3D volume from a remote NIfTI file.</p>"},{"location":"api/dataset/#qortex.Dataset.stream_slice--parameters","title":"Parameters","text":"<p>subject:     Subject ID. modality:     BIDS suffix, e.g. <code>\"bold\"</code>, <code>\"T1w\"</code>, <code>\"dwi\"</code>. time_index:     Volume index for 4D fMRI (ignored for 3D anatomicals). axis:     Slicing axis for 2D extraction (0=sagittal, 1=coronal, 2=axial). slice_index:     Which slice along <code>axis</code> to extract.  <code>None</code> = center slice.</p>"},{"location":"api/dataset/#qortex.Dataset.stream_slice--returns","title":"Returns","text":"<p>np.ndarray     2D array (slice) or 3D array (volume when <code>slice_index=None</code>     and <code>axis</code> is not specified).</p>"},{"location":"api/dataset/#qortex.Dataset.stream_slice--examples","title":"Examples","text":"<p>sl = Dataset(\"ds000001\").stream_slice( ...     subject=\"01\", modality=\"bold\", run=\"1\", time_index=150 ... ) sl.shape  # (64, 64)</p>"},{"location":"api/dataset/#qortex.Dataset.get_lazy_array","title":"get_lazy_array","text":"<pre><code>get_lazy_array(\n    *,\n    subject: str,\n    modality: str = \"T1w\",\n    session: str | None = None,\n    run: str | None = None\n) -&gt; Any\n</code></pre> <p>Return a lazy nibabel ArrayProxy without downloading the full file.</p> <p>The proxy fetches data on-demand when sliced, using nibabel's built-in lazy evaluation for local paths.  For remote URLs, returns the NiftiStreamer's <code>get_lazy_array()</code> object (header + deferred load).</p>"},{"location":"api/dataset/#qortex.Dataset.get_lazy_array--parameters","title":"Parameters","text":"<p>subject:     Subject ID. modality:     BIDS suffix.</p>"},{"location":"api/dataset/#qortex.Dataset.get_lazy_array--returns","title":"Returns","text":"<p>nibabel.ArrayProxy or NiftiStreamer-proxy object</p>"},{"location":"api/dataset/#qortex.Dataset.get_lazy_array--examples","title":"Examples","text":"<p>arr = Dataset(\"ds000001\").get_lazy_array(subject=\"01\", modality=\"T1w\") center = arr[128, 128, 88]   # triggers partial load on remote</p>"},{"location":"api/dataset/#qortex.Dataset.map_labels","title":"map_labels","text":"<pre><code>map_labels(\n    *,\n    source_tsv: str = \"participants\",\n    target_column: str,\n    transform=None,\n    label_map: dict | None = None\n) -&gt; dict[str, Any]\n</code></pre> <p>Read and transform a participants.tsv column into a subject\u2192label mapping.</p> <p>Useful for injecting external labels (age buckets, diagnosis scores, composite phenotypes) into downstream Datasets without re-downloading.</p>"},{"location":"api/dataset/#qortex.Dataset.map_labels--parameters","title":"Parameters","text":"<p>source_tsv:     Which TSV file to read: <code>\"participants\"</code> (default) maps to     <code>participants.tsv</code> at the dataset root; any BIDS-relative path     is accepted. target_column:     Column to extract, e.g. <code>\"age\"</code>, <code>\"diagnosis\"</code>. transform:     Optional callable applied to each raw value before returning.     Example: <code>transform=lambda x: float(x) / 100.0</code> to normalize age. label_map:     Optional raw \u2192 canonical value mapping applied before <code>transform</code>.</p>"},{"location":"api/dataset/#qortex.Dataset.map_labels--returns","title":"Returns","text":"<p>dict     <code>{subject_id: label}</code> mapping where subject_id is the BIDS     <code>sub-XX</code> string.</p>"},{"location":"api/dataset/#qortex.Dataset.map_labels--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") age_map = ds.map_labels( ...     target_column=\"age\", ...     transform=lambda x: (float(x) - 18) / 50,  # normalise 18-68 \u2192 0-1 ... ) age_map[\"sub-01\"] 0.42</p>"},{"location":"api/dataset/#qortex.Dataset.label_landscape","title":"label_landscape","text":"<pre><code>label_landscape(\n    *,\n    label_column: str | None = None,\n    concurrency: int = 24,\n    max_events_files: int | None = None\n)\n</code></pre> <p>Analyse all events TSVs remotely and return a LabelLandscape.</p> <p>Concurrently fetches all events.tsv files from CDN URLs, auto-detects the label column, and computes trial-type statistics, class balance, inter-stimulus interval jitter, cross-subject consistency, and actionable ML recommendations \u2014 with zero bytes downloaded to disk.</p>"},{"location":"api/dataset/#qortex.Dataset.label_landscape--parameters","title":"Parameters","text":"<p>label_column:     Column to use as the class label (e.g. <code>\"trial_type\"</code>).  When     None, auto-detected from the first events file. concurrency:     Number of parallel CDN requests. max_events_files:     Cap for number of events files to fetch (useful for large datasets).</p>"},{"location":"api/dataset/#qortex.Dataset.label_landscape--returns","title":"Returns","text":"<p>LabelLandscape     Rich label analysis.  Call <code>.summary()</code> for a compact report.</p>"},{"location":"api/dataset/#qortex.Dataset.label_landscape--examples","title":"Examples","text":"<p>landscape = Dataset(\"ds000117\").label_landscape() print(landscape.summary()) landscape.imbalance_severity   # \"balanced\" | \"moderate\" | ...</p>"},{"location":"api/dataset/#qortex.Dataset.signal_budget","title":"signal_budget","text":"<pre><code>signal_budget(\n    *,\n    concurrency: int = 24,\n    include_nifti_headers: bool = True\n)\n</code></pre> <p>Estimate total signal hours and achievable windows without downloading.</p> <p>Fetches JSON sidecars remotely for all signal files, extracting SamplingFrequency, RecordingDuration, EEGChannelCount, etc.  For fMRI files with missing TR or volume counts, fetches the NIfTI header (352 bytes via HTTP Range) to fill the gaps.</p>"},{"location":"api/dataset/#qortex.Dataset.signal_budget--parameters","title":"Parameters","text":"<p>concurrency:     Parallel CDN connections. include_nifti_headers:     Whether to fetch NIfTI headers for fMRI files missing TR info.</p>"},{"location":"api/dataset/#qortex.Dataset.signal_budget--returns","title":"Returns","text":"<p>SignalBudget     Per-modality recording hours and window estimates.  Call     <code>budget.estimate_windows(2.0)</code> for a 2-second window breakdown.</p>"},{"location":"api/dataset/#qortex.Dataset.signal_budget--examples","title":"Examples","text":"<p>budget = Dataset(\"ds000117\").signal_budget() budget.estimate_windows(window_duration_s=2.0, overlap=0.5) {'meg': 183200, 'eeg': 42100} budget.minimum_download_for_n_windows(10000, window_s=2.0)</p>"},{"location":"api/dataset/#qortex.Dataset.doctor","title":"doctor","text":"<pre><code>doctor(local_path: Path | None = None) -&gt; DoctorReport\n</code></pre> <p>Return a decision report for download, conversion, and training readiness.</p>"},{"location":"api/dataset/#qortex.Dataset.minimum","title":"minimum","text":"<pre><code>minimum(\n    goal: str = \"first-batch\",\n    *,\n    modality: str | None = None,\n    target: str | None = None,\n    output_dir: Path | None = None\n) -&gt; MinimumPlanReport\n</code></pre> <p>Plan the smallest real download needed for a concrete workflow goal.</p>"},{"location":"api/dataset/#qortex.Dataset.can_train","title":"can_train","text":"<pre><code>can_train(\n    *,\n    modality: str | None = None,\n    target: str | None = None,\n    local_path: Path | None = None\n) -&gt; CanTrainReport\n</code></pre> <p>Assess whether the dataset can support supervised training.</p>"},{"location":"api/dataset/#qortex.Dataset.first_batch","title":"first_batch","text":"<pre><code>first_batch(\n    *,\n    artifact_path: Path | None = None,\n    local_path: Path | None = None,\n    modality: str | None = None,\n    target: str | None = None,\n    limit: int = 8\n) -&gt; FirstBatchReport\n</code></pre> <p>Preview the first rows from an artifact or create a first-batch plan.</p>"},{"location":"api/dataset/#qortex.Dataset.content_status","title":"content_status","text":"<pre><code>content_status(\n    local_path: Path | None = None,\n) -&gt; ContentStatusReport\n</code></pre> <p>Check local content completeness and pointer-like files against the manifest.</p>"},{"location":"api/dataset/#qortex.Dataset.check","title":"check","text":"<pre><code>check(\n    local_path: Path | None = None,\n    conversion_target: str | None = None,\n    inspect_loaders: bool = False,\n) -&gt; ReadinessReport\n</code></pre> <p>Return a decision-oriented readiness report for this dataset.</p>"},{"location":"api/dataset/#qortex.Dataset.plan","title":"plan","text":"<pre><code>plan(\n    subjects: list[str] | None = None,\n    sessions: list[str] | None = None,\n    tasks: list[str] | None = None,\n    modalities: list[str] | None = None,\n    datatypes: list[str] | None = None,\n    include: list[str] | None = None,\n    exclude: list[str] | None = None,\n    include_derivatives: bool = False,\n    metadata_only: bool = False,\n    event_complete: bool = False,\n    label_ready: bool = False,\n    loadable_only: bool = False,\n    max_size_gb: float | None = None,\n    output_dir: Path | None = None,\n)\n</code></pre> <p>Return a DownloadPlan without downloading.</p>"},{"location":"api/dataset/#qortex.Dataset.select","title":"select","text":"<pre><code>select(**kwargs)\n</code></pre> <p>Alias for plan(); returns an explainable DownloadPlan.</p>"},{"location":"api/dataset/#qortex.Dataset.download","title":"download","text":"<pre><code>download(\n    subjects: list[str] | None = None,\n    sessions: list[str] | None = None,\n    tasks: list[str] | None = None,\n    modalities: list[str] | None = None,\n    suffixes: list[str] | None = None,\n    include_derivatives: bool = False,\n    include: list[str] | None = None,\n    exclude: list[str] | None = None,\n    metadata_only: bool = False,\n    event_complete: bool = False,\n    label_ready: bool = False,\n    loadable_only: bool = False,\n    max_size_gb: float | None = None,\n    output_dir: Path | None = None,\n    dry_run: bool = False,\n) -&gt; DownloadResult\n</code></pre> <p>Download the dataset (or a filtered subset) to local disk.</p>"},{"location":"api/dataset/#qortex.Dataset.download--parameters","title":"Parameters","text":"<p>subjects / sessions / tasks / modalities:     Filter which files to download.  <code>None</code> means include all. suffixes:     BIDS suffixes to download, e.g. <code>[\"T1w\", \"bold\", \"dwi\"]</code>.     Translated to <code>include</code> glob patterns and combined with any     explicit <code>include</code> patterns you supply. include_derivatives:     Include the <code>derivatives/</code> subdirectory (default False). include / exclude:     Gitignore-style glob patterns applied on top of entity filters. output_dir:     Override the download destination. dry_run:     Plan the download but don't actually transfer any files.</p>"},{"location":"api/dataset/#qortex.Dataset.download--returns","title":"Returns","text":"<p>DownloadResult     Summary of what was downloaded, skipped, or failed.</p>"},{"location":"api/dataset/#qortex.Dataset.download--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\", \"02\"], suffixes=[\"T1w\"]) ds.download(subjects=[\"01\"], include=[\"/_T1w.\", \"/_bold.\"])</p>"},{"location":"api/dataset/#qortex.Dataset.download_metadata","title":"download_metadata","text":"<pre><code>download_metadata(\n    output_dir: Path | None = None,\n    *,\n    dry_run: bool = False,\n    max_size_gb: float | None = None\n) -&gt; DownloadResult\n</code></pre> <p>Download only essential metadata, sidecars, and small tables.</p>"},{"location":"api/dataset/#qortex.Dataset.download_paths","title":"download_paths","text":"<pre><code>download_paths(\n    paths: list[str],\n    output_dir: Path | None = None,\n    *,\n    with_companions: bool = True,\n    dry_run: bool = False,\n    max_size_gb: float | None = None\n) -&gt; DownloadResult\n</code></pre> <p>Download exact manifest paths, optionally including companions.</p> <p>Paths are matched by set membership \u2014 they are not interpreted as glob patterns.  This avoids misbehaviour when paths contain glob metacharacters (e.g. <code>[</code> or <code>]</code> in dataset-level filenames). Pass <code>include=[...]</code> via <code>download()</code> if you need pattern matching.</p>"},{"location":"api/dataset/#qortex.Dataset.validate","title":"validate","text":"<pre><code>validate(\n    local_path: Path | None = None,\n    *,\n    config_path: Path | None = None,\n    output_json: Path | None = None,\n    ignore_warnings: bool = False,\n    ignore_nifti_headers: bool = False,\n    timeout_s: float = 600.0,\n    use_cache: bool = True,\n    refresh_cache: bool = False\n) -&gt; ValidationReport\n</code></pre> <p>Run the official BIDS Validator on a local dataset path.</p>"},{"location":"api/dataset/#qortex.Dataset.index_local","title":"index_local","text":"<pre><code>index_local(\n    local_path: Path | None = None,\n    *,\n    include_dirs: bool = False,\n    use_pybids: bool = True\n) -&gt; LocalIndexReport\n</code></pre> <p>Index a local BIDS tree and reconcile it with the remote manifest.</p>"},{"location":"api/dataset/#qortex.Dataset.eda","title":"eda","text":"<pre><code>eda(\n    local_path: Path | None = None,\n    output_html: Path | None = None,\n) -&gt; EDAReport\n</code></pre> <p>Run exploratory data analysis on the manifest.</p>"},{"location":"api/dataset/#qortex.Dataset.eda--parameters","title":"Parameters","text":"<p>local_path:     If provided, also reads local JSON sidecars for extra checks     (e.g. sampling-frequency consistency). output_html:     If provided, writes the HTML report to this path.</p>"},{"location":"api/dataset/#qortex.Dataset.convert","title":"convert","text":"<pre><code>convert(\n    output_dir: Path | None = None,\n    output_format: str = \"parquet\",\n    window_duration: float | None = None,\n    window_overlap: float = 0.0,\n    window_tmin: float = 0.0,\n    event_aligned: bool = False,\n    split_strategy: str = \"subject\",\n    stratify_by_label: bool = True,\n    shard_size: int = 1000,\n    skip_missing: bool = True,\n) -&gt; ConversionResult\n</code></pre> <p>Convert a downloaded dataset into an ML-ready artifact.</p> <p>The dataset must have been downloaded first (call <code>download()</code>).</p>"},{"location":"api/dataset/#qortex.Dataset.convert--parameters","title":"Parameters","text":"<p>output_dir:     Destination for the converted artifact. output_format:     One of <code>parquet</code>, <code>zarr</code>, <code>hdf5</code>, <code>webdataset</code>,     <code>huggingface</code>, <code>tfrecord</code>. window_duration:     Window duration in seconds.  <code>None</code> = no windowing (one sample     per file). window_overlap:     Fraction of window overlap for fixed sliding windows (0\u20131).     Only used when event_aligned=False. window_tmin:     Seconds before event onset for event-aligned windows (default 0.0).     A negative value captures a pre-stimulus baseline. event_aligned:     When True, use event_aligned_windows() instead of fixed_windows().     Requires an events TSV to be present for each signal file. split_strategy:     One of <code>subject</code>, <code>random</code>, <code>stratified</code>. stratify_by_label:     When split_strategy=\"subject\", also stratify on majority label per     subject to preserve class balance. shard_size:     Number of samples per output shard. skip_missing:     If True (default), files that fail to load are skipped and counted     in ConversionResult.warnings rather than aborting the pipeline.</p>"},{"location":"api/dataset/#qortex.Dataset.train_test_split","title":"train_test_split","text":"<pre><code>train_test_split(\n    *,\n    test_size: float = 0.2,\n    val_size: float = 0.1,\n    stratify_by: str | None = None,\n    group_by: str = \"subject\",\n    seed: int = 42\n) -&gt; dict[str, list[str]]\n</code></pre> <p>Split subject IDs into train / val / test ensuring no subject leakage.</p> <p>Subject-level splitting: all files for a subject go to one split. When <code>stratify_by</code> is given, the split is class-balanced.</p>"},{"location":"api/dataset/#qortex.Dataset.train_test_split--parameters","title":"Parameters","text":"<p>test_size:     Fraction of subjects for the test split (0\u20131). val_size:     Fraction of subjects for the validation split (0\u20131). stratify_by:     Column in participants.tsv to stratify on (e.g. <code>\"diagnosis\"</code>).     When None, subjects are split without stratification. group_by:     Grouping level.  <code>\"subject\"</code> (default) splits at the subject     level; <code>\"session\"</code> splits at the session level (allows same     subject in multiple splits \u2014 less conservative). seed:     Reproducibility seed.</p>"},{"location":"api/dataset/#qortex.Dataset.train_test_split--returns","title":"Returns","text":"<p>dict with keys <code>\"train\"</code>, <code>\"val\"</code>, <code>\"test\"</code> \u2014 each a list of BIDS subject IDs (e.g. <code>[\"sub-01\", \"sub-02\", ...]</code>).</p>"},{"location":"api/dataset/#qortex.Dataset.train_test_split--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") splits = ds.train_test_split( ...     test_size=0.2, stratify_by=\"diagnosis\", group_by=\"subject\" ... ) splits[\"train\"]  # doctest: +SKIP</p>"},{"location":"api/dataset/#qortex.Dataset.train_test_split--sub-01-sub-03","title":"['sub-01', 'sub-03', ...]","text":""},{"location":"api/dataset/#qortex.Dataset.with_format","title":"with_format","text":"<pre><code>with_format(\n    type: str = \"torch\",\n    *,\n    device: str = \"cpu\",\n    dtype: str | None = None\n)\n</code></pre> <p>Return a <code>BIDSImageDataset</code> pre-configured for the given framework.</p>"},{"location":"api/dataset/#qortex.Dataset.with_format--parameters","title":"Parameters","text":"<p>type:     <code>\"torch\"</code> (default): returns a <code>BIDSImageDataset</code> compatible     with <code>torch.utils.data.DataLoader</code>.     <code>\"numpy\"</code>: same Dataset but items returned as numpy arrays. device:     Target device string for tensor placement, e.g. <code>\"cuda\"</code> or     <code>\"cpu\"</code>.  Only meaningful when <code>type=\"torch\"</code>.</p>"},{"location":"api/dataset/#qortex.Dataset.with_format--returns","title":"Returns","text":"<p>BIDSImageDataset     A PyTorch Dataset covering the locally downloaded data.</p>"},{"location":"api/dataset/#qortex.Dataset.with_format--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\"], suffixes=[\"T1w\"]) torch_ds = ds.with_format(\"torch\", device=\"cuda\") loader = torch_ds.to_dataloader(batch_size=4, num_workers=2)</p>"},{"location":"api/dataset/#qortex.Dataset.torch_dataset","title":"torch_dataset","text":"<pre><code>torch_dataset(\n    output_dir: Path | None = None,\n    split: str | None = \"train\",\n    iterable: bool = False,\n)\n</code></pre> <p>Return a torch Dataset/IterableDataset from a converted artifact.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torch_dataloader","title":"to_torch_dataloader","text":"<pre><code>to_torch_dataloader(\n    *,\n    suffix: str = \"T1w\",\n    datatype: str = \"anat\",\n    extension: str = \".nii.gz\",\n    label_column: str | None = None,\n    batch_size: int = 4,\n    num_workers: int = 0,\n    shuffle: bool = True,\n    transform=None\n)\n</code></pre> <p>Return a PyTorch DataLoader from locally downloaded BIDS image data.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torch_dataloader--parameters","title":"Parameters","text":"<p>suffix:     BIDS suffix for the image files (e.g. <code>\"T1w\"</code>, <code>\"bold\"</code>). label_column:     Column in participants.tsv for labels. <code>None</code> = no labels. batch_size / num_workers / shuffle:     Standard DataLoader arguments. transform:     Optional callable applied to each sample dict before batching.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torch_dataloader--returns","title":"Returns","text":"<p>torch.utils.data.DataLoader</p>"},{"location":"api/dataset/#qortex.Dataset.to_torch_dataloader--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\", \"02\", \"03\"], suffixes=[\"T1w\"]) loader = ds.to_torch_dataloader( ...     suffix=\"T1w\", label_column=\"diagnosis\", ...     batch_size=4, num_workers=2, shuffle=True, ... ) for batch in loader: ...     images = batch[\"image\"]  # (B, 1, X, Y, Z)</p>"},{"location":"api/dataset/#qortex.Dataset.lightning_datamodule","title":"lightning_datamodule","text":"<pre><code>lightning_datamodule(\n    output_dir: Path | None = None,\n    batch_size: int = 32,\n    num_workers: int = 0,\n)\n</code></pre> <p>Return a PyTorch Lightning DataModule.</p>"},{"location":"api/dataset/#qortex.Dataset.sklearn_arrays","title":"sklearn_arrays","text":"<pre><code>sklearn_arrays(\n    output_dir: Path | None = None, split: str | None = None\n)\n</code></pre> <p>Return (X, y) numpy arrays for scikit-learn.</p>"},{"location":"api/dataset/#qortex.Dataset.to_monai_dicts","title":"to_monai_dicts","text":"<pre><code>to_monai_dicts(\n    image_entities: list[str] | None = None,\n    *,\n    label_target: str | None = None,\n    mask_entity: str | None = None,\n    datatype: str = \"anat\",\n    extension: str = \".nii.gz\",\n    include_metadata: bool = False,\n    train_frac: float = 0.7,\n    val_frac: float = 0.15,\n    seed: int = 42\n) -&gt; dict\n</code></pre> <p>Build MONAI-style datalist dicts for CacheDataset / PersistentDataset.</p>"},{"location":"api/dataset/#qortex.Dataset.to_monai_dicts--parameters","title":"Parameters","text":"<p>image_entities:     BIDS suffixes to use as image inputs, e.g. <code>[\"T1w\", \"T2w\"]</code>.     When multiple suffixes share the same MONAI key (default: all map     to <code>\"image\"</code>), they are returned as a list for multi-channel use. label_target:     Column in participants.tsv to use as the label, e.g.     <code>\"diagnosis\"</code>. mask_entity:     BIDS suffix for the segmentation mask, e.g. <code>\"seg\"</code>.  Mapped     to the MONAI <code>\"label\"</code> key. include_metadata:     When True, include sidecar JSON fields in each dict.</p>"},{"location":"api/dataset/#qortex.Dataset.to_monai_dicts--returns","title":"Returns","text":"<p>dict with keys <code>\"training\"</code>, <code>\"validation\"</code>, <code>\"test\"</code>, <code>\"label_classes\"</code>  (int \u2192 canonical label mapping).</p>"},{"location":"api/dataset/#qortex.Dataset.to_monai_dicts--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\", \"02\"], suffixes=[\"T1w\"]) dicts = ds.to_monai_dicts(image_entities=[\"T1w\"], label_target=\"diagnosis\") from monai.data import CacheDataset train_set = CacheDataset(data=dicts[\"training\"], transform=transform)</p>"},{"location":"api/dataset/#qortex.Dataset.to_torcheeg_epochs","title":"to_torcheeg_epochs","text":"<pre><code>to_torcheeg_epochs(\n    *,\n    modality: str = \"eeg\",\n    event_id: str | list[str] | None = None,\n    tmin: float = -0.2,\n    tmax: float = 0.5,\n    resample_hz: float | None = None,\n    stack_to_grid: bool = False,\n    grid_size: tuple[int, int] = (9, 9)\n)\n</code></pre> <p>Return a TorchEEG-compatible epoch Dataset from locally downloaded EEG.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torcheeg_epochs--parameters","title":"Parameters","text":"<p>event_id:     Trial type(s) to include.  <code>None</code> = all. tmin / tmax:     Epoch window in seconds relative to event onset.  Pre-stimulus     baseline is captured when <code>tmin &lt; 0</code>. resample_hz:     Resample all recordings before epoch extraction. stack_to_grid:     Reshape <code>(n_ch, n_t)</code> \u2192 <code>(rows, cols, n_t)</code> 2D electrode grid     for spatial CNN-based models.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torcheeg_epochs--returns","title":"Returns","text":"<p>TorchEEGBridge     Implements <code>__len__</code>/<code>__getitem__</code> returning     <code>{\"eeg\": FloatTensor, \"label\": int}</code> \u2014 TorchEEG's expected format.</p>"},{"location":"api/dataset/#qortex.Dataset.to_torcheeg_epochs--examples","title":"Examples","text":"<p>ds = Dataset(\"ds004130\") ds.download(modalities=[\"eeg\"]) epoch_ds = ds.to_torcheeg_epochs(event_id=\"stimulus\", tmin=-0.2, tmax=0.5) loader = torch.utils.data.DataLoader(epoch_ds, batch_size=64)</p>"},{"location":"api/dataset/#qortex.Dataset.visualize","title":"visualize","text":"<pre><code>visualize(\n    *,\n    subjects: list[str] | None = None,\n    suffixes: list[str] | None = None,\n    datatypes: list[str] | None = None,\n    local_path: Path | None = None,\n    output_dir: Path | None = None,\n    mode: str = \"thumbnail\",\n    max_files: int = 12,\n    n_per_suffix: int = 2,\n    open_browser: bool = False\n) -&gt; \"VisualAuditReport\"\n</code></pre> <p>Render a visual sample of locally-downloaded dataset files.</p> <p>Returns a :class:<code>VisualAuditReport</code> whose <code>.show()</code> opens an HTML gallery in the browser and <code>.to_html()</code> writes the file.</p> <p>Each NIfTI reads exactly one center slice from the nibabel ArrayProxy \u2014 the full volume is never loaded.</p>"},{"location":"api/dataset/#qortex.Dataset.visualize--parameters","title":"Parameters","text":"<p>subjects:     Restrict to these subject IDs (without <code>sub-</code> prefix).     <code>None</code> includes all subjects. suffixes:     BIDS suffixes to include, e.g. <code>[\"T1w\", \"bold\"]</code>.     <code>None</code> includes all visualizable suffixes. datatypes:     BIDS datatype folders, e.g. <code>[\"anat\", \"func\", \"eeg\"]</code>. local_path:     Root of the locally downloaded dataset.  Falls back to the     default cache directory (set during <code>download()</code>). output_dir:     If given, write the HTML report to     <code>{output_dir}/visual_audit.html</code>. mode:     Rendering mode.  <code>\"thumbnail\"</code> (default) reads one slice per     file.  <code>\"static\"</code> renders a multi-panel PNG. max_files:     Hard cap on the number of files rendered. n_per_suffix:     Maximum files per BIDS suffix; ensures variety when many     suffixes are present. open_browser:     Open the generated HTML report automatically.</p>"},{"location":"api/dataset/#qortex.Dataset.visualize--returns","title":"Returns","text":"<p>VisualAuditReport</p>"},{"location":"api/dataset/#qortex.Dataset.visualize--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\", \"02\"], suffixes=[\"T1w\"]) report = ds.visualize(suffixes=[\"T1w\"]) report.show()</p>"},{"location":"api/dataset/#qortex.Dataset.visual_audit","title":"visual_audit","text":"<pre><code>visual_audit(\n    output_dir: Path | str,\n    *,\n    subjects: list[str] | None = None,\n    suffixes: list[str] | None = None,\n    datatypes: list[str] | None = None,\n    local_path: Path | None = None,\n    n_per_suffix: int = 3,\n    max_files: int = 24,\n    open_browser: bool = False\n) -&gt; \"VisualAuditReport\"\n</code></pre> <p>Run a comprehensive visual QC report across the dataset.</p> <p>Generates a dark-theme HTML gallery with one thumbnail per file, grouped by BIDS suffix.  Useful for catching reconstruction artifacts, intensity outliers, or incomplete downloads at a glance.</p> <p>Each NIfTI thumbnail reads one center slice \u2014 the full volume is never loaded.</p>"},{"location":"api/dataset/#qortex.Dataset.visual_audit--parameters","title":"Parameters","text":"<p>output_dir:     Directory where the report is written.     File: <code>{output_dir}/visual_audit.html</code>. subjects:     Subjects to include.  <code>None</code> = all downloaded subjects. suffixes:     BIDS suffixes to include.  <code>None</code> = all visualizable suffixes. datatypes:     BIDS datatype folders to filter on. local_path:     Override the default download path. n_per_suffix:     Files per BIDS suffix.  3 gives variety without overloading. max_files:     Hard cap on total renders. open_browser:     Open the report in the browser when done.</p>"},{"location":"api/dataset/#qortex.Dataset.visual_audit--returns","title":"Returns","text":"<p>VisualAuditReport     Call <code>.to_html()</code>, <code>.show()</code>, or <code>.summary()</code>.</p>"},{"location":"api/dataset/#qortex.Dataset.visual_audit--examples","title":"Examples","text":"<p>ds = Dataset(\"ds000001\") ds.download(subjects=[\"01\", \"02\", \"03\"], suffixes=[\"T1w\", \"bold\"]) report = ds.visual_audit(\"qc/\", suffixes=[\"T1w\"])  # reads 1 slice per file print(report.summary())</p>"},{"location":"api/datasets/","title":"Datasets API","text":"<p><code>qortex.datasets</code> provides Keras-style loaders for open neuroscience datasets. Every module exposes two functions and no more: <code>describe()</code> (no download) and <code>load_data()</code> (downloads on first call, cached afterwards).</p> <pre><code>from qortex.datasets import eegbci\n\ncard = eegbci.describe()         # DatasetCard \u2014 metadata only, no download\nbundle = eegbci.load_data(       # EEGBundle \u2014 downloads and caches via MNE\n    subjects=[1, 2, 3],\n    runs=[4, 8, 12],\n)\nX, y = bundle.to_windows(window_s=4.0, bandpass=(8.0, 30.0))\n</code></pre>"},{"location":"api/datasets/#catalogue-functions","title":"Catalogue functions","text":"<p>Return all registered DatasetCards, sorted by name.</p> <p>Return the DatasetCard for a dataset by name.</p> <p>Load a dataset by name with keyword arguments forwarded to load_data().</p> <p>Print a compact table of all available datasets.</p>"},{"location":"api/datasets/#qortex.datasets.describe--parameters","title":"Parameters","text":"<p>name : Dataset identifier, e.g. \"eegbci\", \"sleep_edf\", \"oasis1\".</p>"},{"location":"api/datasets/#qortex.datasets.describe--raises","title":"Raises","text":"<p>KeyError : If the dataset name is not registered.</p>"},{"location":"api/datasets/#qortex.datasets.describe--examples","title":"Examples","text":"<p>card = qortex.datasets.describe(\"eegbci\") print(card.n_subjects, card.sampling_hz)</p>"},{"location":"api/datasets/#qortex.datasets.load_dataset--parameters","title":"Parameters","text":"<p>name   : Dataset identifier (same as used in <code>describe()</code>). **kwargs : Forwarded verbatim to the dataset module's <code>load_data()</code> function.</p>"},{"location":"api/datasets/#qortex.datasets.load_dataset--returns","title":"Returns","text":"<p>A typed Bundle for the requested dataset.</p>"},{"location":"api/datasets/#qortex.datasets.load_dataset--examples","title":"Examples","text":"<p>bundle = qortex.datasets.load_dataset(\"eegbci\", subjects=[1, 2], runs=[4, 8, 12]) bundle = qortex.datasets.load_dataset(\"sleep_edf\", subjects=[0, 1, 2]) bundle = qortex.datasets.load_dataset(\"msd_brain\", max_cases=10)</p>"},{"location":"api/datasets/#qortex.datasets.summary--examples","title":"Examples","text":"<p>print(qortex.datasets.summary())</p>"},{"location":"api/datasets/#datasetcard","title":"DatasetCard","text":"<p>Immutable metadata record for a Qortex dataset.</p> <p>Describes the dataset without downloading it.  Suitable for offline display, catalogue listings, and tutorial documentation.</p>"},{"location":"api/datasets/#qortex.datasets.DatasetCard.__str__","title":"__str__","text":"<pre><code>__str__() -&gt; str\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetCard.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/datasets/#eegbundle","title":"EEGBundle","text":"<p>Returned by EEG dataset loaders (<code>eegbci</code>, <code>sleep_edf</code>, <code>chbmit</code>).</p> <p>Loaded EEG dataset.</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle--attributes","title":"Attributes","text":"<p>card         : Dataset metadata. subjects     : Subject IDs that were loaded. runs         : Run numbers that were loaded (for EEGBCI-style datasets). sfreq        : Sampling frequency in Hz. channel_names: Channel names (same across all files unless flagged). label_map    : {int \u2192 str} mapping of integer class codes to semantic names. local_paths  : Absolute paths to local EDF/FIF/BDF files. raws         : List of mne.io.BaseRaw objects (populated when preload=True). epochs       : [n_epochs, n_channels, n_times] float32 array after windowing. labels       : [n_epochs] int array after windowing. metadata     : Per-subject/session metadata dict. qc_report    : Signal QC report from qortex.neuroclassic (None until .run_qc()).</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.n_channels","title":"n_channels  <code>property</code>","text":"<pre><code>n_channels: int\n</code></pre>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.n_files","title":"n_files  <code>property</code>","text":"<pre><code>n_files: int\n</code></pre>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.to_windows","title":"to_windows","text":"<pre><code>to_windows(\n    window_s: float = 4.0,\n    overlap: float = 0.5,\n    bandpass: tuple[float, float] | None = None,\n    event_driven: bool = True,\n    tmin: float = 0.0,\n) -&gt; \"tuple[Any, Any]\"\n</code></pre> <p>Extract fixed-length windows and return (X, y) arrays.</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.to_windows--parameters","title":"Parameters","text":"<p>window_s     : Window length in seconds. overlap      : Fraction of overlap between successive windows                (ignored when event_driven=True). bandpass     : Optional (low_hz, high_hz) to filter before windowing. event_driven : If True, extract one window per event (for task EEG).                If False, use sliding windows with given overlap. tmin         : Epoch start relative to event onset (seconds).</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.to_windows--returns","title":"Returns","text":"<p>(X, y)     X : np.ndarray, shape [n_epochs, n_channels, n_samples]     y : np.ndarray, shape [n_epochs], dtype int</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.run_qc","title":"run_qc","text":"<pre><code>run_qc(max_files: int = 5) -&gt; Any\n</code></pre> <p>Run signal QC on the first max_files files.</p> <p>Returns a SignalQualityReport for each file. Requires qortex[neuroclassic] extras.</p>"},{"location":"api/datasets/#qortex.datasets._base.EEGBundle.info","title":"info","text":"<pre><code>info() -&gt; None\n</code></pre> <p>Print a summary of the loaded bundle.</p>"},{"location":"api/datasets/#mribundle","title":"MRIBundle","text":"<p>Returned by structural MRI loaders (<code>oasis1</code>, <code>ixi</code>).</p> <p>Loaded structural MRI dataset (T1, T2, etc.).</p>"},{"location":"api/datasets/#qortex.datasets._base.MRIBundle--attributes","title":"Attributes","text":"<p>card         : Dataset metadata. subjects     : Subject identifiers. modality     : \"T1\", \"T2\", \"PD\", \"DWI\", etc. local_paths  : Absolute paths to NIfTI files (one per subject). metadata_df  : Demographic / clinical table (None until loaded). labels       : [n_subjects] array for the primary task (e.g. age or class). label_col    : Column name from the metadata table used as label. label_map    : {int \u2192 str} for classification tasks; None for regression. affines      : List of 4\u00d74 affine matrices (None until load_images()). images       : List of [x, y, z] arrays (None until load_images()). qc_report    : Image QC report (None until .run_qc()).</p>"},{"location":"api/datasets/#qortex.datasets._base.MRIBundle.n_subjects","title":"n_subjects  <code>property</code>","text":"<pre><code>n_subjects: int\n</code></pre>"},{"location":"api/datasets/#qortex.datasets._base.MRIBundle.load_images","title":"load_images","text":"<pre><code>load_images(max_subjects: int | None = None) -&gt; 'list[Any]'\n</code></pre> <p>Load NIfTI images into memory as numpy arrays.</p> <p>Requires nibabel: pip install 'qortex[mri]'</p> <p>Returns list of [x, y, z] or [x, y, z, t] float32 arrays.</p>"},{"location":"api/datasets/#qortex.datasets._base.MRIBundle.run_qc","title":"run_qc","text":"<pre><code>run_qc(max_subjects: int = 5) -&gt; Any\n</code></pre> <p>Run image QC on the first max_subjects files.</p> <p>Requires nibabel + qortex[neuroclassic].</p>"},{"location":"api/datasets/#qortex.datasets._base.MRIBundle.info","title":"info","text":"<pre><code>info() -&gt; None\n</code></pre> <p>Print bundle summary.</p>"},{"location":"api/datasets/#fmribundle","title":"FMRIBundle","text":"<p>Returned by task fMRI loaders (<code>ds000001</code>).</p> <p>Loaded task fMRI dataset in BIDS layout.</p>"},{"location":"api/datasets/#qortex.datasets._base.FMRIBundle--attributes","title":"Attributes","text":"<p>card        : Dataset metadata. subjects    : Subject IDs. task        : BIDS task name. tr          : Repetition time in seconds. bold_paths  : Per-subject BOLD NIfTI paths. event_paths : Per-subject events.tsv paths. events      : Parsed event tables (list of dicts-of-lists or DataFrames). n_volumes   : Expected volume count per run. preflight   : Preflight readiness report (None until .run_preflight()).</p>"},{"location":"api/datasets/#qortex.datasets._base.FMRIBundle.load_events","title":"load_events","text":"<pre><code>load_events() -&gt; list[dict]\n</code></pre> <p>Parse all events.tsv files into list of row-dicts.</p>"},{"location":"api/datasets/#qortex.datasets._base.FMRIBundle.run_preflight","title":"run_preflight","text":"<pre><code>run_preflight(dataset_path: Path) -&gt; Any\n</code></pre> <p>Run qortex preflight check for fMRI design readiness.</p>"},{"location":"api/datasets/#qortex.datasets._base.FMRIBundle.info","title":"info","text":"<pre><code>info() -&gt; None\n</code></pre>"},{"location":"api/datasets/#segmentationbundle","title":"SegmentationBundle","text":"<p>Returned by segmentation loaders (<code>msd_brain</code>).</p> <p>Loaded medical image segmentation dataset.</p>"},{"location":"api/datasets/#qortex.datasets._base.SegmentationBundle--attributes","title":"Attributes","text":"<p>card         : Dataset metadata. case_ids     : Unique case identifiers. image_paths  : Per-case image file paths (list of lists for multimodal). mask_paths   : Per-case segmentation mask paths. label_map    : {int \u2192 str} mapping of mask integer values to region names. modalities   : List of modality names (e.g. ['FLAIR', 'T1w', 'T1gd', 'T2w']). split        : 'train', 'val', 'test', or 'all'.</p>"},{"location":"api/datasets/#qortex.datasets._base.SegmentationBundle.n_cases","title":"n_cases  <code>property</code>","text":"<pre><code>n_cases: int\n</code></pre>"},{"location":"api/datasets/#qortex.datasets._base.SegmentationBundle.load_pair","title":"load_pair","text":"<pre><code>load_pair(index: int) -&gt; 'tuple[Any, Any]'\n</code></pre> <p>Load one image-mask pair as numpy arrays.</p> <p>Returns (image_array, mask_array). For multimodal: image_array shape is [n_modalities, x, y, z].</p>"},{"location":"api/datasets/#qortex.datasets._base.SegmentationBundle.info","title":"info","text":"<pre><code>info() -&gt; None\n</code></pre>"},{"location":"api/datasets/#datasetregistry","title":"DatasetRegistry","text":"<p>The global registry that backs <code>list_available()</code> and <code>describe()</code>. Advanced users can access it directly via <code>qortex.datasets._REGISTRY</code>.</p> <p>Global registry of all known dataset cards.</p>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.register","title":"register","text":"<pre><code>register(card: DatasetCard) -&gt; None\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.get","title":"get","text":"<pre><code>get(name: str) -&gt; DatasetCard\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.list_all","title":"list_all","text":"<pre><code>list_all() -&gt; list[DatasetCard]\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.list_by_modality","title":"list_by_modality","text":"<pre><code>list_by_modality(modality: str) -&gt; list[DatasetCard]\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.list_by_tutorial","title":"list_by_tutorial","text":"<pre><code>list_by_tutorial(tutorial_id: str) -&gt; list[DatasetCard]\n</code></pre>"},{"location":"api/datasets/#qortex.datasets.DatasetRegistry.summary_table","title":"summary_table","text":"<pre><code>summary_table() -&gt; str\n</code></pre> <p>Compact tabular listing of all datasets.</p>"},{"location":"api/datasets/#available-datasets","title":"Available datasets","text":"Name <code>load_data()</code> returns Tutorials <code>eegbci</code> <code>EEGBundle</code> T01, T02 <code>sleep_edf</code> <code>EEGBundle</code> T03 <code>chbmit</code> <code>EEGBundle</code> T04 <code>oasis1</code> <code>MRIBundle</code> T05 <code>ixi</code> <code>MRIBundle</code> T06 <code>ds000001</code> <code>FMRIBundle</code> T07 <code>msd_brain</code> <code>SegmentationBundle</code> T08"},{"location":"api/datasets/#qortexdatasetseegbci","title":"<code>qortex.datasets.eegbci</code>","text":"<p>PhysioNet EEG Motor Movement/Imagery. 109 subjects, 160 Hz, 64-channel BCI2000.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Download (first call) and load EEGBCI data.</p> <p>Label map constants</p> Constant Runs Classes <code>LABEL_MAP_FIST_IMAGERY</code> 4, 8, 12 rest / left_fist_imagery / right_fist_imagery <code>LABEL_MAP_FEET_IMAGERY</code> 6, 10, 14 rest / both_fists_imagery / both_feet_imagery <code>LABEL_MAP_FIST_EXECUTION</code> 3, 7, 11 rest / left_fist / right_fist <code>LABEL_MAP_FEET_EXECUTION</code> 5, 9, 13 rest / both_fists / both_feet <code>LABEL_MAP_BASELINE</code> 1, 2 eyes_open / eyes_closed"},{"location":"api/datasets/#qortex.datasets.eegbci.load_data--parameters","title":"Parameters","text":"<p>subjects  : Subject numbers 1\u2013109. Defaults to [1, 2, 3]. runs      : Run numbers 1\u201314. Defaults to [4, 8, 12] (left/right fist imagery). preload   : If True, load raw data into memory (required for to_windows()). verbose   : Forward MNE verbosity.</p>"},{"location":"api/datasets/#qortex.datasets.eegbci.load_data--returns","title":"Returns","text":"<p>EEGBundle with label_map, raws, local_paths, sfreq, channel_names.</p>"},{"location":"api/datasets/#qortex.datasets.eegbci.load_data--examples","title":"Examples","text":"<p>bundle = eegbci.load_data(subjects=[1, 2, 3], runs=[4, 8, 12]) X, y = bundle.to_windows(window_s=4.0, bandpass=(8.0, 30.0))</p>"},{"location":"api/datasets/#qortexdatasetssleep_edf","title":"<code>qortex.datasets.sleep_edf</code>","text":"<p>Sleep-EDF Expanded \u2014 5-class AASM sleep staging. Cassette + temazepam subsets.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Download (first call) and load Sleep-EDF data.</p> <p>Label map \u2014 <code>LABEL_MAP = {0: \"Wake\", 1: \"N1\", 2: \"N2\", 3: \"N3\", 4: \"REM\"}</code></p>"},{"location":"api/datasets/#qortex.datasets.sleep_edf.load_data--parameters","title":"Parameters","text":"<p>subjects        : Subject indices (0-based). Defaults to [0, 1, 2]. recording       : \"SC\" (Cassette), \"ST\" (Telemetry), or \"SC_ST\" for both. preload         : Load raw data into memory. crop_wake_mins  : Crop leading/trailing wake to N minutes to reduce                   class imbalance. Set to None to disable. verbose         : MNE verbosity.</p>"},{"location":"api/datasets/#qortex.datasets.sleep_edf.load_data--returns","title":"Returns","text":"<p>EEGBundle with label_map = LABEL_MAP (5-class AASM staging).</p>"},{"location":"api/datasets/#qortex.datasets.sleep_edf.load_data--examples","title":"Examples","text":"<p>bundle = sleep_edf.load_data(subjects=[0, 1, 2, 3]) X, y = bundle.to_windows(window_s=30.0, event_driven=False)</p>"},{"location":"api/datasets/#qortexdatasetschbmit","title":"<code>qortex.datasets.chbmit</code>","text":"<p>CHB-MIT Scalp EEG Seizure Database. 23 cases, binary seizure/non-seizure labels.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Load CHB-MIT data from a local directory.</p> <p>CHB-MIT must be downloaded separately from PhysioNet (~42.6 GB). Provide the root directory containing case subdirectories (chb01/, etc.).</p> <p>A single seizure interval within an EDF file.</p> <p>Parse a chbNN-summary.txt file into a dict of {filename: [SeizureInterval]}.</p> <p>Summary file format (from PhysioNet CHB-MIT documentation):     File Name: chb01_03.edf     File Start Time: 14:43:00     File End Time: 15:43:00     Number of Seizures in File: 1     Seizure Start Time: 2996 seconds     Seizure End Time: 3036 seconds</p> <p>Compute window boundaries and binary labels for one EDF file.</p> <p>Label map \u2014 <code>LABEL_MAP = {0: \"non_seizure\", 1: \"seizure\"}</code></p>"},{"location":"api/datasets/#qortex.datasets.chbmit.load_data--parameters","title":"Parameters","text":"<p>cases           : Case names, e.g. [\"chb01\", \"chb02\"]. Defaults to [\"chb01\"]. seizure_files_only : If True, only load EDF files that contain seizures                    (as listed in summary.txt). Reduces download footprint. local_root      : Path to the downloaded dataset root.                   Falls back to QORTEX_DATA_DIR/chbmit or ~/.cache/qortex/datasets/chbmit. preload         : Load raw data into memory. verbose         : MNE verbosity.</p>"},{"location":"api/datasets/#qortex.datasets.chbmit.load_data--returns","title":"Returns","text":"<p>EEGBundle with label_map = {0: 'non_seizure', 1: 'seizure'}. The bundle also carries <code>.metadata['seizure_map']</code> for window-level labeling.</p>"},{"location":"api/datasets/#qortex.datasets.chbmit.load_data--examples","title":"Examples","text":"<p>bundle = chbmit.load_data(cases=[\"chb01\"], seizure_files_only=True, ...                           local_root=\"/data/chbmit\") windows, labels = chbmit.label_windows_for_file( ...     \"chb01_03.edf\", n_samples=..., sfreq=256.0, ...     window_s=5.0, step_s=1.0, seizure_map=bundle.metadata[\"seizure_map\"])</p>"},{"location":"api/datasets/#qortex.datasets.chbmit.SeizureInterval.overlaps_window","title":"overlaps_window","text":"<pre><code>overlaps_window(\n    window_start: float, window_end: float\n) -&gt; bool\n</code></pre> <p>True if the seizure interval overlaps [window_start, window_end).</p>"},{"location":"api/datasets/#qortex.datasets.chbmit.label_windows_for_file--returns","title":"Returns","text":"<p>(windows, labels)     windows : list of (start_sample, end_sample) tuples     labels  : list of int (0=non_seizure, 1=seizure)</p>"},{"location":"api/datasets/#qortexdatasetsoasis1","title":"<code>qortex.datasets.oasis1</code>","text":"<p>OASIS-1 cross-sectional structural MRI. 416 subjects, dementia classification.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Load OASIS-1 data from a local directory.</p> <p>OASIS-1 requires prior registration and manual download. See describe().access_instructions for the expected directory layout.</p> <p>Parse oasis_cross-sectional.csv into {subject_id: {col: val}}.</p> <p>CDR column: 0=no dementia, 0.5=very mild, 1=mild, 2=moderate AD. CDR binary: CDR=0 \u2192 0 (no_dementia); CDR&gt;0 \u2192 1 (dementia).</p> <p>Label map \u2014 <code>LABEL_MAP = {0: \"no_dementia\", 1: \"dementia\"}</code></p>"},{"location":"api/datasets/#qortex.datasets.oasis1.load_data--parameters","title":"Parameters","text":"<p>local_root          : Root of the extracted OASIS-1 dataset.                       Falls back to QORTEX_DATA_DIR/oasis1. cdr_binary          : Use CDR=0 vs CDR&gt;0 as the binary label. exclude_missing_cdr : Exclude subjects with no CDR score. modality            : \"T1\" (only T1 images are currently supported). max_subjects        : Limit number of subjects (useful for quick tests).</p>"},{"location":"api/datasets/#qortex.datasets.oasis1.load_data--returns","title":"Returns","text":"<p>MRIBundle with:   - label_col = 'cdr_binary'   - label_map = {0: 'no_dementia', 1: 'dementia'}   - metadata = clinical table keyed by subject_id   - labels = np.ndarray of 0/1 values aligned to subjects list</p>"},{"location":"api/datasets/#qortexdatasetsixi","title":"<code>qortex.datasets.ixi</code>","text":"<p>IXI multimodal MRI (T1/T2/PD/MRA/DWI). 600 subjects, age regression / sex classification.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Load IXI data from a local directory.</p> <p>Parse IXI demographics from IXI.csv or IXI.xls.</p> <p>Returns {ixi_id: {age, sex, site, ethnic_group, ...}}.</p> <p>IXI ID is an integer (1\u2013628); the NIfTI filename encodes it as IXI%03d. sex codes: 1=Male, 2=Female.</p> <p>Label map \u2014 <code>LABEL_MAP_SEX = {0: \"male\", 1: \"female\"}</code></p>"},{"location":"api/datasets/#qortex.datasets.ixi.load_data--parameters","title":"Parameters","text":"<p>local_root            : Root of the downloaded IXI dataset.                         Falls back to QORTEX_DATA_DIR/ixi. modalities            : List of modalities to load. Defaults to [\"T1\"].                         Options: \"T1\", \"T2\", \"PD\", \"MRA\", \"DWI\".                         Only the first modality's paths are stored as primary. task                  : \"age_regression\" or \"sex_classification\". exclude_missing_labels : Skip subjects with no label for the chosen task. max_subjects          : Limit for quick experiments.</p>"},{"location":"api/datasets/#qortex.datasets.ixi.load_data--returns","title":"Returns","text":"<p>MRIBundle with:   - For age_regression: label_col=\"AGE\", labels=float array, label_map=None.   - For sex_classification: label_col=\"sex_label\", labels=int array,     label_map={0: \"male\", 1: \"female\"}.</p>"},{"location":"api/datasets/#qortex.datasets.ixi.load_data--examples","title":"Examples","text":"<p>bundle = ixi.load_data(local_root=\"/data/ixi\", task=\"age_regression\", ...                        max_subjects=50) bundle.load_images(max_subjects=10)</p>"},{"location":"api/datasets/#qortexdatasetsds000001","title":"<code>qortex.datasets.ds000001</code>","text":"<p>OpenfMRI balloon analogue risk task (BART). 16 subjects, BIDS fMRI, event design.</p> <p>Return the DatasetCard without downloading anything.</p> <p>Load ds000001 BIDS dataset from a local directory.</p> <p>ds000001 is available for free download \u2014 no registration required.</p>"},{"location":"api/datasets/#qortex.datasets.ds000001.load_data--parameters","title":"Parameters","text":"<p>local_root : Root of the BIDS dataset directory.              Falls back to QORTEX_DATA_DIR/ds000001. subjects   : List of 2-digit subject IDs (e.g. [\"01\", \"02\"]).              Defaults to all discovered subjects. tr         : Repetition time in seconds (default 2.0).</p>"},{"location":"api/datasets/#qortex.datasets.ds000001.load_data--returns","title":"Returns","text":"<p>FMRIBundle with BOLD and event file paths.</p>"},{"location":"api/datasets/#qortex.datasets.ds000001.load_data--examples","title":"Examples","text":"<p>bundle = ds000001.load_data(local_root=\"/data/ds000001\", subjects=[\"01\",\"02\"]) events = bundle.load_events() report = bundle.run_preflight(Path(\"/data/ds000001\"))</p>"},{"location":"api/datasets/#qortexdatasetsmsd_brain","title":"<code>qortex.datasets.msd_brain</code>","text":"<p>MSD Brain Tumour segmentation. 4-class glioma segmentation (FLAIR, T1w, T1gd, T2w).</p> <p>Return the DatasetCard without downloading anything.</p> <p>Load MSD Brain Tumour data via MONAI.</p> <p>Label map \u2014 <code>LABEL_MAP = {0: \"background\", 1: \"NCR_NET\", 2: \"edema\", 3: \"enhancing_tumour\"}</code></p> <p>Modalities \u2014 <code>MODALITIES = [\"FLAIR\", \"T1w\", \"T1gd\", \"T2w\"]</code></p> <p>See the Tutorials for end-to-end usage of each dataset.</p>"},{"location":"api/datasets/#qortex.datasets.msd_brain.load_data--parameters","title":"Parameters","text":"<p>local_root : Directory for dataset storage.              Falls back to QORTEX_DATA_DIR/msd_brain. split      : \"train\" (484 cases with masks) or \"test\" (266 cases, no masks). max_cases  : Limit for fast experiments. download   : Download if not already cached. seed       : Random seed for reproducible split (if MONAI applies one).</p>"},{"location":"api/datasets/#qortex.datasets.msd_brain.load_data--returns","title":"Returns","text":"<p>SegmentationBundle with:   - image_paths: list of lists shaped (case, modality)   - mask_paths: list of mask paths (empty for test split)   - label_map: {0: background, 1: NCR_NET, 2: edema, 3: enhancing_tumour}   - modalities: ['FLAIR', 'T1w', 'T1gd', 'T2w']</p>"},{"location":"api/datasets/#qortex.datasets.msd_brain.load_data--examples","title":"Examples","text":"<p>bundle = msd_brain.load_data(split=\"train\", max_cases=20) image, mask = bundle.load_pair(0) print(image.shape, mask.shape)  # (4, 240, 240, 155), (240, 240, 155)</p>"},{"location":"api/neuroclassic/","title":"Neuroclassic API","text":"<p><code>qortex.neuroclassic</code> provides deterministic, non-LLM computational neuroscience methods. Every function returns a structured report \u2014 not raw arrays.</p> <pre><code>pip install \"qortex[neuroclassic]\"   # scipy\npip install \"qortex[eeg]\"            # + MNE for signal loading\npip install \"qortex[mri]\"            # + nibabel for image loading\n</code></pre>"},{"location":"api/neuroclassic/#base-types","title":"Base types","text":"<p>Every method returns a <code>NeuroClassicResult</code> (single file) or <code>NeuroClassicReport</code> (whole dataset). <code>MetricResult</code> carries individual scalar/vector metrics. <code>CohortMetricReport</code> holds cohort-level descriptive statistics and outlier detection.</p> <p>               Bases: <code>str</code>, <code>Enum</code></p> <p>One named scalar or vector metric from a classical method.</p> <p>Configuration for running a classical neuroanalytic method.</p> <p>Every method must declare its target modality, required inputs, and parameters. Specs are immutable after construction and re-used across subjects/sessions.</p> <p>Raw output of one classical method run on one input unit (file/subject/session).</p> <p>Contains metrics, findings, and provenance \u2014 not rendered for display.</p> <p>Aggregated report from a classical method across multiple input units.</p> <p>Equivalent to a CheckReport but specific to neuroclassic methods.</p> <p>Cohort-level summary across a set of NeuroClassicResults.</p> <p>Computes descriptive statistics over per-subject/session metrics.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.MetricResult.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicSpec.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicResult.add_metric","title":"add_metric","text":"<pre><code>add_metric(m: MetricResult) -&gt; None\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicResult.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicReport.all_warnings","title":"all_warnings  <code>property</code>","text":"<pre><code>all_warnings: list[str]\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicReport.all_blockers","title":"all_blockers  <code>property</code>","text":"<pre><code>all_blockers: list[str]\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicReport.has_blockers","title":"has_blockers  <code>property</code>","text":"<pre><code>has_blockers: bool\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicReport.add_result","title":"add_result","text":"<pre><code>add_result(r: NeuroClassicResult) -&gt; None\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.NeuroClassicReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.CohortMetricReport.compute","title":"compute","text":"<pre><code>compute() -&gt; 'CohortMetricReport'\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.CohortMetricReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#signal-qc","title":"Signal QC","text":"<p>Compute per-channel and recording-level QC metrics.</p> <p>Run signal QC across all matching files in a BIDS dataset.</p> <p>Requires MNE for file loading: pip install 'qortex[eeg]'</p> <p>Full signal QC report for one recording.</p> <p>Maps onto NeuroClassicResult \u2014 use <code>to_result()</code> for pipeline integration.</p> <p>Per-channel quality metrics.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_signal_qc--parameters","title":"Parameters","text":"<p>data:     Signal array, shape [n_channels, n_times] (float). sampling_frequency_hz:     Sampling rate; must be &gt; 0. channel_names:     Optional list of channel names.  Defaults to ch_0, ch_1, \u2026 scope:     Identifier for this recording (file path or sub-ses key). flatline_threshold_sd:     A channel is flatline if its std &lt; this fraction of the median std across channels. saturation_pct:     A channel is saturated if it spends &gt; (100 - saturation_pct)% at its min or max value. compute_psd:     Whether to compute spectral metrics (requires scipy). line_noise_hz:     If given, compute line-noise power at this frequency. compute_correlations:     Whether to compute inter-channel correlation outliers. correlation_outlier_threshold:     Number of SDs above the mean absolute correlation to flag as outlier.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_signal_qc--returns","title":"Returns","text":"<p>SignalQualityReport</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SignalQualityReport.to_result","title":"to_result","text":"<pre><code>to_result() -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SignalQualityReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ChannelQC--metrics","title":"Metrics","text":"<p>is_flatline          : std &lt; flatline_threshold_sd \u00d7 median_std is_saturated         : fraction at phys min/max &gt; (100-saturation_pct)% peak_to_peak         : max - min over finite samples robust_variance      : MAD-based variance (\u00d71.4826\u00b2 for normal consistency) line_noise_power_*   : Welch PSD power summed in \u00b12 Hz band around 50/60 Hz psd_slope            : log-log linear fit slope on 1-40 Hz (1/f exponent) spectral_entropy     : Shannon entropy of normalised Welch PSD (nats / log\u2082) autocorrelation_lag1 : Pearson lag-1 autocorrelation coefficient autocorrelation_decay_ms : first lag (ms) where ACF drops below 0.5 correlation_outlier  : mean abs cross-channel correlation is outlier (&gt;threshold SD below mean) correlation_score    : mean abs correlation with all other channels</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ChannelQC.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#image-qc","title":"Image QC","text":"<p>Compute QC metrics for a 3-D or 4-D NIfTI image array.</p> <p>Run image QC across all NIfTI files in a BIDS dataset.</p> <p>Requires nibabel: pip install 'qortex[mri]'</p> <p>Per-image QC report for one NIfTI volume or fMRI series.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_image_qc--parameters","title":"Parameters","text":"<p>data:     Image array, shape [x, y, z] or [x, y, z, t]. voxel_sizes_mm:     Voxel dimensions in mm. affine:     4\u00d74 affine matrix (optional; used for orientation checks). scope:     Identifier for this image (file path). background_threshold_pct:     Voxels below this percentile of the image max are treated as background. tsnr_robust:     If True, use median/IQR-based tSNR estimate (more robust to outlier volumes).</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_image_qc--returns","title":"Returns","text":"<p>ImageQualityReport</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ImageQualityReport.to_result","title":"to_result","text":"<pre><code>to_result() -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ImageQualityReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#connectivity","title":"Connectivity","text":"<p>Compute Pearson correlation connectivity matrix.</p> <p>Construction path is fully declared in the returned ConnectivityMatrix.</p> <p>Compute graph-theoretic summary from a ConnectivityMatrix.</p> <p>All metrics operate on |adjacency| (undirected, weighted).  The construction summary is propagated so reports always know how the graph was built.</p> <p>Adjacency matrix with full construction metadata.</p> <p>Full construction-path declaration for a connectivity matrix.</p> <p>Graph-theoretic summary computed from a ConnectivityMatrix.</p> <p>All metrics are computed on the absolute-value adjacency (undirected, potentially weighted).  The construction_summary field ensures the graph provenance is always recorded alongside the metrics.</p> <p>Algorithms:   degree/strength/density  \u2014 direct sums over adjacency matrix   clustering_coefficient   \u2014 Watts-Strogatz local CC (binary graph)   global_efficiency        \u2014 harmonic mean of inverse BFS distances   mean_path_length         \u2014 arithmetic mean of BFS shortest paths   betweenness_centrality   \u2014 Brandes (2001) BFS algorithm, normalised   community_assignments    \u2014 greedy modularity maximisation (Newman Q)   modularity               \u2014 Newman-Girvan Q of detected communities   small_world_sigma        \u2014 (C/C_rand)/(L/L_rand), ER null model</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_pearson_connectivity--parameters","title":"Parameters","text":"<p>data:     [n_channels, n_times] float array. channel_names:     Node labels. time_window_s:     Window over which correlation is computed (uses full recording if n_times \u2264 win_samples). sampling_hz:     Sampling rate in Hz. frequency_band:     Optional (low, high) Hz for Butterworth bandpass filtering before correlation. threshold:     Absolute correlation threshold; edges below this are zeroed. scope:     Recording identifier.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_pearson_connectivity--returns","title":"Returns","text":"<p>ConnectivityMatrix     Full adjacency matrix with construction metadata.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_graph_metrics--algorithms","title":"Algorithms","text":"<p>clustering_coefficient : Watts-Strogatz local CC on binary graph, mean over nodes. global_efficiency      : Harmonic mean of inverse BFS path lengths (E_glob). mean_path_length       : Arithmetic mean of BFS shortest paths (reachable pairs). betweenness_centrality : Brandes (2001) normalised betweenness on binary graph. community_assignments  : Greedy modularity maximisation (one-pass, per-node merge). modularity             : Newman-Girvan Q of final community partition. small_world_sigma      : (C/C_rand)/(L/L_rand) vs Erd\u0151s\u2013R\u00e9nyi null model.</p> <p>BFS metrics are skipped (set to None) when n &gt; 200 to avoid O(n\u00b2) overhead without sparse solvers.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ConnectivityMatrix.n_nodes","title":"n_nodes  <code>property</code>","text":"<pre><code>n_nodes: int\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ConnectivityMatrix.to_dict","title":"to_dict","text":"<pre><code>to_dict(include_matrix: bool = False) -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ConnectivitySpec.summary","title":"summary","text":"<pre><code>summary() -&gt; str\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.GraphMetricReport.to_result","title":"to_result","text":"<pre><code>to_result(\n    method_version: str = __version__,\n) -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.GraphMetricReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#statistical-diagnostics","title":"Statistical diagnostics","text":"<p>Compute statistical diagnostics from tabular subject data (participants.tsv rows).</p> <p>Aggregate a specific metric across NeuroClassicResults into a CohortMetricReport.</p> <p>Multivariate statistical summary for workflow safety.</p> <p>Contains descriptive statistics, confound associations, class balance, missingness, and split-balance diagnostics.</p> <p>Descriptive statistics for one variable.</p> <p>Association between a target variable and a potential confound.</p> <p>Balance of a categorical variable across train/val/test splits.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_statistical_diagnostics--parameters","title":"Parameters","text":"<p>rows:     List of dicts with string values (from TSV parsing). target:     Column to treat as the classification target. confound_columns:     Columns to check for association with the target. scope:     File or dataset identifier.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_statistical_diagnostics--returns","title":"Returns","text":"<p>StatisticalDiagnosticReport</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.StatisticalDiagnosticReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.StatisticalDiagnosticReport.to_result","title":"to_result","text":"<pre><code>to_result() -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.VariableSummary.missing_fraction","title":"missing_fraction  <code>property</code>","text":"<pre><code>missing_fraction: float\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.VariableSummary.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ConfoundAssociation--attributes","title":"Attributes","text":"<p>method : str     \"cramers_v\"       \u2014 categorical \u00d7 categorical (bias-corrected)     \"pearson_r\"       \u2014 numeric \u00d7 numeric     \"cohens_d_smd\"    \u2014 numeric \u00d7 categorical (standardised mean difference) p_value_permutation : float | None     Two-tailed permutation p-value (n=999, fixed seed).  None when     sample size &lt; MIN_N_FOR_STATS or method is unsuitable.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ConfoundAssociation.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SplitBalanceSummary.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#information-theory","title":"Information theory","text":"<p>Compute spectral entropy per channel from a 2-D signal array.</p> <p>Spectral entropy H = -\u03a3 p_i log\u2082(p_i) where p_i is the fraction of total PSD power in frequency bin i.  Uses Welch method when scipy is available; falls back to FFT-magnitude-squared estimate otherwise.</p> <p>Compute per-channel autocorrelation summary.</p> <p>Reports lag-1 ACF, ACF half-life, and white-noise flag for each channel. This is a temporal redundancy diagnostic \u2014 used to detect highly autocorrelated channels that may violate i.i.d. assumptions in ML pipelines.</p> <p>Spectral entropy across all channels.</p> <p>H_max = log\u2082(n_psd_bins) \u2014 maximum entropy for uniform PSD. Relative entropy = H / H_max is reported alongside absolute H.</p> <p>Spectral entropy for one channel.</p> <p>Autocorrelation summary across all channels.</p> <p>Temporal redundancy diagnostic \u2014 not a stability or complexity measure.</p> <p>Autocorrelation summary for one channel.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_spectral_entropy--parameters","title":"Parameters","text":"<p>data:     [n_channels, n_times] float array. sampling_frequency_hz:     Sampling rate; must be &gt; 0. channel_names:     Optional channel labels. scope:     Recording identifier. nperseg_s:     Welch segment length in seconds.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_spectral_entropy--returns","title":"Returns","text":"<p>SpectralEntropyReport</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_autocorrelation_summary--parameters","title":"Parameters","text":"<p>data:     [n_channels, n_times] float array. sampling_frequency_hz:     Sampling rate; must be &gt; 0. channel_names:     Optional channel labels. scope:     Recording identifier. max_lag_s:     Maximum lag (seconds) to search for ACF half-life. white_noise_threshold:     |lag1| below this \u2192 is_white_noise=True.     Defaults to 1/sqrt(n_times) (95% CI for white noise).</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.compute_autocorrelation_summary--returns","title":"Returns","text":"<p>AutocorrelationReport</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SpectralEntropyReport.to_result","title":"to_result","text":"<pre><code>to_result() -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SpectralEntropyReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ChannelSpectralEntropy.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.AutocorrelationReport.to_result","title":"to_result","text":"<pre><code>to_result() -&gt; NeuroClassicResult\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.AutocorrelationReport.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#qortex.neuroclassic.ChannelAutocorrelation.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/neuroclassic/#leakage-safe-split-optimisation","title":"Leakage-safe split optimisation","text":"<p>Assign subjects to train/val/test splits respecting group constraints.</p> <p>Configuration for leakage-safe split assignment.</p> <p>Output of leakage-safe split assignment.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.assign_leakage_safe_splits--parameters","title":"Parameters","text":"<p>rows : list[dict[str, str]]     Tabular data (e.g. participants.tsv rows as string dicts). id_column : str     Column whose value uniquely identifies each row (subject ID). constraints : SplitConstraints | None     Split configuration.  Uses default SplitConstraints if None.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.assign_leakage_safe_splits--returns","title":"Returns","text":"<p>SplitAssignmentResult     Contains assignments, class distributions, violations, and diagnostics.     Always returned \u2014 even when constraints are violated \u2014 so the caller     can inspect and decide whether to proceed.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.assign_leakage_safe_splits--notes","title":"Notes","text":"<p>Groups are kept intact: all rows sharing the same group key (concatenated values of group_columns) are assigned to the same split.  If a group is too large to place without violating fraction targets, it is placed in the least-full split and the violation is recorded.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SplitConstraints--attributes","title":"Attributes","text":"<p>train_fraction : float     Target fraction of subjects in the training split. val_fraction : float     Target fraction in validation split. test_fraction : float     Target fraction in test split.     Note: train + val + test must sum to 1.0 (enforced). group_columns : list[str]     Column names whose combined value defines a group.  All rows     sharing a group key must land in the same split.     Typical: [\"site\"], [\"family_id\"], [\"scanner\", \"site\"]. stratify_column : str | None     Column to stratify (balance class distribution across splits). max_imbalance : float     Maximum allowed deviation from the target class fraction within     each split (e.g. 0.1 = \u00b110%).  Violations are reported as     unmet_constraints but do not prevent assignment. random_seed : int     Seed for tie-breaking. Does not change determinism of the greedy     algorithm \u2014 only affects which group wins when two are equally     ranked.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SplitAssignmentResult--attributes","title":"Attributes","text":"<p>assignments : dict[str, str]     {subject_id \u2192 split_name} for every input row.     Split names are \"train\", \"val\", \"test\". class_distribution : dict[str, dict[str, float]]     {split \u2192 {class_label \u2192 fraction}} for the stratify column. residual_imbalance : float     Maximum |actual_fraction - target_fraction| over all splits and     classes.  0.0 = perfectly balanced. optimality_status : str     \"optimal\"     \u2014 residual_imbalance == 0     \"near_optimal\" \u2014 residual_imbalance &lt;= max_imbalance     \"violated\"    \u2014 residual_imbalance &gt; max_imbalance group_violations : list[str]     Group keys that were split across partitions (should be empty). unmet_constraints : list[str]     Human-readable list of constraints that could not be satisfied. train_fraction_actual, val_fraction_actual, test_fraction_actual : float     Actual subject counts / total for each split. solver : str     Algorithm identifier (\"greedy_stratified\"). random_seed : int     Seed used for tie-breaking. runtime_s : float     Wall-clock time.</p>"},{"location":"api/neuroclassic/#qortex.neuroclassic.SplitAssignmentResult.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/visualization/","title":"Visualization API","text":"<p>All visualization functions use lazy loading \u2014 only the slices or segments needed for the current view are read from disk.</p> <pre><code>pip install \"qortex[visual-all]\"   # Plotly + nibabel + MNE\npip install \"qortex[mri]\"          # nibabel only (MRI/fMRI/DWI)\npip install \"qortex[eeg]\"          # MNE only (EEG/MEG)\n</code></pre>"},{"location":"api/visualization/#top-level-functions","title":"Top-level functions","text":"<p>qortex.visualize \u2014 modality-aware visual inspection layer.</p>"},{"location":"api/visualization/#qortex.visualize--architecture","title":"Architecture","text":"<p>Every rendering path goes through three typed stages:</p> <pre><code>inspect(source)  \u2192  VisualAsset   (what is this file?)\nasset.plan()     \u2192  VisualPlan    (what will be rendered?)\nasset.render()   \u2192  VisualResult  (html + png + provenance)\n</code></pre> <p>The universal entry point:</p> <pre><code>visualize(source)  \u2192  VisualResult\n</code></pre> <p>routes automatically to the right viewer based on the asset's detected intent, family, and modality \u2014 no manual dispatch needed.</p>"},{"location":"api/visualization/#qortex.visualize--supported-sources","title":"Supported sources","text":"<p>Volume imaging (3D / 4D):     NIfTI (.nii, .nii.gz) \u2014 anatomical MRI, fMRI BOLD, DWI, PET, stat maps     DICOM series directory \u2014 CT, MR, PET; multi-series study browser     FreeSurfer (.mgz, .mgh) \u2014 anatomical volumes     NumPy arrays / nibabel images (duck-typed)</p> <p>Overlays:     overlay_mask(base, mask)       \u2014 binary mask overlay     overlay_labelmap(base, labels) \u2014 multi-atlas segmentation     overlay_stat(base, stat_map)   \u2014 z/t statistical map with threshold     overlay_pet(base, pet)         \u2014 PET SUVR on anatomy</p> <p>Electrophysiology:     EEG/MEG/iEEG \u2014 via MNE (.fif, .edf, .bdf, .set, .vhdr, \u2026)     BOLD global mean signal from 4D NIfTI</p> <p>DICOM:     browse_dicom(dir) \u2192 HTML study/series browser</p>"},{"location":"api/visualization/#qortex.visualize--quick-usage","title":"Quick usage","text":"<pre><code>from qortex import visualize\n\n# Auto-dispatch \u2014 always returns VisualResult\nresult = visualize.visualize(\"sub-01_T1w.nii.gz\")\nresult.show()                   # open in browser\nresult.to_html(\"output.html\")   # write file + JSON sidecar\n\n# Quality-control dispatch\nqc = visualize.visualize(\"sub-01_task-rest_bold.nii.gz\", mode=\"qc\")\nqc.to_html(\"bold_qc.html\")\n\n# Inspect only (no rendering, very fast)\nasset = visualize.inspect(\"sub-01_task-rest_bold.nii.gz\")\nprint(asset.summary())\nprint(asset.plan().describe())\n\n# Overlay API\nresult = visualize.overlay_stat(\"T1w.nii.gz\", \"zmap.nii.gz\", threshold=2.3)\nresult = visualize.overlay_mask(\"T1w.nii.gz\", \"brain_mask.nii.gz\")\nresult = visualize.overlay_labelmap(\"T1w.nii.gz\", \"aparc+aseg.nii.gz\")\nresult = visualize.overlay_pet(\"T1w.nii.gz\", \"pet_suv.nii.gz\")\n\n# DICOM\nresult = visualize.browse_dicom(\"/path/to/dicom_study/\")\n\n# Explicit viewers\nviewer = visualize.volume(\"bold.nii.gz\", window=\"auto\")\nts     = visualize.timeseries(\"raw.edf\")\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.inspect","title":"inspect","text":"<pre><code>inspect(source: Any) -&gt; 'VisualAsset'\n</code></pre> <p>Inspect any neuroimaging source and return a VisualAsset.</p> <p>Fast: reads only file headers, never loads pixel data.</p>"},{"location":"api/visualization/#qortex.visualize.inspect--parameters","title":"Parameters","text":"<p>source:     Path (str or Path), nibabel image, MNE Raw, or numpy array.</p>"},{"location":"api/visualization/#qortex.visualize.inspect--returns","title":"Returns","text":"<p>VisualAsset with detected intent, modality, shape, warnings, and a recommended rendering strategy.</p>"},{"location":"api/visualization/#qortex.visualize.inspect--examples","title":"Examples","text":"<p>asset = visualize.inspect(\"sub-01_T1w.nii.gz\") print(asset.summary()) print(asset.plan().describe())</p>"},{"location":"api/visualization/#qortex.visualize.visualize","title":"visualize","text":"<pre><code>visualize(\n    source: Any,\n    *,\n    mode: str = \"auto\",\n    overlay: Any = None,\n    window: str | tuple | None = \"auto\",\n    colormap: str | None = None,\n    threshold: float = 2.3,\n    alpha: float = 0.65,\n    title: str = \"\"\n) -&gt; \"VisualResult\"\n</code></pre> <p>Universal renderer: detect modality, choose strategy, return VisualResult.</p>"},{"location":"api/visualization/#qortex.visualize.visualize--parameters","title":"Parameters","text":"<p>source:     Path (str or Path), nibabel image, MNE Raw, or numpy array. mode:     Rendering mode: <code>\"auto\"</code> (default), <code>\"interactive_html\"</code>,     <code>\"static\"</code>, <code>\"thumbnail\"</code>, <code>\"summary\"</code>, or <code>\"qc\"</code>. overlay:     Optional path/array to overlay on the volume (stat map, mask, etc.).     When given, routes to the appropriate overlay function. window:     Window preset or (vmin, vmax) tuple (volumetric only). colormap:     Override colormap name. threshold:     Threshold for stat map overlays. alpha:     Overlay opacity. title:     Override the auto-generated HTML title.</p>"},{"location":"api/visualization/#qortex.visualize.visualize--returns","title":"Returns","text":"<p>VisualResult with .html, .show(), .to_html(), .to_png() methods.</p>"},{"location":"api/visualization/#qortex.visualize.visualize--examples","title":"Examples","text":"<p>result = visualize.visualize(\"sub-01_T1w.nii.gz\") result.show() result.to_html(\"output.html\")</p> <p>result = visualize.visualize(\"T1w.nii.gz\", overlay=\"zmap.nii.gz\", threshold=2.3)</p>"},{"location":"api/visualization/#qortex.visualize.browse_dicom","title":"browse_dicom","text":"<pre><code>browse_dicom(directory: Path | str) -&gt; 'VisualResult'\n</code></pre> <p>Build an interactive HTML study/series browser for a DICOM directory.</p> <p>Returns a VisualResult whose .html is a self-contained study browser with a sortable series table and series-detail panel (panel 7 style).</p>"},{"location":"api/visualization/#qortex.visualize.overlay_mask","title":"overlay_mask","text":"<pre><code>overlay_mask(\n    base: Any, mask: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay a binary mask on an anatomical image.</p>"},{"location":"api/visualization/#qortex.visualize.overlay_labelmap","title":"overlay_labelmap","text":"<pre><code>overlay_labelmap(\n    base: Any, labels: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay a multi-label atlas/segmentation on an anatomical image.</p>"},{"location":"api/visualization/#qortex.visualize.overlay_stat","title":"overlay_stat","text":"<pre><code>overlay_stat(\n    base: Any, stat_map: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay a thresholded statistical map (z/t) on an anatomical image.</p>"},{"location":"api/visualization/#qortex.visualize.overlay_pet","title":"overlay_pet","text":"<pre><code>overlay_pet(\n    base: Any, pet: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay a PET SUVR map on an anatomical background.</p>"},{"location":"api/visualization/#qortex.visualize.overlay_contour","title":"overlay_contour","text":"<pre><code>overlay_contour(\n    base: Any, mask: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay the 1-voxel-thick contour of a binary mask on an anatomical image.</p>"},{"location":"api/visualization/#qortex.visualize.overlay_edges","title":"overlay_edges","text":"<pre><code>overlay_edges(\n    base: Any, mask: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>Overlay gradient-magnitude edges of a mask on an anatomical image.</p>"},{"location":"api/visualization/#qortex.visualize.compare_masks","title":"compare_masks","text":"<pre><code>compare_masks(\n    base: Any, pred: Any, truth: Any, **kwargs\n) -&gt; \"VisualResult\"\n</code></pre> <p>TP/FP/FN diagnostic overlay comparing predicted vs ground-truth masks.</p> <p>Green = True Positive, Red = False Positive, Blue = False Negative. Dice similarity is computed from sampled slices and shown in the report.</p>"},{"location":"api/visualization/#qortex.visualize.dwi_summary","title":"dwi_summary","text":"<pre><code>dwi_summary(\n    dwi_path: Any,\n    bval_path: Any = None,\n    bvec_path: Any = None,\n    **kwargs\n) -&gt; Any\n</code></pre> <p>4-panel DWI QC summary: b0, high-b, b-value histogram, gradient sphere.</p>"},{"location":"api/visualization/#qortex.visualize.fmri_summary","title":"fmri_summary","text":"<pre><code>fmri_summary(bold_path: Any, **kwargs) -&gt; Any\n</code></pre> <p>fMRI QC summary: mean EPI, std, tSNR, global signal, framewise map.</p>"},{"location":"api/visualization/#qortex.visualize.surface_summary","title":"surface_summary","text":"<pre><code>surface_summary(path: Any, **kwargs) -&gt; Any\n</code></pre> <p>Surface QC summary for GIFTI meshes and CIFTI dense matrices.</p>"},{"location":"api/visualization/#core-result-types","title":"Core result types","text":"<p>Every rendering call flows through these types: <code>inspect()</code> returns a <code>VisualAsset</code>, <code>asset.plan()</code> returns a <code>VisualPlan</code>, and <code>asset.render()</code> returns a <code>VisualResult</code>.</p> <p>Structured problem, observation, or recommendation about a visual asset.</p> <p>Everything known about a visualizable file before any pixel is loaded.</p> <p>Built cheaply by reading only file headers / directory metadata. This is the universal language \u2014 the structured answer to \"what is this?\"</p> <p>Pre-render decision: what will be rendered and why.</p> <p>The plan is produced before any pixel data is loaded.  It exposes the reasoning so users can override before committing to rendering.</p> <p>Post-render output: rendered figures + provenance.</p> <p>Every rendering path in qortex.visualize produces a VisualResult so that callers have a consistent interface regardless of modality.</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset--parameters","title":"Parameters","text":"<p>path:     Absolute path to the file or directory. family:     File-format family: \"nifti\", \"dicom\", \"eeg\", \"gifti\", \"unknown\". intent:     What the data represents.  See INTENT_* constants. modality:     Imaging modality: \"mri\", \"ct\", \"pet\", \"fmri\", \"dwi\", \"eeg\", \"meg\". shape:     Voxel/sample dimensions.  Empty tuple if unknown. ndim:     Number of dimensions. spacing:     Voxel sizes in mm (or sample interval in ms for signals). orientation:     RAS/LAS/\u2026 orientation string.  None if unknown. affine:     4\u00d74 world-to-voxel affine.  None if not applicable. dtype:     Numpy dtype string for the data array. n_timepoints:     Number of 4th-dimension frames (fMRI, dynamic PET, EEG). n_channels:     Number of EEG/MEG channels or diffusion directions. is_mask:     True if the data appears to be a binary or label mask. is_stat_map:     True if the data appears to be a statistical map (z/t/F). is_large:     True if full in-memory loading would exceed ~4 GB. recommended_view:     Suggested rendering strategy for this asset. warnings:     Ordered list of VisualWarning items detected during inspection. metadata:     Free-form dict of extra metadata (DICOM tags, sidecar JSON, etc.). companion_paths:     Known companion files (bvec/bval, json sidecar, events.tsv, etc.).</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.is_4d","title":"is_4d  <code>property</code>","text":"<pre><code>is_4d: bool\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.is_dicom_series","title":"is_dicom_series  <code>property</code>","text":"<pre><code>is_dicom_series: bool\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.size_voxels","title":"size_voxels  <code>property</code>","text":"<pre><code>size_voxels: int\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.estimated_memory_mb","title":"estimated_memory_mb  <code>property</code>","text":"<pre><code>estimated_memory_mb: float\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.has_errors","title":"has_errors  <code>property</code>","text":"<pre><code>has_errors: bool\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.voxel_size_str","title":"voxel_size_str  <code>property</code>","text":"<pre><code>voxel_size_str: str\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.shape_str","title":"shape_str  <code>property</code>","text":"<pre><code>shape_str: str\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.warn","title":"warn","text":"<pre><code>warn(code: str, msg: str, sev: str = 'warning') -&gt; None\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.summary","title":"summary","text":"<pre><code>summary() -&gt; str\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.plan","title":"plan","text":"<pre><code>plan(mode: str = 'auto', **kwargs) -&gt; 'VisualPlan'\n</code></pre> <p>Derive a VisualPlan from this asset.</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.render","title":"render","text":"<pre><code>render(mode: str = 'auto', **kwargs) -&gt; 'VisualResult'\n</code></pre> <p>Render this asset and return a VisualResult.</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualAsset.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualPlan.estimated_memory_mb","title":"estimated_memory_mb  <code>property</code>","text":"<pre><code>estimated_memory_mb: float\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualPlan.describe","title":"describe","text":"<pre><code>describe() -&gt; str\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._asset.VisualResult.show","title":"show","text":"<pre><code>show() -&gt; None\n</code></pre> <p>Open the result in the default browser.</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualResult.to_html","title":"to_html","text":"<pre><code>to_html(\n    path: Path | str, *, write_sidecar: bool = True\n) -&gt; Path\n</code></pre> <p>Write HTML to file and optionally write a JSON sidecar.</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualResult.to_png","title":"to_png","text":"<pre><code>to_png(path: Path | str) -&gt; Path\n</code></pre> <p>Write PNG to file (if available).</p>"},{"location":"api/visualization/#qortex.visualize._asset.VisualResult.to_provenance_dict","title":"to_provenance_dict","text":"<pre><code>to_provenance_dict() -&gt; dict\n</code></pre>"},{"location":"api/visualization/#volumeviewer","title":"VolumeViewer","text":"<p>Renders orthogonal slices, lightboxes, and QC figures for NIfTI volumes.</p> <p>Interactive viewer for 3D/4D neuroimaging volumes.</p> <p>Supports NIfTI (.nii, .nii.gz), DICOM (files or directories), and already-loaded nibabel images or Qortex ImageRecord objects.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer--parameters","title":"Parameters","text":"<p>source:     Path to a NIfTI file, DICOM file, DICOM directory, or a nibabel     image object, or a Qortex <code>ImageRecord</code>. modality:     Override the detected modality: <code>\"ct\"</code>, <code>\"mri\"</code>, <code>\"fmri\"</code>,     <code>\"pet\"</code>, <code>\"dwi\"</code>. window:     Window preset name (<code>\"brain\"</code>, <code>\"bone\"</code>, \u2026), a <code>(vmin, vmax)</code>     tuple, or <code>\"auto\"</code> (default). colormap:     Colormap: <code>\"gray\"</code>, <code>\"hot\"</code>, <code>\"plasma\"</code>, <code>\"RdBu_r\"</code>.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.n_volumes","title":"n_volumes  <code>property</code>","text":"<pre><code>n_volumes: int\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.voxel_sizes","title":"voxel_sizes  <code>property</code>","text":"<pre><code>voxel_sizes: tuple[float, float, float]\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.tr","title":"tr  <code>property</code>","text":"<pre><code>tr: float | None\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.ortho","title":"ortho","text":"<pre><code>ortho(\n    x: int | None = None,\n    y: int | None = None,\n    z: int | None = None,\n    *,\n    t: int | None = None,\n    title: str = \"\"\n)\n</code></pre> <p>Return a 3-panel plotly Figure showing orthogonal slices.</p> <p>For nibabel-backed sources reads exactly 3 slices from disk \u2014 the full volume is never loaded into RAM.  For 4D data the display timepoint defaults to the midpoint and can be overridden with <code>t</code>. Voxel-size-aware aspect ratios are applied automatically.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.ortho--parameters","title":"Parameters","text":"<p>x, y, z : int, optional     Slice indices along each axis.  Defaults to the volume centre. t : int, optional     Timepoint index for 4D data.  Defaults to n_volumes // 2. title : str, optional     Override the auto-generated title.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.lightbox","title":"lightbox","text":"<pre><code>lightbox(\n    axis: int = 2,\n    *,\n    n_slices: int = 25,\n    n_cols: int = 5,\n    step: int | None = None,\n    title: str = \"\"\n)\n</code></pre> <p>Return a plotly Figure with a grid of evenly spaced slices.</p> <p>Reads each slice directly from the nibabel ArrayProxy when available \u2014 never materialises the full volume.  Aspect ratios are corrected per panel using the image voxel sizes.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.timeseries_at","title":"timeseries_at","text":"<pre><code>timeseries_at(\n    x: int,\n    y: int,\n    z: int,\n    *,\n    roi_radius: int = 0,\n    title: str = \"\"\n)\n</code></pre> <p>Plot BOLD/fMRI signal at voxel (x,y,z) over time.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.interactive_html","title":"interactive_html","text":"<pre><code>interactive_html(\n    output: Path | str | None = None,\n    *,\n    title: str = \"\",\n    max_slices_per_axis: int = 100,\n    include_time_slider: bool = True,\n    n_time_frames: int = 20\n) -&gt; str\n</code></pre> <p>Build a fully interactive orthogonal viewer as a standalone HTML page.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.to_html","title":"to_html","text":"<pre><code>to_html(output: Path | str, **kwargs) -&gt; Path\n</code></pre> <p>Write interactive HTML viewer to file. Returns the output Path.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.show","title":"show","text":"<pre><code>show() -&gt; None\n</code></pre> <p>Open the interactive viewer in the default web browser.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.overlay","title":"overlay","text":"<pre><code>overlay(\n    stat_map: Any,\n    *,\n    threshold: float = 2.0,\n    colormap: str = \"hot\",\n    alpha: float = 0.6\n) -&gt; \"VolumeViewer\"\n</code></pre> <p>Register a statistical or functional overlay on this viewer.</p> <p>For path-based inputs the overlay is stored as a lazy proxy \u2014 no full volume is loaded.  The window is estimated from sampled slices only. Blending happens per-displayed-slice when the viewer renders.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.overlay--parameters","title":"Parameters","text":"<p>stat_map:     Path to a NIfTI file, or a (H, W, D) numpy array. threshold:     Voxels with |value| &lt; threshold are transparent. colormap:     Colormap applied to suprathreshold voxels. alpha:     Blending opacity (0 = transparent, 1 = opaque).</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.fmri_summary","title":"fmri_summary","text":"<pre><code>fmri_summary(\n    *,\n    max_frames: int = 50,\n    title: str = \"\",\n    events_path: Path | str | None = None,\n    confounds_path: Path | str | None = None\n)\n</code></pre> <p>6-panel fMRI QC summary figure.</p> <p>Panels: 1. Mean EPI \u2014 temporal mean, center axial slice (gray) 2. Middle frame \u2014 single raw volume, center axial slice (gray) 3. Temporal std \u2014 standard deviation map (hot) 4. tSNR \u2014 temporal SNR via Welford streaming (hot) 5. Global signal \u2014 mean-brain signal timeseries 6. Framewise intensity \u2014 slice \u00d7 time heatmap (plasma)</p> <p>Lazy: panels 1\u20134 read the minimum frames using Welford streaming. Panel 6 reads one 3D frame per sampled timepoint.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.mean_epi_figure","title":"mean_epi_figure","text":"<pre><code>mean_epi_figure(*, max_frames: int = 50, title: str = '')\n</code></pre> <p>Orthogonal view of the temporal mean EPI volume.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.mean_volume","title":"mean_volume","text":"<pre><code>mean_volume() -&gt; 'VolumeViewer'\n</code></pre> <p>Return a new VolumeViewer containing only the temporal mean (for 4D).</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.std_epi_figure","title":"std_epi_figure","text":"<pre><code>std_epi_figure(*, max_frames: int = 50, title: str = '')\n</code></pre> <p>Orthogonal view of the temporal standard deviation map.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.tsnr_figure","title":"tsnr_figure","text":"<pre><code>tsnr_figure(*, max_frames: int = 50, title: str = '')\n</code></pre> <p>Orthogonal view of the tSNR map (mean/std, Welford streaming).</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.global_signal_timeseries","title":"global_signal_timeseries","text":"<pre><code>global_signal_timeseries(*, max_frames: int | None = None)\n</code></pre> <p>Plot the brain-masked global mean BOLD signal over time.</p>"},{"location":"api/visualization/#qortex.visualize.volume.VolumeViewer.framewise_preview","title":"framewise_preview","text":"<pre><code>framewise_preview(\n    *,\n    n_frames: int = 50,\n    n_slices: int = 24,\n    title: str = \"\"\n)\n</code></pre> <p>Slice \u00d7 time framewise intensity heatmap for motion/spike QC.</p>"},{"location":"api/visualization/#dwiviewer","title":"DWIViewer","text":"<p>Renders b0 images, high-b images, bval histograms, and gradient sphere plots.</p> <p>Lazy DWI-specific viewer.</p> <p>Reads the minimum data needed for each panel: - b0 image: volumes where bval &lt; 50 - High-b image: volumes where bval &gt; 800 - b-value histogram: just the bval file (no image data) - Gradient sphere: just the bvec file (no image data)</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer--parameters","title":"Parameters","text":"<p>dwi_path:     Path to the 4D DWI NIfTI (.nii or .nii.gz). bval_path:     Path to the FSL .bval file.  Auto-detected from the NIfTI name if None. bvec_path:     Path to the FSL .bvec file.  Auto-detected from the NIfTI name if None.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.b0_indices","title":"b0_indices  <code>property</code>","text":"<pre><code>b0_indices: list[int]\n</code></pre> <p>Volume indices where b-value &lt; 50 (b0 images).</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.high_b_indices","title":"high_b_indices  <code>property</code>","text":"<pre><code>high_b_indices: list[int]\n</code></pre> <p>Volume indices with the highest b-value (diffusion-weighted images).</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.shells","title":"shells  <code>property</code>","text":"<pre><code>shells: dict[int, int]\n</code></pre> <p>Map rounded b-value shell \u2192 number of directions.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.n_volumes","title":"n_volumes  <code>property</code>","text":"<pre><code>n_volumes: int\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.warnings","title":"warnings  <code>property</code>","text":"<pre><code>warnings: list[str]\n</code></pre> <p>Companion-file and gradient-table validation warnings.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.b0","title":"b0","text":"<pre><code>b0(*, title: str = '') -&gt; Any\n</code></pre> <p>Orthogonal view of the mean b0 image.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.high_b","title":"high_b","text":"<pre><code>high_b(*, title: str = '') -&gt; Any\n</code></pre> <p>Center-slice view of the mean high-b diffusion-weighted image.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.bval_histogram","title":"bval_histogram","text":"<pre><code>bval_histogram(*, title: str = '') -&gt; Any\n</code></pre> <p>Bar chart of b-value shell distribution \u2014 reads only the .bval file.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.gradient_sphere","title":"gradient_sphere","text":"<pre><code>gradient_sphere(*, title: str = '') -&gt; Any\n</code></pre> <p>3D scatter plot of gradient directions on the unit sphere.</p> <p>b0 volumes (bval &lt; 50) appear as grey markers at the origin. Each non-zero shell gets a distinct color.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.dwi_summary","title":"dwi_summary","text":"<pre><code>dwi_summary(*, title: str = '') -&gt; Any\n</code></pre> <p>2\u00d72 DWI QC summary figure.</p> <p>Panels: 1. Mean b0 \u2014 axial center slice 2. High-b DWI \u2014 axial center slice 3. b-value histogram 4. Gradient sphere</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.contact_sheet","title":"contact_sheet","text":"<pre><code>contact_sheet(\n    shell: int | str | None = None,\n    *,\n    n_slices: int = 9,\n    n_cols: int = 3,\n    title: str = \"\"\n) -&gt; Any\n</code></pre> <p>Axial DWI montage for slice-dropout and distortion QC.</p>"},{"location":"api/visualization/#qortex.visualize.dwi.DWIViewer.contact_sheet--parameters","title":"Parameters","text":"<p>shell:     <code>None</code> or <code>\"b0\"</code> uses b0 volumes. An integer selects the nearest     rounded b-value shell. <code>\"high\"</code> uses the highest-b shell. n_slices:     Number of evenly spaced axial slices to show. n_cols:     Number of montage columns.</p>"},{"location":"api/visualization/#timeseriesviewer","title":"TimeSeriesViewer","text":"<p>Renders EEG, MEG, and iEEG signal plots.</p> <p>Interactive plotly-based viewer for EEG, MEG, BOLD fMRI, or any time series.</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer--parameters","title":"Parameters","text":"<p>source:     Path to an EEG/MEG raw file (any MNE-supported format), a 4D NIfTI     for BOLD signal extraction, a numpy array (channels \u00d7 samples), or an     MNE Raw object. sfreq:     Sampling frequency in Hz.  Required only when source is a numpy array. modality:     <code>\"eeg\"</code>, <code>\"meg\"</code>, <code>\"bold\"</code>, or <code>\"signal\"</code> (generic).</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.n_channels","title":"n_channels  <code>property</code>","text":"<pre><code>n_channels: int\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.n_samples","title":"n_samples  <code>property</code>","text":"<pre><code>n_samples: int\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.sfreq","title":"sfreq  <code>property</code>","text":"<pre><code>sfreq: float\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.duration_s","title":"duration_s  <code>property</code>","text":"<pre><code>duration_s: float\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.channel_names","title":"channel_names  <code>property</code>","text":"<pre><code>channel_names: list[str]\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.butterfly","title":"butterfly","text":"<pre><code>butterfly(\n    *,\n    tmin: float = 0.0,\n    tmax: float | None = None,\n    channels: list[int | str] | None = None,\n    show_envelope: bool = True,\n    title: str = \"\",\n    max_channels: int = 64\n)\n</code></pre> <p>Butterfly plot: all channels overlaid, with amplitude envelope.</p> <p>Returns a plotly Figure.</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.psd","title":"psd","text":"<pre><code>psd(\n    *,\n    fmin: float = 0.5,\n    fmax: float | None = None,\n    channels: list[int | str] | None = None,\n    nperseg: int = 512,\n    log_scale: bool = True,\n    title: str = \"\",\n    max_channels: int = 32\n)\n</code></pre> <p>Power spectral density (Welch method).</p> <p>Returns a plotly Figure with frequency on X and power (dB) on Y.</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.spectrogram","title":"spectrogram","text":"<pre><code>spectrogram(\n    channel: int | str = 0,\n    *,\n    fmin: float = 0.0,\n    fmax: float | None = None,\n    nperseg: int = 256,\n    colormap: str = \"plasma\",\n    title: str = \"\"\n)\n</code></pre> <p>Time-frequency power spectrogram (STFT) for a single channel.</p> <p>Returns a plotly Figure (Heatmap).</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.topomap","title":"topomap","text":"<pre><code>topomap(\n    t: float = 0.0,\n    *,\n    channels: list[int | str] | None = None,\n    grid_size: int = 80,\n    title: str = \"\"\n)\n</code></pre> <p>Sensor topography at a single time point.</p> <p>Uses stored MNE channel locations when available. For array-backed signals without sensor geometry, channels are placed on a unit circle in recording order so the method remains useful for quick QC.</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.epoched","title":"epoched","text":"<pre><code>epoched(\n    events: ndarray | None = None,\n    event_id: dict[str, int] | None = None,\n    *,\n    tmin: float = -0.2,\n    tmax: float = 0.8,\n    baseline: tuple[float | None, float | None] = (\n        None,\n        0.0,\n    ),\n    channels: list[int | str] | None = None,\n    title: str = \"\"\n)\n</code></pre> <p>Event-locked average (ERP/ERF) with \u00b1SEM shading.</p> <p>If events is None and MNE events are available in the bundle they are used. Requires at least 2 distinct events. Returns a plotly Figure.</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.dashboard","title":"dashboard","text":"<pre><code>dashboard(\n    *, title: str = \"\", channel: int | str = 0\n) -&gt; str\n</code></pre> <p>Build a self-contained HTML dashboard combining all views.</p> <p>Returns an HTML string (self-contained, no server required).</p>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.to_html","title":"to_html","text":"<pre><code>to_html(output: Path | str, **kwargs) -&gt; Path\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.timeseries.TimeSeriesViewer.show","title":"show","text":"<pre><code>show() -&gt; None\n</code></pre>"},{"location":"api/visualization/#visualauditreport","title":"VisualAuditReport","text":"<p>Coverage matrix and QC summary for a local BIDS directory.</p> <p>Dataset-level visual QC report.</p> <p>Use <code>.to_html()</code> to write a self-contained HTML gallery, <code>.show()</code> to open it in a browser, or <code>.summary()</code> for a text digest. <code>.to_json()</code> / <code>.visual_manifest_json()</code> export machine-readable records.</p> <p>Optional manifest-completeness fields (populated by <code>run_visual_audit_with_manifest()</code>):</p> <p>n_expected:       Total files listed in the dataset manifest. n_local_present:  Files that exist on local disk. n_missing_local:  Files expected but absent from disk.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.per_suffix_counts","title":"per_suffix_counts  <code>property</code>","text":"<pre><code>per_suffix_counts: dict[str, int]\n</code></pre> <p>Count of inspected entries per BIDS suffix.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.per_subject_counts","title":"per_subject_counts  <code>property</code>","text":"<pre><code>per_subject_counts: dict[str, int]\n</code></pre> <p>Count of inspected entries per subject ID.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.per_datatype_counts","title":"per_datatype_counts  <code>property</code>","text":"<pre><code>per_datatype_counts: dict[str, int]\n</code></pre> <p>Count of inspected entries per BIDS datatype folder.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.n_expected","title":"n_expected  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>n_expected: int | None = None\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.n_local_present","title":"n_local_present  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>n_local_present: int | None = None\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.n_missing_local","title":"n_missing_local  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>n_missing_local: int | None = None\n</code></pre>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.failed_files","title":"failed_files  <code>property</code>","text":"<pre><code>failed_files: list[AuditEntry]\n</code></pre> <p>All entries whose rendering raised an exception.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.coverage_matrix","title":"coverage_matrix","text":"<pre><code>coverage_matrix() -&gt; dict\n</code></pre> <p>Build a subject \u00d7 BIDS-suffix coverage grid from the audit entries.</p> <p>Returns a dict with keys <code>subjects</code>, <code>suffixes</code>, <code>cells</code>. Each cell value is <code>\"ok\"</code>, <code>\"error\"</code>, or <code>\"missing\"</code>.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.warning_summary","title":"warning_summary","text":"<pre><code>warning_summary() -&gt; dict\n</code></pre> <p>Aggregate all VisualWarnings from all entries into a structured summary.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.warning_summary--returns","title":"Returns","text":"<p>dict     <code>by_code</code>              \u2013 {code: [path_labels]}     <code>by_severity</code>          \u2013 {severity: count}     <code>unusual_orientations</code> \u2013 paths with non-standard image orientation     <code>anisotropic</code>          \u2013 paths with highly anisotropic voxels     <code>large_files</code>          \u2013 paths estimated &gt;4 GB     <code>failed_renders</code>       \u2013 paths whose render raised an exception     <code>total_warnings</code>       \u2013 total warning count across all entries</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.summary","title":"summary","text":"<pre><code>summary() -&gt; str\n</code></pre> <p>Return a short text summary of the audit.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.missing_expected_files","title":"missing_expected_files","text":"<pre><code>missing_expected_files(\n    manifest_files: list, local_root: Path\n) -&gt; list[dict]\n</code></pre> <p>Find manifest entries that should exist locally but do not.</p> <p>Compares <code>manifest_files</code> against the actual filesystem.  Complements <code>coverage_matrix()</code> which only reflects what was rendered \u2014 here we look directly at what is absent from disk.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.missing_expected_files--parameters","title":"Parameters","text":"<p>manifest_files:     FileRecord-like objects with a <code>.path</code> attribute (BIDS-relative     path).  Typically <code>Dataset.manifest().files</code> or a filtered subset. local_root:     Root directory of the locally downloaded dataset.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.missing_expected_files--returns","title":"Returns","text":"<p>list[dict]     Each dict: <code>{\"path\", \"subject\", \"suffix\", \"size_bytes\"}</code>.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.action_items","title":"action_items","text":"<pre><code>action_items() -&gt; list[dict[str, Any]]\n</code></pre> <p>Return prioritized next actions for a curator or ML user.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.to_html","title":"to_html","text":"<pre><code>to_html(path: Path | str | None = None) -&gt; str\n</code></pre> <p>Build a self-contained HTML visual QC grid.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.to_html--parameters","title":"Parameters","text":"<p>path:   If provided, also write the HTML to this file.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.to_html--returns","title":"Returns","text":"<p>str     The complete HTML string.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.to_json","title":"to_json","text":"<pre><code>to_json() -&gt; str\n</code></pre> <p>Serialize the full report to a JSON string.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.to_markdown","title":"to_markdown","text":"<pre><code>to_markdown(path: Path | str | None = None) -&gt; str\n</code></pre> <p>Build a concise Markdown report for lab notes or pull requests.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.visual_manifest_json","title":"visual_manifest_json","text":"<pre><code>visual_manifest_json(path: Path | str) -&gt; Path\n</code></pre> <p>Write a <code>visual_manifest.json</code> summarising this report.</p> <p>The manifest records every inspected file with its visual metadata, warnings, and thumbnail availability \u2014 useful for downstream tooling that needs a machine-readable audit record.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.visual_manifest_json--parameters","title":"Parameters","text":"<p>path:     Output file path.  Parent directories are created as needed.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.visual_manifest_json--returns","title":"Returns","text":"<p>Path     The path that was written.</p>"},{"location":"api/visualization/#qortex.visualize._audit.VisualAuditReport.show","title":"show","text":"<pre><code>show() -&gt; None\n</code></pre> <p>Open the audit report in the default web browser.</p>"},{"location":"api/visualization/#dicom-types","title":"DICOM types","text":"<p>Metadata record for one DICOM series.</p> <p>HTML series browser for a DICOM study directory (panel 7 style).</p> <p>Lists all series with modality, image count, description, date, and a small thumbnail of the middle slice (if pixel data is readable).</p>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeries.shape","title":"shape  <code>property</code>","text":"<pre><code>shape: tuple[int, int, int]\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeries.spacing_str","title":"spacing_str  <code>property</code>","text":"<pre><code>spacing_str: str\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeries.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeriesBrowser--parameters","title":"Parameters","text":"<p>directory:     Path to the DICOM study directory. show_phi:     When False (default), anonymize patient ID and hide date-of-birth.     Set True to show full patient metadata.</p>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeriesBrowser.scan","title":"scan","text":"<pre><code>scan() -&gt; list[DicomSeries]\n</code></pre>"},{"location":"api/visualization/#qortex.visualize.dicom.DicomSeriesBrowser.to_html","title":"to_html","text":"<pre><code>to_html(*, show_phi: bool | None = None) -&gt; str\n</code></pre>"},{"location":"api/visualization/#surface-types","title":"Surface types","text":"<p>One data array inside a GIFTI file.</p> <p>Header-level surface summary for GIFTI/CIFTI files.</p>"},{"location":"artifacts/","title":"Artifacts","text":"<p>An artifact is the output of a conversion run. It lives on local disk and contains one subdirectory per split plus an <code>artifact_manifest.json</code>.</p> <pre><code>artifacts/ds004130/\n  artifact_manifest.json\n  train/\n    shard-000000.parquet\n    shard-000001.parquet\n    ...\n  val/\n    shard-000000.parquet\n  test/\n    shard-000000.parquet\n</code></pre>"},{"location":"artifacts/#opening-an-artifact","title":"Opening an artifact","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\n</code></pre> <p>This loads the manifest but not the data. Data is only read when you call a load method.</p>"},{"location":"artifacts/#artifact-pages","title":"Artifact pages","text":"<p>Manifest \u2014 Reading the artifact_manifest.json: sample counts, feature names, label classes, provenance.</p> <p>Inspect \u2014 Inspect individual samples and the artifact structure.</p> <p>Visualize samples \u2014 Plot individual windows from the artifact.</p> <p>Compare splits \u2014 Check that train/val/test splits have similar distributions.</p> <p>ML bridge \u2014 Load artifacts for PyTorch, scikit-learn, HuggingFace, and Lightning.</p> <p>Next \u2192 NeuroAI runtime \u2014 run inference with a pre-trained model directly on your data source, or return to Guides overview to review the full pipeline.</p>"},{"location":"artifacts/compare-splits/","title":"Compare Splits","text":"<p><code>art.compare_splits()</code> checks whether the train, val, and test splits have similar distributions. Large differences suggest a data issue.</p>"},{"location":"artifacts/compare-splits/#python","title":"Python","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nreport = art.compare_splits()\nreport.show()\n</code></pre>"},{"location":"artifacts/compare-splits/#what-is-compared","title":"What is compared","text":"<p>Class distribution. The fraction of each class in each split should be approximately equal (assuming stratified splitting). A large discrepancy indicates that stratification failed or the dataset has very few subjects with some classes.</p> <p>Mean signal amplitude. The mean feature value per split should be similar. A split with systematically higher amplitude may contain subjects from a different scanner site or a different acquisition session.</p> <p>Feature correlation. The top-10 most correlated feature pairs should have similar correlation strength across splits. Very different correlation structure between train and test is a red flag for distribution shift.</p> <p>Subject count. Displayed per split.</p>"},{"location":"artifacts/compare-splits/#output","title":"Output","text":"<pre><code>Compare splits: artifacts/ds004130/\n  Split sizes:  train=1,200  val=270  test=270\n  Class balance:\n    rest:       train=33.3%  val=33.3%  test=33.3%  \u2713\n    eyes-open:  train=33.2%  val=33.0%  test=32.6%  \u2713\n    task:       train=33.5%  val=33.7%  test=34.1%  \u2713\n  Signal amplitude:\n    train: mean=0.42  val=0.41  test=0.44  \u2713\n  Status: ok \u2014 no distribution shift detected\n</code></pre> <p>If issues are found:</p> <pre><code>  WARNING: 'task' class is absent from val split\n  Suggestion: re-run conversion with stratify_by_label=True\n</code></pre>"},{"location":"artifacts/compare-splits/#programmatic-access","title":"Programmatic access","text":"<pre><code>for finding in report.findings:\n    print(finding.severity, finding.message)\n\nprint(report.class_distribution)   # dict: split \u2192 class \u2192 fraction\nprint(report.amplitude_summary)    # dict: split \u2192 {\"mean\", \"std\"}\n</code></pre>"},{"location":"artifacts/compare-splits/#related","title":"Related","text":"<ul> <li>Splits \u2014 how subjects are assigned to splits</li> <li>Leakage check \u2014 verify no subject leakage</li> </ul>"},{"location":"artifacts/inspect/","title":"Inspect Artifact","text":""},{"location":"artifacts/inspect/#summary","title":"Summary","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nprint(art.summary())\n</code></pre> <p>Output:</p> <pre><code>Artifact: artifacts/ds004130/\nFormat:   parquet\nSource:   ds004130@1.2.0\nCreated:  2024-01-15T14:23:00Z\n\nSplits:\n  train   1,200 samples   61 subjects   classes: rest(400) eyes-open(398) task(402)\n  val       270 samples   14 subjects   classes: rest(90)  eyes-open(89)  task(91)\n  test      270 samples   13 subjects   classes: rest(90)  eyes-open(88)  task(92)\n\nFeatures: 491,520 (64 channels \u00d7 7,680 time points)\nWindow:   30.0 s, 0.5 overlap\n</code></pre>"},{"location":"artifacts/inspect/#peeking-at-a-sample","title":"Peeking at a sample","text":"<pre><code>sample = art.peek(split=\"train\", index=0)\nprint(sample.X.shape)       # (64, 7680)\nprint(sample.y)             # \"rest\"\nprint(sample.subject_id)    # \"01\"\nprint(sample.onset_s)       # 0.0\n</code></pre>"},{"location":"artifacts/inspect/#file-structure","title":"File structure","text":"<pre><code>for split in [\"train\", \"val\", \"test\"]:\n    shards = list(art.shard_paths(split))\n    print(f\"{split}: {len(shards)} shards\")\n</code></pre>"},{"location":"artifacts/inspect/#checking-artifact-integrity","title":"Checking artifact integrity","text":"<pre><code>ok = art.check_integrity()\n# Reads each shard and verifies row counts match the manifest\nprint(ok)  # True / False\n</code></pre> <p>If integrity fails, specific shards are listed. Re-run conversion to fix.</p>"},{"location":"artifacts/manifest/","title":"Artifact Manifest","text":"<p><code>artifact_manifest.json</code> describes the full artifact: where the data came from, how it was converted, and what it contains.</p>"},{"location":"artifacts/manifest/#reading-the-manifest","title":"Reading the manifest","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nmf = art.manifest\n\nprint(mf.source_dataset)     # \"ds004130\"\nprint(mf.source_snapshot)    # \"1.2.0\"\nprint(mf.format)             # \"parquet\"\nprint(mf.n_samples)          # total samples (all splits)\nprint(mf.label_classes)      # [\"rest\", \"eyes-open\", \"task\"]\nprint(mf.feature_names[:5])  # first 5 feature column names\nprint(mf.splits)             # {\"train\": {...}, \"val\": {...}, \"test\": {...}}\n</code></pre>"},{"location":"artifacts/manifest/#split-level-summary","title":"Split-level summary","text":"<pre><code>for split_name, split_info in mf.splits.items():\n    print(f\"{split_name}: {split_info['n_samples']} samples, {split_info['n_subjects']} subjects\")\n</code></pre>"},{"location":"artifacts/manifest/#subject-assignment","title":"Subject assignment","text":"<pre><code>print(mf.train_subjects)  # list of subject ID strings\nprint(mf.val_subjects)\nprint(mf.test_subjects)\n</code></pre>"},{"location":"artifacts/manifest/#conversion-parameters","title":"Conversion parameters","text":"<pre><code>print(mf.window.duration_s)     # 30.0\nprint(mf.window.overlap)        # 0.5\nprint(mf.split.strategy)        # \"subject\"\nprint(mf.split.seed)            # 42\nprint(mf.split.val_frac)        # 0.15\n</code></pre>"},{"location":"artifacts/manifest/#timestamps-and-versions","title":"Timestamps and versions","text":"<pre><code>print(mf.created_at)          # datetime\nprint(mf.qortex_version)      # \"0.3.1\"\n</code></pre>"},{"location":"artifacts/manifest/#json-structure","title":"JSON structure","text":"<p>See Provenance for the full JSON schema.</p>"},{"location":"artifacts/ml-bridge/","title":"ML Bridge","text":"<p>The ML bridge loads a Qortex artifact into the data structures expected by major ML frameworks.</p>"},{"location":"artifacts/ml-bridge/#scikit-learn","title":"scikit-learn","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nX_train, y_train = art.sklearn(split=\"train\")\nX_val,   y_val   = art.sklearn(split=\"val\")\nX_test,  y_test  = art.sklearn(split=\"test\")\n\n# X shape: (n_samples, n_features)\n# y shape: (n_samples,) \u2014 string labels\n\nfrom sklearn.ensemble import GradientBoostingClassifier\nclf = GradientBoostingClassifier()\nclf.fit(X_train, y_train)\nprint(clf.score(X_test, y_test))\n</code></pre> <p>Label encoding: by default, labels are returned as strings. Pass <code>encode_labels=True</code> for integer encoding:</p> <pre><code>X_train, y_train = art.sklearn(split=\"train\", encode_labels=True)\n# y_train is np.int64\n</code></pre>"},{"location":"artifacts/ml-bridge/#pytorch","title":"PyTorch","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\ntrain_ds = art.torch(split=\"train\")\nval_ds   = art.torch(split=\"val\")\n\nfrom torch.utils.data import DataLoader\ntrain_loader = DataLoader(train_ds, batch_size=32, shuffle=True)\n\nfor X, y in train_loader:\n    print(X.shape, y.shape)\n    break\n</code></pre> <p>Requires: <code>pip install \"qortex[torch]\"</code></p>"},{"location":"artifacts/ml-bridge/#pytorch-lightning","title":"PyTorch Lightning","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\ndm = art.lightning_datamodule(batch_size=32)\n\nimport lightning as L\ntrainer = L.Trainer(max_epochs=10)\ntrainer.fit(model, dm)\n</code></pre> <p>Requires: <code>pip install \"qortex[lightning]\"</code></p>"},{"location":"artifacts/ml-bridge/#huggingface-datasets","title":"HuggingFace Datasets","text":"<pre><code>from qortex import Artifact\nfrom datasets import load_from_disk\n\nart = Artifact.open(\"artifacts/ds004130/\")\nhf_ds = art.huggingface()\n\n# Or load directly with datasets library (for HuggingFace format artifacts)\nhf_ds = load_from_disk(\"artifacts/ds004130/train/\")\n</code></pre> <p>Requires: <code>pip install \"qortex[hf]\"</code></p>"},{"location":"artifacts/ml-bridge/#feature-names","title":"Feature names","text":"<pre><code>print(art.manifest.feature_names[:10])\n# ['Fp1_0', 'Fp1_1', ..., 'Fp1_7679', 'Fp2_0', ...]\n</code></pre> <p>Feature names are <code>{channel}_{timepoint}</code> for EEG or <code>voxel_{x}_{y}_{z}</code> for fMRI.</p>"},{"location":"artifacts/ml-bridge/#metadata-columns","title":"Metadata columns","text":"<p>Every artifact includes metadata columns alongside features:</p> <ul> <li><code>subject_id</code> \u2014 BIDS subject label</li> <li><code>label</code> \u2014 string label (from <code>label_col</code>)</li> <li><code>onset_s</code> \u2014 window start time in seconds</li> <li><code>run_id</code> \u2014 BIDS run label (if applicable)</li> </ul> <p>Access metadata separately:</p> <pre><code>import pandas as pd\ndf = pd.read_parquet(\"artifacts/ds004130/train/shard-000000.parquet\")\nprint(df.columns.tolist()[:5])    # feature columns\nprint(df[[\"subject_id\", \"label\", \"onset_s\"]].head())\n</code></pre>"},{"location":"artifacts/ml-bridge/#memory-usage","title":"Memory usage","text":"<p>For large artifacts, use chunked loading:</p> <pre><code># Zarr \u2014 only loads chunks as needed\nimport zarr\nz = zarr.open(\"artifacts/ds004130_zarr/train/data.zarr\")\nX_chunk = z[\"X\"][0:32]  # loads only 32 samples\n</code></pre> <p>For Parquet, use pyarrow with a filter:</p> <pre><code>import pyarrow.parquet as pq\nds = pq.ParquetDataset(\"artifacts/ds004130/train/\")\ntable = ds.read(filters=[(\"label\", \"=\", \"rest\")])\n</code></pre>"},{"location":"artifacts/visualize-samples/","title":"Visualize Samples","text":"<p><code>art.visualize_sample()</code> renders a single sample from an artifact as a plot.</p>"},{"location":"artifacts/visualize-samples/#python","title":"Python","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\n\n# By index\nart.visualize_sample(split=\"train\", index=0)\n\n# By subject and label\nart.visualize_sample(split=\"train\", subject=\"01\", label=\"rest\")\n</code></pre>"},{"location":"artifacts/visualize-samples/#what-is-rendered","title":"What is rendered","text":"<p>The plot type depends on the data shape in the artifact:</p> <ul> <li>EEG/timeseries (<code>n_channels, n_timepoints</code>): Butterfly plot with all channels overlaid. Channel axis is y-axis-shifted for readability.</li> <li>fMRI (<code>n_voxels,</code>): Bar chart of the top 50 most variable features, sorted by absolute value.</li> <li>fMRI 4D (<code>nx, ny, nz, nt</code>): Mean axial slice image.</li> <li>Tabular (<code>n_features,</code>): Horizontal bar chart of feature values.</li> </ul>"},{"location":"artifacts/visualize-samples/#batch-visualization","title":"Batch visualization","text":"<p>To inspect multiple samples:</p> <pre><code>for i in range(5):\n    art.visualize_sample(split=\"train\", index=i)\n</code></pre> <p>Or as a grid (if plotly is available):</p> <pre><code>art.visualize_sample(split=\"train\", indices=range(9), layout=\"3x3\")\n</code></pre>"},{"location":"artifacts/visualize-samples/#saving","title":"Saving","text":"<pre><code>result = art.visualize_sample(split=\"train\", index=0, show=False)\nresult.to_png(\"sample_0.png\")\nresult.to_html(\"sample_0.html\")\n</code></pre>"},{"location":"artifacts/visualize-samples/#related","title":"Related","text":"<ul> <li>Compare splits \u2014 distribution-level comparison</li> <li>Artifact visualization \u2014 visual audit of the artifact</li> </ul>"},{"location":"concepts/","title":"Concepts","text":"<p>Qortex is built around a few design decisions that affect how you use it. Understanding them makes the API predictable.</p>"},{"location":"concepts/#core-ideas","title":"Core ideas","text":"<p>What is Qortex \u2014 Qortex is not a downloader. It is a decision layer that sits between OpenNeuro and your training pipeline. It tells you whether a dataset is usable before you transfer any files.</p> <p>Readiness first \u2014 Every dataset has a readiness state: not inspected, inspected (manifest only), downloaded, validated, and conversion-ready. Qortex checks readiness at each stage and tells you what is blocking progress.</p> <p>OpenNeuro and BIDS \u2014 How Qortex uses the OpenNeuro GraphQL API, what a snapshot is, how BIDS structure maps to Qortex entities. Covers what data the API exposes: metadata, engagement, demographics, README, BIDS validation.</p> <p>Data model \u2014 The types that carry information through the pipeline: <code>RichDatasetInfo</code>, <code>SnapshotSummary</code>, <code>DatasetEngagement</code>, <code>Dataset</code>, <code>Manifest</code>, <code>FileRecord</code>, <code>DatasetProfile</code>, <code>Artifact</code>.</p> <p>Workflow \u2014 The full pipeline from OpenNeuro catalog search to a trained model, with decision points at each stage.</p>"},{"location":"concepts/data-model/","title":"Data Model","text":"<p>Qortex has a layered data model. Each layer adds more specific information as you move through the pipeline.</p>"},{"location":"concepts/data-model/#richdatasetinfo","title":"RichDatasetInfo","text":"<p><code>RichDatasetInfo</code> is the primary metadata object returned by <code>OpenNeuroClient.get_dataset_rich()</code>. It covers every field visible on an OpenNeuro dataset page without downloading or iterating the file tree.</p> <p>Key fields:</p> Field Description <code>id</code>, <code>name</code>, <code>doi</code> Dataset identity <code>license</code>, <code>authors</code>, <code>senior_author</code> Attribution <code>modalities</code>, <code>tasks</code>, <code>species</code> Scientific context <code>study_domain</code>, <code>study_design</code> Study type <code>grant_funder</code>, <code>grant_id</code> Funding <code>associated_paper_doi</code> Linked publication <code>created</code>, <code>publish_date</code> Timestamps <code>engagement</code> <code>DatasetEngagement</code> \u2014 views, downloads, stars, followers <code>latest_snapshot_summary</code> <code>SnapshotSummary</code> \u2014 subjects, sessions, tasks, file counts, demographics <code>data_processed</code> True if preprocessed data is included <pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    info = client.get_dataset_rich(\"ds008039\")\n\nsnap = info.latest_snapshot_summary\nprint(snap.n_subjects, snap.total_size_gb, snap.bids_version)\nprint(snap.funding, snap.references, snap.ethics_approvals)\nprint(snap.demographics_dataframe())   # Polars DataFrame: age, sex, group\n</code></pre>"},{"location":"concepts/data-model/#datasetengagement","title":"DatasetEngagement","text":"<p><code>DatasetEngagement</code> tracks community activity for a dataset on the OpenNeuro platform.</p> <pre><code>info.engagement.views             # integer\ninfo.engagement.downloads         # integer\ninfo.engagement.stars             # integer\ninfo.engagement.followers         # integer\ninfo.engagement.popularity_score  # composite 0\u2013100 score\n</code></pre>"},{"location":"concepts/data-model/#snapshotsummary","title":"SnapshotSummary","text":"<p><code>SnapshotSummary</code> is the API-level BIDS summary for one snapshot. It is returned by <code>get_dataset_rich()</code> (as <code>latest_snapshot_summary</code>) and by <code>get_snapshot_summary()</code>. It does not require a file tree fetch.</p> <pre><code>snap.tag                # \"1.2.0\"\nsnap.n_subjects         # len(snap.subjects)\nsnap.subjects           # [\"sub-01\", \"sub-02\", ...]\nsnap.sessions           # [\"ses-01\", \"ses-02\"]\nsnap.tasks              # [\"rest\", \"nback\"]\nsnap.modalities         # [\"MRI\"]\nsnap.total_files        # integer\nsnap.total_size_gb      # float\nsnap.bids_version       # \"1.8.0\"\nsnap.license            # \"CC0\"\nsnap.funding            # [\"NIH R01 MH123456\", ...]\nsnap.references         # [\"https://doi.org/...\", ...]\nsnap.ethics_approvals   # [\"IRB approval #2024-001\"]\nsnap.how_to_acknowledge # citation string\nsnap.data_processed     # bool\nsnap.subject_demographics  # list[SubjectDemographic]\nsnap.demographics_dataframe()  # Polars DataFrame\nsnap.age_stats()        # {\"n\": 42, \"mean\": 28.4, \"min\": 18, \"max\": 65, ...}\n</code></pre>"},{"location":"concepts/data-model/#dataset","title":"Dataset","text":"<p><code>Dataset</code> is the top-level facade. It holds a dataset ID, an optional snapshot tag, and an optional local data directory. It is cheap to create \u2014 no network calls happen at construction time.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")                        # no network call yet\nds = Dataset(\"ds004130\", snapshot=\"1.2.0\")      # pin a snapshot\nds = Dataset(\"ds004130\", data_dir=Path(\"/data\")) # override default cache\n</code></pre> <p>The manifest is fetched lazily on the first call to <code>ds.manifest()</code>, <code>ds.download()</code>, <code>ds.doctor()</code>, or any other method that needs it.</p>"},{"location":"concepts/data-model/#manifest","title":"Manifest","text":"<p>A <code>Manifest</code> is the result of fetching the remote file tree and parsing it into typed records. It contains:</p> <ul> <li><code>dataset_id</code> and <code>snapshot</code> tag</li> <li><code>doi</code> (dataset DOI)</li> <li><code>files</code> \u2014 a list of <code>FileRecord</code> objects</li> <li><code>summary</code> \u2014 a <code>ManifestSummary</code> with pre-computed counts</li> </ul> <pre><code>manifest = ds.manifest()\nprint(manifest.summary.n_subjects)     # integer\nprint(manifest.summary.modalities)     # list of strings\nprint(manifest.summary.has_events)     # bool\nprint(manifest.summary.total_size)     # bytes\n</code></pre> <p>The manifest is cached in memory. Repeated calls to <code>ds.manifest()</code> return the cached copy unless <code>force_refresh=True</code> is passed.</p>"},{"location":"concepts/data-model/#filerecord","title":"FileRecord","text":"<p>A <code>FileRecord</code> represents one file in the manifest. It has:</p> <ul> <li><code>path</code> \u2014 BIDS-relative path (e.g., <code>sub-01/func/sub-01_task-rest_bold.nii.gz</code>)</li> <li><code>size</code> \u2014 file size in bytes (may be None for some endpoints)</li> <li><code>urls</code> \u2014 list of CDN and S3 URLs for this file</li> <li><code>subject</code>, <code>session</code>, <code>task</code>, <code>run</code>, <code>datatype</code>, <code>suffix</code> \u2014 parsed BIDS entities</li> <li><code>extension</code> \u2014 file extension (<code>.nii.gz</code>, <code>.tsv</code>, etc.)</li> <li><code>is_dir</code> \u2014 True for directory entries</li> </ul> <p>FileRecords are read-only. Filtering happens through <code>manifest.filter()</code> or <code>ds.files()</code>.</p>"},{"location":"concepts/data-model/#selectionspec","title":"SelectionSpec","text":"<p>A <code>SelectionSpec</code> describes which files to include in a download plan. It combines:</p> <ul> <li>Entity filters: <code>subjects</code>, <code>sessions</code>, <code>tasks</code>, <code>modalities</code>, <code>datatypes</code></li> <li>Glob patterns: <code>include</code> and <code>exclude</code> lists</li> <li>Semantic filters: <code>event_complete</code>, <code>label_ready</code>, <code>loadable_only</code></li> <li>Size limit: <code>max_size_gb</code></li> </ul> <p>The planner resolves a SelectionSpec against a Manifest to produce a <code>DownloadPlan</code>.</p>"},{"location":"concepts/data-model/#downloadplan","title":"DownloadPlan","text":"<p>A <code>DownloadPlan</code> lists the exact files to download, with target paths and companion files included. It is produced by <code>DownloadPlanner.plan()</code> before any transfer happens.</p> <pre><code>plan = ds.plan(subjects=[\"01\", \"02\"], suffixes=[\"bold\"])\nprint(len(plan.files))          # file count\nprint(plan.target_dir)          # destination path\nprint(plan.size_gb)             # GB\n</code></pre>"},{"location":"concepts/data-model/#datasetprofile","title":"DatasetProfile","text":"<p><code>DatasetProfile</code> is the result of <code>DatasetInspector.inspect()</code>. It builds on the file tree manifest to produce a full structural breakdown with ML readiness scoring.</p> <pre><code>profile = ds.inspect()\nprint(profile.n_subjects)           # from manifest\nprint(profile.events_coverage)      # fraction with events.tsv\nprint(profile.ml_readiness.grade)   # \"A\"\u2013\"F\"\nprint(profile.rich_info)            # RichDatasetInfo (if available)\n</code></pre>"},{"location":"concepts/data-model/#artifact","title":"Artifact","text":"<p>An <code>Artifact</code> is the output of <code>ConversionPipeline.run()</code>. It lives on local disk at a directory containing an <code>artifact_manifest.json</code> and one subdirectory per split (<code>train/</code>, <code>val/</code>, <code>test/</code>). Each split contains Parquet (or Zarr/HDF5/etc.) shards.</p> <pre><code>from qortex import Artifact\n\nart = Artifact.open(\"converted/parquet/\")\nprint(art.manifest.n_samples)\nprint(art.manifest.splits)      # {\"train\": ..., \"val\": ..., \"test\": ...}\n\nX, y = art.sklearn()\nds_torch = art.torch(split=\"train\")\n</code></pre>"},{"location":"concepts/data-model/#type-relationships","title":"Type relationships","text":"<pre><code>OpenNeuroClient\n  \u251c\u2500 get_dataset_rich()       \u2192  RichDatasetInfo\n  \u2502     \u251c\u2500 DatasetEngagement\n  \u2502     \u2514\u2500 SnapshotSummary\n  \u2502           \u2514\u2500 [SubjectDemographic, ...]\n  \u251c\u2500 get_readme()             \u2192  str | None\n  \u251c\u2500 get_validation_issues()  \u2192  list[dict]\n  \u251c\u2500 get_snapshots()          \u2192  list[SnapshotRef]\n  \u251c\u2500 get_files()              \u2192  (SnapshotRef, list[dict])\n  \u2514\u2500 search_datasets_rich()   \u2192  list[RichDatasetInfo]\n\nDataset (facade)\n  \u2514\u2500 manifest()  \u2192  Manifest\n        \u251c\u2500 ManifestSummary\n        \u2514\u2500 [FileRecord, ...]\n\nDatasetInspector.inspect()  \u2192  DatasetProfile\n  \u251c\u2500 DatasetRef\n  \u251c\u2500 SnapshotRef\n  \u251c\u2500 MLReadinessScore\n  \u2514\u2500 RichDatasetInfo (optional)\n\nDownloadPlanner.plan()  \u2192  DownloadPlan\n  \u2514\u2500 [FileRecord, ...]\n\nConversionPipeline.run()  \u2192  Artifact\n  \u251c\u2500 ArtifactManifest\n  \u2514\u2500 split/\n       \u2514\u2500 *.parquet (or .zarr, .h5, etc.)\n</code></pre>"},{"location":"concepts/openneuro-and-bids/","title":"OpenNeuro and BIDS","text":"<p>Qortex is built specifically for OpenNeuro datasets that follow the BIDS standard. Understanding how these two relate clarifies what Qortex can and cannot do.</p>"},{"location":"concepts/openneuro-and-bids/#openneuro","title":"OpenNeuro","text":"<p>OpenNeuro is a platform for sharing neuroimaging data. Every dataset has a unique ID (e.g., <code>ds004130</code>) and is versioned through snapshots.</p> <p>Qortex communicates with OpenNeuro through its GraphQL API via <code>OpenNeuroClient</code>. This covers:</p> <ul> <li>Full dataset metadata (name, DOI, license, authors, funding, associated paper DOI)</li> <li>Community engagement (views, downloads, stars, followers)</li> <li>Snapshot summaries (subject count, session list, task list, file count, total size, BIDS version)</li> <li>Subject demographics (age, sex, group) from the API-level summary</li> <li>README text (the description shown on the OpenNeuro dataset page)</li> <li>BIDS validation issues (errors and warnings)</li> <li>CDN download URLs for individual files</li> </ul> <p>You do not need an account to read public datasets. Private datasets or embargo-period access require an API token.</p>"},{"location":"concepts/openneuro-and-bids/#snapshots","title":"Snapshots","text":"<p>A snapshot is a versioned, immutable copy of a dataset. Once published, a snapshot's content does not change. Snapshots are tagged with version strings like <code>1.0.0</code> or <code>2.1.3</code>.</p> <p>When you create a <code>Dataset</code> object without specifying a snapshot, Qortex uses the latest published snapshot. To pin a specific version:</p> <pre><code>from qortex import Dataset\nds = Dataset(\"ds000001\", snapshot=\"1.0.3\")\n</code></pre> <p>Pinning a snapshot ensures reproducibility. The same snapshot always has the same files at the same CDN URLs.</p>"},{"location":"concepts/openneuro-and-bids/#bids-structure","title":"BIDS structure","text":"<p>BIDS (Brain Imaging Data Structure) is a naming and organization convention for neuroimaging data. A BIDS dataset has a predictable directory tree:</p> <pre><code>dataset/\n  participants.tsv\n  dataset_description.json\n  sub-01/\n    anat/\n      sub-01_T1w.nii.gz\n      sub-01_T1w.json\n    func/\n      sub-01_task-rest_bold.nii.gz\n      sub-01_task-rest_bold.json\n      sub-01_task-rest_events.tsv\n    eeg/\n      sub-01_task-rest_eeg.set\n      sub-01_task-rest_eeg.json\n      sub-01_task-rest_channels.tsv\n  sub-02/\n    ...\n</code></pre> <p>Each file's name encodes BIDS entities: subject (<code>sub-</code>), session (<code>ses-</code>), task (<code>task-</code>), run (<code>run-</code>), and suffix (the last <code>_word</code> before the extension).</p>"},{"location":"concepts/openneuro-and-bids/#how-qortex-uses-bids","title":"How Qortex uses BIDS","text":"<p>Qortex parses BIDS entity labels from file paths in the manifest. It does not run a full BIDS parser on the remote tree \u2014 it extracts entities from path components using string rules.</p> <p>When you call <code>ds.files(subjects=[\"01\", \"02\"], tasks=[\"rest\"])</code>, Qortex filters the manifest by matching the extracted subject and task labels. The results contain all files whose paths include <code>sub-01/</code> or <code>sub-02/</code> and contain <code>task-rest_</code>.</p> <p>Companion files are resolved by matching the stem of the primary file. A file named <code>sub-01_task-rest_bold.nii.gz</code> has companions at paths like <code>sub-01_task-rest_bold.json</code>, <code>sub-01_task-rest_events.tsv</code>, and <code>sub-01_task-rest_desc-confounds_timeseries.tsv</code>.</p>"},{"location":"concepts/openneuro-and-bids/#bids-derivatives","title":"BIDS derivatives","text":"<p>Derivatives (preprocessed outputs from tools like fMRIPrep or FreeSurfer) live in a <code>derivatives/</code> subdirectory. Qortex excludes derivatives by default. To include them:</p> <pre><code>ds.download(include_derivatives=True)\n# or\nds.plan(include_derivatives=True)\n</code></pre>"},{"location":"concepts/openneuro-and-bids/#what-requires-an-account","title":"What requires an account","text":"<p>Reading public datasets: no account needed.</p> <p>Downloading files: no account needed for public datasets.</p> <p>Accessing private or embargoed datasets: requires an API token stored with <code>qortex login</code>.</p>"},{"location":"concepts/readiness-first/","title":"Readiness First","text":"<p>The central design decision in Qortex is that readiness checks happen before any large data transfer. This is not a convenience \u2014 it is the reason Qortex exists.</p>"},{"location":"concepts/readiness-first/#why-readiness-checks-matter","title":"Why readiness checks matter","text":"<p>Downloading an OpenNeuro dataset takes time and disk space. Many datasets, when fully downloaded, turn out to be unusable for the intended task. The events.tsv files may be missing. The label column may have only one class. The subjects may be too few to split. The NIfTI files may be pointer-like Git LFS objects that were not properly fetched.</p> <p>Running these checks against the remote manifest \u2014 before transfer \u2014 costs almost nothing. The manifest contains the file tree. The sidecar JSON files are small. Qortex reads exactly what it needs.</p>"},{"location":"concepts/readiness-first/#the-five-readiness-states","title":"The five readiness states","text":"<p>A dataset in Qortex moves through distinct states:</p> <p>Not inspected. Only the dataset ID is known. No manifest has been fetched.</p> <p>Manifest-only. The remote file tree has been fetched. Qortex knows which subjects, sessions, tasks, and modalities exist. It knows which files have events.tsv, bval/bvec, or JSON sidecar companions. No data is on local disk.</p> <p>Downloaded. Files exist locally. Content may still be incomplete (partial download, Git LFS pointers).</p> <p>Validated. The local BIDS structure has passed the official BIDS Validator. File counts match the manifest.</p> <p>Conversion-ready. All required companions are present. Windows can be extracted. Splits are feasible.</p>"},{"location":"concepts/readiness-first/#what-readiness-checks-return","title":"What readiness checks return","text":"<p>Each check returns a structured report with a state, findings, and a recommended next action. The reports are machine-readable (Pydantic models, JSON-serializable) so they can be embedded in pipelines.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\nprint(report.to_text())\n# State: manifest_only\n# Subjects: 88  Sessions: 1  Modalities: eeg  Events: yes (88 files)\n# Next action: download \u2014 use ds.minimum(goal=\"first-batch\") to get the smallest subset\n</code></pre>"},{"location":"concepts/readiness-first/#the-minimum-download-principle","title":"The minimum download principle","text":"<p><code>ds.minimum(goal=\"first-batch\")</code> computes the smallest real download that would let you extract one batch of training samples. This is not a rough estimate \u2014 it lists the exact files by path.</p> <p>Goals: - <code>first-batch</code> \u2014 enough data to verify the full pipeline end-to-end - <code>label-check</code> \u2014 enough events.tsv files to verify label coverage - <code>validation</code> \u2014 a representative subset for BIDS validation - <code>metadata</code> \u2014 metadata and sidecar files only, no large imaging files</p>"},{"location":"concepts/readiness-first/#companion-file-awareness","title":"Companion-file awareness","text":"<p>BIDS companion files travel with their primary files. A DWI NIfTI is not useful without its bval/bvec files. An fMRI run is not label-ready without its events.tsv. Qortex tracks companion relationships in the manifest and includes them automatically in any download plan.</p> <p>When you call <code>ds.download(suffixes=[\"bold\"])</code>, the plan automatically includes the JSON sidecar and events.tsv for each selected BOLD file.</p>"},{"location":"concepts/what-is-qortex/","title":"What is Qortex","text":"<p>Qortex is a Python library for preparing OpenNeuro neuroimaging datasets for machine learning. It is not a download manager. It is a decision layer.</p>"},{"location":"concepts/what-is-qortex/#the-problem-it-solves","title":"The problem it solves","text":"<p>OpenNeuro hosts over 1,700 public BIDS datasets. Many are not usable for supervised learning. Before you can tell whether a dataset is usable, you need to know:</p> <ul> <li>Do the fMRI files have events.tsv companions with parseable trial types?</li> <li>Are there enough subjects to split into train/val/test without leakage?</li> <li>Do the DWI files have bval/bvec companions?</li> <li>Is the total size within your storage budget?</li> </ul> <p>None of this requires downloading the data. The OpenNeuro manifest contains the file tree, and the sidecar JSON files are small enough to fetch over CDN without downloading the NIfTI volumes.</p> <p>Qortex reads the manifest first and answers these questions before any bulk transfer.</p>"},{"location":"concepts/what-is-qortex/#what-qortex-is-not","title":"What Qortex is not","text":"<p>Not a download manager. Tools like DataLad and the OpenNeuro CLI handle download protocol details. Qortex uses HTTP downloads with resume support, but its value is in the decisions around what to download, not the transfer mechanism itself.</p> <p>Not a preprocessing pipeline. Qortex converts to ML-ready formats (Parquet, Zarr, HDF5) but does not run fMRIPrep, FreeSurfer, or MNE preprocessing. It works with whatever the dataset contains \u2014 raw or preprocessed.</p> <p>Not a BIDS validator. Qortex wraps the official BIDS Validator for structural correctness checks. It does not reimplement BIDS validation rules.</p>"},{"location":"concepts/what-is-qortex/#what-it-does","title":"What it does","text":"<p>Qortex has five main functions:</p> <ol> <li> <p>Inspect \u2014 Read the remote manifest. Report subjects, sessions, tasks, modalities, companion coverage, and total size.</p> </li> <li> <p>Decide \u2014 Run readiness checks (doctor, minimum, can-train, first-batch) to answer whether a dataset is usable for a specific goal and what the minimum download is.</p> </li> <li> <p>Download \u2014 Transfer files with subject/modality/task filters, resume support, and companion-file awareness.</p> </li> <li> <p>Convert \u2014 Write ML-format artifacts (Parquet, Zarr, HDF5, WebDataset, HuggingFace, TFRecord) with sliding or event-aligned windows, subject-level splits, and provenance metadata.</p> </li> <li> <p>Inspect visually \u2014 Render thumbnails, QC panels, and overlays from local files without loading full volumes. One center slice per NIfTI.</p> </li> </ol>"},{"location":"concepts/what-is-qortex/#scope","title":"Scope","text":"<p>Qortex works with any BIDS-formatted data stored on OpenNeuro. It has been designed primarily around:</p> <ul> <li>fMRI (BOLD, resting-state)</li> <li>EEG/MEG/iEEG</li> <li>Structural MRI (T1w, T2w)</li> <li>DWI</li> <li>PET</li> </ul> <p>Surface data (GIFTI/CIFTI) is partially supported: typed inspection and QC summaries work for GIFTI meshes/scalars/labels and CIFTI dense matrices. Advanced workbench-style surface interaction and volume-to-surface projection are still future work.</p>"},{"location":"concepts/workflow/","title":"Workflow","text":"<p>A full Qortex workflow moves from catalog search to a trained model in six stages. Each stage has a decision gate before proceeding to the next.</p>"},{"location":"concepts/workflow/#stage-1-find-candidate-datasets","title":"Stage 1: Find candidate datasets","text":"<p>Search the local catalog (backed by DuckDB) or the live OpenNeuro API for datasets that match your modality and task requirements.</p> <pre><code>qortex search --modality eeg --task rest --min-subjects 50\n</code></pre> <p>Via Python \u2014 local catalog (fast, offline):</p> <pre><code>from qortex.catalog import DatasetQuery\n\nresults = (\n    DatasetQuery()\n    .modality(\"eeg\")\n    .task(\"rest\")\n    .min_subjects(50)\n    .has_events()\n    .fetch()\n)\n</code></pre> <p>Via Python \u2014 live API with engagement sorting:</p> <pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(\n        modality=\"eeg\", task=\"rest\", min_subjects=50, sort_by=\"downloads\"\n    )\n    for info in results:\n        print(info.id, info.name, info.engagement.downloads)\n</code></pre> <p>If you already know the dataset ID, skip the search:</p> <pre><code>from qortex import Dataset\nds = Dataset(\"ds004130\")\n</code></pre> <p>At this point, no network call has been made. The <code>Dataset</code> object holds only the ID.</p>"},{"location":"concepts/workflow/#stage-2-inspect-the-manifest","title":"Stage 2: Inspect the manifest","text":"<p>Fetch the remote file tree and check structural properties.</p> <pre><code>report = ds.doctor()\nprint(report.to_text())\n</code></pre> <p><code>doctor()</code> reads the manifest and a sample of sidecar files. It reports:</p> <ul> <li>Subject count and session count</li> <li>Modalities present</li> <li>Whether events.tsv files exist and are non-empty</li> <li>Whether bval/bvec companions exist for DWI</li> <li>Total uncompressed size estimate</li> </ul> <p>Decision gate: If <code>report.state</code> is <code>not_usable</code>, stop here. Do not download.</p>"},{"location":"concepts/workflow/#stage-3-verify-label-readiness","title":"Stage 3: Verify label readiness","text":"<p>Before downloading bulk imaging data, confirm that labels are present and have enough coverage.</p> <pre><code>ok = ds.can_train(target_col=\"trial_type\", min_classes=2, min_per_class=10)\nif not ok:\n    print(ds.label_landscape())\n</code></pre> <p>If the dataset does not have usable labels, the imaging data has no value for classification tasks.</p> <p>Decision gate: Proceed only if <code>can_train()</code> returns True.</p>"},{"location":"concepts/workflow/#stage-4-download-the-minimum","title":"Stage 4: Download the minimum","text":"<p><code>minimum()</code> returns the smallest download that enables the next step. <code>first_batch()</code> returns the subset needed to test the pipeline end-to-end.</p> <pre><code>plan = ds.minimum(goal=\"first-batch\")\nds.download_paths(plan.files)\n</code></pre> <p>The plan includes all companion files (events.tsv, JSON sidecars, bval/bvec) automatically.</p> <p>For a full selective download:</p> <pre><code>ds.download(subjects=[\"01\", \"02\", \"03\"], tasks=[\"rest\"], suffixes=[\"bold\"])\n</code></pre> <p>Decision gate: After download, run <code>ds.validate()</code> to confirm the local BIDS structure is complete.</p>"},{"location":"concepts/workflow/#stage-5-convert-to-ml-format","title":"Stage 5: Convert to ML format","text":"<p>Once data is on disk, convert to a format suitable for training.</p> <pre><code>art = ds.convert(\n    output_dir=\"converted/\",\n    format=\"parquet\",\n    window=dict(duration_s=30.0, overlap=0.5),\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\n</code></pre> <p>The <code>ConversionPipeline</code> writes one artifact directory with <code>train/</code>, <code>val/</code>, and <code>test/</code> subdirectories and an <code>artifact_manifest.json</code>.</p> <p>Decision gate: Run <code>ds.leakage_check(art)</code> to confirm no subject appears in two splits.</p>"},{"location":"concepts/workflow/#stage-6-load-for-training","title":"Stage 6: Load for training","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"converted/\")\n\n# PyTorch\ntrain_ds = art.torch(split=\"train\")\nval_ds   = art.torch(split=\"val\")\n\n# scikit-learn\nX_train, y_train = art.sklearn(split=\"train\")\nX_val,   y_val   = art.sklearn(split=\"val\")\n\n# HuggingFace Dataset\nhf_ds = art.huggingface(split=\"train\")\n</code></pre>"},{"location":"concepts/workflow/#skipping-stages","title":"Skipping stages","text":"<p>Not all projects need all stages. A team that already has a downloaded BIDS dataset on disk can start at Stage 5 by calling <code>ds.index_local(data_dir=...)</code> to build the local manifest without fetching from OpenNeuro.</p> <p>A team that already has a converted artifact can start at Stage 6.</p>"},{"location":"concepts/workflow/#parallel-workflows","title":"Parallel workflows","text":"<p>Multiple datasets can be inspected in parallel before committing to any download:</p> <pre><code>import asyncio\nfrom qortex import Dataset\n\nids = [\"ds000001\", \"ds000002\", \"ds004130\"]\ndatasets = [Dataset(d) for d in ids]\n\n# doctor() calls are fast (manifest + sidecar reads only)\nreports = [ds.doctor() for ds in datasets]\nusable = [r for r in reports if r.state != \"not_usable\"]\n</code></pre>"},{"location":"conversion/","title":"Conversion","text":"<p><code>ConversionPipeline</code> reads a local BIDS dataset and writes ML-format artifacts. The output is a directory with one subdirectory per split and an <code>artifact_manifest.json</code> describing the provenance, sample counts, feature names, and label classes.</p>"},{"location":"conversion/#why-a-separate-conversion-step","title":"Why a separate conversion step","text":"<p>Raw neuroimaging data is stored in formats optimized for storage and archive, not for training loops. NIfTI and EEGLAB files are read one volume at a time, have no random-access guarantees, and carry metadata in separate sidecar files. Parquet and Zarr expose array slices efficiently, support chunk caching, and pack all metadata inline.</p> <p>Conversion is a one-time cost. After conversion, loading a batch for training takes milliseconds instead of seconds.</p>"},{"location":"conversion/#conversion-pages","title":"Conversion pages","text":"<p>Pipeline \u2014 The full ConversionPipeline API: how formats, windows, splits, and label extraction are composed.</p> <p>Formats \u2014 Parquet, Zarr, HDF5, WebDataset, HuggingFace Dataset, TFRecord \u2014 requirements and trade-offs.</p> <p>Windows \u2014 Sliding and event-aligned windows: WindowSpec parameters and how windowing interacts with labels.</p> <p>Splits \u2014 Subject-level splits, stratification, and reproducible random seeds.</p> <p>Provenance \u2014 What is recorded in artifact_manifest.json and how to trace an artifact back to its source.</p> <p>EDA \u2014 Exploratory data analysis before conversion: signal statistics, label landscape, class imbalance.</p>"},{"location":"conversion/#quickstart","title":"Quickstart","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\n\nart = ds.convert(\n    output_dir=\"artifacts/ds004130/\",\n    format=\"parquet\",\n    window=dict(duration_s=30.0, overlap=0.5),\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\nprint(art.manifest.n_samples)  # total samples across all splits\n</code></pre> <p>Next \u2192 Artifacts \u2014 open, inspect, and connect your artifact to a training framework.</p>"},{"location":"conversion/eda/","title":"Exploratory Data Analysis","text":"<p><code>ds.eda()</code> runs signal statistics and label analysis on downloaded data before committing to a full conversion. It answers whether the data looks clean and whether the labels are usable.</p>"},{"location":"conversion/eda/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nreport = ds.eda(label_col=\"trial_type\")\nreport.show()\n</code></pre>"},{"location":"conversion/eda/#cli","title":"CLI","text":"<pre><code>qortex eda ds004130 --data-dir data/ds004130/ --label trial_type\n</code></pre>"},{"location":"conversion/eda/#what-eda-reports","title":"What EDA reports","text":""},{"location":"conversion/eda/#label-landscape","title":"Label landscape","text":"<p>Per-class sample counts across all subjects. Which subjects are missing which classes. See Label readiness.</p>"},{"location":"conversion/eda/#signal-statistics-per-subject-per-file","title":"Signal statistics (per subject, per file)","text":"<p>For each data file in the dataset:</p> <ul> <li>Sampling rate (EEG) / TR (fMRI)</li> <li>Duration in seconds</li> <li>Number of channels / voxels</li> <li>Mean signal amplitude</li> <li>Signal range (min, max, 1st/99th percentile)</li> <li>Channels with flatline signal (all-zero or constant) \u2014 likely bad channels</li> <li>Files shorter than the requested window duration (would be skipped at conversion)</li> </ul>"},{"location":"conversion/eda/#global-class-distribution","title":"Global class distribution","text":"<p>Stacked bar chart showing sample counts per class per subject.</p>"},{"location":"conversion/eda/#recommended-window-parameters","title":"Recommended window parameters","text":"<p>Based on the minimum recording duration and event durations, EDA suggests:</p> <pre><code>Recommended window: 30.0 s (max within shortest recording)\nRecommended overlap: 0.5 (standard for sliding windows)\nEstimated samples per split:\n  Train (60 subjects): ~1,200\n  Val   (14 subjects): ~  270\n  Test  (13 subjects): ~  270\n</code></pre>"},{"location":"conversion/eda/#example-output","title":"Example output","text":"<pre><code>EDA: ds004130\nData directory: data/ds004130/\n\nLabel column: trial_type\nClasses: rest (240), eyes-open (236), task (88) [IMBALANCED \u2014 task has 3\u00d7 fewer samples]\n\nSignal summary (88 subjects):\n  Sampling rate:  256 Hz (all subjects)\n  Duration:       10 min 0 s (min), 10 min 0 s (max)\n  Channels:       64 (all subjects)\n  Bad channels:   1 subject has flatline on Fp2\n\nFiles too short for 30 s window: 0\nMissing events.tsv: 2 subjects (sub-23, sub-47)\n</code></pre>"},{"location":"conversion/eda/#limitations","title":"Limitations","text":"<ul> <li>EDA requires a local data directory. It does not work against the remote manifest alone.</li> <li>Signal statistics are computed from small reads (first N seconds) to avoid loading full recordings.</li> <li>Bad channel detection uses a simple flatline heuristic (std &lt; 0.01 \u03bcV). Proper bad channel detection requires MNE-Python's interpolation pipeline.</li> </ul>"},{"location":"conversion/formats/","title":"Output Formats","text":"<p>Qortex supports six output formats for converted artifacts.</p>"},{"location":"conversion/formats/#parquet","title":"Parquet","text":"<p>Default format. Each split is written as a collection of Parquet files (shards). Each row is one window, columns are flattened features plus metadata (<code>subject_id</code>, <code>label</code>, <code>onset_s</code>, <code>run_id</code>).</p> <pre><code>pip install qortex  # no extras needed for Parquet\n</code></pre> <pre><code>art = ds.convert(format=\"parquet\", ...)\n\n# Load with pandas\nimport pandas as pd\ndf = pd.read_parquet(\"artifacts/ds004130/train/\")\nX = df.drop(columns=[\"subject_id\", \"label\", \"onset_s\"]).values\ny = df[\"label\"].values\n\n# Load with Qortex helper\nX, y = art.sklearn(split=\"train\")\n</code></pre> <p>Best for: tabular ML, scikit-learn, pandas workflows, &lt; 10 GB artifacts.</p> <p>Limitation: Parquet stores data as flat rows. For 4D fMRI data, each row contains a flattened volume. For large volumes (e.g., MNI-space BOLD at 91\u00d7109\u00d791), this produces very wide rows that may be slow to read.</p>"},{"location":"conversion/formats/#zarr","title":"Zarr","text":"<p>Chunked array format. Each split is a Zarr store with shape <code>(n_samples, ...)</code>. Supports random access by sample index without reading the full array.</p> <pre><code>pip install \"qortex[zarr]\"\n</code></pre> <pre><code>art = ds.convert(format=\"zarr\", ...)\n\nimport zarr\nz = zarr.open(\"artifacts/ds004130/train/data.zarr\")\nX = z[\"X\"][0:32]  # first 32 samples\ny = z[\"y\"][0:32]\n</code></pre> <p>Best for: large 4D data, random access, Dask pipelines, PyTorch DataPipes.</p>"},{"location":"conversion/formats/#hdf5","title":"HDF5","text":"<p>Similar to Zarr but stored in a single <code>.h5</code> file per split. Compatible with h5py and PyTables.</p> <pre><code>pip install \"qortex[hdf5]\"\n</code></pre> <pre><code>import h5py\nwith h5py.File(\"artifacts/ds004130/train/data.h5\", \"r\") as f:\n    X = f[\"X\"][:32]\n    y = f[\"y\"][:32]\n</code></pre> <p>Best for: legacy pipelines that already use HDF5, MATLAB interop.</p> <p>Limitation: A single <code>.h5</code> file can become a bottleneck under parallel reads.</p>"},{"location":"conversion/formats/#webdataset","title":"WebDataset","text":"<p>Sharded tar archives, each containing one sample as a pair of files: <code>{key}.npy</code> (features) and <code>{key}.cls</code> (label). Compatible with the WebDataset library.</p> <pre><code>pip install \"qortex[torch]\"  # webdataset is bundled with the torch extra\n</code></pre> <pre><code>import webdataset as wds\n\nds_wds = wds.WebDataset(\"artifacts/ds004130/train/shard-{000000..000099}.tar\")\nfor sample in ds_wds:\n    X = sample[\"npy\"]\n    y = sample[\"cls\"]\n</code></pre> <p>Best for: large-scale distributed training, cloud storage (S3, GCS).</p>"},{"location":"conversion/formats/#huggingface-dataset","title":"HuggingFace Dataset","text":"<p>Written as a HuggingFace <code>datasets.Dataset</code> on disk. Can be pushed directly to the HuggingFace Hub.</p> <pre><code>pip install \"qortex[hf]\"\n</code></pre> <pre><code>from datasets import load_from_disk\n\nhf_ds = load_from_disk(\"artifacts/ds004130/train/\")\nX = hf_ds[\"X\"]\ny = hf_ds[\"label\"]\n</code></pre> <p>Push to Hub:</p> <pre><code>hf_ds.push_to_hub(\"my-org/ds004130-eeg-parquet\")\n</code></pre> <p>Best for: sharing processed neuroimaging datasets on the HuggingFace Hub.</p>"},{"location":"conversion/formats/#tfrecord","title":"TFRecord","text":"<p>Serialized TensorFlow Example protos.</p> <pre><code>pip install \"qortex[tf]\"\n</code></pre> <pre><code>import tensorflow as tf\n\nraw_ds = tf.data.TFRecordDataset(\"artifacts/ds004130/train/*.tfrecord\")\n</code></pre> <p>Best for: TensorFlow training pipelines.</p> <p>Limitation: TFRecord is not human-readable and cannot be inspected without TensorFlow. Prefer Parquet for data inspection tasks.</p>"},{"location":"conversion/formats/#format-comparison","title":"Format comparison","text":"Format Random access Human-readable Cloud-friendly Extra needed Parquet Partial (by row group) Yes (via pandas) Yes None Zarr Yes No Yes <code>zarr</code> HDF5 Yes No Limited <code>hdf5</code> WebDataset Sequential No Yes <code>torch</code> HuggingFace Yes Yes (via Hub) Yes <code>hf</code> TFRecord Sequential No Yes <code>tf</code>"},{"location":"conversion/pipeline/","title":"Conversion Pipeline","text":"<p><code>ConversionPipeline</code> orchestrates format conversion from BIDS to ML-ready artifacts. It chains windowing, label extraction, and splitting into one pass.</p>"},{"location":"conversion/pipeline/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nart = ds.convert(\n    output_dir=\"artifacts/ds004130/\",\n    format=\"parquet\",\n    window=dict(duration_s=30.0, overlap=0.5),\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\n</code></pre> <p>Or using the pipeline directly:</p> <pre><code>from qortex.convert import ConversionPipeline, WindowSpec, SplitSpec\n\npipeline = ConversionPipeline(\n    data_dir=\"data/ds004130/\",\n    output_dir=\"artifacts/ds004130/\",\n    format=\"parquet\",\n    window=WindowSpec(duration_s=30.0, overlap=0.5),\n    split=SplitSpec(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\nart = pipeline.run()\n</code></pre>"},{"location":"conversion/pipeline/#parameters","title":"Parameters","text":"Parameter Type Default Description <code>data_dir</code> <code>Path</code> required Local BIDS directory <code>output_dir</code> <code>Path</code> required Where to write the artifact <code>format</code> <code>str</code> <code>\"parquet\"</code> Output format <code>window</code> <code>WindowSpec</code> or dict <code>{\"duration_s\": 30.0}</code> Window parameters <code>split</code> <code>SplitSpec</code> or dict <code>{\"strategy\": \"subject\"}</code> Split parameters <code>label_col</code> <code>str</code> or None None events.tsv column to use as labels <code>subjects</code> <code>list[str]</code> or None None Subjects to include (all if None) <code>tasks</code> <code>list[str]</code> or None None Tasks to include (all if None) <code>suffixes</code> <code>list[str]</code> or None None BIDS suffixes to include <code>seed</code> <code>int</code> <code>42</code> Random seed for split assignment <code>n_workers</code> <code>int</code> <code>4</code> Parallel workers for file reading <code>overwrite</code> <code>bool</code> <code>False</code> Overwrite existing artifact"},{"location":"conversion/pipeline/#processing-order","title":"Processing order","text":"<ol> <li>Scan <code>data_dir</code> and build a local manifest</li> <li>Filter files by <code>subjects</code>, <code>tasks</code>, <code>suffixes</code></li> <li>Assign subjects to splits (see Splits)</li> <li>For each file:    a. Read the time series (NIfTI / EEG / etc.)    b. Apply windowing (see Windows)    c. Extract labels from events.tsv if <code>label_col</code> is set    d. Write windows to the split's output shard</li> <li>Write <code>artifact_manifest.json</code></li> </ol>"},{"location":"conversion/pipeline/#filtering-during-conversion","title":"Filtering during conversion","text":"<p>You can exclude specific subjects or tasks during conversion:</p> <pre><code>art = ds.convert(\n    output_dir=\"artifacts/\",\n    subjects=[f\"{i:02d}\" for i in range(1, 61)],  # first 60 subjects\n    tasks=[\"rest\"],\n    label_col=\"trial_type\",\n)\n</code></pre> <p>This is more efficient than post-hoc filtering because unneeded files are never read.</p>"},{"location":"conversion/pipeline/#cli","title":"CLI","text":"<pre><code>qortex convert ds004130 \\\n    --data-dir data/ds004130/ \\\n    --output artifacts/ds004130/ \\\n    --format parquet \\\n    --window 30 \\\n    --overlap 0.5 \\\n    --label trial_type \\\n    --val-frac 0.15 \\\n    --test-frac 0.15\n</code></pre>"},{"location":"conversion/pipeline/#after-conversion","title":"After conversion","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nprint(art.manifest.n_samples)\nprint(art.manifest.splits)\n</code></pre> <p>See Formats for format-specific loading.</p>"},{"location":"conversion/pipeline/#related","title":"Related","text":"<ul> <li>Formats \u2014 Parquet, Zarr, HDF5, WebDataset, HuggingFace, TFRecord</li> <li>Windows \u2014 WindowSpec: duration, overlap, event alignment</li> <li>Splits \u2014 SplitSpec: subject strategy, stratification, seeds</li> </ul>"},{"location":"conversion/provenance/","title":"Provenance","text":"<p>Every Qortex artifact contains an <code>artifact_manifest.json</code> that records the full provenance of the conversion. This file is the artifact's identity \u2014 it lets you trace any model prediction back to the exact dataset version, conversion parameters, and split assignment that produced the training data.</p>"},{"location":"conversion/provenance/#artifact_manifestjson-structure","title":"artifact_manifest.json structure","text":"<pre><code>{\n  \"qortex_version\": \"0.3.1\",\n  \"created_at\": \"2024-01-15T14:23:00Z\",\n  \"source_dataset\": \"ds004130\",\n  \"source_snapshot\": \"1.2.0\",\n  \"source_doi\": \"10.18112/openneuro.ds004130.v1.2.0\",\n  \"format\": \"parquet\",\n  \"label_col\": \"trial_type\",\n  \"label_classes\": [\"rest\", \"eyes-open\", \"task\"],\n  \"feature_names\": [\"Fp1_0\", \"Fp1_1\", ..., \"O2_7679\"],\n  \"n_samples\": 1740,\n  \"splits\": {\n    \"train\": {\"n_samples\": 1200, \"n_subjects\": 61},\n    \"val\":   {\"n_samples\": 270,  \"n_subjects\": 14},\n    \"test\":  {\"n_samples\": 270,  \"n_subjects\": 13}\n  },\n  \"train_subjects\": [\"01\", \"02\", ...],\n  \"val_subjects\":   [\"63\", \"64\", ...],\n  \"test_subjects\":  [\"77\", \"78\", ...],\n  \"window\": {\n    \"duration_s\": 30.0,\n    \"overlap\": 0.5,\n    \"event_aligned\": false\n  },\n  \"split\": {\n    \"strategy\": \"subject\",\n    \"val_frac\": 0.15,\n    \"test_frac\": 0.15,\n    \"stratify_by_label\": true,\n    \"seed\": 42\n  },\n  \"subjects_included\": [\"01\", \"02\", ..., \"88\"],\n  \"tasks_included\": [\"rest\"],\n  \"suffixes_included\": [\"eeg\"]\n}\n</code></pre>"},{"location":"conversion/provenance/#reading-provenance-from-python","title":"Reading provenance from Python","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130/\")\nmf = art.manifest\n\nprint(mf.source_dataset)    # \"ds004130\"\nprint(mf.source_snapshot)   # \"1.2.0\"\nprint(mf.created_at)        # datetime object\nprint(mf.qortex_version)    # \"0.3.1\"\nprint(mf.window.duration_s) # 30.0\nprint(mf.split.seed)        # 42\n</code></pre>"},{"location":"conversion/provenance/#provenance-from-individual-visualresult","title":"Provenance from individual VisualResult","text":"<p>Each rendered figure also carries a provenance dict:</p> <pre><code>from qortex.visualize import fmri_summary\n\nresult = fmri_summary(\"sub-01_task-rest_bold.nii.gz\")\nprov = result.to_provenance_dict()\nprint(prov[\"qortex_version\"])\nprint(prov[\"file\"])\nprint(prov[\"render_params\"])\n</code></pre>"},{"location":"conversion/provenance/#using-provenance-in-model-cards","title":"Using provenance in model cards","text":"<p>When publishing a model trained on Qortex artifacts, include the artifact manifest in the model card:</p> <pre><code>import json\n\nwith open(\"artifacts/ds004130/artifact_manifest.json\") as f:\n    prov = json.load(f)\n\nprint(f\"Trained on {prov['source_dataset']} @ {prov['source_snapshot']}\")\nprint(f\"DOI: {prov['source_doi']}\")\nprint(f\"Split seed: {prov['split']['seed']}\")\n</code></pre>"},{"location":"conversion/provenance/#limitations","title":"Limitations","text":"<ul> <li>Provenance covers the Qortex conversion step only. If the source data was preprocessed before indexing (e.g., with fMRIPrep), that preprocessing is not recorded in <code>artifact_manifest.json</code>.</li> <li>Subject IDs in the manifest are the raw BIDS subject labels. They do not include PHI \u2014 OpenNeuro datasets use coded participant IDs.</li> </ul>"},{"location":"conversion/splits/","title":"Splits","text":"<p>Qortex assigns subjects to train, val, and test splits before windowing. This ensures that all windows from a given subject land in exactly one split \u2014 preventing subject leakage between train and test.</p>"},{"location":"conversion/splits/#splitspec","title":"SplitSpec","text":"<pre><code>from qortex.convert import SplitSpec\n\nsplit = SplitSpec(\n    strategy=\"subject\",      # always use subject-level assignment\n    val_frac=0.15,           # 15% of subjects to validation\n    test_frac=0.15,          # 15% of subjects to test\n    stratify_by_label=True,  # balance label classes across splits\n    seed=42,                 # random seed for reproducibility\n)\n</code></pre> <p>Pass as a dict to <code>ds.convert()</code>:</p> <pre><code>art = ds.convert(\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    ...\n)\n</code></pre>"},{"location":"conversion/splits/#strategy-subject","title":"Strategy: subject","text":"<p>The only supported strategy. Subjects are randomly assigned to splits. All windows from a given subject go to the same split.</p> <p>Why this is the only strategy: Random window-level splitting (without subject grouping) produces optimistic test metrics. A classifier can learn subject-specific features (head shape, electrode impedance, scanner noise) and generalize to the test set because the test set contains data from the same subjects. This is not what you want. Subject-level splitting enforces that the test set contains subjects the model has never seen.</p>"},{"location":"conversion/splits/#val-and-test-fractions","title":"Val and test fractions","text":"<p>The train fraction is <code>1 - val_frac - test_frac</code>. With 88 subjects and default fractions:</p> <ul> <li>Train: 75 subjects</li> <li>Val: 13 subjects</li> <li>Test: 8 subjects (minimum for 15% of 88)</li> </ul> <p>If the dataset has fewer than 6 subjects, val and test may have only 1 subject each.</p>"},{"location":"conversion/splits/#stratification","title":"Stratification","text":"<p>When <code>stratify_by_label=True</code>, the split assignment tries to balance label class proportions across splits. This is important when classes are rare \u2014 without stratification, a minority class might end up entirely in train with no representation in test.</p> <p>Stratification uses the subject's most common label class as the stratification key. It is an approximation, not a guarantee, because subjects rarely have perfectly balanced trial counts.</p>"},{"location":"conversion/splits/#reproducibility","title":"Reproducibility","text":"<p>The same <code>seed</code> value with the same <code>val_frac</code> and <code>test_frac</code> always produces the same split assignment. The seed is recorded in <code>artifact_manifest.json</code>.</p> <p>If you need to re-run conversion with the same split, pass the original seed:</p> <pre><code>art = ds.convert(split=dict(strategy=\"subject\", seed=42), ...)\n</code></pre>"},{"location":"conversion/splits/#reading-split-assignments","title":"Reading split assignments","text":"<p>After conversion, see which subjects are in each split:</p> <pre><code>art = Artifact.open(\"artifacts/ds004130/\")\nprint(art.manifest.train_subjects)\nprint(art.manifest.val_subjects)\nprint(art.manifest.test_subjects)\n</code></pre>"},{"location":"conversion/splits/#leakage-verification","title":"Leakage verification","text":"<p>After conversion, verify no subject appears in two splits:</p> <pre><code>result = ds.leakage_check(art)\nprint(result.ok)  # True if no leakage\n</code></pre> <p>See Leakage check.</p>"},{"location":"conversion/windows/","title":"Windows","text":"<p>A window is a fixed-length segment of a time series. The conversion pipeline extracts windows from each recording and writes them as rows in the output artifact.</p>"},{"location":"conversion/windows/#windowspec","title":"WindowSpec","text":"<pre><code>from qortex.convert import WindowSpec\n\nwindow = WindowSpec(\n    duration_s=30.0,      # window length in seconds\n    overlap=0.5,           # fraction of overlap between adjacent windows (0\u20131)\n    tmin=None,             # start offset from trial onset (for event-aligned)\n    event_aligned=False,   # if True, use event onsets instead of sliding windows\n)\n</code></pre> <p>Pass as a dict to <code>ds.convert()</code>:</p> <pre><code>art = ds.convert(\n    window=dict(duration_s=30.0, overlap=0.5),\n    ...\n)\n</code></pre>"},{"location":"conversion/windows/#sliding-windows","title":"Sliding windows","text":"<p>The default mode. Starting from time 0, windows are extracted at regular intervals:</p> <ul> <li>Window 1: <code>[0, duration_s)</code></li> <li>Window 2: <code>[duration_s * (1 - overlap), duration_s * (1 - overlap) + duration_s)</code></li> <li>...</li> </ul> <p>Windows that extend past the end of the recording are dropped.</p> <p>With <code>duration_s=30</code> and <code>overlap=0.5</code>, a 10-minute recording yields: <pre><code>(600 - 30) / (30 * 0.5) + 1 = 39 windows\n</code></pre></p>"},{"location":"conversion/windows/#event-aligned-windows","title":"Event-aligned windows","text":"<p>When <code>event_aligned=True</code>, windows are extracted relative to event onsets in the events.tsv:</p> <pre><code>window = WindowSpec(\n    duration_s=1.0,\n    tmin=-0.2,            # 200 ms before onset\n    event_aligned=True,\n)\n</code></pre> <p>Each row in events.tsv with <code>trial_type</code> equal to the target label produces one window. The window runs from <code>onset + tmin</code> to <code>onset + tmin + duration_s</code>.</p> <p>Labels come from the <code>trial_type</code> (or <code>label_col</code>) value for that event.</p>"},{"location":"conversion/windows/#choosing-a-window-strategy","title":"Choosing a window strategy","text":"<p>Use sliding windows when: - The recording is a single condition throughout (resting-state) - You want maximum sample count - You do not have event markers</p> <p>Use event-aligned windows when: - The experiment has discrete trials with onset times - You want each sample to correspond to a single trial - You need the label to be known at window time (not inferred post-hoc)</p>"},{"location":"conversion/windows/#window-size-and-feature-dimensions","title":"Window size and feature dimensions","text":"<p>After windowing, each sample has shape <code>(n_channels, n_timepoints)</code>, where:</p> <pre><code>n_timepoints = duration_s \u00d7 sampling_rate\n</code></pre> <p>For EEG at 256 Hz with 30 s windows: <code>n_timepoints = 7680</code>.</p> <p>For fMRI with TR = 2.0 s and 30 s windows: <code>n_timepoints = 15</code> (in volume units, not seconds).</p> <p>The feature dimension in the Parquet output is flattened: <code>n_channels \u00d7 n_timepoints</code>.</p>"},{"location":"conversion/windows/#handling-recordings-shorter-than-the-window","title":"Handling recordings shorter than the window","text":"<p>If a recording is shorter than <code>duration_s</code>, the file is skipped with a warning. No partial windows are produced.</p> <p>Set <code>min_duration_s</code> to skip files below a threshold:</p> <pre><code>window = WindowSpec(duration_s=30.0, min_duration_s=60.0)\n# Only process recordings of at least 60 seconds\n</code></pre>"},{"location":"conversion/windows/#related","title":"Related","text":"<ul> <li>Pipeline \u2014 full ConversionPipeline configuration</li> <li>Splits \u2014 how subjects are assigned to train/val/test after windowing</li> </ul>"},{"location":"dataset/","title":"Dataset","text":"<p>Qortex provides two main entry points for working with OpenNeuro datasets:</p> <ul> <li><code>Dataset</code> \u2014 the high-level facade for a single dataset: inspect, download, convert, visualize.</li> <li><code>OpenNeuroClient</code> \u2014 the low-level GraphQL client for rich metadata, search, README, and BIDS validation.</li> </ul>"},{"location":"dataset/#dataset-single-dataset-facade","title":"Dataset \u2014 single-dataset facade","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nds = Dataset(\"ds004130\", snapshot=\"1.2.0\")            # pin version\nds = Dataset(\"ds004130\", data_dir=Path(\"/data/ds004130\"))  # local dir\n</code></pre> <p>Creating a <code>Dataset</code> object makes no network calls. Network access begins when you call a method that needs the manifest.</p>"},{"location":"dataset/#openneuroclient-rich-metadata-and-search","title":"OpenNeuroClient \u2014 rich metadata and search","text":"<pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    info    = client.get_dataset_rich(\"ds008039\")          # full metadata in one call\n    readme  = client.get_readme(\"ds008039\")                 # description text\n    issues  = client.get_validation_issues(\"ds008039\", \"1.0.0\")  # BIDS issues\n    snaps   = client.get_snapshots(\"ds008039\")              # all versions\n\n    results = client.search_datasets_rich(\n        modality=\"eeg\", min_subjects=30, sort_by=\"downloads\"\n    )\n</code></pre>"},{"location":"dataset/#pages-in-this-section","title":"Pages in this section","text":"<p>Inspect \u2014 Rich API metadata (<code>RichDatasetInfo</code>) and manifest-level profiles (<code>DatasetProfile</code>). Covers every field visible on an OpenNeuro dataset page, including engagement metrics, demographics, funding, README, and BIDS validation.</p> <p>Metadata \u2014 Read sidecar JSON, events.tsv, participants.tsv, and dataset_description.json without downloading imaging files.</p> <p>Search &amp; filter \u2014 <code>DatasetQuery</code> fluent builder with 10 filters, local catalog, live API search, <code>search_datasets_rich()</code> with engagement sorting.</p> <p>Snapshots \u2014 List available snapshot versions, compare manifests between snapshots.</p> <p>BIDS entities \u2014 The entity labels parsed from file paths: subject, session, task, run, datatype, suffix.</p> <p>Next \u2192 Assess readiness \u2014 once you know what the dataset contains, check whether it's actually usable for training.</p>"},{"location":"dataset/bids-entities/","title":"BIDS Entities","text":"<p>BIDS entity labels are parsed from file paths in the manifest. Qortex uses these labels for filtering files, computing coverage, and checking companion relationships.</p>"},{"location":"dataset/bids-entities/#entity-labels","title":"Entity labels","text":"<p>A BIDS file path encodes several key-value pairs called entities. For example:</p> <pre><code>sub-01/func/sub-01_ses-01_task-rest_run-01_bold.nii.gz\n</code></pre> <p>This file has:</p> Entity Key Value Subject <code>sub</code> <code>01</code> Session <code>ses</code> <code>01</code> Task <code>task</code> <code>rest</code> Run <code>run</code> <code>01</code> Datatype (directory) <code>func</code> Suffix (before extension) <code>bold</code> Extension <code>.nii.gz</code>"},{"location":"dataset/bids-entities/#entities-qortex-parses","title":"Entities Qortex parses","text":"<ul> <li><code>subject</code> \u2014 always present; extracted from <code>sub-{label}/</code> in the path</li> <li><code>session</code> \u2014 present if the path contains <code>ses-{label}/</code></li> <li><code>task</code> \u2014 present if the filename contains <code>task-{label}_</code></li> <li><code>run</code> \u2014 present if the filename contains <code>run-{label}_</code></li> <li><code>acquisition</code> \u2014 present if the filename contains <code>acq-{label}_</code></li> <li><code>direction</code> \u2014 present if the filename contains <code>dir-{label}_</code></li> <li><code>echo</code> \u2014 present if the filename contains <code>echo-{label}_</code></li> <li><code>part</code> \u2014 present if the filename contains <code>part-{label}_</code></li> <li><code>datatype</code> \u2014 directory containing the file (<code>anat</code>, <code>func</code>, <code>eeg</code>, <code>dwi</code>, <code>perf</code>, <code>pet</code>, <code>meg</code>, <code>ieeg</code>, <code>beh</code>)</li> <li><code>suffix</code> \u2014 the last underscore-delimited segment before the file extension</li> <li><code>extension</code> \u2014 file extension including the dot (<code>.nii.gz</code>, <code>.tsv</code>, <code>.json</code>, <code>.set</code>, <code>.fif</code>, <code>.edf</code>, <code>.bval</code>, <code>.bvec</code>)</li> </ul>"},{"location":"dataset/bids-entities/#accessing-entity-labels","title":"Accessing entity labels","text":"<p>From a FileRecord:</p> <pre><code>manifest = ds.manifest()\nfor f in manifest.files:\n    print(f.subject, f.task, f.suffix)\n</code></pre> <p>From a filter call:</p> <pre><code>bold_files = ds.files(subjects=[\"01\", \"02\"], tasks=[\"rest\"], suffixes=[\"bold\"])\n</code></pre>"},{"location":"dataset/bids-entities/#filtering-rules","title":"Filtering rules","text":"<p>When you pass a list to <code>subjects=</code>, <code>tasks=</code>, <code>suffixes=</code>, or <code>sessions=</code>:</p> <ul> <li><code>None</code> means \"no filter on this entity\" (include all values)</li> <li>An empty list <code>[]</code> means \"exclude all\" (returns nothing)</li> <li>A list of strings means \"include only these values\"</li> </ul> <pre><code># Only resting-state BOLD from subjects 01\u201305\nds.files(\n    subjects=[\"01\", \"02\", \"03\", \"04\", \"05\"],\n    tasks=[\"rest\"],\n    suffixes=[\"bold\"],\n)\n\n# All EEG files regardless of task\nds.files(datatypes=[\"eeg\"])\n\n# All anatomical files\nds.files(datatypes=[\"anat\"])\n</code></pre>"},{"location":"dataset/bids-entities/#companion-file-relationships","title":"Companion file relationships","text":"<p>Qortex uses entity labels to resolve companions. The companion of <code>sub-01_task-rest_bold.nii.gz</code> is:</p> <ul> <li><code>sub-01_task-rest_bold.json</code> \u2014 same entities, suffix <code>bold</code>, extension <code>.json</code></li> <li><code>sub-01_task-rest_events.tsv</code> \u2014 same entities, suffix <code>events</code>, extension <code>.tsv</code></li> <li><code>sub-01_task-rest_desc-confounds_timeseries.tsv</code> \u2014 same entities (if present)</li> </ul> <p>For DWI, the companions of <code>sub-01_dwi.nii.gz</code> are:</p> <ul> <li><code>sub-01_dwi.bval</code></li> <li><code>sub-01_dwi.bvec</code></li> <li><code>sub-01_dwi.json</code></li> </ul> <p>When you download a NIfTI file, Qortex includes its companions in the download plan by default. Suppress this with <code>include_companions=False</code> in the download call.</p>"},{"location":"dataset/bids-entities/#entities-that-qortex-does-not-use","title":"Entities that Qortex does not use","text":"<p>Qortex does not parse <code>space</code>, <code>res</code>, <code>den</code>, <code>hemi</code>, <code>label</code>, or <code>desc</code> entities. Files with these entities are still included in the manifest and can be downloaded, but the entities are not parsed for filtering or companion matching.</p>"},{"location":"dataset/inspect/","title":"Inspect","text":"<p>Qortex provides two levels of dataset inspection: a lightweight manifest-level profile built from the file tree (<code>DatasetInspector</code>), and a rich API-level metadata object sourced directly from the OpenNeuro GraphQL API (<code>RichDatasetInfo</code>). Use them together to get the full picture before downloading anything.</p>"},{"location":"dataset/inspect/#rich-api-metadata","title":"Rich API metadata","text":"<p><code>OpenNeuroClient.get_dataset_rich()</code> returns a <code>RichDatasetInfo</code> object covering every field visible on an OpenNeuro dataset page \u2014 without downloading or iterating the file tree.</p> <pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    info = client.get_dataset_rich(\"ds008039\")\n\n# Identity\ninfo.id                               # \"ds008039\"\ninfo.name                             # dataset name from dataset_description.json\ninfo.doi                              # \"10.18112/openneuro.ds008039.v1.0.0\"\ninfo.license                          # \"CC0\"\ninfo.authors                          # [\"Doe J\", \"Smith A\", ...]\ninfo.senior_author                    # last/senior author name\n\n# Scientific context\ninfo.modalities                       # [\"MRI\"]\ninfo.tasks                            # [\"rest\", \"nback\"]\ninfo.species                          # \"human\"\ninfo.study_domain                     # e.g. \"cognitive neuroscience\"\ninfo.study_design                     # e.g. \"longitudinal\"\ninfo.data_processed                   # True/False \u2014 preprocessed data included\n\n# Publication &amp; funding\ninfo.associated_paper_doi             # linked journal article DOI\ninfo.grant_funder                     # funding agency name\ninfo.grant_id                         # grant number\n\n# Dates\ninfo.created                          # ISO 8601 string: \"2024-03-15T10:22:00.000Z\"\ninfo.publish_date                     # ISO 8601 string\n\n# Community engagement\ninfo.engagement.views                 # total page views\ninfo.engagement.downloads             # total file downloads\ninfo.engagement.stars                 # number of stars\ninfo.engagement.followers             # number of followers\ninfo.engagement.popularity_score      # composite 0\u2013100 score\n\n# Snapshot summary (subject counts, file counts \u2014 no file tree needed)\nsnap = info.latest_snapshot_summary\nsnap.tag                              # \"1.0.0\"\nsnap.n_subjects                       # 42\nsnap.n_sessions                       # 2\nsnap.n_tasks                          # 3\nsnap.subjects                         # [\"sub-01\", \"sub-02\", ...]\nsnap.sessions                         # [\"ses-01\", \"ses-02\"]\nsnap.tasks                            # [\"rest\", \"nback\"]\nsnap.total_files                      # 1847\nsnap.total_size_gb                    # 14.23\nsnap.bids_version                     # \"1.8.0\"\nsnap.funding                          # [\"NIH R01 MH123456\", ...]\nsnap.references                       # [\"https://doi.org/...\", ...]\nsnap.ethics_approvals                 # [\"IRB approval #2024-001\"]\nsnap.how_to_acknowledge               # citation string\n\n# Subject demographics\nsnap.demographics_dataframe()         # Polars DataFrame: participant_id, age, sex, group\nsnap.age_stats()                      # {\"n\": 42, \"mean\": 28.4, \"min\": 18, \"max\": 65, ...}\n</code></pre> <p>Export to dict or JSON:</p> <pre><code>import json\nprint(json.dumps(info.to_dict(), indent=2))\n</code></pre>"},{"location":"dataset/inspect/#readme-description-text","title":"README / description text","text":"<p>The text shown as \"Description\" on the OpenNeuro dataset page comes from the README file. Fetch it in one call:</p> <pre><code>with OpenNeuroClient() as client:\n    readme = client.get_readme(\"ds008039\")           # latest snapshot\n    readme = client.get_readme(\"ds008039\", \"1.0.0\")  # specific version\n\nprint(readme)  # full README text\n</code></pre> <p>Returns <code>None</code> if the dataset has no README.</p>"},{"location":"dataset/inspect/#bids-validation-issues","title":"BIDS validation issues","text":"<pre><code>with OpenNeuroClient() as client:\n    issues = client.get_validation_issues(\"ds008039\", \"1.0.0\")\n\nfor issue in issues:\n    print(issue[\"severity\"], issue[\"key\"], issue[\"reason\"])\n    # \"error\"   MISSING_FILE   \"Required file /participants.tsv not found\"\n</code></pre> <p>Each issue dict has: <code>severity</code> (<code>\"error\"</code> or <code>\"warning\"</code>), <code>key</code>, <code>reason</code>, <code>files</code>, <code>helpUrl</code>.</p>"},{"location":"dataset/inspect/#manifest-level-profile","title":"Manifest-level profile","text":"<p><code>DatasetInspector.inspect()</code> fetches the file tree for a snapshot and builds a structural profile with BIDS entity parsing, modality breakdowns, and ML readiness scoring.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nprofile = ds.inspect()\n\nprint(profile)           # human-readable summary\n</code></pre> <p>The returned <code>DatasetProfile</code> has:</p> <pre><code>profile.dataset_ref        # DatasetRef (id, name, doi, license, authors, modalities, tasks)\nprofile.snapshot           # SnapshotRef (tag, hexsha, doi)\nprofile.all_snapshots      # list[SnapshotRef] \u2014 all available versions\n\n# Structural counts\nprofile.n_subjects         # integer\nprofile.n_sessions         # integer\nprofile.n_tasks            # integer\nprofile.subjects           # [\"sub-01\", ...]\nprofile.sessions           # [\"ses-01\", ...]\nprofile.tasks              # [\"rest\", ...]\n\n# Modality breakdown\nprofile.modality_breakdown  # {\"eeg\": ModalityBreakdown(...), ...}\n\n# Cross-subject coverage matrices\nprofile.subject_task_matrix     # {\"sub-01\": [\"rest\", \"nback\"], ...}\nprofile.subject_session_matrix  # {\"sub-01\": [\"ses-01\", \"ses-02\"], ...}\n\n# File counts\nprofile.n_signal_files      # signal-type files only\nprofile.n_events_files      # *_events.tsv count\nprofile.n_sidecar_files     # *.json sidecar count\nprofile.n_derivative_files  # derivatives/* count\nprofile.total_size_gb       # float\nprofile.per_subject_avg_gb  # float\n\n# Companion coverage (fraction of signal files with companion)\nprofile.events_coverage     # 0.0\u20131.0\nprofile.channels_coverage   # 0.0\u20131.0\nprofile.sidecar_coverage    # 0.0\u20131.0\n\n# Structure checks\nprofile.has_participants_tsv        # bool\nprofile.has_dataset_description     # bool\nprofile.has_readme                  # bool\n\n# ML readiness\nprofile.ml_readiness        # MLReadinessScore\nprofile.ml_readiness.total  # 0\u2013100\nprofile.ml_readiness.grade  # \"A\" | \"B\" | \"C\" | \"D\" | \"F\"\n\n# Recommendations\nprofile.recommendations     # list[str] \u2014 human-readable suggestions\n\n# Rich API metadata (populated when OpenNeuroClient is available)\nprofile.rich_info           # RichDatasetInfo or None\n</code></pre>"},{"location":"dataset/inspect/#cli","title":"CLI","text":"<pre><code>qortex inspect ds004130\n</code></pre> <p>Output:</p> <pre><code>Dataset:   ds004130 \u2014 EEG resting-state\nSnapshot:  1.2.0\nDOI:       10.18112/openneuro.ds004130.v1.2.0\nSubjects:  88\nSessions:  1\nModalities: eeg\nTasks:     rest\nFiles:     1,056\nSize:      4.2 GB\nEvents:    yes (88 files, columns: onset duration trial_type)\n</code></pre> <p>Add <code>--json</code> for machine-readable output:</p> <pre><code>qortex inspect ds004130 --json\n</code></pre>"},{"location":"dataset/inspect/#all-available-snapshots","title":"All available snapshots","text":"<pre><code>with OpenNeuroClient() as client:\n    snapshots = client.get_snapshots(\"ds008039\")\n\nfor s in snapshots:\n    print(s.tag, s.created, s.size)\n</code></pre>"},{"location":"dataset/inspect/#local-dataset","title":"Local dataset","text":"<p>If you have a local BIDS directory and want to build a manifest from disk rather than OpenNeuro:</p> <pre><code>ds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nds.index_local()     # scan local tree, write .qortex/manifest.json\nprofile = ds.inspect()\n</code></pre> <p>CLI equivalent:</p> <pre><code>qortex local-index data/ds004130/ --dataset-id ds004130\nqortex inspect ds004130 --local\n</code></pre>"},{"location":"dataset/inspect/#related","title":"Related","text":"<ul> <li><code>DatasetQuery</code> \u2014 search and filter across all public datasets</li> <li><code>ds.metadata()</code> \u2014 read sidecar files, events.tsv, participants.tsv</li> <li><code>ds.doctor()</code> \u2014 full ML readiness report</li> </ul>"},{"location":"dataset/metadata/","title":"Metadata","text":"<p>Qortex can read sidecar files, events.tsv, participants.tsv, and dataset_description.json without downloading the full imaging data. These files are small and fetched directly from the OpenNeuro CDN.</p>"},{"location":"dataset/metadata/#dataset-level-metadata","title":"Dataset-level metadata","text":"<pre><code>ds = Dataset(\"ds004130\")\n\n# Root-level files\ndesc = ds.metadata()         # dataset_description.json as dict\nparts = ds.participants()    # participants.tsv as pandas DataFrame\n</code></pre> <p>The participants.tsv DataFrame has columns like <code>participant_id</code>, <code>age</code>, <code>sex</code>, and any other columns that dataset depositors included. Column names vary per dataset.</p>"},{"location":"dataset/metadata/#file-level-sidecars","title":"File-level sidecars","text":"<pre><code># JSON sidecar for a specific file\nsidecar = ds.sidecar(\"sub-01/eeg/sub-01_task-rest_eeg.json\")\n\n# Events for a specific run\nevents = ds.events(subject=\"01\", task=\"rest\")\n# returns a pandas DataFrame: onset, duration, trial_type, ...\n</code></pre> <p><code>sidecar()</code> accepts either a path string or a FileRecord object. It fetches the JSON from CDN and returns a dict.</p> <p><code>events()</code> fetches the events.tsv for the matching file. If the subject has multiple sessions or runs, pass <code>session=</code> and <code>run=</code> to disambiguate.</p>"},{"location":"dataset/metadata/#cli","title":"CLI","text":"<pre><code># Print dataset_description.json\nqortex metadata ds004130\n\n# Print participants.tsv\nqortex metadata ds004130 --participants\n\n# Print a specific sidecar\nqortex metadata ds004130 sub-01/eeg/sub-01_task-rest_eeg.json\n</code></pre>"},{"location":"dataset/metadata/#downloading-metadata-only","title":"Downloading metadata only","text":"<p>To download only the small metadata files (no large imaging data):</p> <pre><code>ds.download_metadata(data_dir=\"data/ds004130/\")\n</code></pre> <p>This downloads: - <code>dataset_description.json</code> - <code>participants.tsv</code> - <code>README</code>, <code>CHANGES</code> - All <code>*.json</code> sidecar files - All <code>*_events.tsv</code> files - All <code>*_channels.tsv</code> files - All <code>*_coordsystem.json</code> files - All <code>*.bval</code>, <code>*.bvec</code> files</p> <p>Imaging files (<code>.nii.gz</code>, <code>.set</code>, <code>.fif</code>, <code>.edf</code>, etc.) are excluded.</p> <p>CLI equivalent:</p> <pre><code>qortex download ds004130 --metadata-only --data-dir data/ds004130/\n</code></pre>"},{"location":"dataset/metadata/#what-metadata-tells-you-before-downloading","title":"What metadata tells you before downloading","text":"<p>A metadata-only pass reveals:</p> <ul> <li>Whether events.tsv files exist and are non-empty</li> <li>What trial types appear in events.tsv (potential label columns)</li> <li>Whether subjects are age/sex balanced</li> <li>EEG channel count and type from channels.tsv</li> <li>fMRI TR, task name, slice timing from JSON sidecars</li> <li>DWI b-values from bval files</li> </ul> <p>This information is often sufficient to decide whether a dataset is worth the full download.</p>"},{"location":"dataset/metadata/#related","title":"Related","text":"<ul> <li><code>ds.inspect()</code> \u2014 manifest-level structural summary</li> <li><code>ds.doctor()</code> \u2014 includes a metadata-validity check</li> <li><code>ds.label_landscape()</code> \u2014 aggregate view of label coverage</li> </ul>"},{"location":"dataset/search-catalog/","title":"Search &amp; Filter","text":"<p>Qortex offers two search paths: a local DuckDB catalog (fast, offline, incremental) and live search against the OpenNeuro GraphQL API (always current, no prior refresh needed). Both expose the same fluent <code>DatasetQuery</code> interface.</p>"},{"location":"dataset/search-catalog/#quick-reference","title":"Quick reference","text":"Need Method Fast filtered search (offline) <code>DatasetQuery().modality(\"eeg\").fetch()</code> Always-current search <code>DatasetQuery().modality(\"eeg\").live()</code> Rich metadata + engagement sort <code>client.search_datasets_rich(modality=\"eeg\", sort_by=\"downloads\")</code> Free-text across name/description <code>DatasetQuery().containing(\"auditory cortex\").fetch()</code> Single dataset \u2014 all metadata <code>client.get_dataset_rich(\"ds008039\")</code>"},{"location":"dataset/search-catalog/#datasetquery-fluent-builder","title":"DatasetQuery \u2014 fluent builder","text":"<p><code>DatasetQuery</code> chains filter methods and terminates with <code>.fetch()</code> (local catalog) or <code>.live()</code> (API).</p> <pre><code>from qortex.catalog import DatasetQuery\n\n# Local catalog \u2014 fast, works offline\nresults = (\n    DatasetQuery()\n    .modality(\"eeg\")\n    .task(\"rest\")\n    .min_subjects(30)\n    .containing(\"auditory\")\n    .has_events()\n    .limit(20)\n    .fetch()\n)\n\nfor row in results:\n    print(row[\"dataset_id\"], row[\"n_subjects\"], row[\"size_gb\"])\n</code></pre>"},{"location":"dataset/search-catalog/#available-filters","title":"Available filters","text":"Method Type Description <code>.containing(text)</code> str Free-text match on dataset name and description <code>.modality(mod)</code> str Filter by modality: <code>\"eeg\"</code>, <code>\"mri\"</code>, <code>\"fmri\"</code>, <code>\"meg\"</code>, <code>\"ieeg\"</code>, <code>\"pet\"</code>, <code>\"nirs\"</code> <code>.task(task)</code> str Filter by task name (substring match) <code>.author(name)</code> str Filter by author name (substring match) <code>.license(name)</code> str Filter by license: <code>\"CC0\"</code>, <code>\"CC-BY\"</code>, <code>\"PDDL\"</code>, etc. <code>.min_subjects(n)</code> int Minimum number of subjects <code>.max_size_gb(gb)</code> float Maximum total dataset size in GB <code>.has_events(True)</code> bool Only datasets with events.tsv files <code>.has_derivatives(True)</code> bool Only datasets with a derivatives/ directory <code>.limit(n)</code> int Maximum results (default: 50) <code>.offset(n)</code> int Pagination offset"},{"location":"dataset/search-catalog/#execution-methods","title":"Execution methods","text":"<pre><code># Local catalog\nresults = query.fetch()             # list[dict]\npage    = query.fetch_page()        # PageResult(results, total, offset)\n\n# Live API (always current, no catalog refresh needed)\nresults = query.live()              # list[RichDatasetInfo]\nresults = query.live(sync_local=True)  # also updates local catalog\n\n# Facet counts (distribution of modalities, tasks, licenses)\nfacets = DatasetQuery().modality(\"eeg\").facets()\nprint(facets[\"tasks\"])    # {\"rest\": 42, \"nback\": 18, ...}\n</code></pre>"},{"location":"dataset/search-catalog/#refresh-the-local-catalog","title":"Refresh the local catalog","text":"<p>Pull the latest index from OpenNeuro once and search it offline:</p> <pre><code>qortex catalog-refresh\n</code></pre> <p>Writes to <code>~/.qortex/catalog.duckdb</code>. Incremental on subsequent runs.</p>"},{"location":"dataset/search-catalog/#cli-search","title":"CLI search","text":"<pre><code>qortex search --modality eeg\nqortex search --modality eeg --task rest\nqortex search --modality eeg --min-subjects 50\nqortex search --modality fmri --task nback --min-subjects 30\nqortex search --max-size 10           # GB\nqortex search --query \"resting state\" # free-text on name/description\nqortex search --has-events\nqortex search --license CC0\n</code></pre> <p>Results:</p> <pre><code>ID         Name                          Subjects  Size    Modalities\nds004130   EEG resting-state alpha...    88        4.2 GB  eeg\nds003490   Resting state EEG...          64        2.1 GB  eeg\n</code></pre> <p>Add <code>--json</code> or <code>--csv</code> for machine-readable output.</p>"},{"location":"dataset/search-catalog/#rich-search-via-the-api","title":"Rich search via the API","text":"<p><code>search_datasets_rich()</code> searches the OpenNeuro API directly and returns fully populated <code>RichDatasetInfo</code> objects \u2014 with engagement metrics, demographics, funding, and snapshot summaries.</p> <pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(\n        modality=\"eeg\",\n        task=\"rest\",\n        min_subjects=30,\n        min_downloads=100,\n        sort_by=\"downloads\",  # \"downloads\" | \"views\" | \"stars\" | \"subjects\" | \"size\" | \"recent\"\n        limit=20,\n    )\n\nfor info in results:\n    snap = info.latest_snapshot_summary\n    print(\n        info.id,\n        info.name,\n        snap.n_subjects,\n        info.engagement.downloads,\n        info.engagement.popularity_score,\n    )\n</code></pre>"},{"location":"dataset/search-catalog/#sort-options-for-search_datasets_rich","title":"Sort options for <code>search_datasets_rich</code>","text":"<code>sort_by</code> Orders by <code>\"downloads\"</code> Most downloaded first (default) <code>\"views\"</code> Most viewed first <code>\"stars\"</code> Most starred first <code>\"subjects\"</code> Most subjects first <code>\"size\"</code> Largest datasets first <code>\"recent\"</code> Most recently published first"},{"location":"dataset/search-catalog/#combining-search-with-inspect","title":"Combining search with inspect","text":"<p>A typical workflow: search for candidates, then inspect the most promising ones:</p> <pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\n# Step 1 \u2014 find candidates (local, fast)\ncandidates = (\n    DatasetQuery()\n    .modality(\"eeg\")\n    .min_subjects(50)\n    .has_events()\n    .limit(5)\n    .fetch()\n)\n\n# Step 2 \u2014 get full metadata for each\nwith OpenNeuroClient() as client:\n    for row in candidates:\n        info = client.get_dataset_rich(row[\"dataset_id\"])\n        readme = client.get_readme(row[\"dataset_id\"])\n        print(info.id, info.engagement.downloads, info.grant_funder)\n        print(readme[:200] if readme else \"(no README)\")\n</code></pre>"},{"location":"dataset/search-catalog/#catalog-profile","title":"Catalog profile","text":"<pre><code>qortex catalog-profile\n</code></pre> <p>Shows: total datasets indexed, last refresh timestamp, modality distribution.</p>"},{"location":"dataset/search-catalog/#related","title":"Related","text":"<ul> <li>Inspect \u2014 full metadata and file-level detail for a specific dataset</li> <li>Snapshots \u2014 list available versions of a dataset</li> <li>Metadata \u2014 read sidecar files without downloading imaging data</li> </ul>"},{"location":"dataset/snapshots/","title":"Snapshots","text":"<p>A snapshot is a versioned, immutable copy of an OpenNeuro dataset. Every time a dataset depositor updates their data, a new snapshot is created with an incremented version number. Old snapshots remain accessible indefinitely.</p>"},{"location":"dataset/snapshots/#why-snapshots-matter-for-reproducibility","title":"Why snapshots matter for reproducibility","text":"<p>If you pin a snapshot version, the manifest and all CDN file URLs remain constant. The same Qortex command will produce the same download regardless of when it runs.</p> <p>If you do not pin a snapshot, Qortex uses the latest published snapshot at the time you first fetch the manifest. Subsequent runs may resolve to a different (newer) snapshot.</p>"},{"location":"dataset/snapshots/#list-available-snapshots","title":"List available snapshots","text":"<pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    snapshots = client.get_snapshots(\"ds004130\")\n\nfor s in snapshots:\n    print(s.tag, s.created, s.size)\n</code></pre> <p>From the CLI:</p> <pre><code>qortex metadata ds004130 --snapshots\n</code></pre> <p>Output:</p> <pre><code>ds004130 snapshots:\n  1.0.0   2022-03-15   87 subjects   4.1 GB\n  1.1.0   2022-08-20   88 subjects   4.2 GB\n  1.2.0   2023-01-10   88 subjects   4.2 GB  \u2190 latest\n</code></pre>"},{"location":"dataset/snapshots/#pin-a-snapshot","title":"Pin a snapshot","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", snapshot=\"1.0.0\")\nprofile = ds.inspect()\nprint(profile.n_subjects)  # 87\n</code></pre> <p>From the CLI, add <code>--snapshot</code>:</p> <pre><code>qortex inspect ds004130 --snapshot 1.0.0\nqortex download ds004130 --snapshot 1.0.0 --data-dir data/ds004130_v1/\n</code></pre>"},{"location":"dataset/snapshots/#get-summary-for-a-specific-snapshot","title":"Get summary for a specific snapshot","text":"<pre><code>from qortex.client import OpenNeuroClient\n\nwith OpenNeuroClient() as client:\n    summary = client.get_snapshot_summary(\"ds004130\", \"1.0.0\")\n\nprint(summary.n_subjects)    # 87\nprint(summary.total_size_gb) # 4.1\nprint(summary.bids_version)  # \"1.6.0\"\nprint(summary.tasks)         # [\"rest\"]\nprint(summary.sessions)      # []\nprint(summary.funding)       # [\"NIH ...\"]\n</code></pre>"},{"location":"dataset/snapshots/#compare-snapshots","title":"Compare snapshots","text":"<p>To see which files changed between two snapshots:</p> <pre><code>from qortex import Dataset\n\nds_old = Dataset(\"ds004130\", snapshot=\"1.0.0\")\nds_new = Dataset(\"ds004130\", snapshot=\"1.2.0\")\n\nold_paths = {f.path for f in ds_old.manifest().files}\nnew_paths = {f.path for f in ds_new.manifest().files}\n\nadded   = new_paths - old_paths\nremoved = old_paths - new_paths\nprint(f\"Added: {len(added)}  Removed: {len(removed)}\")\n</code></pre>"},{"location":"dataset/snapshots/#recording-the-snapshot-in-provenance","title":"Recording the snapshot in provenance","text":"<p>When you convert a dataset, the snapshot version is recorded in the artifact manifest automatically:</p> <pre><code>art = ds.convert(...)\nprint(art.manifest.source_snapshot)  # \"ds004130@1.2.0\"\n</code></pre> <p>This lets you trace any artifact back to the exact dataset version it came from.</p>"},{"location":"dataset/snapshots/#limitations","title":"Limitations","text":"<ul> <li>Very old snapshots (pre-2020) may have stale CDN URLs that return 404. Use a newer snapshot if possible.</li> <li>The snapshot list is fetched from the OpenNeuro GraphQL API. A network error here means you cannot list snapshots.</li> </ul>"},{"location":"download/","title":"Download","text":"<p>Qortex downloads files from the OpenNeuro CDN to a local directory. The download engine supports resume, companion file inclusion, and selective filtering.</p>"},{"location":"download/#core-principle-plan-before-you-download","title":"Core principle: plan before you download","text":"<p>Qortex separates planning from execution. <code>ds.plan()</code> returns a <code>DownloadPlan</code> that lists exact files with target paths. You can inspect the plan before committing to the transfer.</p> <pre><code>plan = ds.plan(subjects=[\"01\", \"02\"], tasks=[\"rest\"])\nprint(f\"{len(plan.files)} files, {plan.size_gb:.1f} GB\")\nds.download_paths(plan.files)\n</code></pre> <p>Or combine planning and execution:</p> <pre><code>ds.download(subjects=[\"01\", \"02\"], tasks=[\"rest\"], data_dir=\"data/ds004130/\")\n</code></pre>"},{"location":"download/#what-you-can-do","title":"What you can do","text":"<p>Plan \u2014 Create a DownloadPlan and inspect it before downloading.</p> <p>Selective download \u2014 Filter by subject, session, task, run, suffix, and size.</p> <p>Metadata only \u2014 Download only sidecar JSON, events.tsv, bval/bvec, and participants files.</p> <p>Cache \u2014 Where files are stored, how the cache works, and how to clear it.</p> <p>Local index \u2014 Scan a pre-existing BIDS directory and build a local manifest.</p> <p>Resume \u2014 How interrupted downloads recover.</p> <p>Next \u2192 Visualize \u2014 QC your local files before conversion to catch bad data early.</p>"},{"location":"download/cache/","title":"Cache","text":"<p>Qortex stores downloaded files in a local cache directory. Understanding how the cache works prevents duplicate downloads and helps manage disk space.</p>"},{"location":"download/cache/#default-cache-location","title":"Default cache location","text":"<pre><code>~/.qortex/data/{dataset_id}/{snapshot}/\n</code></pre> <p>For example:</p> <pre><code>~/.qortex/data/ds004130/1.2.0/sub-01/eeg/sub-01_task-rest_eeg.set\n</code></pre> <p>Override the default with <code>data_dir</code>:</p> <pre><code>ds.download(data_dir=\"/scratch/datasets/ds004130/\")\n</code></pre> <p>When <code>data_dir</code> is set, files go directly into that directory (without the <code>~/.qortex/data/</code> prefix).</p>"},{"location":"download/cache/#cache-inspection-from-cli","title":"Cache inspection from CLI","text":"<pre><code># Show total cache size and dataset list\nqortex cache info\n\n# List cached datasets with sizes\nqortex cache list\n\n# Show what is cached for a specific dataset\nqortex cache list ds004130\n</code></pre> <p>Output example:</p> <pre><code>ds004130\n  Snapshot: 1.2.0\n  Path:     ~/.qortex/data/ds004130/1.2.0/\n  Files:    1,056\n  Size:     4.2 GB\n  Last used: 2024-01-15\n</code></pre>"},{"location":"download/cache/#remove-cache-entries","title":"Remove cache entries","text":"<pre><code># Remove all cached files for a dataset\nqortex cache remove ds004130\n\n# Remove a specific snapshot\nqortex cache remove ds004130 --snapshot 1.0.0\n\n# Clear the entire cache (all datasets)\nqortex cache clear\n</code></pre> <p>From Python:</p> <pre><code>from qortex.cache import clear_cache, remove_dataset_cache\n\nclear_cache()\nremove_dataset_cache(\"ds004130\", snapshot=\"1.0.0\")\n</code></pre>"},{"location":"download/cache/#avoiding-duplicate-downloads","title":"Avoiding duplicate downloads","text":"<p>If a file already exists at its target path and its size matches the manifest record, Qortex skips the download. Qortex does not compare checksums during the skip check \u2014 it only checks file size.</p> <p>If you suspect a file is corrupted, delete it manually and re-download.</p>"},{"location":"download/cache/#disk-space-planning","title":"Disk space planning","text":"<p>Before downloading, estimate required space:</p> <pre><code>plan = ds.plan(subjects=[\"01\", \"02\", \"03\"], tasks=[\"rest\"])\nprint(f\"Required: {plan.size_gb:.1f} GB\")\n</code></pre>"},{"location":"download/cache/#manifests-and-sidecar-caches","title":"Manifests and sidecar caches","text":"<p>The manifest JSON and fetched sidecar files are cached at:</p> <pre><code>~/.qortex/manifests/{dataset_id}/{snapshot}.json\n~/.qortex/sidecars/{dataset_id}/{snapshot}/{path}.json\n</code></pre> <p>These are small (&lt; 10 MB per dataset) and are kept indefinitely. Delete them if you need to force a manifest re-fetch:</p> <pre><code>rm ~/.qortex/manifests/ds004130/1.2.0.json\n</code></pre> <p>Or force refresh in Python:</p> <pre><code>manifest = ds.manifest(force_refresh=True)\n</code></pre>"},{"location":"download/local-index/","title":"Local Index","text":"<p>If you already have a BIDS dataset on disk \u2014 from OpenNeuro CLI, DataLad, or your institution's storage \u2014 you can point Qortex at it without downloading anything.</p> <p><code>index_local()</code> scans the local directory and builds a manifest that Qortex uses for all subsequent operations.</p>"},{"location":"download/local-index/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"/scratch/datasets/ds004130/\")\nds.index_local()\n\n# Now all readiness and conversion commands work\nreport = ds.doctor()\nart = ds.convert(output_dir=\"artifacts/\")\n</code></pre>"},{"location":"download/local-index/#cli","title":"CLI","text":"<pre><code>qortex local-index /scratch/datasets/ds004130/ --dataset-id ds004130\n</code></pre> <p>After indexing, pass <code>--local</code> to other commands to use the local manifest:</p> <pre><code>qortex inspect ds004130 --local\nqortex doctor ds004130 --local\n</code></pre>"},{"location":"download/local-index/#what-local-indexing-does","title":"What local indexing does","text":"<ol> <li>Walks the directory tree recursively</li> <li>Parses BIDS entity labels from each file path</li> <li>Computes file sizes from disk</li> <li>Resolves companion relationships</li> <li>Writes <code>.qortex/manifest.json</code> inside the data directory</li> </ol> <p>The local manifest does not include CDN URLs. If you want to download missing files after indexing, Qortex fetches the remote manifest and cross-references with the local index.</p>"},{"location":"download/local-index/#checking-for-missing-files-after-indexing","title":"Checking for missing files after indexing","text":"<pre><code>ds.index_local()\nreport = ds.doctor()\n# report.missing_files lists files in the remote manifest that are absent locally\n</code></pre> <p>Or use visual audit:</p> <pre><code>report = ds.visual_audit(mode=\"local\")\nfor item in report.missing_expected_files():\n    print(item)\n</code></pre>"},{"location":"download/local-index/#limitations","title":"Limitations","text":"<ul> <li>Local indexing does not validate BIDS structure. It only parses entity labels from path names. Run <code>ds.validate()</code> for structural validation.</li> <li>Files that do not follow BIDS naming conventions are indexed but have None for most entity fields. They appear in the manifest with unknown datatype and suffix.</li> <li>Symlinks are followed during indexing. If the link target does not exist, the file is skipped.</li> </ul>"},{"location":"download/local-index/#related","title":"Related","text":"<ul> <li>Cache \u2014 where Qortex stores downloaded files</li> <li>Validate \u2014 check the local BIDS structure after indexing</li> </ul>"},{"location":"download/metadata-only/","title":"Metadata-Only Download","text":"<p>A metadata-only download fetches everything except the large imaging files. The result is a local directory with JSON sidecars, TSV files, bval/bvec files, and root-level metadata \u2014 but no NIfTIs, EEG recordings, or DICOM files.</p>"},{"location":"download/metadata-only/#why-metadata-first","title":"Why metadata first","text":"<p>Before spending an hour downloading gigabytes of NIfTI data, a metadata-only pass answers:</p> <ul> <li>Are events.tsv files present for all subjects? (label readiness)</li> <li>What trial types are in the events files? (class distribution)</li> <li>What is the TR, number of volumes, and slice order? (preprocessing planning)</li> <li>How many EEG channels and what type? (channel configuration)</li> <li>What b-values appear in the DWI acquisition? (shell count)</li> </ul> <p>This information costs seconds to fetch and requires almost no disk space.</p>"},{"location":"download/metadata-only/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nds.download_metadata(data_dir=\"data/ds004130/\")\n</code></pre> <p>Files downloaded:</p> <ul> <li><code>dataset_description.json</code></li> <li><code>participants.tsv</code></li> <li><code>README</code></li> <li><code>CHANGES</code></li> <li><code>*_bold.json</code>, <code>*_T1w.json</code>, <code>*_eeg.json</code>, etc. (all sidecar JSON)</li> <li><code>*_events.tsv</code>, <code>*_channels.tsv</code>, <code>*_electrodes.tsv</code>, <code>*_coordsystem.json</code></li> <li><code>*.bval</code>, <code>*.bvec</code></li> <li><code>*_physio.json</code> (not the .tsv.gz)</li> </ul> <p>Imaging files excluded:</p> <ul> <li><code>*.nii.gz</code>, <code>*.nii</code></li> <li><code>*.set</code>, <code>*.edf</code>, <code>*.fif</code>, <code>*.cnt</code></li> <li><code>*.dcm</code> and DICOM directories</li> <li><code>*.tgz</code>, <code>*.zip</code></li> </ul>"},{"location":"download/metadata-only/#cli","title":"CLI","text":"<pre><code>qortex download ds004130 --metadata-only --data-dir data/ds004130/\n</code></pre>"},{"location":"download/metadata-only/#check-label-coverage-after-metadata-download","title":"Check label coverage after metadata download","text":"<p>After a metadata download, the events.tsv files are present. Use <code>ds.label_landscape()</code> to see the label distribution across subjects:</p> <pre><code>ds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nprint(ds.label_landscape())\n</code></pre> <p>Output:</p> <pre><code>trial_type  count  subjects_with_label\nrest         88    88/88\neyes-open    86    86/88  \u2190 2 subjects missing\ntask         88    88/88\n</code></pre>"},{"location":"download/metadata-only/#size-estimate","title":"Size estimate","text":"<p>For most datasets, a metadata-only download is 1\u20135 MB total, regardless of dataset size. The events.tsv files are the largest component.</p>"},{"location":"download/metadata-only/#related","title":"Related","text":"<ul> <li>Selective download \u2014 download specific imaging files after metadata review</li> <li>Label readiness \u2014 check label coverage after metadata download</li> </ul>"},{"location":"download/plan/","title":"Plan","text":"<p><code>ds.plan()</code> constructs a <code>DownloadPlan</code> without starting any transfers. The plan lists every file that would be downloaded, its target local path, and the CDN URL.</p>"},{"location":"download/plan/#basic-usage","title":"Basic usage","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nplan = ds.plan(subjects=[\"01\", \"02\"], tasks=[\"rest\"], suffixes=[\"eeg\"])\n\nprint(f\"Files: {len(plan.files)}\")\nprint(f\"Size:  {plan.size_gb:.2f} GB\")\nprint(f\"Target: {plan.target_dir}\")\n\nfor f in plan.files[:5]:\n    print(f.path, f.size)\n</code></pre>"},{"location":"download/plan/#plan-properties","title":"Plan properties","text":"Property Type Description <code>files</code> <code>list[FileRecord]</code> Files that will be downloaded <code>target_dir</code> <code>Path</code> Root directory for downloads <code>size_gb</code> <code>float</code> Total size in GB <code>n_subjects</code> <code>int</code> Number of subjects in plan <code>includes_companions</code> <code>bool</code> Whether companions were added"},{"location":"download/plan/#serialize-the-plan","title":"Serialize the plan","text":"<pre><code>import json\n\nplan_dict = plan.to_dict()\nwith open(\"plan.json\", \"w\") as f:\n    json.dump(plan_dict, f, indent=2)\n</code></pre> <p>Reload and execute later:</p> <pre><code>from qortex.download import DownloadPlan\n\nplan = DownloadPlan.from_file(\"plan.json\")\nds.download_paths(plan.files)\n</code></pre>"},{"location":"download/plan/#cli","title":"CLI","text":"<pre><code>qortex plan ds004130 --subjects 01 02 --tasks rest --suffixes eeg\n</code></pre> <p>Output shows file count and total size. Add <code>--output plan.json</code> to save:</p> <pre><code>qortex plan ds004130 --subjects 01 02 --output plan.json\n</code></pre>"},{"location":"download/plan/#minimum-plan","title":"Minimum plan","text":"<p><code>ds.minimum()</code> builds a plan for the smallest set of subjects that enables a specific goal:</p> <pre><code>plan = ds.minimum(goal=\"first-batch\")\nplan = ds.minimum(goal=\"label-check\")\nplan = ds.minimum(goal=\"validation\")\nplan = ds.minimum(goal=\"metadata\")\n</code></pre> <p><code>first-batch</code> picks enough subjects (usually 3\u20135) to run one full pipeline pass: download \u2192 convert \u2192 train step.</p>"},{"location":"download/plan/#companion-file-inclusion","title":"Companion file inclusion","text":"<p>By default, any imaging file in the plan brings its companions with it. A BOLD NIfTI automatically includes its JSON sidecar and events.tsv. To suppress this:</p> <pre><code>plan = ds.plan(subjects=[\"01\"], include_companions=False)\n</code></pre> <p>Excluding companions is rarely useful \u2014 conversion will fail without sidecars and events.</p>"},{"location":"download/plan/#related","title":"Related","text":"<ul> <li>Selective download \u2014 filter options</li> <li>Resume \u2014 what happens if a transfer is interrupted mid-plan</li> </ul>"},{"location":"download/resume/","title":"Resume","text":"<p>Downloads interrupted by network errors, timeouts, or Ctrl-C resume automatically. Qortex uses HTTP range requests to continue from where a transfer stopped.</p>"},{"location":"download/resume/#how-it-works","title":"How it works","text":"<p>Qortex tracks download progress per file using a <code>.part</code> file alongside the destination path. When a download resumes:</p> <ol> <li>The <code>.part</code> file is detected at the target location</li> <li>Qortex issues a <code>Range: bytes={size}-</code> HTTP request for the remaining bytes</li> <li>The partial content is appended to the <code>.part</code> file</li> <li>On completion, the <code>.part</code> file is atomically moved to the final path</li> </ol> <p>If the CDN does not support range requests (rare), Qortex falls back to restarting the file from the beginning.</p>"},{"location":"download/resume/#resuming-a-stopped-download","title":"Resuming a stopped download","text":"<p>Simply re-run the same download command:</p> <pre><code>ds.download(subjects=[\"01\", \"02\"], data_dir=\"data/ds004130/\")\n# stopped at 60% of sub-02 EEG file\n\n# Run again \u2014 picks up from where it stopped\nds.download(subjects=[\"01\", \"02\"], data_dir=\"data/ds004130/\")\n</code></pre> <p>Files already fully downloaded are skipped. The partial file resumes.</p>"},{"location":"download/resume/#parallel-downloads","title":"Parallel downloads","text":"<p>Qortex uses a thread pool for concurrent file transfers. The default concurrency is 4 simultaneous downloads. Override with:</p> <pre><code>ds.download(subjects=[\"01\"], concurrency=8, data_dir=\"data/\")\n</code></pre> <p>Or set in configuration:</p> <pre><code>qortex download ds004130 --concurrency 8 --data-dir data/\n</code></pre> <p>Higher concurrency does not always improve throughput \u2014 CDN rate limiting may kick in above 4\u20138 concurrent streams.</p>"},{"location":"download/resume/#handling-cdn-errors","title":"Handling CDN errors","text":"<p>OpenNeuro CDN links expire after a few hours. If a download fails with HTTP 403 or 410, the link has expired. Re-running <code>ds.download()</code> fetches fresh links before retrying.</p> <p>Qortex automatically refreshes expired CDN links during a resume \u2014 you do not need to restart from scratch.</p>"},{"location":"download/resume/#integrity-check-after-download","title":"Integrity check after download","text":"<p>After all files complete, Qortex checks that each file's size on disk matches the expected size from the manifest. Files with size mismatches are logged as warnings:</p> <pre><code>WARNING: sub-03_task-rest_bold.nii.gz size mismatch: expected 450 MB, got 312 MB\n</code></pre> <p>To re-download size-mismatched files:</p> <pre><code>ds.download(subjects=[\"03\"], force=True, data_dir=\"data/\")\n</code></pre> <p><code>force=True</code> re-downloads even files that already exist at the correct size.</p>"},{"location":"download/resume/#limitations","title":"Limitations","text":"<ul> <li>Checksums are not verified after download. Qortex only checks file size. A file with the right size but corrupted content will pass.</li> <li>If the manifest changes between the initial download and a resume (e.g., a snapshot was updated), some CDN URLs may differ. Pin a snapshot to avoid this.</li> </ul>"},{"location":"download/selective-download/","title":"Selective Download","text":"<p>You rarely need an entire OpenNeuro dataset. Selective download lets you specify exactly which subjects, sessions, tasks, runs, and file types to transfer.</p>"},{"location":"download/selective-download/#filter-parameters","title":"Filter parameters","text":"<p>All filter parameters are accepted by both <code>ds.download()</code> and <code>ds.plan()</code>:</p> Parameter Type Example <code>subjects</code> <code>list[str]</code> <code>[\"01\", \"02\", \"03\"]</code> <code>sessions</code> <code>list[str]</code> <code>[\"baseline\", \"followup\"]</code> <code>tasks</code> <code>list[str]</code> <code>[\"rest\", \"nback\"]</code> <code>runs</code> <code>list[str]</code> <code>[\"01\", \"02\"]</code> <code>suffixes</code> <code>list[str]</code> <code>[\"bold\", \"T1w\"]</code> <code>datatypes</code> <code>list[str]</code> <code>[\"func\", \"anat\"]</code> <code>include</code> <code>list[str]</code> glob patterns to include <code>exclude</code> <code>list[str]</code> glob patterns to exclude <code>max_size_gb</code> <code>float</code> skip files larger than this"},{"location":"download/selective-download/#examples","title":"Examples","text":"<p>Download resting-state BOLD from the first 10 subjects:</p> <pre><code>ds.download(\n    subjects=[f\"{i:02d}\" for i in range(1, 11)],\n    tasks=[\"rest\"],\n    suffixes=[\"bold\"],\n    data_dir=\"data/ds004130/\",\n)\n</code></pre> <p>Download all EEG files including events:</p> <pre><code>ds.download(\n    datatypes=[\"eeg\"],\n    data_dir=\"data/ds004130/\",\n)\n</code></pre> <p>Download using glob patterns:</p> <pre><code>ds.download(\n    include=[\"sub-01/**\", \"sub-02/**\"],\n    exclude=[\"**/*_desc-confounds_*\"],\n    data_dir=\"data/ds004130/\",\n)\n</code></pre> <p>Skip files larger than 500 MB:</p> <pre><code>ds.download(max_size_gb=0.5, data_dir=\"data/ds004130/\")\n</code></pre>"},{"location":"download/selective-download/#cli","title":"CLI","text":"<pre><code># Subjects 01\u201310, resting-state BOLD\nqortex download ds004130 \\\n    --subjects 01 02 03 04 05 06 07 08 09 10 \\\n    --tasks rest \\\n    --suffixes bold \\\n    --data-dir data/ds004130/\n\n# All EEG\nqortex download ds004130 --datatypes eeg --data-dir data/ds004130/\n\n# Skip large files\nqortex download ds004130 --max-size 0.5 --data-dir data/ds004130/\n</code></pre>"},{"location":"download/selective-download/#companion-files","title":"Companion files","text":"<p>When you download a primary imaging file, its companions (sidecar JSON, events.tsv, bval/bvec) are included automatically. The companion inclusion follows the BIDS specification:</p> <ul> <li>BOLD \u2192 includes <code>*_bold.json</code>, <code>*_events.tsv</code>, optionally <code>*_desc-confounds_timeseries.tsv</code></li> <li>DWI \u2192 includes <code>*.bval</code>, <code>*.bvec</code>, <code>*_dwi.json</code></li> <li>EEG \u2192 includes <code>*_eeg.json</code>, <code>*_channels.tsv</code>, <code>*_coordsystem.json</code></li> </ul> <p>Root-level essential files (<code>participants.tsv</code>, <code>dataset_description.json</code>) are always included.</p> <p>To suppress companion inclusion:</p> <pre><code>ds.download(suffixes=[\"bold\"], include_companions=False, data_dir=\"data/\")\n</code></pre>"},{"location":"download/selective-download/#checking-what-will-be-downloaded-before-transferring","title":"Checking what will be downloaded before transferring","text":"<pre><code>plan = ds.plan(subjects=[\"01\"], tasks=[\"rest\"])\nprint(f\"{len(plan.files)} files, {plan.size_gb:.2f} GB\")\n</code></pre>"},{"location":"download/selective-download/#related","title":"Related","text":"<ul> <li>Plan \u2014 inspect the file list before downloading</li> <li>Metadata only \u2014 fetch only JSON, TSV, bval/bvec</li> </ul>"},{"location":"getting-started/","title":"Get Started","text":"<p>Qortex turns an OpenNeuro dataset ID into a training-ready artifact. It checks whether a dataset is actually usable \u2014 before you download a single file.</p>"},{"location":"getting-started/#new-to-qortex","title":"New to Qortex?","text":"<p>Follow this path. It takes about 20 minutes.</p> <p>1 \u00b7 Install</p> <pre><code>pip install qortex\n</code></pre> <p>\u2192 Full install instructions and optional extras</p> <p>2 \u00b7 Run the quickstart</p> <p>Inspect a real dataset, check its readiness, download a minimal subset, and convert to Parquet \u2014 20 lines of code.</p> <p>\u2192 Quickstart</p> <p>3 \u00b7 Try a hands-on tutorial</p> <p>Pick a modality you work with and follow a complete end-to-end example with a real open dataset.</p> <p>\u2192 Tutorial index</p>"},{"location":"getting-started/#already-installed","title":"Already installed?","text":"I want to\u2026 Go to Work through a real dataset end-to-end Tutorials Check if a dataset is usable Assess readiness Download only what I need Download guide Convert data to Parquet / Zarr / HDF5 Conversion guide Look up what a function returns API reference Fix a specific error Troubleshooting"},{"location":"getting-started/#how-qortex-works","title":"How Qortex works","text":"<p>Qortex is a readiness layer \u2014 not a downloader, not a training framework. It sits between OpenNeuro and your pipeline.</p> <pre><code>OpenNeuro API\n     \u2193\n  inspect      \u2190 manifest only, no download\n  assess       \u2190 label coverage, class balance, split feasibility\n  plan         \u2190 exact file list + byte count for your goal\n     \u2193\n  download     \u2190 selective, resumable\n  visualize    \u2190 QC before converting\n  convert      \u2190 Parquet / Zarr / HDF5 / HuggingFace / TFRecord\n     \u2193\nML artifact    \u2190 subject-level splits, leakage verified, provenance recorded\n</code></pre> <p>The key principle: every check runs on the manifest and sidecar files \u2014 not on imaging data. You know whether a dataset is usable before paying for the download.</p> <p>\u2192 Core concepts \u2014 readiness-first design, data model, the full workflow</p>"},{"location":"getting-started/#visual-audit-first-practical-step","title":"Visual audit (first practical step)","text":"<p>After install, the fastest way to understand what Qortex does is to run a visual audit on a local BIDS dataset.</p> <p>\u2192 First visual audit</p>"},{"location":"getting-started/first-visual-audit/","title":"First Visual Audit","text":"<p>A visual audit is a coverage check with thumbnails. It answers two questions:</p> <ol> <li>Which subjects/sessions/tasks have each expected file type?</li> <li>What do the center slices look like \u2014 any obvious acquisition failures?</li> </ol>"},{"location":"getting-started/first-visual-audit/#requirements","title":"Requirements","text":"<pre><code>pip install \"qortex[visual-all]\"\n</code></pre>"},{"location":"getting-started/first-visual-audit/#run-against-a-local-bids-directory","title":"Run against a local BIDS directory","text":"<p>If you have a dataset on disk at <code>data/ds004130/</code>:</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nreport = ds.visual_audit()\nreport.show()  # opens browser\n</code></pre> <p><code>show()</code> renders an HTML page with:</p> <ul> <li>A coverage matrix (subjects \u00d7 suffixes, green/red cells)</li> <li>Per-suffix file counts</li> <li>Warning summary (missing files, unexpected file sizes)</li> <li>Thumbnails for a sample of each suffix</li> </ul>"},{"location":"getting-started/first-visual-audit/#run-from-the-manifest-only-no-download-needed","title":"Run from the manifest only (no download needed)","text":"<p>You can run a coverage-only audit against the remote manifest without downloading any data:</p> <pre><code>ds = Dataset(\"ds004130\")\nreport = ds.visual_audit(mode=\"manifest\")\nreport.show()\n</code></pre> <p>In manifest mode, you get the coverage matrix and warnings but no thumbnails.</p>"},{"location":"getting-started/first-visual-audit/#cli","title":"CLI","text":"<pre><code>qortex visual-audit ds004130 --data-dir data/ds004130/ --output audit_report.html\n</code></pre> <p>Without <code>--data-dir</code>, runs in manifest mode.</p>"},{"location":"getting-started/first-visual-audit/#save-the-report","title":"Save the report","text":"<pre><code>report.to_html(\"audit_report.html\")\nreport.to_json(\"audit_report.json\")\nreport.to_markdown(\"audit_report.md\")\n</code></pre>"},{"location":"getting-started/first-visual-audit/#interpreting-the-coverage-matrix","title":"Interpreting the coverage matrix","text":"<p>Each cell is colored by whether a file was found for that subject/session combination:</p> <ul> <li>Green \u2014 file present</li> <li>Red \u2014 file expected (based on other subjects) but missing</li> <li>Gray \u2014 not applicable (subject did not have this session or task)</li> </ul> <p>The <code>action_items()</code> method returns a list of the highest-priority missing files sorted by how many subjects are affected:</p> <pre><code>for item in report.action_items():\n    print(item)\n# WARNING: sub-03_task-rest_events.tsv missing (affects 1/88 subjects)\n# WARNING: sub-17_T1w.nii.gz missing (affects 1/88 subjects)\n</code></pre>"},{"location":"getting-started/first-visual-audit/#next-steps","title":"Next steps","text":"<ul> <li>Visual audit reference \u2014 full VisualAuditReport API</li> <li>fMRI QC \u2014 per-file QC panels</li> <li>Download \u2014 download the flagged missing files</li> </ul>"},{"location":"getting-started/install/","title":"Install","text":""},{"location":"getting-started/install/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10 or later</li> <li>pip 23 or later</li> </ul>"},{"location":"getting-started/install/#base-install","title":"Base install","text":"<p>The base package contains the core Dataset facade, all CLI commands, the download engine, and the readiness checks. It does not include optional modality-specific visualization or ML-framework integrations.</p> <pre><code>pip install qortex\n</code></pre>"},{"location":"getting-started/install/#optional-extras","title":"Optional extras","text":"<p>Qortex uses optional extras to avoid pulling in large dependencies unless you need them.</p> Extra What it adds Command <code>mri</code> nibabel, nilearn \u2014 NIfTI inspection and volume visualization <code>pip install \"qortex[mri]\"</code> <code>dwi</code> nibabel, dipy \u2014 DWI bval/bvec handling and gradient sphere plots <code>pip install \"qortex[dwi]\"</code> <code>eeg</code> mne \u2014 EEG/MEG reading, time-frequency, topomap <code>pip install \"qortex[eeg]\"</code> <code>visual</code> nibabel, plotly \u2014 interactive HTML viewers <code>pip install \"qortex[visual]\"</code> <code>visual-all</code> All of the above <code>pip install \"qortex[visual-all]\"</code> <code>dicom</code> pydicom \u2014 DICOM browser and metadata extraction <code>pip install \"qortex[dicom]\"</code> <code>zarr</code> zarr \u2014 Zarr output format <code>pip install \"qortex[zarr]\"</code> <code>hdf5</code> h5py \u2014 HDF5 output format <code>pip install \"qortex[hdf5]\"</code> <code>torch</code> torch \u2014 PyTorch Dataset and DataLoader integration <code>pip install \"qortex[torch]\"</code> <code>lightning</code> lightning \u2014 LightningDataModule integration <code>pip install \"qortex[lightning]\"</code> <code>tf</code> tensorflow \u2014 TFRecord output format <code>pip install \"qortex[tf]\"</code> <code>hf</code> datasets \u2014 HuggingFace Dataset output format <code>pip install \"qortex[hf]\"</code> <code>sklearn</code> scikit-learn \u2014 sklearn arrays helper <code>pip install \"qortex[sklearn]\"</code> <code>dashboard</code> panel \u2014 Local dashboard server <code>pip install \"qortex[dashboard]\"</code> <code>validation</code> bids-validator \u2014 BIDS structural validation <code>pip install \"qortex[validation]\"</code> <code>all</code> Everything <code>pip install \"qortex[all]\"</code> <p>Combine extras with commas:</p> <pre><code>pip install \"qortex[mri,torch,zarr]\"\n</code></pre>"},{"location":"getting-started/install/#verify-install","title":"Verify install","text":"<pre><code>qortex --version\n# qortex 0.x.y\n</code></pre> <p>Check that an optional extra loaded correctly:</p> <pre><code>from qortex.visualize import VolumeViewer  # requires [mri] or [visual]\n</code></pre> <p>If an extra is missing, Qortex raises an <code>ImportError</code> with a message that tells you exactly which extra to install.</p>"},{"location":"getting-started/install/#development-install","title":"Development install","text":"<p>Clone the repo and install in editable mode:</p> <pre><code>git clone https://github.com/GinkgoQ/qortex.git\ncd qortex\npip install -e \".[all]\"\n</code></pre>"},{"location":"getting-started/install/#known-install-issues","title":"Known install issues","text":"<p>Conflict with existing nibabel. If your environment already has nibabel installed from another package (e.g., nilearn), check that the version is compatible:</p> <pre><code>python -c \"import nibabel; print(nibabel.__version__)\"\n</code></pre> <p>Qortex requires nibabel \u2265 5.0.</p> <p>Apple Silicon (MRI extras). Nibabel and nilearn install cleanly on ARM. No special flags required.</p> <p>Windows. All extras work on Windows. The <code>validation</code> extra (bids-validator) requires Node.js on PATH if using the full JS validator. The Python-only validator shipped with newer bids-validator packages does not need Node.</p>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This page walks through the minimal path from dataset ID to a Parquet artifact ready for training.</p> <p>The example uses <code>ds004130</code>, an EEG resting-state dataset from OpenNeuro with 88 subjects.</p>"},{"location":"getting-started/quickstart/#1-inspect-the-dataset","title":"1. Inspect the dataset","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\nprint(report.to_text())\n</code></pre> <p>Expected output:</p> <pre><code>Dataset: ds004130 (snapshot: 1.2.0)\nState:   manifest_only\nSubjects: 88    Sessions: 1    Modalities: eeg\nEvents:   yes \u2014 88 files found\nLabels:   trial_type has 3 classes (rest, eyes-open, task)\nSize:     ~4.2 GB\nNext action: download\n  Use ds.minimum(goal=\"first-batch\") to get the smallest subset.\n</code></pre> <p>The manifest fetch takes a few seconds. No data is transferred.</p>"},{"location":"getting-started/quickstart/#2-check-label-readiness","title":"2. Check label readiness","text":"<pre><code>ok = ds.can_train(target_col=\"trial_type\", min_classes=2, min_per_class=10)\nprint(ok)  # True\n</code></pre> <p>If this returns False, call <code>ds.label_landscape()</code> to see which subjects are missing labels or have too few samples.</p>"},{"location":"getting-started/quickstart/#3-plan-the-minimum-download","title":"3. Plan the minimum download","text":"<pre><code>plan = ds.minimum(goal=\"first-batch\")\nprint(f\"{len(plan.files)} files, {plan.size_gb:.1f} GB\")\n# 12 files, 0.4 GB\n</code></pre> <p><code>minimum()</code> picks the smallest real set of subjects whose data can complete one full pipeline pass. It includes the primary data files AND their sidecar companions automatically.</p>"},{"location":"getting-started/quickstart/#4-download","title":"4. Download","text":"<pre><code>ds.download_paths(plan.files, data_dir=\"data/ds004130/\")\n</code></pre> <p>Progress is logged per file. If interrupted, re-running resumes incomplete files.</p>"},{"location":"getting-started/quickstart/#5-convert-to-parquet","title":"5. Convert to Parquet","text":"<pre><code>art = ds.convert(\n    data_dir=\"data/ds004130/\",\n    output_dir=\"artifacts/ds004130_parquet/\",\n    format=\"parquet\",\n    window=dict(duration_s=30.0, overlap=0.5),\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\nprint(art.manifest.n_samples)\n</code></pre>"},{"location":"getting-started/quickstart/#6-load-for-training","title":"6. Load for training","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130_parquet/\")\nX_train, y_train = art.sklearn(split=\"train\")\nX_val,   y_val   = art.sklearn(split=\"val\")\n</code></pre> <p>Or with PyTorch:</p> <pre><code>train_ds = art.torch(split=\"train\")\nval_ds   = art.torch(split=\"val\")\n</code></pre>"},{"location":"getting-started/quickstart/#cli-equivalent","title":"CLI equivalent","text":"<p>All of the above can be run from the command line:</p> <pre><code>qortex doctor ds004130\nqortex can-train ds004130 --label trial_type\nqortex download ds004130 --min-goal first-batch --data-dir data/ds004130/\nqortex convert ds004130 \\\n    --data-dir data/ds004130/ \\\n    --output artifacts/ds004130_parquet/ \\\n    --format parquet \\\n    --window 30 \\\n    --overlap 0.5 \\\n    --label trial_type\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next steps","text":"<ul> <li>Selective download \u2014 filter by subject, session, task, run</li> <li>Visual audit \u2014 inspect file coverage before downloading</li> <li>Conversion formats \u2014 Zarr, HDF5, WebDataset, HuggingFace</li> </ul>"},{"location":"guides/","title":"Guides","text":"<p>Guides are task-oriented. They answer \"how do I do X?\" without explaining why things are designed this way \u2014 see Get Started for that.</p> <p>Qortex works as a pipeline. Each guide covers one stage.</p>"},{"location":"guides/#1-inspect","title":"1 \u2192 Inspect","text":"<p>Examine a dataset without downloading it.</p> <p>Fetch the manifest, count subjects and modalities, read labels from sidecar files, and get a size estimate \u2014 all from the OpenNeuro API. No data transferred.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\nprint(report.to_text())\n</code></pre> <p>\u2192 Inspect a dataset</p>"},{"location":"guides/#2-assess-readiness","title":"2 \u2192 Assess readiness","text":"<p>Decide if the dataset is usable before committing to a download.</p> <p>Check label coverage, class balance, event file completeness, minimum sample counts, and split feasibility. Get a binary yes/no with an actionable explanation.</p> <pre><code>ok = ds.can_train(target_col=\"trial_type\", min_classes=2, min_per_class=10)\nreport = ds.label_readiness(target_col=\"trial_type\")\n</code></pre> <p>\u2192 Assess readiness</p>"},{"location":"guides/#3-download","title":"3 \u2192 Download","text":"<p>Fetch exactly what you need \u2014 nothing more.</p> <p>Plan before downloading. Filter by subject, task, modality, or suffix. Resume interrupted transfers. Download metadata only for offline inspection.</p> <pre><code>plan = ds.minimum(goal=\"first-batch\")\nds.download_paths(plan.files, data_dir=\"data/\")\n</code></pre> <p>\u2192 Download</p>"},{"location":"guides/#4-visualize","title":"4 \u2192 Visualize","text":"<p>Inspect local files before running anything.</p> <p>Render center slices, channel plots, event timelines, and QC panels without loading full volumes. Catch bad files, wrong orientations, and mismatched labels before conversion.</p> <pre><code>ds.visualize(data_dir=\"data/ds004130/\", output_dir=\"qa/\")\n</code></pre> <p>\u2192 Visualize</p>"},{"location":"guides/#5-convert","title":"5 \u2192 Convert","text":"<p>Transform raw BIDS data into ML-ready formats.</p> <p>One command writes Parquet, Zarr, HDF5, WebDataset, HuggingFace Dataset, or TFRecord with subject-level splits, reproducible seeds, and a full provenance record.</p> <pre><code>from qortex.convert import ConversionPipeline\n\npipeline = ConversionPipeline(data_dir=\"data/ds004130/\", format=\"parquet\")\nartifact = pipeline.run(splits={\"train\": 0.7, \"val\": 0.15, \"test\": 0.15})\n</code></pre> <p>\u2192 Convert</p>"},{"location":"guides/#6-use-artifacts","title":"6 \u2192 Use artifacts","text":"<p>Load, inspect, and bridge to your training framework.</p> <p>Open an artifact, check for subject leakage, visualize sample distributions, and connect to PyTorch, Lightning, or HuggingFace <code>datasets</code> in one line.</p> <pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130_parquet/\")\nart.inspect()\nloader = art.to_torch_dataset(split=\"train\")\n</code></pre> <p>\u2192 Artifacts</p>"},{"location":"guides/#neuroai-runtime","title":"NeuroAI runtime","text":"<p>Run a full inference pipeline declaratively.</p> <p>Connect a data source, a pre-trained model, and output sinks through a single YAML. The runtime checks source/model compatibility before loading any weights.</p> <pre><code>source:\n  type: bids\n  path: data/ds004130\n  modality: eeg\nmodel:\n  hub: braindecode/eegnet-motor-imagery\noutputs:\n  - type: parquet\n    path: predictions/\n</code></pre> <p>\u2192 NeuroAI runtime</p>"},{"location":"guides/#which-guide-do-i-need","title":"Which guide do I need?","text":"Situation Go to I have a dataset ID and want to know if it's usable Inspect + Assess readiness I want the smallest download to test a pipeline Download \u2192 Minimum subset Something looks wrong with my local data Visualize I want to train a model and need Parquet/HDF5 Convert I want to run inference with a pre-trained model NeuroAI runtime I need to verify my splits have no leakage Artifacts \u2192 Leakage check"},{"location":"modalities/","title":"Modalities","text":"<p>Go directly to your modality:</p> Modality BIDS datatype What Qortex can do EEG <code>eeg</code> inspect \u00b7 signal preview \u00b7 events \u00b7 convert MEG <code>meg</code> inspect \u00b7 signal preview \u00b7 events \u00b7 convert iEEG <code>ieeg</code> inspect \u00b7 contacts \u00b7 events \u00b7 signal preview fNIRS <code>nirs</code> inspect \u00b7 metadata MRI <code>anat</code> <code>func</code> <code>dwi</code> <code>fmap</code> inspect \u00b7 visualize \u00b7 QC \u00b7 convert PET <code>pet</code> inspect \u00b7 visualize \u00b7 overlay \u00b7 convert Behavioral <code>beh</code> events labels \u00b7 readiness \u00b7 conversion <p>Each modality page lists the files Qortex reads, which companion files matter, what can be inspected before download, and what can be converted into ML artifacts.</p>"},{"location":"modalities/#mri","title":"MRI","text":"<p>MRI is the broadest modality family in OpenNeuro. Qortex treats structural MRI, fMRI/BOLD, DWI, and fieldmaps as related but distinct workflows.</p> MRI overview <p>How Qortex detects MRI files, reads NIfTI metadata, handles BIDS entities, and prepares MRI files for visual QC.</p> Structural MRI <p>T1w, T2w, FLAIR, masks, segmentations, anatomical references, and orthogonal visualization.</p> fMRI / BOLD <p>4D BOLD files, events, TR, mean EPI, temporal standard deviation, tSNR, and global signal QC.</p> DWI / Diffusion <p>DWI NIfTI files, b-values, b-vectors, b0 previews, high-b previews, shell summaries, and gradient plots.</p> Fieldmaps <p>Magnitude, phase, phasediff, fieldmap metadata, intended-for relationships, and distortion-correction readiness.</p>"},{"location":"modalities/#mri-detection","title":"MRI detection","text":"<p>Qortex primarily detects MRI data from:</p> <ul> <li>BIDS datatype folders: <code>anat</code>, <code>func</code>, <code>dwi</code>, <code>fmap</code></li> <li>BIDS suffixes: <code>T1w</code>, <code>T2w</code>, <code>FLAIR</code>, <code>bold</code>, <code>dwi</code>, <code>fieldmap</code>, <code>phasediff</code>, <code>magnitude</code></li> <li>File extensions: <code>.nii</code>, <code>.nii.gz</code>, <code>.mgz</code>, <code>.mgh</code></li> <li>JSON sidecars and filename entities</li> </ul>"},{"location":"modalities/#mri-visualization","title":"MRI visualization","text":"<p>Qortex can render:</p> <ul> <li>orthogonal anatomical views</li> <li>slice-lightbox views</li> <li>fMRI QC summaries</li> <li>DWI QC summaries</li> <li>mask overlays</li> <li>statistical overlays</li> <li>PET-on-MRI overlays</li> <li>segmentation comparisons</li> </ul>"},{"location":"modalities/#pet","title":"PET","text":"<p>PET datasets usually contain tracer-specific image data and metadata. Qortex treats PET as a quantitative imaging modality where metadata and units matter.</p> PET overview <p>How PET files are detected and how tracer metadata is handled.</p> PET metadata <p>Tracer names, units, injection metadata, acquisition timing, and BIDS sidecars.</p> PET visualization <p>PET volume viewing, PET colormaps, and PET overlay on anatomical images.</p>"},{"location":"modalities/#pet-detection","title":"PET detection","text":"<p>Qortex detects PET data from:</p> <ul> <li>BIDS datatype folder: <code>pet</code></li> <li>suffix: <code>pet</code></li> <li>tracer entities such as <code>trc-</code></li> <li>NIfTI image files and PET JSON sidecars</li> </ul>"},{"location":"modalities/#pet-visualization","title":"PET visualization","text":"<p>PET images can be shown as standalone volumes or overlaid on anatomical MRI when matching geometry is available.</p>"},{"location":"modalities/#meg","title":"MEG","text":"<p>MEG datasets are signal-based. They usually depend on event files, channel metadata, head-position information, and acquisition-specific formats.</p> MEG overview <p>MEG file detection, event handling, and signal inspection.</p> MEG files <p>Common MEG raw formats, sidecars, channel files, and BIDS metadata.</p> MEG events <p>Events TSV files, trial labels, stimulus timing, and readiness for supervised learning.</p>"},{"location":"modalities/#meg-detection","title":"MEG detection","text":"<p>Qortex detects MEG data from:</p> <ul> <li>BIDS datatype folder: <code>meg</code></li> <li>MEG raw file formats such as <code>.fif</code></li> <li>MEG metadata sidecars</li> <li><code>events.tsv</code>, <code>channels.tsv</code>, and related companion files</li> </ul>"},{"location":"modalities/#meg-visualization","title":"MEG visualization","text":"<p>MEG visualization should focus on:</p> <ul> <li>short raw signal previews</li> <li>channel-level summaries</li> <li>event-aligned previews</li> <li>PSD and spectrogram views</li> <li>readiness for conversion into windowed ML samples</li> </ul>"},{"location":"modalities/#eeg","title":"EEG","text":"<p>EEG is common in OpenNeuro and often appears in several raw formats. Qortex should treat EEG as a signal modality where events, channels, sampling frequency, and labels are central.</p> EEG overview <p>How Qortex detects EEG files and prepares them for inspection and conversion.</p> EEG files <p>EDF, BDF, BrainVision, EEGLAB, sidecars, channel metadata, and sampling metadata.</p> EEG events <p>Events TSV files, labels, trial timing, and event-aligned windows.</p> EEG visualization <p>Butterfly plots, PSD, spectrograms, epoched previews, and signal QC.</p>"},{"location":"modalities/#eeg-detection","title":"EEG detection","text":"<p>Qortex detects EEG data from:</p> <ul> <li>BIDS datatype folder: <code>eeg</code></li> <li>file formats such as <code>.edf</code>, <code>.bdf</code>, <code>.vhdr</code>, <code>.vmrk</code>, <code>.eeg</code>, <code>.set</code>, <code>.fdt</code></li> <li><code>channels.tsv</code></li> <li><code>events.tsv</code></li> <li>EEG JSON sidecars</li> </ul>"},{"location":"modalities/#eeg-visualization","title":"EEG visualization","text":"<p>Qortex signal visualization should answer:</p> <ul> <li>Does the file load?</li> <li>How many channels are present?</li> <li>What is the sampling frequency?</li> <li>Are there obvious flat or noisy channels?</li> <li>Are events available?</li> <li>Can event windows be created for ML?</li> </ul>"},{"location":"modalities/#ieeg","title":"iEEG","text":"<p>iEEG datasets are signal-based but often need electrode and coordinate metadata to be useful. Qortex should treat iEEG as both a signal and localization modality.</p> iEEG overview <p>Intracranial EEG detection, raw signals, electrodes, and coordinate metadata.</p> iEEG files <p>Common iEEG signal formats, BIDS sidecars, events, and channel metadata.</p> Electrodes and coordinates <p>Electrode tables, coordinate systems, anatomical references, and localization readiness.</p>"},{"location":"modalities/#ieeg-detection","title":"iEEG detection","text":"<p>Qortex detects iEEG data from:</p> <ul> <li>BIDS datatype folder: <code>ieeg</code></li> <li>raw electrophysiology files</li> <li><code>channels.tsv</code></li> <li><code>electrodes.tsv</code></li> <li><code>coordsystem.json</code></li> <li><code>events.tsv</code></li> </ul>"},{"location":"modalities/#ieeg-visualization","title":"iEEG visualization","text":"<p>For early support, iEEG visualization should focus on signal previews and metadata completeness. Later support can include electrode localization over anatomical images.</p>"},{"location":"modalities/#fnirs","title":"fNIRS","text":"<p>fNIRS support is useful for BIDS completeness, but it should be presented honestly as an evolving modality in Qortex unless the loader and visualization paths are fully tested.</p> fNIRS overview <p>Functional near-infrared spectroscopy files, metadata, optodes, and readiness checks.</p> fNIRS files <p>SNIRF files, channel metadata, optode metadata, and events.</p>"},{"location":"modalities/#fnirs-detection","title":"fNIRS detection","text":"<p>Qortex should detect fNIRS data from:</p> <ul> <li>BIDS datatype folder: <code>nirs</code></li> <li><code>.snirf</code> files</li> <li><code>channels.tsv</code></li> <li><code>optodes.tsv</code></li> <li><code>events.tsv</code></li> <li>fNIRS JSON sidecars</li> </ul>"},{"location":"modalities/#current-status","title":"Current status","text":"<p>fNIRS should be documented as metadata/readiness-first unless the full visualization and conversion path has test coverage.</p>"},{"location":"modalities/#behavioral-and-events","title":"Behavioral and events","text":"<p>Behavioral and event files are central to Qortex because they often define labels for supervised learning.</p> Behavioral overview <p>Behavioral tables, task metadata, and non-imaging records.</p> Events TSV <p>Onsets, durations, trial types, responses, conditions, and task labels.</p> Labels and trial types <p>How Qortex detects candidate labels and evaluates supervised-learning readiness.</p>"},{"location":"modalities/#behavioral-detection","title":"Behavioral detection","text":"<p>Qortex detects behavioral and event data from:</p> <ul> <li><code>events.tsv</code></li> <li><code>events.json</code></li> <li><code>beh</code> datatype folders</li> <li><code>.tsv</code> and <code>.csv</code> tables</li> <li><code>participants.tsv</code></li> <li>task sidecars</li> </ul>"},{"location":"modalities/#why-this-matters","title":"Why this matters","text":"<p>For many ML workflows, the imaging or signal file is not enough. The dataset becomes trainable only when Qortex can connect recordings to usable labels, split policy, and provenance.</p>"},{"location":"modalities/#cross-modality-workflows","title":"Cross-modality workflows","text":"<p>Some datasets contain multiple modalities. Qortex should not treat modalities as isolated silos. It should connect files through BIDS entities, companions, and logical recordings.</p>"},{"location":"modalities/#common-cross-modality-questions","title":"Common cross-modality questions","text":"<ul> <li>Which subjects have both T1w and BOLD?</li> <li>Which BOLD runs have matching events?</li> <li>Which DWI files have both bval and bvec companions?</li> <li>Which PET scans have anatomical references?</li> <li>Which EEG files have channels and events?</li> <li>Which iEEG files have electrodes and coordinate systems?</li> <li>Which files can be visualized before conversion?</li> <li>Which recordings can become ML samples?</li> </ul>"},{"location":"modalities/#relevant-qortex-tools","title":"Relevant Qortex tools","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds000001\")\n\nmanifest = ds.manifest()\nplan = ds.plan(subjects=[\"01\"], modalities=[\"fmri\"])\nreport = ds.visual_audit(\"qc/\", suffixes=[\"T1w\", \"bold\", \"dwi\"])\n</code></pre>"},{"location":"modalities/conversion/","title":"Modality Conversion","text":"<p>Qortex converts each modality to ML-ready artifacts (Parquet, Zarr, HDF5, WebDataset, HuggingFace, TFRecord). The core conversion concepts \u2014 windows, splits, labels, provenance \u2014 are the same across modalities. What differs is the per-sample shape, windowing mode, and channel selection.</p>"},{"location":"modalities/conversion/#common-conversion-call","title":"Common conversion call","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nart = ds.convert(\n    output_dir=\"artifacts/\",\n    format=\"parquet\",\n    window=dict(duration_s=2.0, overlap=0.5),\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n    label_col=\"trial_type\",\n)\n</code></pre>"},{"location":"modalities/conversion/#per-modality-output-shape","title":"Per-modality output shape","text":"Modality Per-sample shape Notes Structural MRI <code>(x, y, z)</code> One sample per file fMRI BOLD (sliding) <code>(n_voxels, n_time_points)</code> Flattened spatial \u00d7 windowed time fMRI BOLD (event-aligned) <code>(n_voxels, n_time_points)</code> Anchored to event onsets DWI <code>(x, y, z, n_directions)</code> One sample per file MEG <code>(n_channels, n_time_points)</code> Magnetometers + gradiometers EEG <code>(n_channels, n_time_points)</code> EEG-type channels only iEEG <code>(n_channels, n_time_points)</code> ieeg/seeg/ecog-type channels fNIRS <code>(n_channels, n_time_points)</code> HbO + HbR interleaved PET <code>(x, y, z)</code> per frame One sample per frame"},{"location":"modalities/conversion/#windowing-modes","title":"Windowing modes","text":"<p>Sliding window \u2014 fixed-length overlapping windows across the full timeseries:</p> <pre><code>window=dict(duration_s=30.0, overlap=0.5)\n</code></pre> <p>Best for: resting-state fMRI, MEG/EEG without discrete trials, fNIRS continuous recordings.</p> <p>Event-aligned \u2014 windows centered on each trial onset in events.tsv:</p> <pre><code>window=dict(mode=\"event_aligned\", tmin=-0.2, tmax=0.8)\n</code></pre> <p>Best for: task-based EEG/MEG with clear trial structure, fMRI block designs.</p>"},{"location":"modalities/conversion/#label-sources","title":"Label sources","text":"Source Modalities How to specify <code>events.tsv \u2192 trial_type</code> fMRI, EEG, MEG, iEEG, fNIRS <code>label_col=\"trial_type\"</code> <code>participants.tsv \u2192 column</code> All (subject-level) <code>label_col=\"age\"</code>, <code>label_source=\"participants\"</code> No labels Structural MRI, DWI, unsupervised Omit <code>label_col</code>"},{"location":"modalities/conversion/#mri-specific-conversion","title":"MRI-specific conversion","text":"<pre><code># T1w: one sample per subject, no windows\nart = ds.convert(\n    format=\"zarr\",\n    suffixes=[\"T1w\"],\n    split=dict(strategy=\"subject\"),\n)\n# Sample shape: (x, y, z) float32\n\n# BOLD: sliding windows, trial_type labels\nart = ds.convert(\n    format=\"parquet\",\n    suffixes=[\"bold\"],\n    window=dict(duration_s=20.0, overlap=0.5),\n    label_col=\"trial_type\",\n)\n</code></pre>"},{"location":"modalities/conversion/#signal-specific-conversion-eeg-meg-ieeg-fnirs","title":"Signal-specific conversion (EEG / MEG / iEEG / fNIRS)","text":"<pre><code># Event-aligned\nart = ds.convert(\n    format=\"parquet\",\n    datatypes=[\"eeg\"],\n    window=dict(mode=\"event_aligned\", tmin=-0.2, tmax=0.8),\n    label_col=\"trial_type\",\n)\n# Sample shape: (n_eeg_channels, n_time_points)\n</code></pre>"},{"location":"modalities/conversion/#dwi-conversion","title":"DWI conversion","text":"<pre><code>art = ds.convert(\n    format=\"zarr\",\n    datatypes=[\"dwi\"],\n    split=dict(strategy=\"subject\"),\n)\n# Sample shape: (x, y, z, n_directions)\n</code></pre> <p>bval and bvec arrays are stored as sample metadata in the artifact manifest.</p>"},{"location":"modalities/conversion/#pet-conversion","title":"PET conversion","text":"<pre><code>art = ds.convert(\n    format=\"zarr\",\n    datatypes=[\"pet\"],\n    split=dict(strategy=\"subject\"),\n)\n# One sample per PET frame if dynamic, one sample total if static\n</code></pre>"},{"location":"modalities/conversion/#load-the-artifact","title":"Load the artifact","text":"<p>All modalities produce the same artifact interface regardless of shape:</p> <pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/\")\nX, y = art.sklearn(split=\"train\")\nds_torch = art.torch(split=\"train\")\nhf_ds = art.huggingface(split=\"train\")\n</code></pre>"},{"location":"modalities/conversion/#related","title":"Related","text":"<ul> <li>Conversion pipeline</li> <li>Windows</li> <li>Formats</li> <li>ML bridge</li> </ul>"},{"location":"modalities/readiness/","title":"Modality Readiness","text":"<p>Each modality has different companion files, quality indicators, and ML prerequisites. This page summarizes what Qortex checks per modality before recommending a dataset for training.</p>"},{"location":"modalities/readiness/#overview","title":"Overview","text":"Modality Required companions Key readiness checks Structural MRI (T1w/T2w) JSON sidecar voxel size, orientation, shape fMRI BOLD JSON sidecar, events.tsv TR present, events exist, label column usable DWI bval, bvec, JSON sidecar bval/bvec present, \u2265 30 directions, multi-shell MEG channels.tsv, events.tsv channel count, SSS status, events present EEG channels.tsv, events.tsv channel count, sampling rate, events present iEEG channels.tsv, events.tsv, electrodes.tsv electrode positions present, events present fNIRS channels.tsv, events.tsv HbO/HbR channels detected, events present PET JSON sidecar (with tracer info) TracerName, FrameTimesStart, body weight"},{"location":"modalities/readiness/#run-readiness-checks","title":"Run readiness checks","text":"<p>The <code>doctor()</code> check applies modality-specific rules automatically:</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\nprint(report.to_text())\n</code></pre> <p>Use a built-in recipe for stricter modality-specific thresholds:</p> <pre><code>report = ds.doctor(recipe=\"eeg-classification\")\nreport = ds.doctor(recipe=\"fmri-classification\")\nreport = ds.doctor(recipe=\"dwi\")\n</code></pre>"},{"location":"modalities/readiness/#structural-mri","title":"Structural MRI","text":"<p>No events required. Labels come from <code>participants.tsv</code> (age, sex, group, diagnosis).</p> <pre><code>ok = ds.can_train(\n    target_col=\"group\",          # from participants.tsv\n    label_source=\"participants\",\n    min_subjects=30,\n)\n</code></pre> <p>Readiness flags: - <code>MISSING_T1W</code> \u2014 no T1w file for some subjects - <code>SHAPE_MISMATCH</code> \u2014 T1w shapes differ across subjects (preprocessing may be needed)</p>"},{"location":"modalities/readiness/#fmri-bold","title":"fMRI BOLD","text":"<pre><code>ok = ds.can_train(target_col=\"trial_type\")\n</code></pre> <p>Readiness flags: - <code>NO_EVENTS</code> \u2014 no events.tsv files - <code>MISSING_TR</code> \u2014 TR not in JSON sidecar - <code>NO_COMPANIONS</code> \u2014 events.tsv missing for some subjects that have BOLD</p>"},{"location":"modalities/readiness/#dwi","title":"DWI","text":"<pre><code>report = ds.doctor(recipe=\"dwi\")\n</code></pre> <p>Readiness flags: - <code>NO_BVAL</code> \u2014 bval file missing - <code>NO_BVEC</code> \u2014 bvec file missing - <code>FEW_DIRECTIONS</code> \u2014 fewer than 30 gradient directions (warning) - <code>SINGLE_SHELL</code> \u2014 only one non-zero b-value (warning for multi-shell models)</p>"},{"location":"modalities/readiness/#eeg-meg","title":"EEG / MEG","text":"<pre><code>ok = ds.can_train(target_col=\"trial_type\")\nreport = ds.doctor(recipe=\"eeg-classification\")\n</code></pre> <p>Readiness flags: - <code>NO_CHANNELS_TSV</code> \u2014 channels.tsv missing - <code>NO_EVENTS</code> \u2014 events.tsv missing or empty - <code>LOW_SFREQ</code> \u2014 sampling frequency below threshold (warning)</p>"},{"location":"modalities/readiness/#ieeg","title":"iEEG","text":"<p>Same checks as EEG, plus:</p> <ul> <li><code>NO_ELECTRODES</code> \u2014 electrodes.tsv missing (spatial analysis not possible)</li> <li><code>NO_COORDSYSTEM</code> \u2014 coordsystem.json missing</li> </ul>"},{"location":"modalities/readiness/#fnirs","title":"fNIRS","text":"<p>Same event checks as EEG, plus:</p> <ul> <li><code>NO_HBO_CHANNELS</code> \u2014 no HbO channels detected after loading</li> <li><code>NO_OPTODES</code> \u2014 optodes.tsv missing</li> </ul>"},{"location":"modalities/readiness/#pet","title":"PET","text":"<ul> <li><code>MISSING_TRACER</code> \u2014 TracerName or TracerRadionuclide absent in sidecar</li> <li><code>MISSING_FRAME_TIMES</code> \u2014 FrameTimesStart not in sidecar (static PET assumed)</li> <li><code>NO_SUV_FIELDS</code> \u2014 BodyWeight or InjectedRadioactivity absent (SUV not possible)</li> </ul>"},{"location":"modalities/readiness/#related","title":"Related","text":"<ul> <li>Doctor \u2014 full readiness report</li> <li>Recipes \u2014 modality-specific recipe presets</li> <li>Modality selection \u2014 choosing a modality</li> </ul>"},{"location":"modalities/selection/","title":"Modality Selection","text":"<p>Choosing the right modality for a machine learning project depends on the question being asked, the available hardware, and what data exists on OpenNeuro. This page helps you match a research question to a modality and then find datasets using Qortex.</p>"},{"location":"modalities/selection/#quick-guide","title":"Quick guide","text":"Question Best modality Why Brain region activation from task fMRI BOLD Best spatial resolution for cortical mapping Fast neural dynamics (&lt; 100 ms) EEG / MEG Millisecond temporal resolution Cortical decoding in clinical patients iEEG Direct contact with cortex; high SNR Whole-brain connectivity at rest fMRI (resting-state) Spatial coverage, established pipeline White matter tract integrity DWI Only modality directly sensitive to axon microstructure Haemodynamic response in low-cost settings fNIRS Portable, wearable; no MRI required Metabolic / receptor mapping PET Tracer-specific quantification Behavior and task performance only Behavioral / events No imaging required"},{"location":"modalities/selection/#modality-characteristics","title":"Modality characteristics","text":"Modality Temporal res. Spatial res. File size (typical) Online processing Structural MRI static 1 mm 30\u201380 MB no fMRI BOLD 0.5\u20132 s (TR) 2\u20133 mm 100 MB \u2013 2 GB no DWI static 2\u20133 mm 200 MB \u2013 2 GB no MEG &lt; 1 ms 5\u201310 mm (reconstructed) 500 MB \u2013 5 GB possible EEG &lt; 1 ms low (scalp) 5\u2013500 MB yes iEEG &lt; 1 ms &lt; 1 mm (contact) 50 MB \u2013 5 GB yes fNIRS 0.1\u20131 s 1\u20133 cm 1\u201350 MB yes PET frames (15 s \u2013 30 min) 2\u20135 mm 20\u2013200 MB no"},{"location":"modalities/selection/#search-by-modality","title":"Search by modality","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\n# Check what's available per modality\nwith OpenNeuroClient() as client:\n    facets = DatasetQuery().facets()\n    print(facets[\"modalities\"])\n    # {\"MRI\": 1243, \"EEG\": 156, \"MEG\": 89, \"PET\": 52, ...}\n\n# Find EEG datasets with task events, \u2265 50 subjects\nresults = (\n    DatasetQuery()\n    .modality(\"eeg\")\n    .min_subjects(50)\n    .has_events()\n    .fetch()\n)\n</code></pre>"},{"location":"modalities/selection/#switching-modality-mid-project","title":"Switching modality mid-project","text":"<p>Qortex's pipeline is modality-agnostic. The same <code>Dataset \u2192 inspect \u2192 download \u2192 convert</code> flow works for every modality. The output artifact format (Parquet, Zarr, etc.) is the same regardless of modality \u2014 only the per-sample shape changes.</p> <pre><code># EEG: sample shape is (n_channels, n_time_points)\n# fMRI: sample shape is (n_voxels, n_time_points) or (x, y, z, t_window)\n# DWI: sample shape is (x, y, z, n_directions)\n# PET: sample shape is (x, y, z) per frame\n</code></pre>"},{"location":"modalities/selection/#related","title":"Related","text":"<ul> <li>Modality readiness \u2014 per-modality readiness checks</li> <li>Modality conversion \u2014 per-modality conversion considerations</li> <li>Search &amp; filter \u2014 DatasetQuery reference</li> </ul>"},{"location":"modalities/behavioral/","title":"Behavioral and Events","text":"<p>Behavioral data in BIDS includes events.tsv files, response logs, and behavioral task records. For Qortex, behavioral data is not just supplementary \u2014 it is often what makes a dataset trainable. Events.tsv defines the trial-type labels that supervised ML depends on.</p>"},{"location":"modalities/behavioral/#what-counts-as-behavioral-data","title":"What counts as behavioral data","text":"<ul> <li><code>events.tsv</code> \u2014 trial onsets, durations, trial types, response times (paired with every signal recording)</li> <li><code>beh/</code> datatype \u2014 standalone behavioral tables without an imaging companion</li> <li><code>participants.tsv</code> \u2014 subject-level covariates: age, sex, diagnosis, group</li> </ul>"},{"location":"modalities/behavioral/#why-behavioral-data-matters-for-ml","title":"Why behavioral data matters for ML","text":"<p>A BOLD or EEG file without <code>events.tsv</code> cannot be used for supervised classification. Qortex's readiness checks verify event coverage before you download any large imaging files.</p>"},{"location":"modalities/behavioral/#behavioral-pages","title":"Behavioral pages","text":"<p>Events TSV \u2014 Structure of events.tsv files, required and optional columns, BIDS rules.</p> <p>Labels and trial types \u2014 How Qortex detects label columns, evaluates class distributions, and checks supervised-learning readiness.</p>"},{"location":"modalities/behavioral/#check-behavioral-readiness","title":"Check behavioral readiness","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n\n# Is any label column usable?\nok = ds.can_train(target_col=\"trial_type\")\nprint(ok)\n\n# Detailed label landscape\nprint(ds.label_landscape())\n</code></pre> <p>CLI:</p> <pre><code>qortex can-train ds004130 --label trial_type\nqortex eda ds004130 --label trial_type\n</code></pre>"},{"location":"modalities/behavioral/#access-behavioral-data","title":"Access behavioral data","text":"<pre><code># participants.tsv\nparticipants = ds.participants()\nprint(participants.head())\n\n# events.tsv for a subject\nevents = ds.events(subject=\"01\", task=\"rest\")\nprint(events.head())\n\n# standalone behavioral file\nbeh_files = ds.files(datatypes=[\"beh\"])\n</code></pre>"},{"location":"modalities/behavioral/events-tsv/","title":"Events TSV","text":"<p><code>events.tsv</code> is the BIDS standard for task timing and labels. It is the primary source of supervised learning labels for signal recordings (EEG, MEG, fMRI, iEEG, fNIRS).</p>"},{"location":"modalities/behavioral/events-tsv/#structure","title":"Structure","text":"<pre><code>onset    duration  trial_type    response_time  stim_file\n0.000    1.500     fixation      n/a            n/a\n1.500    1.500     go            0.412          stimuli/go_001.png\n3.000    1.500     nogo          n/a            stimuli/nogo_001.png\n4.500    1.500     go            0.398          stimuli/go_002.png\n</code></pre> <p>Required columns:</p> Column Description <code>onset</code> Event start time relative to recording start (seconds) <code>duration</code> Event duration in seconds (0 for instantaneous) <p>Recommended:</p> Column Description <code>trial_type</code> Primary label for ML; categorical condition name <code>response_time</code> Time from stimulus to response (seconds) \u2014 for regression tasks <code>stim_file</code> Path to stimulus file relative to dataset root <code>HED</code> Hierarchical Event Descriptors (for annotation-based workflows)"},{"location":"modalities/behavioral/events-tsv/#naming-convention","title":"Naming convention","text":"<p>Each <code>events.tsv</code> is linked to one recording via its filename entities:</p> <pre><code>sub-01/func/sub-01_task-nback_events.tsv      \u2192 sub-01_task-nback_bold.nii.gz\nsub-01/eeg/sub-01_task-rest_run-01_events.tsv \u2192 sub-01_task-rest_run-01_eeg.set\n</code></pre>"},{"location":"modalities/behavioral/events-tsv/#read-eventstsv","title":"Read events.tsv","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n\n# From CDN (before download \u2014 events.tsv are small)\nevents = ds.events(subject=\"01\", task=\"rest\")\n\n# After metadata download\nevents = ds.events(subject=\"01\", task=\"rest\")  # reads from local file\n\nprint(events.dtypes)        # column types\nprint(events.shape)         # (n_trials, n_columns)\nprint(events.head(5))\n</code></pre>"},{"location":"modalities/behavioral/events-tsv/#missing-eventstsv","title":"Missing events.tsv","text":"<p>Qortex reports missing events.tsv in the doctor check:</p> <pre><code>report = ds.doctor()\n# ERROR [NO_EVENTS]: no events.tsv found for any subject\n# WARNING [PARTIAL_EVENTS]: sub-03, sub-17 missing events.tsv\n</code></pre> <p>Datasets with no events.tsv can still be downloaded and used for unsupervised or regression tasks where labels come from <code>participants.tsv</code>.</p>"},{"location":"modalities/behavioral/events-tsv/#eventsjson-sidecar","title":"events.json sidecar","text":"<p>An optional <code>events.json</code> describes columns in <code>events.tsv</code>:</p> <pre><code>{\n  \"trial_type\": {\n    \"LongName\": \"Trial type\",\n    \"Description\": \"Experimental condition\",\n    \"Levels\": {\n      \"go\": \"Go trial \u2014 respond to stimulus\",\n      \"nogo\": \"No-go trial \u2014 withhold response\"\n    }\n  }\n}\n</code></pre> <p>Qortex does not currently parse <code>events.json</code> for column descriptions, but it is available via:</p> <pre><code>desc = ds.sidecar(\"sub-01/func/sub-01_task-flanker_events.json\")\nprint(desc[\"trial_type\"][\"Levels\"])\n</code></pre>"},{"location":"modalities/behavioral/events-tsv/#related","title":"Related","text":"<ul> <li>Labels and trial types</li> <li>Label readiness</li> <li>Event-aligned windows</li> </ul>"},{"location":"modalities/behavioral/labels-and-trial-types/","title":"Labels and Trial Types","text":"<p>Qortex extracts labels from <code>trial_type</code> in <code>events.tsv</code> and numeric columns from both <code>events.tsv</code> and <code>participants.tsv</code>. Understanding how label detection works helps you choose the right target column and verify readiness before committing to a full download.</p>"},{"location":"modalities/behavioral/labels-and-trial-types/#auto-detection","title":"Auto-detection","text":"<p>When <code>target_col=None</code>, Qortex auto-selects the best label column:</p> <ol> <li>Read all <code>events.tsv</code> files across subjects (fetched from CDN \u2014 no large download needed)</li> <li>Score each column by number of distinct non-null values, excluding known non-label columns: <code>onset</code>, <code>duration</code>, <code>stim_file</code>, <code>HED</code>, <code>response_time</code>, <code>trial_type_id</code></li> <li>Select the column with the most distinct values that is not purely numeric</li> </ol> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nok = ds.can_train()  # auto-detects label column\nprint(ds.label_landscape())\n# Label column: trial_type (auto-detected)\n</code></pre> <p>Specify explicitly to avoid surprises:</p> <pre><code>ok = ds.can_train(target_col=\"trial_type\")\n</code></pre>"},{"location":"modalities/behavioral/labels-and-trial-types/#label-landscape","title":"Label landscape","text":"<p><code>ds.label_landscape()</code> shows per-class counts and missing subjects:</p> <pre><code>landscape = ds.label_landscape(target_col=\"trial_type\")\nprint(landscape)\n</code></pre> <p>Output:</p> <pre><code>Label column: trial_type\nTotal subjects: 88   With labels: 86   Missing labels: 2\n\nClass distribution:\n  rest        2,112 samples   (88 subjects)\n  task          528 samples   (86 subjects)  \u2190 2 subjects missing\n\nSubjects missing 'task':\n  sub-23  sub-47\n</code></pre>"},{"location":"modalities/behavioral/labels-and-trial-types/#regression-labels","title":"Regression labels","text":"<p>Numeric columns (response time, accuracy, behavioral score) can be used for regression:</p> <pre><code># From events.tsv\nok = ds.can_train(target_col=\"response_time\", task_type=\"regression\")\n\n# From participants.tsv\nparts = ds.participants()\nprint(parts.select([\"participant_id\", \"age\", \"diagnosis\"]))\n</code></pre> <p>For regression from <code>participants.tsv</code>, labels are subject-level, not trial-level. One label per subject propagates to all windows from that subject.</p>"},{"location":"modalities/behavioral/labels-and-trial-types/#hierarchical-trial-types","title":"Hierarchical trial types","text":"<p>BIDS permits slash notation: <code>condition/stimulus</code>. Qortex treats the full string as the label by default.</p> <pre><code>landscape = ds.label_landscape(target_col=\"trial_type\")\n# Classes: [\"go/compatible\", \"go/incompatible\", \"nogo\"] \u2192 3 classes\n\n# Collapse to top level:\nimport polars as pl\nevents = ds.events(subject=\"01\", task=\"flanker\")\nevents = events.with_columns(\n    pl.col(\"trial_type\").str.split(\"/\").list.get(0)\n)\n# Classes: [\"go\", \"go\", \"nogo\"] \u2192 2 classes\n</code></pre>"},{"location":"modalities/behavioral/labels-and-trial-types/#class-imbalance","title":"Class imbalance","text":"<p>Severe imbalance makes train/val/test split difficult without stratification. Check per-class counts:</p> <pre><code>landscape = ds.label_landscape(target_col=\"trial_type\")\n# If one class has &lt; 30 samples total: warning in doctor()\n</code></pre> <p>Qortex does not automatically balance classes during conversion. Handle imbalance in your training loop (class weights, oversampling, or undersampling).</p>"},{"location":"modalities/behavioral/labels-and-trial-types/#can-train-requirements","title":"Can-train requirements","text":"<p><code>can_train()</code> requires all of the following:</p> Check Default threshold <code>min_classes</code> 2 <code>min_per_class</code> 10 samples total <code>min_subjects</code> 10 subjects with the label <code>require_events</code> True <p>Adjust thresholds for your use case:</p> <pre><code>ok = ds.can_train(\n    target_col=\"trial_type\",\n    min_classes=3,\n    min_per_class=50,\n    min_subjects=30,\n)\n</code></pre>"},{"location":"modalities/behavioral/labels-and-trial-types/#related","title":"Related","text":"<ul> <li>Events TSV</li> <li>Can train</li> <li>Label readiness</li> <li>EDA</li> </ul>"},{"location":"modalities/eeg/","title":"EEG","text":"<p>Electroencephalography (EEG) records scalp electrical potentials from electrode arrays. EEG is the most common electrophysiology modality on OpenNeuro. The EEG signal is high-dimensional in time (sampling rates 250\u20132048 Hz), requires companion channel and event files, and is the primary target for Qortex's event-aligned window conversion.</p>"},{"location":"modalities/eeg/#install","title":"Install","text":"<pre><code>pip install \"qortex[eeg]\"    # MNE-Python is used for all EEG reading\n</code></pre>"},{"location":"modalities/eeg/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  eeg/\n    sub-01_task-rest_eeg.set          # EEG data file\n    sub-01_task-rest_eeg.fdt          # data companion (EEGLAB)\n    sub-01_task-rest_eeg.json         # required sidecar\n    sub-01_task-rest_channels.tsv     # channel metadata\n    sub-01_task-rest_events.tsv       # trial labels\n    sub-01_task-rest_electrodes.tsv   # electrode positions (optional)\n    sub-01_coordsystem.json           # coordinate system (optional)\n</code></pre>"},{"location":"modalities/eeg/#eeg-pages","title":"EEG pages","text":"<p>EEG files \u2014 Formats, channel types, sampling metadata, loading.</p> <p>EEG events \u2014 Events.tsv, trial types, label extraction.</p> <p>EEG visualization \u2014 Butterfly plots, PSD, spectrograms, topomaps, epoched previews.</p>"},{"location":"modalities/eeg/#search-for-eeg-datasets","title":"Search for EEG datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\n# Local catalog\nresults = DatasetQuery().modality(\"eeg\").min_subjects(20).has_events().fetch()\n\n# Live \u2014 sorted by downloads\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"EEG\", sort_by=\"downloads\", limit=20)\n</code></pre> <p>CLI:</p> <pre><code>qortex search --modality eeg --min-subjects 20 --has-events\n</code></pre>"},{"location":"modalities/eeg/events/","title":"EEG Events","text":"<p>EEG events define the onset and label of each trial in a recording. Qortex reads <code>events.tsv</code> files and uses them to construct supervised training samples via event-aligned windowing.</p>"},{"location":"modalities/eeg/events/#bids-eventstsv","title":"BIDS events.tsv","text":"<pre><code>onset    duration  trial_type  response_time\n0.000    1.5       rest        n/a\n1.500    1.5       task        0.612\n3.000    1.5       rest        n/a\n4.500    1.5       task        0.498\n</code></pre> <p>Required columns: <code>onset</code>, <code>duration</code>. Recommended column: <code>trial_type</code>.</p>"},{"location":"modalities/eeg/events/#check-event-availability","title":"Check event availability","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\n# Events: yes \u2014 88 files, columns: onset duration trial_type\n\nok = ds.can_train(target_col=\"trial_type\", min_classes=2, min_per_class=10)\nprint(ok)  # True\n\nprint(ds.label_landscape(target_col=\"trial_type\"))\n</code></pre>"},{"location":"modalities/eeg/events/#read-events-for-a-specific-subject","title":"Read events for a specific subject","text":"<pre><code>events = ds.events(subject=\"01\", task=\"rest\")\n# DataFrame: onset  duration  trial_type  ...\nprint(events[\"trial_type\"].unique())   # [\"rest\", \"task\"]\nprint(events.shape)                    # (240, 4)\n</code></pre> <p>For multiple sessions or runs:</p> <pre><code>events = ds.events(subject=\"01\", task=\"rest\", session=\"01\", run=\"01\")\n</code></pre>"},{"location":"modalities/eeg/events/#event-aligned-window-conversion","title":"Event-aligned window conversion","text":"<pre><code>art = ds.convert(\n    data_dir=\"data/ds004130/\",\n    output_dir=\"artifacts/\",\n    format=\"parquet\",\n    window=dict(mode=\"event_aligned\", tmin=-0.2, tmax=0.8),\n    label_col=\"trial_type\",\n)\n# Each sample: (n_channels, n_time_points) \u2014 200 ms pre- to 800 ms post-onset\n</code></pre>"},{"location":"modalities/eeg/events/#stimulus-channel-triggers-no-eventstsv","title":"Stimulus channel triggers (no events.tsv)","text":"<p>Some datasets have trigger codes only in a <code>STI</code> or <code>Status</code> channel. Qortex's readiness check warns when <code>events.tsv</code> is absent but a stimulus channel is detected:</p> <pre><code>report = ds.doctor()\n# WARNING [NO_EVENTS_TSV]: sub-03 has STI channel but no events.tsv\n# Suggestion: extract events manually with mne.find_events() and save as events.tsv\n</code></pre>"},{"location":"modalities/eeg/events/#hierarchical-trial-types","title":"Hierarchical trial types","text":"<p>BIDS allows slash-delimited trial types: <code>condition/stimulus</code>. Qortex treats the full string as the label class.</p> <pre><code># events: [\"go/compatible\", \"go/incompatible\", \"nogo\"]\n# \u2192 3 classes in the classifier\n</code></pre> <p>Collapse to top-level:</p> <pre><code>import polars as pl\nevents = ds.events(subject=\"01\", task=\"flanker\")\nevents = events.with_columns(\n    pl.col(\"trial_type\").str.split(\"/\").list.get(0).alias(\"trial_type\")\n)\n# \u2192 [\"go\", \"go\", \"nogo\"]\n</code></pre>"},{"location":"modalities/eeg/events/#related","title":"Related","text":"<ul> <li>EEG files</li> <li>Label readiness</li> <li>Windows \u2014 event-aligned</li> </ul>"},{"location":"modalities/eeg/files/","title":"EEG Files","text":"<p>EEG data files on OpenNeuro come in several vendor-specific formats. Qortex uses MNE-Python to read all of them through a single <code>EEGLoader</code> interface.</p>"},{"location":"modalities/eeg/files/#supported-formats","title":"Supported formats","text":"Extension Format System <code>.set</code> + <code>.fdt</code> EEGLAB MATLAB-based toolbox (most common on OpenNeuro) <code>.edf</code> European Data Format Clinical / portable <code>.bdf</code> BioSemi Data Format BioSemi ActiveTwo <code>.fif</code> MNE native / Elekta Converted from EEG systems <code>.vhdr</code> + <code>.vmrk</code> + <code>.eeg</code> BrainVision Brain Products <code>.cnt</code> Neuroscan Older clinical <code>.mff</code> EGI MFF Electric Geodesics <code>.gdf</code> General Data Format Open-source"},{"location":"modalities/eeg/files/#check-before-download","title":"Check before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\neeg_files = ds.files(datatypes=[\"eeg\"])\nfor f in eeg_files:\n    print(f.subject, f.extension, f.size)\n</code></pre>"},{"location":"modalities/eeg/files/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/eeg/sub-01_task-rest_eeg.set\")\n\ninfo[\"sfreq\"]                # 256.0  (Hz)\ninfo[\"n_channels\"]           # 64\ninfo[\"channel_type_counts\"]  # {\"eeg\": 61, \"eog\": 2, \"misc\": 1}\ninfo[\"n_bad_channels\"]       # 0\ninfo[\"powerline_freq\"]       # 50.0  (Hz)\ninfo[\"eeg_reference\"]        # \"CZ\"\ninfo[\"software_filters\"]     # {\"Highpass\": \"0.016 Hz\", \"Lowpass\": \"100 Hz\"}\n</code></pre>"},{"location":"modalities/eeg/files/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.eeg import EEGLoader\nfrom pathlib import Path\n\nloader = EEGLoader()\nrecord = loader.load(file_record, local_path=Path(\"sub-01/eeg/sub-01_task-rest_eeg.set\"))\n\nrecord.sfreq          # 256.0\nrecord.n_channels     # 61  (EEG-type channels only \u2014 EOG excluded by default)\nrecord.duration       # 480.0  (seconds)\nrecord.channel_names  # [\"Fp1\", \"Fp2\", \"F3\", ...]\nrecord.channel_types  # [\"eeg\", \"eeg\", \"eeg\", ...]\nrecord.metadata[\"eeg_reference\"]   # \"CZ\"\nrecord.metadata[\"bad_channels\"]    # []\n\ndata = loader.to_numpy(record)   # (n_eeg_channels, n_times) float64 array\n</code></pre> <p>Qortex selects EEG-type channels and excludes EOG, ECG, EMG, and stimulus channels by default. This is the data used for ML conversion.</p>"},{"location":"modalities/eeg/files/#mne-bids-integration","title":"MNE-BIDS integration","text":"<p>Qortex reads EEG via MNE-BIDS when a BIDS root can be inferred, which correctly applies sidecar inheritance for channel metadata. If MNE-BIDS is unavailable, it falls back to direct MNE reading.</p>"},{"location":"modalities/eeg/files/#lazy-loading","title":"Lazy loading","text":"<pre><code>record = loader.lazy_load(file_record, local_path=...)\n# MNE Raw object \u2014 data stays on disk until to_numpy() or slicing\n</code></pre>"},{"location":"modalities/eeg/files/#related","title":"Related","text":"<ul> <li>EEG events</li> <li>EEG visualization</li> <li>Channels metadata</li> </ul>"},{"location":"modalities/eeg/visualization/","title":"EEG Visualization","text":"<p>Qortex visualizes EEG signals using MNE-Python for rendering and Plotly for interactive HTML output.</p>"},{"location":"modalities/eeg/visualization/#install","title":"Install","text":"<pre><code>pip install \"qortex[eeg,visual]\"\n</code></pre>"},{"location":"modalities/eeg/visualization/#butterfly-plot","title":"Butterfly plot","text":"<p>Overlay all EEG channels on a single time axis. Reveals flat or noisy channels, common artifacts, and overall signal quality.</p> <pre><code>from qortex.visualize import TimeSeriesViewer\n\nviewer = TimeSeriesViewer(\"data/ds004130/sub-01/eeg/sub-01_task-rest_eeg.set\")\nfig = viewer.butterfly()\nfig.show()\n</code></pre> <p>CLI:</p> <pre><code>qortex visualize ds004130 --subject 01 --suffix eeg --data-dir data/ds004130/\n</code></pre>"},{"location":"modalities/eeg/visualization/#power-spectral-density","title":"Power spectral density","text":"<p>PSD reveals frequency-band characteristics and powerline noise:</p> <pre><code>fig = viewer.psd(fmin=1, fmax=80)\nfig.show()\n</code></pre> <p>Expected features in clean EEG: alpha peak (8\u201312 Hz), 1/f slope, sharp 50/60 Hz notch if filtered.</p>"},{"location":"modalities/eeg/visualization/#spectrogram","title":"Spectrogram","text":"<p>Time\u2013frequency representation:</p> <pre><code>fig = viewer.spectrogram(\n    channel=\"Oz\",\n    fmin=1,\n    fmax=50,\n    baseline=(-0.2, 0.0),    # baseline period in seconds\n)\nfig.show()\n</code></pre>"},{"location":"modalities/eeg/visualization/#topomap","title":"Topomap","text":"<p>Scalp distribution map at a specific time or frequency:</p> <pre><code>fig = viewer.topomap(time=0.1)           # \u03bcV at 100 ms\nfig = viewer.topomap(freq_band=(8, 12))  # alpha band power\nfig.show()\n</code></pre> <p>Topomap requires electrode positions from <code>electrodes.tsv</code> or a standard 10-20 layout. Qortex falls back to a standard layout if no positions are available.</p>"},{"location":"modalities/eeg/visualization/#epoched-preview","title":"Epoched preview","text":"<p>Event-aligned epochs overlaid per trial type:</p> <pre><code>fig = viewer.epoched(\n    events=\"data/ds004130/sub-01/eeg/sub-01_task-rest_events.tsv\",\n    event_id={\"rest\": 1, \"task\": 2},\n    tmin=-0.2,\n    tmax=0.8,\n)\nfig.show()\n</code></pre>"},{"location":"modalities/eeg/visualization/#eeg-dashboard","title":"EEG dashboard","text":"<p>All panels combined in one local HTML report:</p> <pre><code>viewer.dashboard(\"sub01_eeg_qc.html\")\n</code></pre> <p>Or from CLI:</p> <pre><code>qortex visualize ds004130 --subject 01 \\\n    --suffix eeg --data-dir data/ --output eeg_qc.html\n</code></pre>"},{"location":"modalities/eeg/visualization/#related","title":"Related","text":"<ul> <li>EEG files</li> <li>EEG events</li> <li>EEG viewer reference</li> </ul>"},{"location":"modalities/fnirs/","title":"fNIRS","text":"<p>Functional near-infrared spectroscopy (fNIRS) measures haemodynamic responses by detecting changes in oxygenated (HbO) and deoxygenated (HbR) haemoglobin concentrations via light absorption. fNIRS is non-invasive and increasingly available in OpenNeuro BIDS datasets.</p>"},{"location":"modalities/fnirs/#install","title":"Install","text":"<pre><code>pip install \"qortex[eeg]\"    # MNE-Python reads SNIRF and NIRX files\n</code></pre>"},{"location":"modalities/fnirs/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  nirs/\n    sub-01_task-rest_nirs.snirf\n    sub-01_task-rest_nirs.json\n    sub-01_task-rest_channels.tsv\n    sub-01_task-rest_events.tsv\n    sub-01_optodes.tsv\n    sub-01_coordsystem.json\n</code></pre>"},{"location":"modalities/fnirs/#fnirs-pages","title":"fNIRS pages","text":"<p>fNIRS files \u2014 SNIRF, NIRX formats, HbO/HbR channels, optode metadata, loading.</p>"},{"location":"modalities/fnirs/#search-for-fnirs-datasets","title":"Search for fNIRS datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\nresults = DatasetQuery().modality(\"nirs\").fetch()\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"NIRS\", sort_by=\"downloads\")\n</code></pre>"},{"location":"modalities/fnirs/files/","title":"fNIRS Files","text":"<p>fNIRS data in BIDS uses the <code>.snirf</code> format (Shared Near Infrared spectroscopy Format) as the primary container. Legacy datasets may use NIRX format. Qortex separates HbO and HbR channels automatically.</p>"},{"location":"modalities/fnirs/files/#supported-formats","title":"Supported formats","text":"Extension Format <code>.snirf</code> SNIRF (BIDS required) \u2014 HDF5-based <code>.nirs</code> Homer2 legacy format (NIRX directory)"},{"location":"modalities/fnirs/files/#inspect-before-download","title":"Inspect before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004148\")\nnirs_files = ds.files(datatypes=[\"nirs\"])\nfor f in nirs_files:\n    print(f.subject, f.extension, f.path)\n</code></pre>"},{"location":"modalities/fnirs/files/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/nirs/sub-01_task-rest_nirs.snirf\")\n\ninfo[\"sfreq\"]                  # 7.8125  (Hz \u2014 low sampling rate typical for fNIRS)\ninfo[\"n_channels\"]             # 52  (HbO + HbR channels)\ninfo[\"channel_type_counts\"]    # {\"hbo\": 26, \"hbr\": 26}\ninfo[\"duration_s\"]             # 600.0\ninfo[\"short_channel_count\"]    # 4  (short-separation channels)\ninfo[\"manufacturer\"]           # \"NIRx\"\n</code></pre>"},{"location":"modalities/fnirs/files/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.fnirs import FNIRSLoader\nfrom pathlib import Path\n\nloader = FNIRSLoader()\nrecord = loader.load(file_record, local_path=Path(\"sub-01/nirs/sub-01_task-rest_nirs.snirf\"))\n\nrecord.sfreq         # 7.8125\nrecord.n_channels    # 52\nrecord.duration      # 600.0\nrecord.channel_names # [\"S1_D1 hbo\", \"S1_D1 hbr\", \"S1_D2 hbo\", ...]\nrecord.channel_types # [\"hbo\", \"hbr\", \"hbo\", ...]\n\nrecord.metadata[\"n_hbo_channels\"]  # 26\nrecord.metadata[\"n_hbr_channels\"]  # 26\nrecord.metadata[\"short_channel_count\"]  # 4\n\ndata = loader.to_numpy(record)   # (n_channels, n_times) \u2014 HbO and HbR interleaved\n</code></pre>"},{"location":"modalities/fnirs/files/#hbo-and-hbr-channel-access","title":"HbO and HbR channel access","text":"<pre><code>hbo_idx = record.metadata[\"hbo_picks\"]   # indices into channel list\nhbr_idx = record.metadata[\"hbr_picks\"]\n\ndata_hbo = data[hbo_idx, :]   # (n_source_detector_pairs, n_times)\ndata_hbr = data[hbr_idx, :]\n</code></pre>"},{"location":"modalities/fnirs/files/#optode-metadata","title":"Optode metadata","text":"<pre><code>import polars as pl\noptodes = pl.read_csv(\n    \"data/ds004148/sub-01/nirs/sub-01_optodes.tsv\",\n    separator=\"\\t\",\n)\n# name   type    x       y       z\n# S1     source  -35.2   75.1    22.4\n# D1     detector -30.8  80.3    19.2\n</code></pre>"},{"location":"modalities/fnirs/files/#conversion","title":"Conversion","text":"<p>fNIRS converts as a low-sampling-rate signal. Event-aligned windows are common:</p> <pre><code>art = ds.convert(\n    data_dir=\"data/ds004148/\",\n    output_dir=\"artifacts/\",\n    format=\"parquet\",\n    datatypes=[\"nirs\"],\n    window=dict(mode=\"event_aligned\", tmin=-2.0, tmax=20.0),\n    label_col=\"trial_type\",\n)\n</code></pre> <p>Each sample is a <code>(n_channels, n_time_points)</code> array. Low sampling rate means small arrays even for long windows.</p>"},{"location":"modalities/fnirs/files/#related","title":"Related","text":"<ul> <li>fNIRS overview</li> <li>EEG events \u2014 events work the same for fNIRS</li> </ul>"},{"location":"modalities/ieeg/","title":"iEEG","text":"<p>Intracranial EEG (iEEG) records electrical activity directly from the brain surface or depth using implanted electrodes. It encompasses stereo-EEG (sEEG) depth probes, electrocorticography (ECoG) grids and strips, and local field potentials (LFP) from single-unit probes. iEEG datasets require both signal files and electrode localization metadata.</p>"},{"location":"modalities/ieeg/#install","title":"Install","text":"<pre><code>pip install \"qortex[eeg]\"\n</code></pre>"},{"location":"modalities/ieeg/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  ieeg/\n    sub-01_task-memory_ieeg.edf\n    sub-01_task-memory_ieeg.json\n    sub-01_task-memory_channels.tsv\n    sub-01_task-memory_events.tsv\n    sub-01_electrodes.tsv          # electrode locations\n    sub-01_coordsystem.json        # coordinate system definition\n</code></pre>"},{"location":"modalities/ieeg/#ieeg-pages","title":"iEEG pages","text":"<p>iEEG files \u2014 Signal formats, channel metadata, loading.</p> <p>Electrodes and coordinates \u2014 Electrode tables, coordinate systems, anatomical references.</p>"},{"location":"modalities/ieeg/#search-for-ieeg-datasets","title":"Search for iEEG datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\nresults = DatasetQuery().modality(\"ieeg\").fetch()\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"iEEG\", sort_by=\"downloads\")\n</code></pre>"},{"location":"modalities/ieeg/electrodes-and-coordinates/","title":"Electrodes and Coordinates","text":"<p>Electrode localization is what separates iEEG from scalp EEG for spatial analysis. The <code>electrodes.tsv</code> file maps each electrode label to a 3D coordinate. The <code>coordsystem.json</code> defines the coordinate system and optional anatomical reference image.</p>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#electrodestsv","title":"electrodes.tsv","text":"<pre><code>name   x        y       z       size   group       hemisphere\nLA1    -36.2    -5.8    -18.3   1.5    amygdala    L\nLA2    -40.1    -7.3    -17.1   1.5    amygdala    L\nLB1    -52.4    -14.0   5.7     1.5    hippocampus L\n</code></pre> <p>Required columns: <code>name</code>, <code>x</code>, <code>y</code>, <code>z</code>. Optional but common: <code>size</code> (mm), <code>group</code> (anatomical label), <code>hemisphere</code>.</p>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#coordsystemjson","title":"coordsystem.json","text":"<pre><code>{\n  \"iEEGCoordinateSystem\": \"ACPC\",\n  \"iEEGCoordinateUnits\": \"mm\",\n  \"iEEGCoordinateSystemDescription\": \"AC-PC aligned\",\n  \"iEEGCoordinateProcessingDescription\": \"Electrode contacts localized using post-implant CT co-registered to pre-op MRI\",\n  \"IntendedFor\": \"anat/sub-01_T1w.nii.gz\"\n}\n</code></pre> <p>Common coordinate systems: <code>ACPC</code>, <code>MNI152Lin</code>, <code>Talairach</code>, <code>CapTrak</code>, <code>PIXEL</code>.</p>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#read-before-download","title":"Read before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds003688\")\n\n# Check that electrodes.tsv exists\nmanifest = ds.manifest()\nelec_files = [f for f in manifest.files if \"electrodes.tsv\" in f.path]\nprint(f\"{len(elec_files)} electrode files found\")\n</code></pre>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#read-electrode-positions","title":"Read electrode positions","text":"<pre><code>import polars as pl\n\nelectrodes = pl.read_csv(\n    \"data/ds003688/sub-01/ieeg/sub-01_electrodes.tsv\",\n    separator=\"\\t\",\n)\n\nprint(electrodes.head())\nprint(electrodes[\"group\"].value_counts())  # anatomical distribution\n</code></pre> <p>Or via Qortex metadata:</p> <pre><code>elec_df = ds.metadata_files(\n    subject=\"01\",\n    filename=\"sub-01_electrodes.tsv\",\n)\n</code></pre>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#read-coordinate-system","title":"Read coordinate system","text":"<pre><code>coord = ds.sidecar(\"sub-01/ieeg/sub-01_coordsystem.json\")\nprint(coord[\"iEEGCoordinateSystem\"])    # \"ACPC\"\nprint(coord[\"iEEGCoordinateUnits\"])     # \"mm\"\nprint(coord.get(\"IntendedFor\"))         # anatomical reference path\n</code></pre>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#use-electrode-positions-in-ml","title":"Use electrode positions in ML","text":"<p>Electrode coordinates can serve as spatial features or for defining channel neighborhoods:</p> <pre><code>import polars as pl\nimport numpy as np\n\nelectrodes = pl.read_csv(\"sub-01_electrodes.tsv\", separator=\"\\t\")\ncoords = electrodes.select([\"x\", \"y\", \"z\"]).to_numpy()\nnames  = electrodes[\"name\"].to_list()\n\n# Distance matrix for graph-based methods\nfrom scipy.spatial.distance import cdist\ndist = cdist(coords, coords)\n</code></pre>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#readiness-missing-electrodes","title":"Readiness: missing electrodes","text":"<p>Some iEEG datasets have signal files but no <code>electrodes.tsv</code>. Qortex detects this:</p> <pre><code>report = ds.doctor()\n# WARNING [NO_ELECTRODES]: sub-05 has ieeg/ but no electrodes.tsv\n</code></pre> <p>Signal data can still be downloaded and converted, but spatial analysis is not possible without electrode positions.</p>"},{"location":"modalities/ieeg/electrodes-and-coordinates/#related","title":"Related","text":"<ul> <li>iEEG files</li> <li>Metadata \u2014 read sidecar files from CDN</li> </ul>"},{"location":"modalities/ieeg/files/","title":"iEEG Files","text":"<p>iEEG signal files are structurally similar to EEG but recorded from implanted electrodes at much higher spatial specificity. Qortex reads iEEG files using the same MNE-Python infrastructure as EEG.</p>"},{"location":"modalities/ieeg/files/#supported-formats","title":"Supported formats","text":"Extension Format <code>.edf</code> European Data Format \u2014 most common for clinical iEEG <code>.bdf</code> BioSemi <code>.vhdr</code> + <code>.vmrk</code> + <code>.eeg</code> BrainVision <code>.set</code> EEGLAB <code>.fif</code> MNE native <code>.nwb</code> Neurodata Without Borders"},{"location":"modalities/ieeg/files/#check-before-download","title":"Check before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds003688\")\nieeg_files = ds.files(datatypes=[\"ieeg\"])\nfor f in ieeg_files:\n    print(f.subject, f.extension, f.path)\n</code></pre>"},{"location":"modalities/ieeg/files/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/ieeg/sub-01_task-memory_ieeg.edf\")\n\ninfo[\"sfreq\"]                # 1000.0  (Hz)\ninfo[\"n_channels\"]           # 128\ninfo[\"channel_type_counts\"]  # {\"seeg\": 124, \"eog\": 2, \"misc\": 2}\ninfo[\"n_bad_channels\"]       # 3\ninfo[\"powerline_freq\"]       # 60.0\n</code></pre>"},{"location":"modalities/ieeg/files/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.ieeg import IEEGLoader\nfrom pathlib import Path\n\nloader = IEEGLoader()\nrecord = loader.load(file_record, local_path=Path(\"sub-01/ieeg/sub-01_task-memory_ieeg.edf\"))\n\nrecord.sfreq          # 1000.0\nrecord.n_channels     # 124  (ieeg-type channels only)\nrecord.duration       # 600.0  (seconds)\nrecord.channel_names  # [\"LA1\", \"LA2\", \"LB1\", ...]\nrecord.channel_types  # [\"seeg\", \"seeg\", \"seeg\", ...]\n\ndata = loader.to_numpy(record)   # (n_ieeg_channels, n_times) float64 array\n</code></pre>"},{"location":"modalities/ieeg/files/#channel-types","title":"Channel types","text":"<p>BIDS distinguishes several iEEG channel types in <code>channels.tsv</code>:</p> Type Description <code>SEEG</code> Stereo-EEG depth electrode contact <code>ECOG</code> Electrocorticography grid or strip <code>LFP</code> Local field potential <code>DBS</code> Deep brain stimulation recording <p>These are exposed as <code>channel_type_counts</code> in the inspect output.</p>"},{"location":"modalities/ieeg/files/#related","title":"Related","text":"<ul> <li>Electrodes and coordinates</li> <li>EEG visualization \u2014 butterfly, PSD, epoched views work for iEEG too</li> </ul>"},{"location":"modalities/meg/","title":"MEG","text":"<p>Magnetoencephalography (MEG) measures tiny magnetic fields produced by neuronal currents using arrays of SQUID sensors. MEG datasets on OpenNeuro are large raw signal files paired with channel metadata, head-position files, and event tables.</p>"},{"location":"modalities/meg/#install","title":"Install","text":"<pre><code>pip install \"qortex[eeg]\"    # MNE-Python is used for all MEG and EEG reading\n</code></pre>"},{"location":"modalities/meg/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  meg/\n    sub-01_task-rest_meg.fif\n    sub-01_task-rest_meg.json\n    sub-01_task-rest_channels.tsv\n    sub-01_task-rest_events.tsv\n    sub-01_coordsystem.json\n    sub-01_headshape.pos\n</code></pre>"},{"location":"modalities/meg/#supported-formats","title":"Supported formats","text":"Format Extension System MNE / Elekta <code>.fif</code> Neuromag (now MEGIN) TRIUX, VectorView CTF <code>.ds</code> (directory) CTF MEG KIT / Yokogawa <code>.sqd</code>, <code>.con</code> KIT system BTi / 4D <code>.4d</code> 4D Neuroimaging"},{"location":"modalities/meg/#search-for-meg-datasets","title":"Search for MEG datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\nresults = DatasetQuery().modality(\"meg\").fetch()\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"MEG\", sort_by=\"downloads\", limit=10)\n</code></pre>"},{"location":"modalities/meg/#meg-pages","title":"MEG pages","text":"<p>MEG files \u2014 File formats, channel types, SSS detection, loading.</p> <p>MEG events \u2014 Events.tsv, stim channel events, trial types, label readiness.</p>"},{"location":"modalities/meg/events/","title":"MEG Events","text":"<p>MEG events define the timing of stimuli and responses within a recording. In BIDS, events are stored in <code>events.tsv</code>. Some older MEG datasets store events only in the stimulus trigger channel inside the raw file.</p>"},{"location":"modalities/meg/events/#bids-eventstsv","title":"BIDS events.tsv","text":"<pre><code>onset   duration  trial_type     stim_file\n0.300   0.0       face/famous    stimuli/f001.bmp\n0.700   0.0       face/scrambled stimuli/s001.bmp\n1.100   0.0       face/unfamiliar stimuli/u001.bmp\n</code></pre> <ul> <li><code>onset</code> \u2014 time relative to recording start (seconds)</li> <li><code>duration</code> \u2014 event duration (0 for instantaneous stimuli)</li> <li><code>trial_type</code> \u2014 label for ML; may use slash notation for hierarchical conditions</li> </ul>"},{"location":"modalities/meg/events/#check-event-coverage","title":"Check event coverage","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds000117\")\nreport = ds.doctor()\n# Events: yes \u2014 102 files, columns: onset duration trial_type stim_file\n\n# Per-subject detail\nprint(ds.label_landscape(target_col=\"trial_type\"))\n</code></pre>"},{"location":"modalities/meg/events/#read-events-for-a-specific-file","title":"Read events for a specific file","text":"<pre><code>events = ds.events(subject=\"01\", task=\"facerecognition\")\n# Polars or pandas DataFrame: onset, duration, trial_type, ...\nprint(events[\"trial_type\"].value_counts())\n</code></pre>"},{"location":"modalities/meg/events/#stim-channel-events-no-eventstsv","title":"Stim-channel events (no events.tsv)","text":"<p>Some legacy datasets store events only in the trigger channel. MNE-Python can extract them:</p> <pre><code>import mne\nraw = mne.io.read_raw_fif(\"sub-01/meg/sub-01_task-rest_meg.fif\", preload=False)\nevents = mne.find_events(raw, stim_channel=\"STI014\")\n# events: array of [sample, prev_value, event_id]\n</code></pre> <p>Qortex's readiness checks flag datasets that have a stim channel but no <code>events.tsv</code> \u2014 these require manual event extraction before supervised conversion.</p>"},{"location":"modalities/meg/events/#event-aligned-windows-for-ml","title":"Event-aligned windows for ML","text":"<pre><code>art = ds.convert(\n    data_dir=\"data/ds000117/\",\n    output_dir=\"artifacts/\",\n    format=\"parquet\",\n    window=dict(mode=\"event_aligned\", tmin=-0.2, tmax=0.8),\n    label_col=\"trial_type\",\n    datatypes=[\"meg\"],\n)\n</code></pre> <p>Each sample is a <code>(n_channels, n_time_points)</code> array. The time axis spans <code>tmin</code> to <code>tmax</code> relative to each event onset.</p>"},{"location":"modalities/meg/events/#readiness-considerations","title":"Readiness considerations","text":"<ul> <li><code>trial_type</code> with slash notation (<code>face/famous</code>) \u2014 Qortex treats the full string as the label class. Flatten if needed: <code>\"famous\"</code> instead of <code>\"face/famous\"</code>.</li> <li>Response columns (<code>response</code>, <code>response_time</code>) can serve as regression targets.</li> <li>Recordings with stim-channel-only events need manual preprocessing outside Qortex before supervised conversion.</li> </ul>"},{"location":"modalities/meg/events/#related","title":"Related","text":"<ul> <li>MEG files</li> <li>Label readiness</li> <li>Event-aligned windows</li> </ul>"},{"location":"modalities/meg/files/","title":"MEG Files","text":"<p>MEG raw files are large (often 1\u201310 GB per run) and format-specific. Qortex uses MNE-Python to read all common MEG formats and selects gradiometer and magnetometer channels automatically.</p>"},{"location":"modalities/meg/files/#inspect-before-download","title":"Inspect before download","text":"<p>The manifest tells you file sizes and paths before downloading. For a typical 6-minute resting MEG run in <code>.fif</code> format, expect 500 MB\u20132 GB.</p> <pre><code>from qortex import Dataset\n\nds = Dataset(\"ds000117\")\nmeg_files = ds.files(datatypes=[\"meg\"])\nfor f in meg_files:\n    print(f.subject, f.path, f\"{f.size / 1e9:.1f} GB\" if f.size else \"size unknown\")\n</code></pre>"},{"location":"modalities/meg/files/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/meg/sub-01_task-facerecognition_meg.fif\")\n# Note: nifti_info works on MEG files too via the modality-specific inspector\n\ninfo[\"sfreq\"]                 # 1100.0 Hz\ninfo[\"n_channels\"]            # 306\ninfo[\"channel_type_counts\"]   # {\"grad\": 204, \"mag\": 102, \"stim\": 1}\ninfo[\"meg_channel_count\"]     # 306\ninfo[\"sss_applied\"]           # True  \u2014 Maxwell Spatial Suppression detected\ninfo[\"n_bad_channels\"]        # 2\ninfo[\"powerline_freq\"]        # 50  (Europe) or 60 (North America)\n</code></pre>"},{"location":"modalities/meg/files/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.meg import MEGLoader\nfrom pathlib import Path\n\nloader = MEGLoader()\nrecord = loader.load(file_record, local_path=Path(\"sub-01/meg/sub-01_task-rest_meg.fif\"))\n\nrecord.sfreq          # 1100.0\nrecord.n_channels     # 306 (after excluding bad channels and ref channels)\nrecord.duration       # 360.0  (seconds)\nrecord.channel_names  # [\"MEG0111\", \"MEG0112\", ...]\nrecord.channel_types  # [\"grad\", \"grad\", \"mag\", ...]\n\nrecord.metadata[\"sss_applied\"]    # True\nrecord.metadata[\"bad_channels\"]   # [\"MEG0141\"]\nrecord.metadata[\"powerline_freq\"] # 50.0\n\ndata = loader.to_numpy(record)    # (n_channels, n_times) float64 array\n</code></pre> <p>Qortex selects magnetometers and gradiometers (<code>meg=True</code>) and excludes bad channels and reference channels automatically. Pass a <code>SignalRecord</code> directly to the <code>TimeSeriesViewer</code> for visualization.</p>"},{"location":"modalities/meg/files/#maxwell-spatial-suppression-sssmaxfilter","title":"Maxwell Spatial Suppression (SSS/MaxFilter)","text":"<p>SSS removes environmental noise from Elekta/MEGIN systems. When SSS has been applied, the signal has already been projected and the effective rank is reduced (typically to 64 or 80). Qortex detects SSS from the processing history in the <code>.fif</code> file header and reports it via <code>sss_applied</code>.</p> <p>For ML: after SSS, the number of meaningful components is the SSS basis rank, not the raw channel count. Expect to apply PCA or ICA before windowed conversion.</p>"},{"location":"modalities/meg/files/#lazy-loading","title":"Lazy loading","text":"<p>Large MEG files should be loaded lazily to avoid memory exhaustion:</p> <pre><code>record = loader.lazy_load(file_record, local_path=...)\n# raw MNE object backed by disk \u2014 data read on first access\n</code></pre>"},{"location":"modalities/meg/files/#related","title":"Related","text":"<ul> <li>MEG events</li> <li>EEG viewer \u2014 visualization also works for MEG</li> <li>DWI QC</li> </ul>"},{"location":"modalities/mri/","title":"MRI","text":"<p>MRI is the largest modality family on OpenNeuro. Qortex treats four MRI workflows as related but distinct: structural T1w/T2w, functional BOLD, diffusion DWI, and fieldmaps. Each has its own loader, BIDS suffix set, companion requirements, and conversion path.</p>"},{"location":"modalities/mri/#install","title":"Install","text":"<pre><code>pip install \"qortex[mri]\"\n</code></pre>"},{"location":"modalities/mri/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  anat/\n    sub-01_T1w.nii.gz\n    sub-01_T1w.json\n    sub-01_T2w.nii.gz\n  func/\n    sub-01_task-rest_bold.nii.gz\n    sub-01_task-rest_bold.json\n    sub-01_task-rest_events.tsv\n  dwi/\n    sub-01_dwi.nii.gz\n    sub-01_dwi.bval\n    sub-01_dwi.bvec\n    sub-01_dwi.json\n  fmap/\n    sub-01_magnitude1.nii.gz\n    sub-01_phasediff.nii.gz\n    sub-01_phasediff.json\n</code></pre>"},{"location":"modalities/mri/#searching-for-mri-datasets","title":"Searching for MRI datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\n# Local catalog\nresults = DatasetQuery().modality(\"mri\").min_subjects(30).fetch()\n\n# Live \u2014 sorted by downloads\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"MRI\", sort_by=\"downloads\", limit=20)\n</code></pre> <p>CLI:</p> <pre><code>qortex search --modality mri --min-subjects 30\nqortex search --modality mri --task rest --has-events\n</code></pre>"},{"location":"modalities/mri/#mri-pages","title":"MRI pages","text":"<p>Structural MRI \u2014 T1w, T2w, FLAIR: inspection, loading, visualization.</p> <p>fMRI / BOLD \u2014 BOLD timeseries: TR, events, QC panels, windowed conversion.</p> <p>DWI / Diffusion \u2014 Gradient table, b-values, shells, b0/high-b previews.</p> <p>Fieldmaps \u2014 Magnitude, phasediff, EPI-fieldmap: intended-for relationships.</p>"},{"location":"modalities/mri/dwi-diffusion/","title":"DWI / Diffusion","text":"<p>Diffusion-weighted MRI (DWI) measures water diffusivity along gradient directions. Each DWI file is a 4D NIfTI with one volume per gradient direction, plus two required companion files: <code>.bval</code> (b-values per volume) and <code>.bvec</code> (gradient unit vectors per volume).</p>"},{"location":"modalities/mri/dwi-diffusion/#bids-files","title":"BIDS files","text":"File Required Description <code>*_dwi.nii.gz</code> yes 4D DWI volume <code>*_dwi.bval</code> yes b-values per volume (whitespace-separated) <code>*_dwi.bvec</code> yes Gradient vectors, 3 \u00d7 n_volumes matrix <code>*_dwi.json</code> yes Scanner metadata sidecar <p>Qortex downloads all three companions automatically when you download a DWI NIfTI.</p>"},{"location":"modalities/mri/dwi-diffusion/#check-before-download","title":"Check before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds000001\")\nreport = ds.doctor()\n# NO_COMPANIONS error appears if bval/bvec are missing for any DWI file\n\n# Inspect companion coverage\nmanifest = ds.manifest()\ndwi_files = ds.files(datatypes=[\"dwi\"], suffixes=[\"dwi\"])\nfor f in dwi_files:\n    print(f.subject, f.path)\n</code></pre>"},{"location":"modalities/mri/dwi-diffusion/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/dwi/sub-01_dwi.nii.gz\")\n\ninfo[\"shape\"]                   # [96, 96, 60, 65]\ninfo[\"n_directions\"]            # 65\ninfo[\"voxel_size_mm\"]           # [2.0, 2.0, 2.0]\ninfo[\"b_values\"]                # [0, 1000, 2000]  (unique shells, rounded)\ninfo[\"n_b0_volumes\"]            # 7  (b &lt; 50 s/mm\u00b2)\ninfo[\"bval_available\"]          # True\ninfo[\"bvec_available\"]          # True\ninfo[\"magnetic_field_strength\"] # 3.0\ninfo[\"echo_time\"]               # 0.085\ninfo[\"total_readout_time\"]      # 0.0684\n</code></pre>"},{"location":"modalities/mri/dwi-diffusion/#visualize","title":"Visualize","text":"<pre><code>from qortex.visualize import dwi_summary\n\nfig = dwi_summary(\n    \"data/ds000001/sub-01/dwi/sub-01_dwi.nii.gz\",\n    bval=\"data/ds000001/sub-01/dwi/sub-01_dwi.bval\",\n    bvec=\"data/ds000001/sub-01/dwi/sub-01_dwi.bvec\",\n)\nfig.show()\n</code></pre> <p>The 4-panel summary includes: b0 reference slice, highest-b-value slice, b-value histogram, and gradient direction sphere.</p> <p>CLI:</p> <pre><code>qortex dwi-qc sub-01/dwi/sub-01_dwi.nii.gz \\\n    --bval sub-01/dwi/sub-01_dwi.bval \\\n    --bvec sub-01/dwi/sub-01_dwi.bvec \\\n    --output sub01_dwi_qc.html\n</code></pre>"},{"location":"modalities/mri/dwi-diffusion/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.dwi import DWILoader\n\nloader = DWILoader()\nrecord = loader.load(file_record, local_path=Path(\"sub-01/dwi/sub-01_dwi.nii.gz\"))\n\nrecord.img          # NiBabel image (4D: x, y, z, directions)\nrecord.shape        # (96, 96, 60, 65)\nrecord.metadata[\"b_values\"]       # list of floats per volume\nrecord.metadata[\"bvecs\"]          # (3, n_dirs) numpy array\nrecord.metadata[\"n_shells\"]       # number of unique b-value shells\nrecord.metadata[\"b0_indices\"]     # indices of b0 volumes\n\ndata = loader.to_numpy(record)    # float32 array, shape (96, 96, 60, 65)\n</code></pre>"},{"location":"modalities/mri/dwi-diffusion/#conversion","title":"Conversion","text":"<p>DWI typically converts to whole-volume samples. One sample per subject per acquisition.</p> <pre><code>art = ds.convert(\n    data_dir=\"data/ds000001/\",\n    output_dir=\"artifacts/dwi/\",\n    format=\"zarr\",\n    datatypes=[\"dwi\"],\n    split=dict(strategy=\"subject\"),\n)\n</code></pre> <p>Each artifact sample is a <code>(x, y, z, n_directions)</code> float32 array plus b-value metadata.</p>"},{"location":"modalities/mri/dwi-diffusion/#readiness-considerations","title":"Readiness considerations","text":"<ul> <li>bval and bvec are required. Conversion fails without them. Qortex includes them automatically in every download plan.</li> <li>Shell count matters. Multi-shell acquisitions (b=0, 1000, 2000) enable more diffusion models than single-shell.</li> <li>Minimum directions. Most tractography algorithms need \u2265 30 unique gradient directions. Warn if fewer.</li> <li>No events. DWI does not use events.tsv. Labels for ML typically come from participants.tsv (diagnosis, group).</li> </ul>"},{"location":"modalities/mri/dwi-diffusion/#related","title":"Related","text":"<ul> <li>DWI QC \u2014 QC viewer reference</li> <li>Readiness recipes \u2014 <code>dwi</code> recipe</li> </ul>"},{"location":"modalities/mri/fieldmaps/","title":"Fieldmaps","text":"<p>Fieldmaps estimate the magnetic field inhomogeneity in a scanner and enable geometric distortion correction (SDC) for fMRI and DWI acquisitions. BIDS supports several fieldmap types, each with different file layouts.</p>"},{"location":"modalities/mri/fieldmaps/#bids-fieldmap-types","title":"BIDS fieldmap types","text":"Type Files Description Phase-difference <code>magnitude1.nii.gz</code>, <code>magnitude2.nii.gz</code>, <code>phasediff.nii.gz</code> + JSON Two echo magnitudes + phase difference Two phase images <code>magnitude1.nii.gz</code>, <code>magnitude2.nii.gz</code>, <code>phase1.nii.gz</code>, <code>phase2.nii.gz</code> Separate phase images per echo Direct fieldmap <code>fieldmap.nii.gz</code> + JSON Hz/rad/s map already computed EPI fieldmap <code>epi.nii.gz</code> Blip-up/blip-down EPI pair <p>All types require a JSON sidecar. The JSON must include <code>IntendedFor</code>, pointing to the functional run(s) the fieldmap is meant to correct.</p>"},{"location":"modalities/mri/fieldmaps/#check-fieldmap-availability","title":"Check fieldmap availability","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nfmap_files = ds.files(datatypes=[\"fmap\"])\nfor f in fmap_files:\n    print(f.suffix, f.path, f.size)\n</code></pre>"},{"location":"modalities/mri/fieldmaps/#inspect-a-fieldmap-sidecar","title":"Inspect a fieldmap sidecar","text":"<pre><code>sidecar = ds.sidecar(\"sub-01/fmap/sub-01_phasediff.json\")\n\nsidecar[\"EchoTime1\"]    # 0.00492\nsidecar[\"EchoTime2\"]    # 0.00738\nsidecar[\"IntendedFor\"]  # [\"func/sub-01_task-rest_bold.nii.gz\"]\nsidecar[\"Units\"]        # \"rad/s\"\n</code></pre>"},{"location":"modalities/mri/fieldmaps/#distortion-correction-readiness","title":"Distortion correction readiness","text":"<p>Qortex checks whether functional runs have a matching fieldmap via the <code>IntendedFor</code> field:</p> <pre><code>report = ds.doctor()\n# finding: WARNING [SDC_MISSING] \u2014 sub-03 has BOLD but no matched fieldmap\n</code></pre>"},{"location":"modalities/mri/fieldmaps/#download-fieldmaps-with-functional-data","title":"Download fieldmaps with functional data","text":"<p>Fieldmaps are included automatically when you download BOLD files:</p> <pre><code>ds.download(\n    subjects=[\"01\", \"02\"],\n    datatypes=[\"func\", \"fmap\"],\n    data_dir=\"data/ds004130/\",\n)\n</code></pre>"},{"location":"modalities/mri/fieldmaps/#limitations","title":"Limitations","text":"<p>Qortex does not perform fieldmap-based distortion correction itself. Fieldmap files are read, sidecar metadata is inspected, and <code>IntendedFor</code> relationships are resolved. Actual SDC requires fMRIPrep or a dedicated preprocessing tool.</p>"},{"location":"modalities/mri/fieldmaps/#related","title":"Related","text":"<ul> <li>Selective download \u2014 include fieldmaps in a plan</li> <li>Metadata \u2014 read sidecars without downloading volumes</li> </ul>"},{"location":"modalities/mri/fmri-bold/","title":"fMRI / BOLD","text":"<p>fMRI BOLD (Blood Oxygen Level Dependent) is the dominant functional imaging modality on OpenNeuro. Each BOLD file is a 4D NIfTI: spatial dimensions plus time. The TR (repetition time) determines the temporal resolution. Events.tsv files define task labels aligned to acquisition time.</p>"},{"location":"modalities/mri/fmri-bold/#bids-files","title":"BIDS files","text":"Suffix Description <code>bold</code> Primary BOLD timeseries <code>sbref</code> Single-band reference volume (EPI geometry) <code>events</code> <code>events.tsv</code> \u2014 onset, duration, trial_type <code>physio</code> Physiological recordings (respiration, cardiac) <p>Companions: - <code>*_bold.json</code> \u2014 TR, task name, slice timing, field strength - <code>*_events.tsv</code> \u2014 required for supervised learning - <code>*_desc-confounds_timeseries.tsv</code> \u2014 motion and nuisance regressors (from fMRIPrep)</p>"},{"location":"modalities/mri/fmri-bold/#inspect-before-download","title":"Inspect before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n\n# Check BOLD file count and event coverage\nreport = ds.doctor()\nprint(report.to_text())\n# Events: yes \u2014 88 files, columns: onset duration trial_type\n# Size: 4.2 GB\n\n# Full event coverage check\nprint(ds.label_landscape(target_col=\"trial_type\"))\n</code></pre>"},{"location":"modalities/mri/fmri-bold/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/func/sub-01_task-rest_bold.nii.gz\")\n\ninfo[\"shape\"]                    # [64, 64, 32, 240]\ninfo[\"n_volumes\"]                # 240\ninfo[\"tr_s\"]                     # 2.0  (seconds)\ninfo[\"voxel_size_mm\"]            # [3.0, 3.0, 3.5]\ninfo[\"total_acquisition_time_s\"] # 480.0\ninfo[\"task_name\"]                # \"rest\"\ninfo[\"slice_timing_available\"]   # True\ninfo[\"magnetic_field_strength\"]  # 3.0\n</code></pre>"},{"location":"modalities/mri/fmri-bold/#visualize-qc","title":"Visualize (QC)","text":"<pre><code>from qortex.visualize import fmri_summary\n\nfig = fmri_summary(\n    \"data/ds004130/sub-01/func/sub-01_task-rest_bold.nii.gz\",\n    events=\"data/ds004130/sub-01/func/sub-01_task-rest_events.tsv\",\n)\nfig.show()\n</code></pre> <p>The 6-panel QC figure includes: mean EPI, standard deviation map, tSNR, global signal timeseries, framewise displacement, and a trial-type overlay on the global signal.</p> <p>CLI:</p> <pre><code>qortex fmri-qc sub-01/func/sub-01_task-rest_bold.nii.gz \\\n    --events sub-01/func/sub-01_task-rest_events.tsv \\\n    --output sub01_bold_qc.html\n</code></pre>"},{"location":"modalities/mri/fmri-bold/#events-and-labels","title":"Events and labels","text":"<p>Events.tsv for BOLD defines the label for each trial window:</p> <pre><code>onset   duration  trial_type\n0.0     20.0      rest\n20.0    20.0      task\n40.0    20.0      rest\n</code></pre> <p>Qortex reads events automatically when converting event-aligned windows:</p> <pre><code>art = ds.convert(\n    data_dir=\"data/ds004130/\",\n    output_dir=\"artifacts/\",\n    format=\"parquet\",\n    window=dict(mode=\"event_aligned\", tmin=-2.0, tmax=18.0),\n    label_col=\"trial_type\",\n)\n</code></pre>"},{"location":"modalities/mri/fmri-bold/#conversion-sliding-vs-event-aligned-windows","title":"Conversion \u2014 sliding vs event-aligned windows","text":"Mode Description <code>sliding</code> Fixed-length windows stepped across the full timeseries <code>event_aligned</code> Windows anchored to event onsets in events.tsv <pre><code># Sliding window \u2014 30 s, 50% overlap\nart = ds.convert(\n    format=\"parquet\",\n    window=dict(duration_s=30.0, overlap=0.5),\n    label_col=\"trial_type\",\n)\n\n# Event-aligned \u2014 2 s before to 18 s after onset\nart = ds.convert(\n    format=\"parquet\",\n    window=dict(mode=\"event_aligned\", tmin=-2.0, tmax=18.0),\n    label_col=\"trial_type\",\n)\n</code></pre> <p>Each sample in the artifact is a <code>(n_voxels, n_time_points)</code> array or a flattened feature vector depending on the output format.</p>"},{"location":"modalities/mri/fmri-bold/#readiness-considerations","title":"Readiness considerations","text":"<ul> <li>Events.tsv is required for supervised classification. Use <code>ds.can_train()</code> to verify.</li> <li>TR must be in JSON sidecar. If the sidecar is missing, TR falls back to the NIfTI pixdim[4] which is unreliable.</li> <li>Confound files. fMRIPrep confound TSVs are in <code>derivatives/</code>. They are excluded by default. Set <code>include_derivatives=True</code> to include them.</li> <li>Motion. High motion subjects are not automatically flagged. Use framewise displacement from the confound file.</li> </ul>"},{"location":"modalities/mri/fmri-bold/#related","title":"Related","text":"<ul> <li>Readiness recipes \u2014 <code>fmri-classification</code> recipe</li> <li>fMRI QC \u2014 full QC reference</li> <li>Windows \u2014 sliding vs event-aligned window details</li> </ul>"},{"location":"modalities/mri/structural-mri/","title":"Structural MRI","text":"<p>Structural MRI \u2014 primarily T1w and T2w \u2014 provides the anatomical reference for most multi-modal neuroimaging studies. Qortex can inspect, load, visualize, and use structural MRI for overlay rendering or as a covariate in ML pipelines.</p>"},{"location":"modalities/mri/structural-mri/#bids-files","title":"BIDS files","text":"Suffix Description <code>T1w</code> T1-weighted (e.g. MPRAGE) \u2014 most common anatomical reference <code>T2w</code> T2-weighted \u2014 grey/white contrast complementary to T1w <code>FLAIR</code> Fluid-attenuated inversion recovery <code>PD</code> Proton density <code>T1map</code>, <code>T2map</code> Quantitative relaxometry maps <code>mask</code> Brain masks (in <code>anat/</code>) <code>dseg</code> Discrete segmentation maps <code>probseg</code> Probabilistic tissue maps <p>File extension: <code>.nii.gz</code> or <code>.nii</code>. JSON sidecar carries scanner metadata.</p>"},{"location":"modalities/mri/structural-mri/#inspect-before-download","title":"Inspect before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nmanifest = ds.manifest()\n\n# All T1w files across all subjects\nt1w_files = ds.files(datatypes=[\"anat\"], suffixes=[\"T1w\"])\nfor f in t1w_files:\n    print(f.subject, f.size, f.path)\n</code></pre>"},{"location":"modalities/mri/structural-mri/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/anat/sub-01_T1w.nii.gz\")\n\ninfo[\"shape\"]                    # [256, 256, 176]\ninfo[\"voxel_size_mm\"]            # [1.0, 1.0, 1.0]\ninfo[\"orientation\"]              # \"RAS\"\ninfo[\"magnetic_field_strength\"]  # 3.0\ninfo[\"manufacturer\"]             # \"Siemens\"\ninfo[\"echo_time\"]                # 0.00296\ninfo[\"repetition_time\"]          # 2.3\n</code></pre> <p>All values come from the NIfTI header and JSON sidecar \u2014 the NIfTI data array is not loaded.</p>"},{"location":"modalities/mri/structural-mri/#visualize","title":"Visualize","text":"<pre><code>from qortex.visualize import VolumeViewer\n\nviewer = VolumeViewer(\"data/ds004130/sub-01/anat/sub-01_T1w.nii.gz\")\nviewer.ortho()           # three-plane orthogonal view\nviewer.lightbox()        # grid of axial slices\nviewer.interactive_html(\"sub01_T1w.html\")\n</code></pre> <p>CLI:</p> <pre><code>qortex visualize ds004130 --subject 01 --suffix T1w --data-dir data/ds004130/\n</code></pre>"},{"location":"modalities/mri/structural-mri/#load-into-python","title":"Load into Python","text":"<pre><code>from qortex.parse.mri import MRILoader\nfrom qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nfiles = ds.files(suffixes=[\"T1w\"])\n\nloader = MRILoader()\nrecord = loader.load(files[0], local_path=...)\n\nrecord.img         # NiBabel NIfTI1Image \u2014 data not yet loaded\nrecord.shape       # (256, 256, 176)\nrecord.voxel_size  # (1.0, 1.0, 1.0)\nrecord.affine      # 4\u00d74 numpy array (RAS)\n\ndata = loader.to_numpy(record)  # numpy float32 array, shape (256, 256, 176)\n</code></pre> <p><code>lazy_load()</code> returns the same <code>ImageRecord</code> but defers data loading until the array is accessed:</p> <pre><code>record = loader.lazy_load(files[0], local_path=...)\n# no data in memory yet\ndata = loader.to_numpy(record)  # reads on first access\n</code></pre>"},{"location":"modalities/mri/structural-mri/#conversion","title":"Conversion","text":"<p>Structural MRI converts to 3D array slices. Each subject contributes one sample per T1w file.</p> <pre><code>art = ds.convert(\n    data_dir=\"data/ds004130/\",\n    output_dir=\"artifacts/\",\n    format=\"zarr\",\n    suffixes=[\"T1w\"],\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n)\n</code></pre> <p>Each sample is a <code>(256, 256, 176)</code> float32 array in the artifact. There are no windows or events for structural MRI \u2014 one file equals one sample.</p>"},{"location":"modalities/mri/structural-mri/#readiness-considerations","title":"Readiness considerations","text":"<ul> <li>No events required. Structural MRI does not need events.tsv for conversion.</li> <li>Label source. Labels typically come from <code>participants.tsv</code> (age, sex, diagnosis, group). These are covariate labels, not trial-type labels.</li> <li>Size. A T1w NIfTI at 1 mm isotropic is typically 30\u201380 MB compressed. Plan storage accordingly.</li> <li>Orientation. Qortex reorients all volumes to RAS canonical by default via nibabel. Pass <code>canonical=False</code> to skip.</li> </ul>"},{"location":"modalities/mri/structural-mri/#related","title":"Related","text":"<ul> <li>fMRI / BOLD \u2014 functional volumes using structural as reference</li> <li>Overlays \u2014 overlay masks or stat maps on T1w</li> <li>Visual audit \u2014 T1w coverage matrix</li> </ul>"},{"location":"modalities/pet/","title":"PET","text":"<p>Positron emission tomography (PET) measures radiotracer concentration in tissue over time. PET datasets on OpenNeuro are quantitative: files contain radioactivity values, and the JSON sidecar carries all the physics metadata needed to interpret them (tracer name, injected activity, body weight, frame timing).</p>"},{"location":"modalities/pet/#install","title":"Install","text":"<pre><code>pip install \"qortex[mri]\"    # nibabel is sufficient for PET NIfTI loading\n</code></pre>"},{"location":"modalities/pet/#bids-structure","title":"BIDS structure","text":"<pre><code>sub-01/\n  pet/\n    sub-01_trc-18FFDG_pet.nii.gz    # radiotracer image\n    sub-01_trc-18FFDG_pet.json      # required sidecar\n    sub-01_trc-18FFDG_blood.tsv     # optional blood data\n    sub-01_trc-18FFDG_blood.json\n</code></pre> <p>The <code>trc-</code> entity identifies the tracer. PET files always use the <code>pet</code> datatype and <code>pet</code> suffix.</p>"},{"location":"modalities/pet/#search-for-pet-datasets","title":"Search for PET datasets","text":"<pre><code>from qortex.catalog import DatasetQuery\nfrom qortex.client import OpenNeuroClient\n\nresults = DatasetQuery().modality(\"pet\").fetch()\n\nwith OpenNeuroClient() as client:\n    results = client.search_datasets_rich(modality=\"PET\", sort_by=\"downloads\")\n</code></pre>"},{"location":"modalities/pet/#pet-pages","title":"PET pages","text":"<p>PET metadata \u2014 Tracer name, injected activity, frame times, SUV normalization, BIDS sidecar fields.</p> <p>PET visualization \u2014 Volume viewing, colormaps, overlay on structural MRI.</p>"},{"location":"modalities/pet/metadata/","title":"PET Metadata","text":"<p>PET images are quantitative. Raw scanner values are in units of radioactivity (Bq/mL). To interpret them for biology or for ML, you need the metadata from the JSON sidecar.</p>"},{"location":"modalities/pet/metadata/#required-sidecar-fields-bids-pet","title":"Required sidecar fields (BIDS PET)","text":"Field Description <code>TracerName</code> Radiotracer identifier (e.g. <code>18F-FDG</code>) <code>TracerRadionuclide</code> Radionuclide (e.g. <code>F18</code>) <code>InjectedRadioactivity</code> Activity at time of injection (MBq) <code>InjectedRadioactivityUnits</code> Usually <code>\"MBq\"</code> <code>InjectionStart</code> Time of injection relative to scan start (s) <code>FrameTimesStart</code> Start time of each frame (s) \u2014 list <code>FrameDuration</code> Duration of each frame (s) \u2014 list or scalar <code>BodyWeight</code> Subject body weight (kg) \u2014 required for SUV <code>ReconMethodName</code> Reconstruction algorithm <code>DecayCorrectionFactor</code> Applied decay correction"},{"location":"modalities/pet/metadata/#read-pet-metadata-before-download","title":"Read PET metadata before download","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds001421\")\nsidecar = ds.sidecar(\"sub-01/pet/sub-01_trc-18FFDG_pet.json\")\n\nprint(sidecar[\"TracerName\"])              # \"18F-FDG\"\nprint(sidecar[\"InjectedRadioactivity\"])   # 185.0\nprint(sidecar[\"BodyWeight\"])              # 72.4\nprint(sidecar[\"FrameTimesStart\"])         # [0, 15, 30, 60, 90, ...]\nprint(sidecar[\"FrameDuration\"])           # [15, 15, 30, 30, 60, ...]\n</code></pre>"},{"location":"modalities/pet/metadata/#inspect-after-download","title":"Inspect after download","text":"<pre><code>info = ds.nifti_info(\"sub-01/pet/sub-01_trc-18FFDG_pet.nii.gz\")\n\ninfo[\"shape\"]                    # [128, 128, 63, 23]\ninfo[\"n_frames\"]                 # 23\ninfo[\"voxel_size_mm\"]            # [2.09, 2.09, 2.43]\ninfo[\"total_scan_time_s\"]        # 3600.0\ninfo[\"tracer_name\"]              # \"18F-FDG\"\ninfo[\"tracer_radionuclide\"]      # \"F18\"\ninfo[\"body_weight_kg\"]           # 72.4\ninfo[\"injected_activity_bq\"]     # 185000000.0\ninfo[\"suv_normalization_possible\"]  # True \u2014 both weight and activity available\ninfo[\"reconstruction_method\"]    # \"3D-OSEM\"\n</code></pre>"},{"location":"modalities/pet/metadata/#suv-normalization","title":"SUV normalization","text":"<p>Standardized Uptake Value (SUV) normalizes raw PET values by injected activity and body weight:</p> <pre><code>SUV = (PET_value [Bq/mL] \u00d7 body_weight [kg]) / (injected_activity [Bq])\n</code></pre> <p>Qortex checks whether SUV normalization is possible by verifying both <code>BodyWeight</code> and <code>InjectedRadioactivity</code> are present in the sidecar. When loading PET data for ML, apply SUV normalization before training unless your model explicitly handles raw radioactivity values.</p>"},{"location":"modalities/pet/metadata/#blood-data","title":"Blood data","text":"<p>Some PET datasets include blood time-activity curves in <code>*_blood.tsv</code>. These are not loaded by default. Access via:</p> <pre><code>blood_files = ds.files(datatypes=[\"pet\"], suffixes=[\"blood\"])\n</code></pre>"},{"location":"modalities/pet/metadata/#related","title":"Related","text":"<ul> <li>PET visualization</li> <li>Metadata \u2014 read sidecars from CDN without downloading volumes</li> </ul>"},{"location":"modalities/pet/visualization/","title":"PET Visualization","text":"<p>PET volumes can be viewed as standalone quantitative images or overlaid on a structural MRI when co-registered anatomy is available.</p>"},{"location":"modalities/pet/visualization/#install","title":"Install","text":"<pre><code>pip install \"qortex[mri,visual]\"\n</code></pre>"},{"location":"modalities/pet/visualization/#view-a-pet-volume","title":"View a PET volume","text":"<pre><code>from qortex.visualize import VolumeViewer\n\nviewer = VolumeViewer(\"data/ds001421/sub-01/pet/sub-01_trc-18FFDG_pet.nii.gz\")\nviewer.ortho()      # orthogonal view with PET colormap\nviewer.lightbox()   # axial slice grid\n</code></pre> <p>PET volumes are rendered with the <code>hot</code> colormap by default (black \u2192 red \u2192 yellow \u2192 white). Change colormap:</p> <pre><code>viewer.ortho(colormap=\"plasma\")\n</code></pre>"},{"location":"modalities/pet/visualization/#overlay-pet-on-structural-mri","title":"Overlay PET on structural MRI","text":"<p>When a T1w and a PET are co-registered (same space), overlay them:</p> <pre><code>from qortex.visualize import overlay_pet\n\nfig = overlay_pet(\n    background=\"data/ds001421/sub-01/anat/sub-01_T1w.nii.gz\",\n    pet=\"data/ds001421/sub-01/pet/sub-01_trc-18FFDG_pet.nii.gz\",\n    alpha=0.6,\n    colormap=\"hot\",\n    threshold=0.3,   # fraction of max value \u2014 hide low-uptake voxels\n)\nfig.show()\n</code></pre> <p>The overlay clips the PET at the threshold and applies the colormap on top of the grayscale anatomical. Qortex does not perform spatial registration \u2014 the two files must already share the same affine.</p>"},{"location":"modalities/pet/visualization/#cli","title":"CLI","text":"<pre><code>qortex visualize-overlay sub-01/anat/sub-01_T1w.nii.gz \\\n    --overlay sub-01/pet/sub-01_trc-18FFDG_pet.nii.gz \\\n    --type pet \\\n    --alpha 0.6 \\\n    --output sub01_pet_overlay.html\n</code></pre>"},{"location":"modalities/pet/visualization/#frame-by-frame-viewing","title":"Frame-by-frame viewing","text":"<p>PET files are 4D when dynamic. To view a specific frame:</p> <pre><code>viewer = VolumeViewer(\"sub-01/pet/sub-01_trc-18FFDG_pet.nii.gz\")\nviewer.ortho(volume_index=5)   # frame 5\n</code></pre>"},{"location":"modalities/pet/visualization/#related","title":"Related","text":"<ul> <li>PET metadata \u2014 tracer, frames, SUV information</li> <li>Overlays \u2014 full overlay API reference</li> <li>Colormaps and windows \u2014 PET presets</li> </ul>"},{"location":"neuroai/","title":"NeuroAI Runtime","text":"<p>The NeuroAI runtime connects neuroimaging data sources to AI models and structured output sinks through a single declarative pipeline. It separates compatibility checking from execution: the model is never loaded until the runtime has confirmed the source and model can actually work together.</p>"},{"location":"neuroai/#the-core-loop","title":"The core loop","text":"<pre><code>source.probe()          \u2190 header-only scan, no data loaded\nmodel.inspect()         \u2190 config.json only, no weights\nCompatibilityEngine     \u2190 reports required transforms, blockers, unknowns\nPreprocessPlanner       \u2190 builds the documented transform chain\nmodel.load()            \u2190 weights loaded only when check passes\nsource.stream()         \u2190 windowed data\nTransformExecutor       \u2190 applies the plan\nmodel.predict()         \u2190 inference\noutput.write()          \u2190 one or many sinks\nArtifactWriter          \u2190 9-file provenance directory\n</code></pre> <p>Nothing in this chain is implicit. Each step produces a typed contract: <code>SourceProfile</code>, <code>ModelProfile</code>, <code>CompatibilityReport</code>, <code>PreprocessPlan</code>, <code>PipelineRunReport</code>, <code>ArtifactContract</code>. These contracts flow into the artifact directory written after each run.</p>"},{"location":"neuroai/#when-to-use-it","title":"When to use it","text":"<p>Use the Dataset workflow (<code>qortex.Dataset</code>) when your goal is to work with OpenNeuro BIDS datasets \u2014 catalog search, download, conversion, readiness checks.</p> <p>Use the NeuroAI runtime when: - You have local data (EDF, NWB, XDF, DICOM, video) and want to run a model on it - You have a live data stream (LSL, BrainFlow) and need real-time inference - You want to evaluate model compatibility before committing to a full run - You need a traceable artifact with provenance for every inference run</p>"},{"location":"neuroai/#contents","title":"Contents","text":"<ul> <li>Pipeline \u2014 YAML format, Python API, <code>check()</code>, <code>run()</code>, <code>benchmark()</code>, <code>replay()</code></li> <li>Sources \u2014 BIDS, DICOM, NWB, XDF, LSL, BrainFlow, image, video</li> <li>Models \u2014 HuggingFace, ONNX, Torch, MONAI, Ultralytics, custom plugins</li> <li>Outputs \u2014 JSONL, Parquet, CSV, LSL markers, NIfTI, DICOM-SEG, BIDS, COCO, YOLO, overlay, HTTP</li> <li>Contracts \u2014 SourceProfile, ModelProfile, CompatibilityReport, PreprocessPlan, ArtifactContract</li> <li>Ring buffer \u2014 lock-free windowing for real-time sources; Python fallback + Rust extension</li> </ul>"},{"location":"neuroai/#quick-example","title":"Quick example","text":"<pre><code>from qortex.neuroai import Pipeline\n\npipe = Pipeline.from_yaml(\"pipeline.yaml\")\n\nreport = pipe.check()\nprint(report.summary())\n# CompatibilityReport: COMPATIBLE_WITH_TRANSFORMS\n#   source=local_file:sub-01.edf  model=braindecode/EEGNet\n#   Required transforms (2):\n#     \u2022 resample(from_hz=250.0, to_hz=128.0)  [irreversible]  ...\n#     \u2022 normalize(method=zscore)  [irreversible]  ...\n#   Warnings (1):\n#     \u26a0 channel labels inferred from index, not names\n#   Unknowns: model.sampling_rate_hz is not specified\n\nif report.is_runnable:\n    run = pipe.run(artifact_dir=\"artifacts/run_001\")\n    print(run.latency_report.summary())\n</code></pre> <pre><code>qortex neuroai check pipeline.yaml\nqortex neuroai run pipeline.yaml --artifact-dir artifacts/run_001\nqortex neuroai suggest-models data.edf\n</code></pre>"},{"location":"neuroai/models/","title":"Models","text":"<p>Every model adapter implements <code>inspect()</code> (no weights), <code>required_input()</code>, <code>output_schema()</code>, <code>load(runtime_spec)</code>, <code>predict(data)</code>, and <code>unload()</code>. <code>inspect()</code> returns a <code>ModelProfile</code> from config files only \u2014 weights are not downloaded until <code>load()</code> is called.</p> <p>Model provider is set by <code>model.provider</code> in the pipeline YAML.</p>"},{"location":"neuroai/models/#huggingface","title":"HuggingFace","text":"<pre><code>model:\n  provider: huggingface\n  id: braindecode/EEGNet\n  task: eeg_classification\n  revision: main\n</code></pre> <p>Uses <code>transformers.AutoConfig</code> for <code>inspect()</code> and <code>AutoModel.from_pretrained()</code> for <code>load()</code>. The <code>task</code> hint is passed to <code>AutoModel.from_pretrained(task)</code>. If the model repo has a <code>config.json</code>, <code>inspect()</code> reads input shape, hidden size, and label map from it.</p> <p>For models that are not in the <code>transformers</code> ecosystem (e.g., braindecode models hosted on HF Hub), use <code>provider: braindecode</code> instead.</p>"},{"location":"neuroai/models/#onnx","title":"ONNX","text":"<pre><code>model:\n  provider: onnx\n  id: model.onnx              # local path or HF path like hf://org/repo/model.onnx\n  task: classification\n</code></pre> <p><code>inspect()</code> reads the ONNX graph without creating an inference session. Input names, output names, and shapes are extracted from <code>onnx.load_model()</code>.</p> <p><code>load()</code> creates an <code>onnxruntime.InferenceSession</code> with <code>ExecutionProviders</code> selected from <code>runtime.device</code>: - <code>cpu</code> \u2192 <code>CPUExecutionProvider</code> - <code>cuda</code> / <code>cuda:N</code> \u2192 <code>CUDAExecutionProvider</code></p> <p><code>predict()</code> runs <code>session.run(output_names, {input_name: data})</code>. Input must be <code>float32</code> numpy array.</p>"},{"location":"neuroai/models/#pytorch-torchscript","title":"PyTorch / TorchScript","text":"<pre><code>model:\n  provider: torch             # or: torchscript / ts for TorchScript\n  id: model.pt                # or model.ts for TorchScript\n  task: classification\n</code></pre> <p>For TorchScript (<code>provider: torchscript</code> or path ending in <code>.ts</code>), loads with <code>torch.jit.load()</code>. For standard PyTorch, loads with <code>torch.load(weights_only=False)</code>.</p> <p>Input shape is inferred from the TorchScript graph or from the first layer's attributes (<code>in_features</code> for linear, <code>in_channels</code> for conv). FP16 is applied with <code>.half()</code> when <code>runtime.fp16=True</code> and device contains <code>\"cuda\"</code>.</p> <p><code>predict()</code> output parsing: - 1D output \u2192 softmax \u2192 <code>ClassificationOutput</code> - Multi-dimensional output \u2192 argmax \u2192 segmentation mask</p>"},{"location":"neuroai/models/#braindecode","title":"Braindecode","text":"<pre><code>model:\n  provider: braindecode\n  id: braindecode/EEGNet      # or ShallowFBCSPNet, DeepFBCSPNet, EEGConformer, etc.\n</code></pre> <p>Loads <code>config.json</code> from HF Hub to get <code>n_chans</code>, <code>n_times</code>, <code>n_outputs</code>, and <code>id2label</code>. Known model names map to the corresponding braindecode class:</p> HF model name Braindecode class <code>eegnet</code> <code>EEGNetv4</code> <code>shallowfbcspnet</code> <code>ShallowFBCSPNet</code> <code>deepfbcspnet</code> <code>Deep4Net</code> <code>eegconformer</code> <code>EEGConformer</code> <code>tidnet</code> <code>TIDNet</code> <p>Unknown names fall back to <code>AutoModel.from_pretrained()</code>. Input is always <code>[batch, channels, time]</code>. Output has softmax applied.</p>"},{"location":"neuroai/models/#monai-bundle","title":"MONAI bundle","text":"<pre><code>model:\n  provider: monai\n  id: wholeBody_ct_segmentation   # MONAI Hub bundle name, local path, or local ZIP\n</code></pre> <p>Bundle resolution order: local directory \u2192 local ZIP (extracted to tempdir) \u2192 MONAI Hub download.</p> <p><code>inspect()</code> reads <code>configs/metadata.json</code> and <code>configs/inference.json</code> for network parameters. <code>load()</code> uses <code>monai.bundle.ConfigParser</code> for the network definition and <code>torch.load</code> for weights.</p> <p><code>predict()</code> uses <code>monai.inferers.sliding_window_inference(roi_size=(96, 96, 96))</code> for volumetric data. Returns <code>VolumePredictionOutput</code>.</p>"},{"location":"neuroai/models/#ultralytics-yolov8","title":"Ultralytics (YOLOv8)","text":"<pre><code>model:\n  provider: ultralytics\n  id: yolov8n.pt              # local path or Ultralytics model name\n  task: detect                # detect | segment | classify\n</code></pre> <p><code>inspect()</code> loads the YOLO model (Ultralytics always loads weights at init \u2014 no separate inspect). Task dispatch: - <code>detect</code> \u2192 <code>DetectionOutput</code> with <code>BoundingBox</code> list - <code>segment</code> \u2192 <code>SegmentationOutput</code> with argmax mask - <code>classify</code> \u2192 <code>ClassificationOutput</code></p> <p>Input conversion: numpy CHW float \u2192 HWC uint8.</p> <p>Box parsing: <code>result.boxes.xyxy</code>, <code>result.boxes.conf</code>, <code>result.boxes.cls</code>.</p>"},{"location":"neuroai/models/#custom-plugin","title":"Custom plugin","text":"<pre><code>model:\n  provider: plugin\n  id: my_model.py             # path to a Python file\n  trust_remote_code: true     # required \u2014 must be set explicitly\n</code></pre> <p>Loads <code>my_model.py</code> with <code>importlib.util.spec_from_file_location</code>. Validates that the module defines a <code>QortexPlugin</code> class with all required methods: <code>inspect</code>, <code>required_input</code>, <code>output_schema</code>, <code>load</code>, <code>predict</code>.</p> <p><code>trust_remote_code: true</code> must be set in the pipeline spec. Without it, the adapter raises <code>CompatibilityError</code> before loading anything.</p> <p>All plugin calls are wrapped in try/except with structured error messages so one bad prediction does not crash the full run.</p>"},{"location":"neuroai/models/#plugin-interface","title":"Plugin interface","text":"<pre><code>class QortexPlugin:\n    def inspect(self) -&gt; dict:\n        \"\"\"Return model metadata as a dict (no weights loaded).\"\"\"\n        return {\n            \"model_id\": \"my_model\",\n            \"task\": \"eeg_classification\",\n            \"input_shape\": [None, 64, 512],  # [batch, channels, time]\n            \"n_outputs\": 4,\n        }\n\n    def required_input(self) -&gt; dict:\n        return {\"shape\": [None, 64, 512], \"dtype\": \"float32\"}\n\n    def output_schema(self) -&gt; dict:\n        return {\"type\": \"classification\", \"n_classes\": 4}\n\n    def load(self, runtime_spec) -&gt; None:\n        \"\"\"Load weights. Called once before streaming starts.\"\"\"\n        self.model = load_my_model()\n\n    def predict(self, data) -&gt; object:\n        \"\"\"Run inference on one window. data is [batch, channels, time] float32.\"\"\"\n        return self.model(data)\n</code></pre>"},{"location":"neuroai/models/#model-profile","title":"Model profile","text":"<p><code>inspect()</code> returns a <code>ModelProfile</code>:</p> <pre><code>profile.model_id              # str \u2014 HF ID, local path, or model name\nprofile.provider              # str \u2014 \"huggingface\" | \"onnx\" | \"torch\" | ...\nprofile.task                  # str | None \u2014 \"eeg_classification\" | \"segmentation\" | ...\nprofile.revision              # str | None \u2014 git revision or None\nprofile.model_hash            # str | None \u2014 SHA-256 of weights file when available\nprofile.license               # str | None \u2014 SPDX license identifier\nprofile.trusted               # bool \u2014 True when trust_remote_code was accepted\nprofile.input_contract        # InputContract | None \u2014 formal input requirements\nprofile.output_contract       # OutputContract | None \u2014 formal output schema\nprofile.estimated_params      # int | None \u2014 number of model parameters\nprofile.estimated_memory_mb   # float | None \u2014 estimated GPU/CPU memory in MB\nprofile.supported_devices     # list[str] \u2014 declared supported devices\nprofile.warnings              # list[WarningItem] \u2014 non-fatal inspection warnings\n</code></pre> <p>The <code>InputContract</code> inside <code>profile.input_contract</code> carries what the model formally requires:</p> <pre><code>contract = profile.input_contract\n\ncontract.modality             # \"eeg\" | \"mri\" | \"image\" | ...\ncontract.axis_convention      # AxisConvention \u2014 e.g. batch_channels_time\ncontract.n_channels           # int | None \u2014 required channel count\ncontract.sampling_rate_hz     # float | None \u2014 required sampling frequency\ncontract.window_duration_s    # float | None \u2014 required window length\ncontract.spatial_shape        # tuple[int, ...] | None \u2014 (H, W) or (Z, Y, X)\ncontract.voxel_sizes_mm       # tuple[float, ...] | None \u2014 spatial resolution\ncontract.dtype                # str \u2014 \"float32\" (default)\ncontract.evidence_status      # EvidenceStatus \u2014 confirmed | inferred | unknown\n</code></pre> <p>The <code>OutputContract</code> inside <code>profile.output_contract</code> declares what the model produces:</p> <pre><code>out = profile.output_contract\n\nout.output_type               # \"classification\" | \"segmentation\" | \"detection\" | ...\nout.classes                   # list[str] \u2014 class names (empty when unknown)\nout.n_classes                 # int | None\nout.output_dtype              # str \u2014 \"float32\"\nout.produces_probabilities    # bool \u2014 True when output is a probability distribution\n</code></pre> <p>The compatibility engine reads <code>input_contract</code> to check modality, channel count, sampling rate, spatial shape, and axis convention against <code>SourceProfile</code>. Fields with <code>evidence_status=unknown</code> produce <code>uncertain</code> compatibility rather than a blocker.</p> <p>The plugin adapter raises <code>ModelAdapterError</code> (not <code>CompatibilityError</code>) when <code>trust_remote_code</code> is not set.</p>"},{"location":"neuroai/outputs/","title":"Outputs","text":"<p>Every output adapter implements <code>open()</code>, <code>write(output, metadata)</code>, and <code>close()</code> as a context manager. Multiple outputs can be listed in one pipeline \u2014 all are opened before streaming starts and closed after the run finishes.</p> <p>Output type is set by <code>outputs[n].type</code> in the pipeline YAML.</p>"},{"location":"neuroai/outputs/#file-outputs","title":"File outputs","text":""},{"location":"neuroai/outputs/#jsonl","title":"JSONL","text":"<pre><code>outputs:\n  - type: jsonl\n    path: predictions.jsonl\n    append: false\n</code></pre> <p>One JSON object per prediction window. Schema varies by output type (classification, detection, segmentation, etc.) but always includes timestamp, index, <code>window_index</code>, source, pipeline hash, and <code>output_type</code> when available.</p>"},{"location":"neuroai/outputs/#parquet","title":"Parquet","text":"<pre><code>outputs:\n  - type: parquet\n    path: predictions.parquet\n</code></pre> <p>Columnar output for offline analytics. Core columns include timestamp, output type, class, class index, top probability, regression value, pipeline hash, and run metadata. Probability vectors are flattened into per-class columns for direct filtering and aggregation.</p>"},{"location":"neuroai/outputs/#csv","title":"CSV","text":"<pre><code>outputs:\n  - type: csv\n    path: predictions.csv\n    append: true\n</code></pre> <p>Appendable CSV for lightweight analytics and spreadsheet inspection. It writes stable columns for timestamp, index, pipeline hash, output type, class, class index, top probability, regression value, window index, trigger state, source, probabilities JSON, runtime metadata JSON, model-output metadata JSON, and compact summaries for raw arrays, masks, and embeddings. <code>append: true</code> opens in append mode so multiple runs accumulate in one file.</p>"},{"location":"neuroai/outputs/#streaming-outputs","title":"Streaming outputs","text":""},{"location":"neuroai/outputs/#lsl-marker","title":"LSL marker","text":"<pre><code>outputs:\n  - type: lsl_marker\n    stream_name: qortex_markers\n</code></pre> <p>Creates a <code>pylsl.StreamInfo</code> with <code>type=\"Markers\"</code>, <code>channel_count=1</code>, <code>channel_format=cf_string</code>. Pushes one string per prediction: <code>\"{class_name}:{class_index}\"</code>. When <code>metadata[\"trigger_value\"]</code> is set, that value is pushed instead.</p> <p>This is the standard interface for BCI applications \u2014 BCI2000, OpenViBE, and similar platforms can receive these markers on the LSL network.</p>"},{"location":"neuroai/outputs/#websocket","title":"WebSocket","text":"<pre><code>outputs:\n  - type: websocket\n    path: ws://localhost:8765/predictions\n</code></pre> <p>Uses <code>websocket-client</code> (synchronous). Sends a JSON payload per <code>write()</code>. Connection is opened in <code>open()</code> and closed in <code>close()</code>. Does not retry on write failure \u2014 errors are logged.</p>"},{"location":"neuroai/outputs/#http-callback","title":"HTTP callback","text":"<pre><code>outputs:\n  - type: http\n    path: https://api.example.com/predictions\n    extra:\n      auth:\n        type: bearer\n        token: \"${API_TOKEN}\"\n      retry_max: 3\n</code></pre> <p>Uses <code>requests.Session</code>. Sends a JSON POST per window. Retries up to 3 times with <code>sleep(0.5 * attempt)</code> backoff. Supports bearer token and basic auth via <code>spec.extra[\"auth\"]</code>.</p>"},{"location":"neuroai/outputs/#imaging-outputs","title":"Imaging outputs","text":""},{"location":"neuroai/outputs/#nifti","title":"NIfTI","text":"<pre><code>outputs:\n  - type: nifti\n    path: mask.nii.gz\n</code></pre> <p>Writes a <code>nibabel.Nifti1Image</code> from a <code>SegmentationOutput</code> or <code>VolumePredictionOutput</code>. Validates that the affine determinant is positive before writing (a negative determinant indicates a flipped orientation). Writes a JSON sidecar (<code>mask_provenance.json</code>) with pipeline reference and model ID.</p>"},{"location":"neuroai/outputs/#dicom-seg","title":"DICOM-SEG","text":"<pre><code>outputs:\n  - type: dicom_seg\n    path: output_seg/\n</code></pre> <p>Creates a <code>highdicom.seg.Segmentation</code> with <code>SegmentDescription</code> and <code>AlgorithmIdentificationSequence</code>. Validates mask shape against the source series dimensions. Falls back to saving as <code>.npy</code> if the <code>highdicom</code> API raises (API changes across versions).</p>"},{"location":"neuroai/outputs/#dicom-sr","title":"DICOM-SR","text":"<pre><code>outputs:\n  - type: dicom_sr\n    path: output_sr/\n</code></pre> <p>Creates a <code>highdicom.sr.EnhancedSR</code> structured report using TID 1500 MeasurementReport. Used for <code>RegressionOutput</code> and <code>ReportOutput</code> types. Falls back to <code>.json</code> on <code>highdicom</code> API failure.</p>"},{"location":"neuroai/outputs/#image-overlay","title":"Image overlay","text":""},{"location":"neuroai/outputs/#overlay","title":"Overlay","text":"<pre><code>outputs:\n  - type: overlay\n    path: annotated_frames/\n    extra:\n      format: png        # png | jpg (default: png)\n      alpha: 0.45        # mask transparency, 0\u20131 (default: 0.45)\n      line_width: 2      # bounding-box border width in pixels\n</code></pre> <p>Renders model predictions on top of source images and saves annotated frames to disk. Requires Pillow or OpenCV (checked at runtime; falls back automatically between the two).</p> <p>The source image must be passed through <code>metadata[\"source_image\"]</code> as a <code>[H, W]</code> or <code>[H, W, C]</code> array when calling <code>out_adapter.write(output, metadata={\"source_image\": frame, ...})</code>.</p> <p>What gets drawn per output type:</p> Output type Rendered annotation <code>DetectionOutput</code> Coloured bounding boxes with class label and confidence <code>SegmentationOutput</code> Per-class colour overlay at configured <code>alpha</code> transparency <code>ClassificationOutput</code> Black banner with <code>class_name: confidence%</code> in the top-left corner <p>When no <code>source_image</code> is provided (pure signal pipelines), the adapter writes a JSON sidecar per window instead of a frame.</p> <p>When a trigger fires, <code>write_marker(EventMarkerOutput)</code> writes a companion <code>trigger_{frame_idx}.json</code> next to the annotated frame.</p>"},{"location":"neuroai/outputs/#annotation-outputs","title":"Annotation outputs","text":""},{"location":"neuroai/outputs/#bids-derivative","title":"BIDS derivative","text":"<pre><code>outputs:\n  - type: bids\n    path: derivatives/qortex/\n    extra:\n      subject: \"01\"\n      session: null\n      task: rest\n      run: \"01\"\n</code></pre> <p>Creates a BIDS derivative directory structure. Writes <code>dataset_description.json</code> with <code>GeneratedBy: [{Name: \"Qortex\"}]</code>. Filenames follow BIDS entity naming:</p> <pre><code>sub-01[_ses-01][_task-rest][_run-01]_{suffix}.json\n</code></pre> <p>For segmentation output, writes a NIfTI via nibabel instead of JSON. On <code>close()</code>, writes <code>provenance.json</code> summarizing all outputs in the session.</p>"},{"location":"neuroai/outputs/#coco-json","title":"COCO JSON","text":"<pre><code>outputs:\n  - type: coco\n    path: predictions_coco.json\n</code></pre> <p>Accumulates <code>images</code>, <code>annotations</code>, and <code>categories</code> in memory across all windows. Writes the full COCO JSON on <code>close()</code>. Bounding boxes are in COCO format: <code>[x, y, width, height]</code>.</p>"},{"location":"neuroai/outputs/#yolo-text","title":"YOLO text","text":"<pre><code>outputs:\n  - type: yolo\n    path: yolo_labels/\n</code></pre> <p>One <code>.txt</code> file per image: <code>{class_id} {cx} {cy} {w} {h}</code> with normalized coordinates (0\u20131). On <code>close()</code>, writes <code>classes.txt</code> listing all class names.</p>"},{"location":"neuroai/outputs/#canonical-output-types","title":"Canonical output types","text":"<p>All adapters receive one of these typed objects:</p> <pre><code>ClassificationOutput(\n    class_name=\"motor_imagery\",\n    class_index=1,\n    confidence=0.92,\n    top_k=[(\"motor_imagery\", 0.92), (\"rest\", 0.08)],\n)\n\nDetectionOutput(\n    boxes=[\n        BoundingBox(x1=0.1, y1=0.2, x2=0.4, y2=0.6,\n                    class_name=\"lesion\", class_index=0, confidence=0.87)\n    ],\n    image_shape=(512, 512),\n)\n\nSegmentationOutput(\n    mask=np.array(..., dtype=np.int16),   # [z, y, x] or [h, w]\n    n_classes=5,\n    class_labels={0: \"background\", 1: \"WM\", 2: \"GM\", 3: \"CSF\", 4: \"tumor\"},\n    affine=np.eye(4).tolist(),\n    voxel_sizes=(1.0, 1.0, 1.0),\n)\n\n# Convenience: build from a list of class names\nSegmentationOutput.from_label_list(\n    mask=np.array(..., dtype=np.int16),\n    class_labels=[\"background\", \"WM\", \"GM\", \"CSF\", \"tumor\"],\n    affine=np.eye(4).tolist(),\n    voxel_sizes=(1.0, 1.0, 1.0),\n)\n\nRegressionOutput(value=3.72, units=\"mm\", confidence_interval=(3.1, 4.3))\n\nEmbeddingOutput(vector=np.array(...), dimensionality=128, model_layer=\"encoder\")\n\nTimeSeriesPredictionOutput(\n    predictions=np.array(...),    # [time, n_classes]\n    timestamps=np.array(...),\n    sampling_rate_hz=250.0,\n    label_map={0: \"N2\", 1: \"N3\", 2: \"REM\", 3: \"Wake\"},\n)\n\nEventMarkerOutput(\n    event_type=\"trigger\",\n    label=\"motor_imagery\",\n    timestamp_utc=\"2024-06-28T12:00:00+00:00\",\n    confidence=0.92,\n    window_index=42,\n    source_id=\"bids:ds004130\",\n    emit_payload={\"lsl_value\": \"1\", \"action\": \"send_cue\"},\n)\n\nVolumePredictionOutput(\n    mask=np.array(...),\n    affine=np.eye(4),\n    voxel_sizes=(1.0, 1.0, 1.0),\n    n_classes=3,\n    class_labels=[\"background\", \"lesion\", \"edema\"],\n)\n\nReportOutput(\n    title=\"Brain Lesion Analysis\",\n    findings=[\"Single lesion detected in right hemisphere\"],\n    measurements={\"volume_ml\": 4.2},\n    confidence=0.87,            # numeric score in [0, 1]\n    warnings=[],\n    source_id=\"dicom:study_001\",\n    model_id=\"org/lesion-detector\",\n)\n# report.confidence_level \u2192 \"high\" | \"medium\" | \"low\"\n</code></pre> <p><code>BoundingBox</code> has helpers for format conversion:</p> <pre><code>box.to_coco()              # [x, y, width, height]\nbox.to_yolo(img_w=512, img_h=512)   # [cx, cy, w, h] normalized\n</code></pre> <p><code>ClassificationOutput.from_probs(probs, top_k=5)</code> builds from a raw probability array.</p>"},{"location":"neuroai/outputs/#artifact-directory","title":"Artifact directory","text":"<p>When <code>pipe.run(artifact_dir=\"...\")</code> is called, <code>ArtifactWriter</code> writes 9 files:</p> <pre><code>artifacts/run_001/\n  provenance.json           pipeline spec, source, model, git hash, timestamps\n  compatibility_report.json CompatibilityReport fields\n  preprocess_plan.json      PreprocessPlan \u2014 transforms applied and why\n  runtime_report.json       windows processed, outputs written, errors\n  latency_report.json       p50/p95/p99 per stage\n  warnings.json             non-fatal issues during the run\n  pipeline.yaml             copy of the pipeline spec\n  artifact_contract.json    schema, hash, provenance summary\n  artifact_manifest.json    SHA-256 and file size for every file above\n</code></pre> <p><code>artifact_manifest.json</code> lets any downstream consumer verify the integrity of the artifact without re-running anything.</p>"},{"location":"neuroai/pipeline/","title":"Pipeline","text":"<p><code>Pipeline</code> is the top-level object for the NeuroAI runtime. It wraps the full check \u2192 plan \u2192 load \u2192 run \u2192 artifact sequence and exposes each step as an explicit method.</p>"},{"location":"neuroai/pipeline/#yaml-format","title":"YAML format","text":"<p>A pipeline YAML has five sections: <code>source</code>, <code>window</code>, <code>model</code>, <code>outputs</code>, and <code>runtime</code>. All keys are documented below.</p> <pre><code>name: eeg_classifier          # used in provenance and artifact filenames\n\nsource:\n  type: bids                  # see Sources page for all types\n  path: data/ds004130\n  modality: eeg\n  subject: \"01\"               # optional \u2014 single subject\n  session: null               # optional\n  task: null                  # optional\n\nwindow:\n  duration_s: 4.0             # window length in seconds\n  step_s: 2.0                 # step between windows (overlap = duration - step)\n  max_windows: null           # null = no limit\n\nmodel:\n  provider: huggingface       # huggingface | onnx | torch | torchscript | braindecode | monai | ultralytics | plugin\n  id: braindecode/EEGNet      # HF repo ID, local path, or model identifier\n  task: eeg_classification    # task hint passed to the model adapter\n  revision: main              # HF revision (branch, tag, commit)\n  trust_remote_code: false    # must be true for plugin provider\n\npreprocessing:\n  resample_hz: 128            # target sampling rate (null = no resampling)\n  z_score: true               # z-score per channel\n  bandpass_hz: [1.0, 40.0]   # [low, high] Hz, null = skip\n  channel_select: null        # list of channel names to keep, null = all\n\noutputs:\n  - type: jsonl\n    path: predictions.jsonl\n    append: false\n  - type: lsl_marker\n    stream_name: qortex_markers\n  - type: parquet\n    path: predictions.parquet\n\nruntime:\n  device: cpu                 # cpu | cuda | cuda:0 | mps\n  fp16: false                 # half precision \u2014 cuda only\n  latency_budget_ms: 50.0    # warning threshold for p95 latency\n  num_threads: null           # ONNX/Torch thread count, null = default\n</code></pre>"},{"location":"neuroai/pipeline/#validation","title":"Validation","text":"<p><code>PipelineSpec.validate()</code> returns a list of error strings before anything is loaded. The CLI runs it automatically; call it explicitly in Python before <code>check()</code>:</p> <pre><code>errors = pipe._spec.validate()\nif errors:\n    for e in errors:\n        print(e)\n</code></pre> <p>Checks performed: - <code>source.type</code> is present - <code>model.id</code> and <code>model.provider</code> are present - <code>model.provider</code> is a known value (<code>huggingface</code>, <code>onnx</code>, <code>torch</code>, <code>torchscript</code>, <code>monai</code>, <code>braindecode</code>, <code>ultralytics</code>, <code>custom</code>, <code>plugin</code>) - <code>trust_remote_code=True</code> is flagged as a security warning - At least one output is declared - <code>window.duration_s &gt; 0</code> and <code>window.step_s &gt; 0</code> (when provided) - <code>window.step_s &lt;= window.duration_s</code> - All values in <code>preprocessing.allow</code> and <code>preprocessing.deny</code> are valid <code>TransformKind</code> names</p>"},{"location":"neuroai/pipeline/#python-api","title":"Python API","text":""},{"location":"neuroai/pipeline/#construction","title":"Construction","text":"<pre><code>from qortex.neuroai import Pipeline\n\npipe = Pipeline.from_yaml(\"pipeline.yaml\")\n\n# Or from a dict:\npipe = Pipeline.from_dict({\n    \"name\": \"ad_hoc\",\n    \"source\": {\"type\": \"local_file\", \"path\": \"data.edf\"},\n    \"model\": {\"provider\": \"huggingface\", \"id\": \"braindecode/EEGNet\"},\n    \"outputs\": [{\"type\": \"jsonl\", \"path\": \"out.jsonl\"}],\n})\n</code></pre>"},{"location":"neuroai/pipeline/#check-compatibility-gate","title":"<code>check()</code> \u2014 compatibility gate","text":"<pre><code>report = pipe.check()\n</code></pre> <p><code>check()</code> probes the source (header only, no data loaded) and inspects the model (config.json only, no weights). It returns a <code>CompatibilityReport</code> with:</p> <ul> <li><code>status</code>: <code>compatible</code> | <code>compatible_with_transforms</code> | <code>uncertain</code> | <code>incompatible</code></li> <li><code>is_runnable</code>: bool \u2014 True when status is <code>compatible</code> or <code>compatible_with_transforms</code></li> <li><code>required_transforms</code>: list of transforms the runtime will apply automatically</li> <li><code>blockers</code>: list of hard incompatibilities (wrong modality, incompatible shape, missing dependency)</li> <li><code>warnings</code>: non-fatal issues (unknown channel labels, inferred sampling rate)</li> <li><code>unknowns</code>: fields that could not be determined without loading data</li> </ul> <pre><code>print(report.summary())\n# CompatibilityReport: COMPATIBLE_WITH_TRANSFORMS\n# transforms: [resample(250\u2192128Hz), normalize(zscore)]\n# blockers: []\n# warnings: [channel labels inferred from index, not names]\n# unknowns: []\n\nfor b in report.blockers:\n    print(b.code, b.message)\n\nif not report.is_runnable:\n    # Do not call run() \u2014 it will raise RuntimeExecutionError.\n    pass\n</code></pre> <p>The distinction between <code>uncertain</code> and <code>incompatible</code> matters: <code>uncertain</code> means a required property (e.g., model input shape) could not be read from the config \u2014 the pipeline might work, but <code>check()</code> cannot guarantee it. <code>incompatible</code> means a hard blocker was found (modality mismatch, insufficient channels, etc.).</p>"},{"location":"neuroai/pipeline/#plan_preprocessing-inspect-the-transform-chain","title":"<code>plan_preprocessing()</code> \u2014 inspect the transform chain","text":"<pre><code>plan = pipe.plan_preprocessing()  # calls check() if not already done\n\nfor t in plan.transforms:\n    print(f\"{t.kind.value}: {t.required_by} (reversible={t.reversible})\")\n</code></pre> <p>Each <code>TransformDescriptor</code> has: - <code>kind</code>: <code>TransformKind</code> enum (<code>resample</code>, <code>z_score</code>, <code>bandpass</code>, <code>channel_select</code>, <code>transpose</code>, <code>to_float32</code>, <code>normalize</code>, <code>window</code>) - <code>required_by</code>: why this transform is in the plan - <code>params</code>: dict of transform parameters - <code>reversible</code>: whether the transform can be undone - <code>evidence</code>: <code>EvidenceStatus</code> for the requirement</p>"},{"location":"neuroai/pipeline/#run-execute-the-pipeline","title":"<code>run()</code> \u2014 execute the pipeline","text":"<pre><code>run = pipe.run(artifact_dir=\"artifacts/run_001\")\n</code></pre> <p><code>run()</code> loads model weights, streams windows from the source, applies the preprocessing plan, runs inference, writes all outputs, then unloads the model. If <code>check()</code> has not been called yet, <code>run()</code> calls it automatically.</p> <p><code>run()</code> raises if <code>report.is_runnable</code> is False.</p> <p><code>artifact_dir</code> is optional. When provided, <code>ArtifactWriter</code> writes 9 files to that directory (see Outputs).</p> <p>The returned <code>PipelineRunReport</code> contains:</p> <pre><code>run.n_windows_processed   # int\nrun.n_outputs_written     # int\nrun.errors                # list[str]\nrun.latency_report        # LatencyReport\nrun.artifact_contract     # ArtifactContract | None\n</code></pre>"},{"location":"neuroai/pipeline/#benchmark-latency-profiling","title":"<code>benchmark()</code> \u2014 latency profiling","text":"<pre><code>bench = pipe.benchmark(n_windows=50)\nprint(bench.summary())\n# p50=12.3ms  p95=18.7ms  p99=23.1ms  budget=50ms  status=ok\n</code></pre> <p><code>benchmark()</code> loads weights, runs <code>n_windows</code> windows through source \u2192 preprocess \u2192 inference, and records per-stage timing. No output adapters are opened \u2014 nothing is written to disk or LSL.</p> <p><code>LatencyReport</code> fields:</p> <pre><code>bench.p50_ms, bench.p95_ms, bench.p99_ms\nbench.per_stage          # dict: stage_name \u2192 {p50, p95, p99}\nbench.budget_ms          # from spec.runtime.latency_budget_ms\nbench.budget_met         # bool\nbench.summary()          # human-readable string\n</code></pre>"},{"location":"neuroai/pipeline/#replay-replay-a-recorded-session","title":"<code>replay()</code> \u2014 replay a recorded session","text":"<pre><code>pipe.replay(\"recording.xdf\", speed=2.0, output_dir=Path(\"replay_out/\"))\n</code></pre> <p><code>replay()</code> swaps the pipeline's source adapter to read from <code>source_path</code> and runs the full pipeline. <code>speed=2.0</code> plays back twice as fast as real-time (for XDF/EDF sources that simulate timing via <code>source.replay(speed)</code>). <code>output_dir</code> redirects output paths.</p>"},{"location":"neuroai/pipeline/#trigger-system","title":"Trigger system","text":"<p>A <code>trigger</code> block causes the runtime to evaluate a condition against each prediction and fire a structured <code>EventMarkerOutput</code> when the condition is met.</p> <pre><code>trigger:\n  when:\n    class: motor_imagery\n    probability_gte: 0.85\n    stable_for: 3          # must fire on 3 consecutive windows\n  emit:\n    lsl_value: \"1\"\n    action: send_cue\n</code></pre> <p>When the trigger fires, the runtime:</p> <ol> <li>Calls <code>out_adapter.write_marker(EventMarkerOutput(...))</code> on every output adapter that exposes <code>write_marker</code>.</li> <li>Sets <code>metadata[\"trigger_fired\"] = True</code> in the normal <code>write()</code> call for all adapters.</li> <li>Resets the consecutive-window counter.</li> </ol> <p>Supported <code>when</code> keys: <code>class</code>, <code>probability_gte</code>, <code>stable_for</code>.</p> <p>The <code>EventMarkerOutput</code> emitted contains: <code>event_type</code>, <code>label</code>, <code>confidence</code>, <code>window_index</code>, <code>source_id</code>, <code>timestamp_utc</code>, <code>emit_payload</code>.</p> <p>Adapters that implement <code>write_marker</code>: <code>OverlayOutputAdapter</code> (writes a <code>.json</code> trigger sidecar), <code>JSONLOutputAdapter</code> (appends a marker record), <code>LSLMarkerOutputAdapter</code> (pushes the trigger label as a marker string).</p>"},{"location":"neuroai/pipeline/#accessing-intermediate-state","title":"Accessing intermediate state","text":"<p>After <code>check()</code> or <code>run()</code>, the pipeline caches all intermediate results:</p> <pre><code>pipe.source_profile       # SourceProfile\npipe.model_profile        # ModelProfile\npipe.compatibility_report # CompatibilityReport\npipe.preprocess_plan      # PreprocessPlan\n</code></pre> <p>These are also written into the artifact directory, so you don't need to keep the Pipeline object alive to inspect them later.</p>"},{"location":"neuroai/pipeline/#shortcut-qortexneuroaicheck","title":"Shortcut: <code>qortex.neuroai.check()</code>","text":"<p>For a quick compatibility check without a YAML file:</p> <pre><code>import qortex.neuroai\n\nreport = qortex.neuroai.check(\n    source=\"data.edf\",\n    model=\"braindecode/eegnet\",\n    task=\"eeg_classification\",\n)\nprint(report.summary())\n</code></pre>"},{"location":"neuroai/pipeline/#cli","title":"CLI","text":"<pre><code># Compatibility check \u2014 no weights downloaded.\nqortex neuroai check pipeline.yaml\n\n# Run the pipeline.\nqortex neuroai run pipeline.yaml --artifact-dir artifacts/run_001\n\n# Latency benchmark.\nqortex neuroai benchmark pipeline.yaml --n-windows 100\n\n# Replay from a file.\nqortex neuroai replay pipeline.yaml --source recording.xdf --speed 2.0\n\n# Probe a source and rank compatible HuggingFace models.\nqortex neuroai suggest-models data.edf --task eeg_classification --limit 10\n\n# Inspect a source without any model.\nqortex neuroai inspect-source data.edf\n</code></pre>"},{"location":"neuroai/sources/","title":"Sources","text":"<p>Every source adapter implements three methods: <code>probe()</code> returns a <code>SourceProfile</code> from headers only; <code>stream()</code> yields windowed data as <code>QortexTimeSeries</code> or <code>QortexVolume</code>; <code>replay(speed)</code> simulates real-time playback.</p> <p>Source type is set by <code>source.type</code> in the pipeline YAML. The adapter is chosen by <code>make_source_adapter()</code>.</p>"},{"location":"neuroai/sources/#local-file-edfbdffif","title":"Local file (EDF/BDF/FIF)","text":"<pre><code>source:\n  type: local_file          # or: edf, bdf, fif, mff, cnt\n  path: data/sub-01.edf\n  modality: eeg             # eeg | meg | ieeg | fnirs\n</code></pre> <p>Uses MNE to read any format MNE supports. <code>probe()</code> reads the file info without loading raw data. Yields <code>QortexTimeSeries</code> windows with shape <code>[channels, samples]</code>.</p> <p>Auto-detection: if <code>type</code> is omitted, the extension triggers automatic routing \u2014 <code>.edf</code> \u2192 <code>LocalFileAdapter</code>, <code>.nwb</code> \u2192 <code>NWBAdapter</code>, <code>.xdf</code> \u2192 <code>XDFAdapter</code>, image extensions \u2192 <code>ImageVideoAdapter</code>, and so on.</p>"},{"location":"neuroai/sources/#bids-dataset","title":"BIDS dataset","text":"<pre><code>source:\n  type: bids\n  path: data/ds004130\n  modality: eeg\n  subject: \"01\"             # optional \u2014 single subject or list\n  session: null             # optional\n  task: rest                # optional\n</code></pre> <p>Uses <code>mne_bids.BIDSPath</code> to locate and load recordings. <code>probe()</code> reads the BIDS sidecar without loading signal data.</p>"},{"location":"neuroai/sources/#dicom-folder","title":"DICOM folder","text":"<pre><code>source:\n  type: dicom\n  path: dicom/study/\n</code></pre> <p>Groups <code>.dcm</code> files by <code>SeriesInstanceUID</code>. Sorts slices by <code>InstanceNumber</code> or <code>ImagePositionPatient</code> z-coordinate. Applies <code>RescaleSlope</code> and <code>RescaleIntercept</code> to convert to Hounsfield units. Builds a 4\u00d74 affine from <code>ImageOrientationPatient</code>, <code>ImagePositionPatient</code>, <code>PixelSpacing</code>, and <code>SliceThickness</code>.</p> <p>Returns <code>QortexVolume</code> with <code>axes=[\"z\",\"y\",\"x\"]</code>, <code>units=\"HU\"</code>, <code>coordinate_frame=\"patient_lps\"</code>, <code>axis_convention=\"spatial_zyx\"</code>. <code>SourceProfile</code> includes <code>voxel_sizes_mm</code> and <code>spatial_shape</code> for downstream compatibility checks.</p> <p>PHI handling. <code>PatientName</code>, <code>PatientID</code>, <code>PatientBirthDate</code>, <code>PatientSex</code>, <code>PatientAge</code>, <code>PatientAddress</code>, <code>ReferringPhysicianName</code>, and <code>InstitutionName</code> are never written to <code>SourceProfile</code> fields, logs, or provenance records. The <code>source_id</code> is derived from the directory name only. The <code>extra[\"phi_redacted\"] = True</code> flag in <code>SourceProfile</code> confirms redaction occurred.</p> <p>Preprocessing. The <code>PreprocessPlanner</code> auto-inserts a <code>rescale_intensity</code> transform for DICOM sources to map HU values to <code>[0, 1]</code>, since most deep learning models expect normalized inputs. Add <code>rescale_intensity</code> to <code>preprocessing.deny</code> to suppress this.</p> <p>Coordinate frame. DICOM uses LPS (Left-Posterior-Superior). If the model's <code>InputContract.axis_convention</code> is <code>RAS</code>, the <code>CompatibilityEngine</code> inserts a <code>reorient(from=LPS, to=RAS)</code> transform automatically.</p> <p>Auto-detection: a directory without <code>dataset_description.json</code> but containing <code>.dcm</code> files is routed to <code>DICOMFolderAdapter</code>.</p>"},{"location":"neuroai/sources/#dicomweb","title":"DICOMweb","text":"<pre><code>source:\n  type: dicomweb\n  path: https://dicomweb.server/wado/rs\n  extra:\n    study_uid: \"1.2.3.4\"\n    series_uid: \"1.2.3.4.5\"\n    auth:\n      type: bearer\n      token: \"${DICOM_TOKEN}\"   # or: type: basic, username: ..., password: ...\n</code></pre> <p>Uses QIDO-RS to fetch instance metadata and WADO-RS to retrieve pixel data. Authentication is bearer token or basic auth, passed via <code>spec.extra[\"auth\"]</code>.</p> <p>URL pattern: <code>{base}/studies/{study_uid}/series/{series_uid}/instances</code></p>"},{"location":"neuroai/sources/#nwb","title":"NWB","text":"<pre><code>source:\n  type: nwb\n  path: data/sub-01.nwb\n</code></pre> <p>Opens the NWB file with <code>pynwb.NWBHDF5IO</code>. Finds <code>ElectricalSeries</code> objects in the acquisition group. NWB stores data as <code>[time, channels]</code> \u2014 the adapter transposes to <code>[channels, time]</code> before returning. Channel names are extracted from electrode labels when present. Sampling rate comes from <code>nominal_srate</code> or is inferred from timestamp differences.</p>"},{"location":"neuroai/sources/#xdf","title":"XDF","text":"<pre><code>source:\n  type: xdf\n  path: recording.xdf\n  query:                    # optional \u2014 select one stream\n    type: EEG               # or: name: my_stream\n</code></pre> <p>Uses <code>pyxdf.load_xdf()</code>. Without a query, the first EEG stream is used. XDF stores <code>[time, channels]</code> \u2014 transposed to <code>[channels, time]</code>. <code>replay(speed)</code> sleeps between windows to simulate real-time timing.</p>"},{"location":"neuroai/sources/#lsl-stream","title":"LSL stream","text":"<pre><code>source:\n  type: lsl\n  query:\n    type: EEG               # LSL stream type\n    name: null              # or match by name\n  extra:\n    wait_time_s: 5.0        # how long to wait for a stream to appear\n</code></pre> <p>Calls <code>pylsl.resolve_streams(wait_time=5.0)</code>. Uses the ring buffer for windowed streaming. Channel names are extracted from the LSL stream's XML descriptor. <code>pull_chunk(timeout=win_dur/4, max_samples=512)</code> is polled in a loop.</p> <p>For real-time use, the ring buffer handles the producer-consumer gap between LSL's chunk delivery and the pipeline's window size.</p>"},{"location":"neuroai/sources/#brainflow-board","title":"BrainFlow board","text":"<pre><code>source:\n  type: brainflow\n  extra:\n    board_id: 0             # BoardIds enum integer\n    serial_port: /dev/ttyUSB0\n    mac_address: null\n    ip_address: null\n    ip_port: null\n</code></pre> <p><code>probe()</code> calls <code>BoardShim.get_eeg_channels()</code>, <code>get_sampling_rate()</code>, and <code>get_eeg_names()</code> without opening a board session. Session opens on first <code>stream()</code> call. Uses ring buffer for windowed streaming. <code>board.get_board_data(win_samples)</code> is polled every <code>win_dur / 4</code> seconds.</p>"},{"location":"neuroai/sources/#image-and-video","title":"Image and video","text":"<pre><code>source:\n  type: image               # or: video\n  path: frames/             # directory of images, or single video file\n</code></pre> <p>Images (<code>.png</code>, <code>.jpg</code>, <code>.tif</code>, <code>.bmp</code>, <code>.webp</code>) are loaded with PIL/Pillow. Videos (<code>.mp4</code>, <code>.avi</code>, <code>.mov</code>, <code>.mkv</code>) use OpenCV. <code>stream()</code> yields batches of <code>window_spec.duration_s * fps</code> frames as <code>QortexVolume(axes=\"nhwc\")</code>.</p>"},{"location":"neuroai/sources/#source-profile","title":"Source profile","text":"<p><code>probe()</code> returns a <code>SourceProfile</code> regardless of adapter:</p> <pre><code>profile.source_id           # str \u2014 path, stream name, or directory name\nprofile.source_type         # str \u2014 \"local_file\" | \"bids\" | \"dicom\" | \"lsl\" | ...\nprofile.modality            # \"eeg\" | \"meg\" | \"mri\" | \"dicom\" | ...\nprofile.n_channels          # int | None\nprofile.sampling_rate_hz    # float | None\nprofile.duration_s          # float | None \u2014 None for live streams\nprofile.channel_names       # list[str] \u2014 empty when unknown\nprofile.spatial_shape       # tuple[int, ...] | None \u2014 (Z, Y, X) or (H, W) for volumes\nprofile.voxel_sizes_mm      # tuple[float, ...] | None \u2014 spatial resolution\nprofile.dtype               # str | None \u2014 \"float32\" etc.\nprofile.axis_convention     # AxisConvention | str | None \u2014 e.g. \"spatial_zyx\" for DICOM\nprofile.evidence_status     # EvidenceStatus \u2014 overall confidence level\nprofile.evidence            # dict[str, EvidenceStatus | str] \u2014 per-field evidence\nprofile.warnings            # list[WarningItem] \u2014 non-fatal probe issues\nprofile.extra               # dict \u2014 adapter-specific metadata (e.g. \"phi_redacted\")\n</code></pre> <p>The overall <code>evidence_status</code> is <code>confirmed</code> when all header fields were read directly from the file, <code>inferred</code> when derived, and <code>unknown</code> for live streams. The <code>CompatibilityEngine</code> uses these statuses to decide whether a mismatch is a hard blocker or an uncertainty.</p>"},{"location":"neuroai/sources/#internal-data-types","title":"Internal data types","text":"<p>Adapters yield one of these types into the pipeline:</p> Type Shape convention Use case <code>QortexTimeSeries</code> <code>[channels, samples]</code> EEG, MEG, iEEG, LFP, fNIRS <code>QortexVolume</code> <code>[z, y, x]</code> or <code>[n, h, w, c]</code> MRI, CT, DICOM, image batches <code>QortexImageSeries</code> <code>[n, h, w, c]</code> with timestamps Ordered 2D images <code>QortexVideo</code> <code>[n, h, w, c]</code> with fps Video frames <code>QortexEmbeddingTable</code> <code>[n, d]</code> Embedding vectors <code>QortexStream</code> descriptor only Live stream (not data)"},{"location":"readiness/","title":"Readiness","text":"<p>Readiness checks answer \"is this dataset usable for training?\" before you download anything expensive. Each check works on the manifest and small sidecar files \u2014 not on imaging data.</p>"},{"location":"readiness/#checks-and-what-they-answer","title":"Checks and what they answer","text":"<p>Doctor \u2014 Full structural readiness report. Covers subjects, modalities, companion files, label coverage, split feasibility, and total size. Start here.</p> <p>Minimum \u2014 What is the smallest download that achieves a specific goal? Returns a file list with sizes.</p> <p>Can train \u2014 Binary check: does this dataset have enough labeled samples to train a classifier? Takes a target column and minimum requirements.</p> <p>First batch \u2014 Return a minimal subset of subjects sufficient to run one complete pipeline pass end-to-end.</p> <p>Label readiness \u2014 Per-subject label coverage, class counts, and missing subjects. Works against either manifest or local events.tsv files.</p> <p>Content status \u2014 After download, verify that local files are complete and not Git LFS pointers.</p> <p>Recipes \u2014 Predefined task-specific readiness recipes (e.g., \"fmri-classification\", \"eeg-regression\") that bundle the most common check parameters.</p> <p>Leakage check \u2014 After conversion, verify that no subject appears in two splits.</p>"},{"location":"readiness/#reading-readiness-reports","title":"Reading readiness reports","text":"<p>All readiness methods return structured Pydantic objects with:</p> <ul> <li><code>state</code> \u2014 a string enum (e.g., <code>\"not_usable\"</code>, <code>\"manifest_only\"</code>, <code>\"download_ready\"</code>)</li> <li><code>findings</code> \u2014 list of <code>Finding</code> objects with severity, code, and message</li> <li><code>next_action</code> \u2014 a string describing the recommended next step</li> <li><code>to_text()</code> \u2014 human-readable summary</li> <li><code>to_dict()</code> / <code>to_json()</code> \u2014 machine-readable output</li> </ul> <pre><code>report = ds.doctor()\nif report.state == \"not_usable\":\n    print(report.to_text())\n    for f in report.findings:\n        if f.severity == \"error\":\n            print(f\"  ERROR: {f.message}\")\n</code></pre> <p>Next \u2192 Download \u2014 once readiness is confirmed, fetch exactly the files you need.</p>"},{"location":"readiness/can-train/","title":"Can Train","text":"<p><code>ds.can_train()</code> returns True or False: does this dataset have enough labeled samples to train a classifier with the given requirements?</p> <p>It is a binary gate. Use it before committing to a full download.</p>"},{"location":"readiness/can-train/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n\nok = ds.can_train(\n    target_col=\"trial_type\",\n    min_classes=2,\n    min_per_class=10,\n    min_subjects=20,\n)\nprint(ok)  # True or False\n</code></pre>"},{"location":"readiness/can-train/#parameters","title":"Parameters","text":"Parameter Default Meaning <code>target_col</code> <code>None</code> Events.tsv column to use as labels. If None, auto-detects. <code>min_classes</code> <code>2</code> Minimum number of distinct label classes required. <code>min_per_class</code> <code>10</code> Minimum total samples per class across all subjects. <code>min_subjects</code> <code>10</code> Minimum number of subjects with the target label. <code>require_events</code> <code>True</code> Fail if no events.tsv files exist."},{"location":"readiness/can-train/#cli","title":"CLI","text":"<pre><code>qortex can-train ds004130 --label trial_type --min-classes 2 --min-per-class 10\n</code></pre> <p>Output:</p> <pre><code>can_train: True\n  Label column: trial_type\n  Classes: rest (240), eyes-open (236), task (88)\n  Subjects with labels: 88/88\n</code></pre> <p>If False:</p> <pre><code>can_train: False\n  Reason: Only 1 class found in trial_type. Need at least 2.\n  Suggestion: Check label_landscape() for per-subject class distribution.\n</code></pre>"},{"location":"readiness/can-train/#auto-detecting-the-label-column","title":"Auto-detecting the label column","text":"<p>If <code>target_col=None</code>, Qortex scans all events.tsv columns and selects the one with the most non-null distinct values. It skips columns named <code>onset</code>, <code>duration</code>, <code>stim_file</code>, <code>HED</code>, and <code>response_time</code>.</p> <p>Auto-detection is useful when you are exploring a new dataset and do not know the events structure yet. However, for production use, specify the column explicitly to avoid surprises when the data changes.</p>"},{"location":"readiness/can-train/#what-can_train-does-not-check","title":"What can_train does NOT check","text":"<ul> <li>Whether the subjects are balanced across classes</li> <li>Whether enough subjects exist for a meaningful train/val/test split</li> <li>Whether the label column has the right encoding for your task</li> <li>Whether the imaging data is free of motion artifacts</li> </ul> <p>Use <code>ds.doctor()</code> for the full report that covers split feasibility and data quality warnings.</p>"},{"location":"readiness/can-train/#related","title":"Related","text":"<ul> <li>Doctor \u2014 comprehensive report including label checks</li> <li>Label readiness \u2014 per-subject label coverage detail</li> <li>First batch \u2014 end-to-end pipeline test</li> </ul>"},{"location":"readiness/content-status/","title":"Content Status","text":"<p><code>ds.content_status()</code> checks the local files after download. It answers: are the files real data, or are some of them Git LFS pointers or incomplete transfers?</p>"},{"location":"readiness/content-status/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nstatus = ds.content_status()\nprint(status.to_text())\n</code></pre> <p>Output:</p> <pre><code>Content status: ok\n  Files checked: 1,056\n  Size on disk:  4.2 GB\n  LFS pointers: 0\n  Incomplete:    0\n  Unreadable:    0\n</code></pre> <p>If problems are found:</p> <pre><code>Content status: issues_found\n  Files checked: 1,056\n  LFS pointers: 3\n    sub-03/func/sub-03_task-rest_bold.nii.gz  (134 bytes \u2014 pointer)\n    sub-07/func/sub-07_task-rest_bold.nii.gz  (134 bytes \u2014 pointer)\n    sub-12/func/sub-12_task-rest_bold.nii.gz  (134 bytes \u2014 pointer)\n  Incomplete: 1\n    sub-05/eeg/sub-05_task-rest_eeg.set (312 MB / 450 MB expected)\n</code></pre>"},{"location":"readiness/content-status/#cli","title":"CLI","text":"<pre><code>qortex content-status ds004130 --data-dir data/ds004130/\n</code></pre>"},{"location":"readiness/content-status/#what-is-checked","title":"What is checked","text":"<p>LFS pointer detection. A file is flagged as an LFS pointer if its size is between 100 and 200 bytes and its content starts with <code>version https://git-lfs.github.com/spec/</code>. These files look like real files to the OS but contain no imaging data.</p> <p>Incomplete transfers. Files whose on-disk size is more than 5% smaller than the manifest-reported size. These may be partial downloads interrupted before completion.</p> <p>Unreadable files. Files where a basic read (first 512 bytes) raises an IOError. Rare \u2014 usually indicates filesystem corruption.</p> <p>Missing files. Files in the manifest that have no local counterpart. These are listed separately as \"not downloaded.\"</p>"},{"location":"readiness/content-status/#fixing-lfs-pointers","title":"Fixing LFS pointers","text":"<p>LFS pointers appear when a dataset was downloaded through DataLad or Git annex without running <code>git-annex get</code> or <code>datalad get</code>. The solution depends on how the dataset was obtained:</p> <pre><code># DataLad\ndatalad get sub-03/func/sub-03_task-rest_bold.nii.gz\n\n# Git annex\ngit annex get sub-03/func/sub-03_task-rest_bold.nii.gz\n</code></pre> <p>Or re-download through Qortex:</p> <pre><code>ds.download(subjects=[\"03\", \"07\", \"12\"], force=True, data_dir=\"data/ds004130/\")\n</code></pre>"},{"location":"readiness/content-status/#related","title":"Related","text":"<ul> <li>Resume \u2014 fix incomplete downloads</li> <li>Doctor \u2014 includes a content status check when data_dir is set</li> </ul>"},{"location":"readiness/doctor/","title":"Doctor","text":"<p><code>ds.doctor()</code> is the first readiness check to run on any new dataset. It fetches the manifest and a sample of sidecar files, then produces a structured report covering structural properties and potential blockers.</p>"},{"location":"readiness/doctor/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor()\nprint(report.to_text())\n</code></pre> <p>Example output:</p> <pre><code>Dataset:      ds004130 \u2014 Resting-state EEG\nSnapshot:     1.2.0\nState:        manifest_only\nSubjects:     88    Sessions: 1\nModalities:   eeg\nTasks:        rest\nEvents:       yes \u2014 88 files, columns: onset duration trial_type\nLabels:       trial_type \u2192 3 classes (rest, eyes-open, task)\nSize:         4.2 GB\nSplit check:  ok \u2014 88 subjects supports 70/15/15 split (\u2265 3 per class in test)\n\nNext action:  download\n  Use ds.minimum(goal=\"first-batch\") to download 3 subjects for a pipeline test.\n</code></pre>"},{"location":"readiness/doctor/#cli","title":"CLI","text":"<pre><code>qortex doctor ds004130\nqortex doctor ds004130 --snapshot 1.0.0\nqortex doctor ds004130 --json\n</code></pre>"},{"location":"readiness/doctor/#what-doctor-checks","title":"What doctor checks","text":"<p>Manifest availability. Can the manifest be fetched from OpenNeuro? If not, the state is <code>api_error</code>.</p> <p>Subject count. Are there enough subjects to form train/val/test splits? Threshold defaults to 10. Configurable with <code>--min-subjects</code>.</p> <p>Companion coverage. For BOLD files: are events.tsv present? For DWI: are bval/bvec present? For EEG: is channels.tsv present?</p> <p>Events file validity. Are events.tsv files non-empty? Do they have an <code>onset</code> column? Is there a parseable label column?</p> <p>Label class coverage. After selecting the best label column, how many classes are there? Are there enough samples per class?</p> <p>Split feasibility. Can the subject count support a 70/15/15 train/val/test split with at least 3 subjects per class in test?</p> <p>Size estimate. Total compressed size from manifest records.</p>"},{"location":"readiness/doctor/#findings-and-severity-levels","title":"Findings and severity levels","text":"<p>Each finding has a <code>severity</code>:</p> <ul> <li><code>info</code> \u2014 informational, not a blocker</li> <li><code>warning</code> \u2014 may cause issues; review before proceeding</li> <li><code>error</code> \u2014 blocks the next pipeline stage</li> </ul> <pre><code>for f in report.findings:\n    print(f\"{f.severity.upper()}: [{f.code}] {f.message}\")\n</code></pre> <p>Common error codes:</p> Code Meaning <code>NO_EVENTS</code> No events.tsv files found <code>EMPTY_EVENTS</code> Events files exist but are empty or have no trial_type <code>NO_COMPANIONS</code> DWI files missing bval/bvec <code>TOO_FEW_SUBJECTS</code> Not enough subjects for splits <code>ONE_CLASS</code> Only one label class in events <code>LFS_POINTERS</code> Files appear to be Git LFS pointers (local only)"},{"location":"readiness/doctor/#doctor-after-download","title":"Doctor after download","text":"<p>Running <code>doctor()</code> after a local download also checks content:</p> <pre><code>ds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nreport = ds.doctor()\n# Checks file count vs manifest, LFS pointer detection, JSON sidecar parsability\n</code></pre>"},{"location":"readiness/doctor/#related","title":"Related","text":"<ul> <li>Minimum \u2014 smallest download for a specific goal</li> <li>Can train \u2014 binary label readiness check</li> <li>Content status \u2014 post-download content integrity</li> </ul>"},{"location":"readiness/first-batch/","title":"First Batch","text":"<p><code>ds.first_batch()</code> runs a complete end-to-end pipeline test on the minimum subset of subjects: download \u2192 convert \u2192 extract one batch \u2192 return it.</p> <p>It is the equivalent of a smoke test. If <code>first_batch()</code> succeeds, the full pipeline will work.</p>"},{"location":"readiness/first-batch/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nbatch = ds.first_batch(\n    target_col=\"trial_type\",\n    data_dir=\"data/ds004130/\",\n    format=\"parquet\",\n)\nprint(batch.X.shape)    # (n_samples, n_features)\nprint(batch.y[:5])      # first 5 labels\nprint(batch.subject_ids[:5])\n</code></pre> <p>The returned object has:</p> <ul> <li><code>X</code> \u2014 numpy array, shape <code>(n_samples, n_features)</code></li> <li><code>y</code> \u2014 numpy array of label strings or integers</li> <li><code>subject_ids</code> \u2014 list of subject IDs, one per row in X</li> <li><code>feature_names</code> \u2014 list of feature names (channel \u00d7 time, or channel \u00d7 frequency, etc.)</li> <li><code>artifact_dir</code> \u2014 path to the temporary artifact written during the test</li> </ul>"},{"location":"readiness/first-batch/#what-it-does-internally","title":"What it does internally","text":"<ol> <li>Calls <code>ds.minimum(goal=\"first-batch\")</code> to select 3\u20135 subjects</li> <li>Downloads those subjects' files to <code>data_dir</code></li> <li>Runs <code>ConversionPipeline</code> with default window settings</li> <li>Loads the first batch from the resulting Parquet artifact</li> <li>Returns the batch</li> </ol> <p>If any step fails, a descriptive exception is raised with the stage that failed.</p>"},{"location":"readiness/first-batch/#cli","title":"CLI","text":"<pre><code>qortex first-batch ds004130 \\\n    --label trial_type \\\n    --data-dir data/ds004130/ \\\n    --format parquet\n</code></pre> <p>Output on success:</p> <pre><code>first_batch: ok\n  Subjects downloaded: 3 (sub-01, sub-02, sub-03)\n  Samples:   96\n  Features:  6,400 (64 channels \u00d7 100 time points)\n  Classes:   rest (32), eyes-open (32), task (32)\n  Batch shape: (96, 6400)\n</code></pre>"},{"location":"readiness/first-batch/#debugging-first_batch-failures","title":"Debugging first_batch failures","text":"<p>Common failures and what they mean:</p> <p><code>EventsNotFound</code> \u2014 events.tsv missing for one of the selected subjects. Usually means the manifest is not consistent \u2014 some subjects have events and some do not.</p> <p><code>EmptyWindowError</code> \u2014 the window duration is longer than the shortest trial. Set a shorter <code>window_s</code>.</p> <p><code>LFSPointerError</code> \u2014 the downloaded file is a Git LFS pointer, not real data. The dataset uses DataLad or Git LFS and was not properly fetched. Try <code>git-annex get</code> on the raw dataset.</p> <p><code>NoLabelError</code> \u2014 the target column is not in events.tsv. Check the actual column names with <code>ds.events(subject=\"01\")</code>.</p>"},{"location":"readiness/first-batch/#related","title":"Related","text":"<ul> <li>Minimum \u2014 understand the subject selection logic</li> <li>Can train \u2014 binary label check before running first_batch</li> <li>Conversion pipeline \u2014 what happens during the convert step</li> </ul>"},{"location":"readiness/label-readiness/","title":"Label Readiness","text":"<p>Label readiness tells you whether a dataset has enough labeled samples to train a classifier. It goes beyond the binary <code>can_train()</code> check by showing per-subject and per-class detail.</p>"},{"location":"readiness/label-readiness/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nlandscape = ds.label_landscape()\nprint(landscape)\n</code></pre> <p>Output:</p> <pre><code>Label column: trial_type\nTotal subjects: 88   With labels: 88   Missing labels: 0\n\nClass distribution:\n  rest        2,112 samples  (88 subjects)\n  eyes-open   2,064 samples  (86 subjects)  \u2190 2 subjects missing\n  task           88 samples  (88 subjects)\n\nSubjects missing 'eyes-open':\n  sub-23  sub-47\n</code></pre>"},{"location":"readiness/label-readiness/#what-label_landscape-shows","title":"What label_landscape shows","text":"<ul> <li>Which events.tsv column was selected (or the column you specified)</li> <li>Per-class sample counts summed across all subjects</li> <li>Which subjects are missing each class</li> <li>Whether label coverage is balanced</li> </ul>"},{"location":"readiness/label-readiness/#specify-the-label-column","title":"Specify the label column","text":"<pre><code>landscape = ds.label_landscape(target_col=\"response_type\")\n</code></pre> <p>If <code>target_col</code> is None, Qortex auto-selects the column with the most distinct non-null values (excluding <code>onset</code>, <code>duration</code>, <code>stim_file</code>, <code>HED</code>, <code>response_time</code>).</p>"},{"location":"readiness/label-readiness/#cli","title":"CLI","text":"<pre><code>qortex eda ds004130 --label trial_type\n</code></pre> <p>The <code>eda</code> command runs a more comprehensive exploratory analysis that includes label_landscape plus signal statistics.</p>"},{"location":"readiness/label-readiness/#working-from-local-eventstsv-files","title":"Working from local events.tsv files","text":"<p>If you have already downloaded the metadata:</p> <pre><code>ds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nlandscape = ds.label_landscape()  # reads from local events.tsv files\n</code></pre> <p>Without a local directory, <code>label_landscape()</code> fetches events.tsv files from CDN. This is fast because events files are small.</p>"},{"location":"readiness/label-readiness/#interpreting-class-imbalance","title":"Interpreting class imbalance","text":"<p>A class with fewer samples per subject is not automatically a problem. If <code>task</code> appears only once per 10-minute run while <code>rest</code> appears every 20 seconds, the imbalance is by design. Review the events.tsv structure before deciding to exclude subjects or classes.</p>"},{"location":"readiness/label-readiness/#related","title":"Related","text":"<ul> <li>Can train \u2014 binary gate based on label requirements</li> <li>Doctor \u2014 includes label landscape in the full report</li> <li>EDA \u2014 full exploratory data analysis including signal statistics</li> </ul>"},{"location":"readiness/leakage-check/","title":"Leakage Check","text":"<p><code>ds.leakage_check()</code> verifies that no subject appears in more than one split of a converted artifact. Subject leakage \u2014 where the same person's data is in both training and test splits \u2014 invalidates generalization metrics.</p>"},{"location":"readiness/leakage-check/#python","title":"Python","text":"<pre><code>from qortex import Dataset, Artifact\n\nds = Dataset(\"ds004130\")\nart = Artifact.open(\"artifacts/ds004130_parquet/\")\n\nresult = ds.leakage_check(art)\nprint(result.ok)          # True if no leakage\nprint(result.to_text())\n</code></pre> <p>Output with no leakage:</p> <pre><code>Leakage check: ok\n  Splits: train (61 subjects), val (14 subjects), test (13 subjects)\n  Overlap: none\n</code></pre> <p>Output with leakage:</p> <pre><code>Leakage check: FAILED\n  sub-03 appears in train AND test\n  sub-17 appears in val AND test\n  Action: re-run convert with strategy=\"subject\"\n</code></pre>"},{"location":"readiness/leakage-check/#cli","title":"CLI","text":"<pre><code>qortex leakage-check ds004130 --artifact artifacts/ds004130_parquet/\n</code></pre>"},{"location":"readiness/leakage-check/#what-is-checked","title":"What is checked","text":"<p>Subject-level overlap. Each sample in the artifact has a <code>subject_id</code> metadata field. The check reads subject IDs from all splits and looks for IDs that appear in more than one.</p> <p>Session-level overlap (optional). If the dataset has multiple sessions per subject, the check can also verify that no (subject, session) pair crosses split boundaries:</p> <pre><code>result = ds.leakage_check(art, level=\"session\")\n</code></pre> <p>Temporal overlap (not checked). Qortex does not check for overlapping time windows within a subject across splits. This is by design \u2014 temporal overlap within a subject is fine as long as subjects are assigned to splits at the subject level.</p>"},{"location":"readiness/leakage-check/#when-leakage-occurs","title":"When leakage occurs","text":"<p>Leakage in Qortex output means the conversion was run without <code>strategy=\"subject\"</code> in the SplitSpec. This can happen if:</p> <ul> <li>You used <code>strategy=\"random\"</code> or <code>strategy=\"time\"</code> during conversion</li> <li>You manually merged artifacts from two separate conversion runs</li> <li>A custom split was applied after conversion that did not respect subject boundaries</li> </ul> <p>Fix by re-running conversion with the default subject-level split:</p> <pre><code>art = ds.convert(\n    output_dir=\"artifacts/ds004130_fixed/\",\n    split=dict(strategy=\"subject\", val_frac=0.15, test_frac=0.15),\n)\n</code></pre>"},{"location":"readiness/leakage-check/#limitations","title":"Limitations","text":"<ul> <li>Leakage check reads the artifact's <code>subject_id</code> column. If your conversion did not preserve subject IDs in the metadata, the check cannot detect leakage.</li> <li>Phenotypic leakage (e.g., samples from the same scanner site in train and test) is not detected. This requires domain-specific knowledge about the dataset.</li> </ul>"},{"location":"readiness/minimum/","title":"Minimum","text":"<p><code>ds.minimum()</code> computes the smallest real download that enables a specific goal. It returns a <code>DownloadPlan</code> with exactly the files needed \u2014 no padding, no extras beyond required companions.</p>"},{"location":"readiness/minimum/#python","title":"Python","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\n\nplan = ds.minimum(goal=\"first-batch\")\nprint(f\"{len(plan.files)} files, {plan.size_gb:.2f} GB\")\n# 12 files, 0.4 GB\n</code></pre>"},{"location":"readiness/minimum/#goals","title":"Goals","text":"Goal What it means <code>first-batch</code> Enough subjects to run one complete pipeline pass (inspect \u2192 download \u2192 convert \u2192 train step). Usually 3\u20135 subjects. <code>label-check</code> Only the events.tsv and sidecar JSON files. No imaging data. <code>validation</code> A representative subset for BIDS validation \u2014 one subject per modality/task combination. <code>metadata</code> All JSON sidecars, TSV, bval/bvec, root-level files. No imaging data."},{"location":"readiness/minimum/#cli","title":"CLI","text":"<pre><code>qortex minimum ds004130 --goal first-batch\nqortex minimum ds004130 --goal label-check\nqortex minimum ds004130 --goal metadata\n</code></pre> <p>Output:</p> <pre><code>Goal: first-batch\nSubjects: sub-01, sub-02, sub-03\nFiles: 12\nSize: 0.4 GB\n</code></pre> <p>Add <code>--download</code> to execute the plan immediately:</p> <pre><code>qortex minimum ds004130 --goal first-batch --download --data-dir data/ds004130/\n</code></pre>"},{"location":"readiness/minimum/#how-first-batch-is-computed","title":"How first-batch is computed","text":"<p><code>first-batch</code> selects subjects that maximize label class coverage within the minimum count:</p> <ol> <li>Fetch the manifest and events.tsv for all subjects</li> <li>For each subject, count label occurrences per class</li> <li>Select the minimum number of subjects such that every class appears at least once</li> <li>Add one extra subject as a buffer for val/test split</li> <li>Include all companions (JSON, events, bval/bvec) for the selected subjects</li> </ol> <p>If the dataset has no label column (no events.tsv), <code>first-batch</code> falls back to selecting 3 subjects arbitrarily.</p>"},{"location":"readiness/minimum/#using-the-plan","title":"Using the plan","text":"<pre><code>plan = ds.minimum(goal=\"first-batch\")\n\n# Inspect before committing\nfor f in plan.files:\n    print(f.path, f.size)\n\n# Download\nds.download_paths(plan.files, data_dir=\"data/ds004130/\")\n</code></pre>"},{"location":"readiness/minimum/#after-first-batch-succeeds","title":"After first-batch succeeds","text":"<p>A successful first-batch run confirms:</p> <ul> <li>The download engine works for this dataset</li> <li>The conversion pipeline can extract windows and labels</li> <li>The ML bridge (PyTorch/sklearn) can load the artifact</li> </ul> <p>If first-batch fails, the error is almost always a data issue (missing events, malformed sidecar, LFS pointers) rather than a library bug.</p>"},{"location":"readiness/minimum/#related","title":"Related","text":"<ul> <li>Plan \u2014 for custom file selections</li> <li>First batch \u2014 detailed description of the first-batch diagnostic</li> </ul>"},{"location":"readiness/recipes/","title":"Recipes","text":"<p>A recipe is a named set of readiness parameters tuned for a specific task type. Instead of specifying every check parameter manually, you pick a recipe and get sensible defaults for that modality and task combination.</p>"},{"location":"readiness/recipes/#using-a-recipe","title":"Using a recipe","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\")\nreport = ds.doctor(recipe=\"eeg-classification\")\n</code></pre> <p>Or from the CLI:</p> <pre><code>qortex make-recipe eeg-classification\nqortex doctor ds004130 --recipe eeg-classification\n</code></pre>"},{"location":"readiness/recipes/#available-recipes","title":"Available recipes","text":""},{"location":"readiness/recipes/#eeg-classification","title":"eeg-classification","text":"<p>For supervised classification from EEG epochs:</p> <ul> <li>Requires <code>trial_type</code> or equivalent events column</li> <li>Minimum 20 subjects</li> <li>Minimum 2 classes, 30 samples per class</li> <li>Checks for <code>channels.tsv</code> and <code>coordsystem.json</code></li> <li>Window: 1.0 s, no overlap (event-aligned)</li> </ul>"},{"location":"readiness/recipes/#eeg-regression","title":"eeg-regression","text":"<p>For regression from EEG (e.g., predicting reaction time):</p> <ul> <li>Requires a numeric events column</li> <li>Minimum 20 subjects</li> <li>Checks continuous range of target column</li> </ul>"},{"location":"readiness/recipes/#fmri-classification","title":"fmri-classification","text":"<p>For supervised classification from fMRI BOLD:</p> <ul> <li>Requires <code>events.tsv</code> with <code>trial_type</code></li> <li>Minimum 15 subjects</li> <li>TR check: warns if TR &gt; 2.5 s</li> <li>Checks for fMRIPrep confounds if present</li> </ul>"},{"location":"readiness/recipes/#fmri-regression","title":"fmri-regression","text":"<p>For regression from fMRI (e.g., predicting behavioral scores from resting-state):</p> <ul> <li>Requires numeric column in participants.tsv</li> <li>Minimum 30 subjects (regression needs more data)</li> <li>Checks for resting-state BOLD (task=rest or no task)</li> </ul>"},{"location":"readiness/recipes/#dwi","title":"dwi","text":"<p>For DWI-based analysis (tractography, diffusion metrics):</p> <ul> <li>Requires <code>bval</code> and <code>bvec</code> files</li> <li>Checks number of gradient directions (warns if &lt; 30)</li> <li>Checks for multiple b-value shells</li> </ul>"},{"location":"readiness/recipes/#anat-segmentation","title":"anat-segmentation","text":"<p>For anatomical segmentation tasks:</p> <ul> <li>Requires T1w or T2w</li> <li>No events required</li> <li>Minimum 30 subjects</li> </ul>"},{"location":"readiness/recipes/#creating-and-saving-a-recipe","title":"Creating and saving a recipe","text":"<pre><code>from qortex.readiness import Recipe\n\nrecipe = Recipe(\n    name=\"my-custom-recipe\",\n    modality=\"eeg\",\n    target_col=\"condition\",\n    min_subjects=50,\n    min_classes=3,\n    min_per_class=20,\n    window_s=2.0,\n)\nrecipe.save(\"my_recipe.json\")\n</code></pre> <p>Load and use:</p> <pre><code>qortex run-recipe my_recipe.json --dataset ds004130\n</code></pre>"},{"location":"readiness/recipes/#recipe-output","title":"Recipe output","text":"<p>A recipe run produces the same structured report as <code>doctor()</code>, but the findings are evaluated against the recipe's thresholds rather than the defaults. This makes comparison across datasets using the same criteria straightforward.</p>"},{"location":"readiness/recipes/#related","title":"Related","text":"<ul> <li>Doctor \u2014 the underlying readiness check engine</li> <li>Can train \u2014 binary version of the label check</li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>This section covers the most common errors and how to resolve them.</p> <p>Install \u2014 Dependency conflicts, missing extras, Python version issues.</p> <p>OpenNeuro \u2014 API errors, rate limiting, manifest fetch failures.</p> <p>Downloads \u2014 CDN 403/410 errors, LFS pointers, incomplete files, resume failures.</p> <p>BIDS \u2014 Manifest parsing errors, missing entity labels, non-standard naming.</p> <p>Labels \u2014 Missing events.tsv, empty label columns, encoding issues.</p> <p>Visualization \u2014 Missing extras, blank figures, memory errors.</p> <p>Overlays \u2014 Shape mismatch, affine mismatch, resampling errors.</p> <p>DICOM \u2014 Missing pydicom, compressed DICOM, multi-frame errors.</p> <p>Artifacts \u2014 Corrupted shards, missing manifest, feature count mismatch.</p>"},{"location":"troubleshooting/#quick-reference-common-error-codes","title":"Quick reference: common error codes","text":"Error Location Fix <code>NO_EVENTS</code> doctor events.tsv missing for all subjects <code>EMPTY_EVENTS</code> doctor events.tsv exists but has no rows <code>LFS_POINTER</code> content-status file is a Git LFS pointer, not real data <code>AFFINE_MISMATCH</code> overlay background and overlay have different affines <code>MISSING_EXTRA</code> any install the required extra (<code>pip install \"qortex[mri]\"</code>) <code>HTTP_403</code> download CDN link expired \u2014 re-run to refresh <code>BIDS_PARSE_ERROR</code> index_local file path does not follow BIDS naming <code>SPLIT_TOO_SMALL</code> convert not enough subjects for val/test split"},{"location":"troubleshooting/artifacts/","title":"Artifact Troubleshooting","text":""},{"location":"troubleshooting/artifacts/#artifact_manifestjson-not-found","title":"artifact_manifest.json not found","text":"<pre><code>ArtifactError: artifact_manifest.json not found in artifacts/ds004130/\n</code></pre> <p>The directory may not be a valid Qortex artifact. Check:</p> <ol> <li>Did the conversion complete successfully? If it was interrupted, the manifest is written last and may be absent.</li> <li>Is the path pointing to the artifact root (not to a split subdirectory)?</li> </ol> <pre><code># Wrong\nart = Artifact.open(\"artifacts/ds004130/train/\")\n\n# Correct\nart = Artifact.open(\"artifacts/ds004130/\")\n</code></pre>"},{"location":"troubleshooting/artifacts/#feature-count-mismatch-after-re-conversion","title":"Feature count mismatch after re-conversion","text":"<p>If you re-run conversion with different parameters and the feature count changes, any model trained on the old artifact will not work with the new one. Always check:</p> <pre><code>old_art = Artifact.open(\"artifacts/ds004130_old/\")\nnew_art = Artifact.open(\"artifacts/ds004130_new/\")\nprint(len(old_art.manifest.feature_names))  # e.g., 491520\nprint(len(new_art.manifest.feature_names))  # must match for model reuse\n</code></pre>"},{"location":"troubleshooting/artifacts/#shard-integrity-check-fails","title":"Shard integrity check fails","text":"<pre><code>IntegrityError: shard-000003.parquet: expected 128 rows, found 127\n</code></pre> <p>A shard was written incompletely. Re-run conversion with <code>overwrite=True</code>:</p> <pre><code>art = ds.convert(output_dir=\"artifacts/ds004130/\", overwrite=True, ...)\n</code></pre>"},{"location":"troubleshooting/artifacts/#sklearn-returns-wrong-shape","title":"sklearn() returns wrong shape","text":"<p>If <code>art.sklearn()</code> returns X with unexpected shape, check the window parameters:</p> <pre><code>print(art.manifest.window.duration_s)     # 30.0 seconds\nprint(art.manifest.feature_names[:3])     # ['Fp1_0', 'Fp1_1', 'Fp1_2']\n# n_features = n_channels \u00d7 (duration_s \u00d7 sampling_rate)\n</code></pre>"},{"location":"troubleshooting/artifacts/#pytorch-dataloader-workers0-required","title":"PyTorch DataLoader: workers=0 required","text":"<p>On some systems, PyTorch DataLoader with <code>num_workers &gt; 0</code> fails when reading Parquet files. This is a multiprocessing / file handle issue.</p> <p>Set <code>num_workers=0</code> as a workaround:</p> <pre><code>loader = DataLoader(train_ds, batch_size=32, num_workers=0)\n</code></pre> <p>For production training, use Zarr format which is more compatible with multiprocessing:</p> <pre><code>art = ds.convert(format=\"zarr\", ...)\n</code></pre>"},{"location":"troubleshooting/artifacts/#related","title":"Related","text":"<ul> <li>Conversion pipeline \u2014 re-run conversion</li> <li>Artifact inspect \u2014 check artifact structure</li> <li>Compare splits \u2014 distribution checks</li> </ul>"},{"location":"troubleshooting/bids/","title":"BIDS Troubleshooting","text":""},{"location":"troubleshooting/bids/#bids-entity-parsing-returns-none","title":"BIDS entity parsing returns None","text":"<p>If <code>file.subject</code> or <code>file.task</code> is None for a file you expect to have those entities, the file path does not follow BIDS naming conventions.</p> <p>Check the actual path:</p> <pre><code>manifest = ds.manifest()\nfor f in manifest.files:\n    if f.subject is None:\n        print(f.path)\n</code></pre> <p>Files at the root of the dataset (<code>dataset_description.json</code>, <code>participants.tsv</code>, etc.) intentionally have <code>subject=None</code> \u2014 they are dataset-level files.</p> <p>Files with unexpected paths (e.g., <code>extra_files/notes.txt</code>) will also have <code>subject=None</code>.</p>"},{"location":"troubleshooting/bids/#filtering-returns-empty-list","title":"Filtering returns empty list","text":"<pre><code>files = ds.files(subjects=[\"01\"], suffixes=[\"bold\"])\nprint(len(files))  # 0\n</code></pre> <p>Check the actual subject and suffix labels in the manifest:</p> <pre><code>subjects = {f.subject for f in ds.manifest().files if f.subject}\nsuffixes = {f.suffix for f in ds.manifest().files if f.suffix}\nprint(subjects)  # might be {\"sub-01\"} instead of {\"01\"}\nprint(suffixes)\n</code></pre> <p>BIDS subject IDs do not include the <code>sub-</code> prefix. If you see <code>sub-01</code> in the subjects set, the paths are being parsed incorrectly. This indicates a non-standard dataset.</p>"},{"location":"troubleshooting/bids/#bids-validation-fails","title":"BIDS validation fails","text":"<p><code>ds.validate()</code> wraps the official BIDS Validator. If it fails:</p> <ol> <li>Check the error output \u2014 each BIDS violation is listed with the file path and rule code</li> <li>BIDS Validator requires the <code>validation</code> extra: <code>pip install \"qortex[validation]\"</code></li> <li>Common validation issues:</li> <li>Missing <code>dataset_description.json</code> (required at root)</li> <li><code>participants.tsv</code> missing the <code>participant_id</code> column</li> <li>JSON sidecar has <code>TaskName</code> but events.tsv is missing</li> </ol>"},{"location":"troubleshooting/bids/#local-index-skips-files","title":"local-index skips files","text":"<p><code>local-index</code> skips files it cannot parse as BIDS. To see which files are skipped:</p> <pre><code>qortex local-index data/ds004130/ --dataset-id ds004130 --verbose\n</code></pre>"},{"location":"troubleshooting/bids/#related","title":"Related","text":"<ul> <li>BIDS entities \u2014 how Qortex parses entity labels</li> <li>Local index \u2014 building a manifest from a local directory</li> </ul>"},{"location":"troubleshooting/dicom/","title":"DICOM Troubleshooting","text":""},{"location":"troubleshooting/dicom/#importerror-pydicom-not-found","title":"ImportError: pydicom not found","text":"<pre><code>ImportError: DICOM support requires pydicom.\nInstall with: pip install \"qortex[dicom]\"\n</code></pre> <pre><code>pip install \"qortex[dicom]\"\n</code></pre>"},{"location":"troubleshooting/dicom/#compressed-dicom-pixel-data-cannot-be-read","title":"Compressed DICOM: pixel data cannot be read","text":"<pre><code>AttributeError: 'PixelData' / NotImplementedError: Pixel Data with Transfer Syntax ...\n</code></pre> <p>The DICOM files use JPEG or JPEG 2000 compression. pydicom needs an additional decoder:</p> <pre><code>pip install pylibjpeg pylibjpeg-openjpeg\n</code></pre> <p>After installing, pydicom will automatically use the decoder when reading compressed files.</p>"},{"location":"troubleshooting/dicom/#files-not-recognized-as-dicom","title":"Files not recognized as DICOM","text":"<p><code>browse_dicom</code> walks the directory looking for files with valid DICOM headers. Files without a DICOM preamble are skipped. If your DICOM files have no extension (common for older scanners), they should still be recognized.</p> <p>If files are being skipped unexpectedly, check:</p> <pre><code>import pydicom\ntry:\n    ds = pydicom.dcmread(\"path/to/file\")\n    print(ds.Modality)\nexcept Exception as e:\n    print(f\"Not valid DICOM: {e}\")\n</code></pre>"},{"location":"troubleshooting/dicom/#multi-frame-dicom-shows-only-one-slice","title":"Multi-frame DICOM shows only one slice","text":"<p>Enhanced DICOM stores all slices in a single file. <code>browse_dicom</code> handles this by counting <code>NumberOfFrames</code>. If it shows only one slice, the file's <code>NumberOfFrames</code> tag may be missing or incorrect.</p>"},{"location":"troubleshooting/dicom/#segmentation-objects-dicom-seg","title":"Segmentation objects (DICOM-SEG)","text":"<p>DICOM-SEG files are recognized and shown as metadata summaries. The thumbnail shows one label contour on the reference slice, not a rendering of the full segmentation.</p> <p>For full DICOM-SEG rendering, use a dedicated viewer like 3D Slicer.</p>"},{"location":"troubleshooting/downloads/","title":"Download Troubleshooting","text":""},{"location":"troubleshooting/downloads/#http-403-or-410-link-expired","title":"HTTP 403 or 410: link expired","text":"<p>OpenNeuro CDN links expire after a few hours. If you see:</p> <pre><code>DownloadError: HTTP 403 for sub-01_task-rest_bold.nii.gz\n</code></pre> <p>Re-run the download command. Qortex fetches fresh CDN URLs before each download session.</p> <pre><code>ds.download(subjects=[\"01\"], data_dir=\"data/\")\n</code></pre>"},{"location":"troubleshooting/downloads/#files-are-git-lfs-pointers","title":"Files are Git LFS pointers","text":"<p>If a downloaded file is 134 bytes and contains <code>version https://git-lfs.github.com/spec/</code>, it is an LFS pointer, not real data. This happens when a dataset is cloned via DataLad or Git without running <code>git-annex get</code>.</p> <p>Check with content-status:</p> <pre><code>qortex content-status ds004130 --data-dir data/ds004130/\n</code></pre> <p>Fix: 1. If you used DataLad: <code>datalad get data/ds004130/sub-01/</code> 2. If you used Qortex download and got pointers: re-run download with <code>force=True</code></p> <pre><code>ds.download(subjects=[\"01\"], force=True, data_dir=\"data/ds004130/\")\n</code></pre> <p>Some datasets on OpenNeuro have LFS pointer issues at the source. If Qortex downloads return pointers even with <code>force=True</code>, the CDN itself is serving pointers. Report this to OpenNeuro.</p>"},{"location":"troubleshooting/downloads/#incomplete-download-size-mismatch","title":"Incomplete download (size mismatch)","text":"<p>After a download, content-status shows a file is smaller than expected:</p> <pre><code>INCOMPLETE: sub-05/eeg/sub-05_task-rest_eeg.set (312 MB / 450 MB expected)\n</code></pre> <p>The download was interrupted mid-file. Re-run to resume:</p> <pre><code>ds.download(subjects=[\"05\"], data_dir=\"data/ds004130/\")\n</code></pre> <p>Qortex detects the <code>.part</code> file and continues from byte 312 MB.</p>"},{"location":"troubleshooting/downloads/#disk-full-during-download","title":"Disk full during download","text":"<p>If disk fills during a download, partial files are left in place. After freeing space, re-run the download \u2014 partial files resume automatically.</p>"},{"location":"troubleshooting/downloads/#very-slow-downloads","title":"Very slow downloads","text":"<p>OpenNeuro CDN throughput varies by region and time of day. For large datasets (&gt; 50 GB), consider:</p> <ol> <li>Increase concurrency: <code>qortex download --concurrency 8</code></li> <li>Run the download overnight when CDN load is lower</li> <li>Use AWS <code>--region</code> if the CDN is in a specific S3 region near you</li> </ol>"},{"location":"troubleshooting/downloads/#related","title":"Related","text":"<ul> <li>Resume \u2014 how resume works</li> <li>Cache \u2014 where files are stored</li> </ul>"},{"location":"troubleshooting/install/","title":"Install Troubleshooting","text":""},{"location":"troubleshooting/install/#importerror-no-module-named-nibabel","title":"ImportError: No module named 'nibabel'","text":"<p>You are trying to use a visualization or MRI feature without installing the required extra.</p> <pre><code>pip install \"qortex[mri]\"\n</code></pre> <p>The error message always names the missing module and which extra provides it.</p>"},{"location":"troubleshooting/install/#nibabel-version-conflict","title":"nibabel version conflict","text":"<p>If your environment already has nibabel (e.g., from nilearn), you may have a version conflict.</p> <p>Check the version:</p> <pre><code>python -c \"import nibabel; print(nibabel.__version__)\"\n</code></pre> <p>Qortex requires nibabel \u2265 5.0. If you have an older version, upgrade:</p> <pre><code>pip install \"nibabel&gt;=5.0\"\n</code></pre>"},{"location":"troubleshooting/install/#python-version-error","title":"Python version error","text":"<p>Qortex requires Python 3.10 or later. Check your version:</p> <pre><code>python --version\n</code></pre> <p>If you need to maintain a Python 3.9 environment, create a separate conda environment:</p> <pre><code>conda create -n qortex_env python=3.11\nconda activate qortex_env\npip install qortex\n</code></pre>"},{"location":"troubleshooting/install/#pip-install-fails-with-could-not-build-wheels","title":"pip install fails with \"could not build wheels\"","text":"<p>Some extras (dipy, MNE) have C extensions. If the build fails:</p> <pre><code># Install build tools\npip install --upgrade pip setuptools wheel\n\n# Then retry\npip install \"qortex[dwi]\"\n</code></pre> <p>On Linux, you may need system headers:</p> <pre><code>sudo apt-get install python3-dev build-essential\n</code></pre>"},{"location":"troubleshooting/install/#extras-installed-but-still-getting-importerror","title":"Extras installed but still getting ImportError","text":"<p>Python may be loading the wrong environment. Verify which Python is being used:</p> <pre><code>which python\npython -c \"import sys; print(sys.executable)\"\n</code></pre> <p>If Qortex was installed in a different environment, activate the correct one or reinstall.</p>"},{"location":"troubleshooting/labels/","title":"Label Troubleshooting","text":""},{"location":"troubleshooting/labels/#no-eventstsv-found","title":"No events.tsv found","text":"<pre><code>NO_EVENTS: No events.tsv files found in manifest\n</code></pre> <p>The dataset does not have events files. This is expected for datasets without task-based paradigms (resting-state EEG, structural MRI).</p> <p>If you expected events files: 1. Check the manifest: <code>qortex inspect ds004130 --json | jq '.suffixes'</code> 2. Events files may be named differently (e.g., <code>*_stim.tsv</code> instead of <code>*_events.tsv</code>) 3. The BIDS spec requires suffix <code>events</code> \u2014 if the dataset uses a different name, Qortex will not recognize them</p>"},{"location":"troubleshooting/labels/#eventstsv-exists-but-label_col-is-empty","title":"events.tsv exists but label_col is empty","text":"<pre><code>EMPTY_LABEL: trial_type column has no non-null values\n</code></pre> <p>Check the events file directly:</p> <pre><code>events = ds.events(subject=\"01\")\nprint(events.head())\nprint(events[\"trial_type\"].value_counts())\n</code></pre> <p>The <code>trial_type</code> column may be all NaN for some datasets where conditions are encoded differently (e.g., in a numeric <code>stim_id</code> column).</p> <p>Use <code>label_landscape()</code> with a different column:</p> <pre><code>landscape = ds.label_landscape(target_col=\"stim_id\")\n</code></pre>"},{"location":"troubleshooting/labels/#events-file-has-only-one-class","title":"Events file has only one class","text":"<pre><code>ONE_CLASS: trial_type has only 1 unique value: 'rest'\n</code></pre> <p>Resting-state datasets often have a single trial type. For classification, you need multiple classes. Options:</p> <ol> <li>Use a participant-level label from <code>participants.tsv</code> instead (e.g., group, age_group, sex)</li> <li>Use self-supervised learning that does not require event-based labels</li> <li>Compute summary statistics per window and regress against a continuous behavioral measure</li> </ol>"},{"location":"troubleshooting/labels/#label-column-has-mixed-types","title":"Label column has mixed types","text":"<p>If <code>trial_type</code> contains both strings and NaN, pandas may infer a mixed dtype. Qortex handles this by converting NaN to an explicit \"unknown\" class. Check:</p> <pre><code>events = ds.events(subject=\"01\")\nprint(events[\"trial_type\"].dtype)  # should be object (string)\nevents = events.dropna(subset=[\"trial_type\"])\n</code></pre>"},{"location":"troubleshooting/labels/#events-file-not-aligned-with-recording","title":"Events file not aligned with recording","text":"<p>If onset times in events.tsv exceed the recording duration, some events will be skipped during event-aligned windowing. Qortex logs these as warnings:</p> <pre><code>WARNING: 3 events for sub-01 extend past recording end (600.0 s)\n</code></pre> <p>This can happen if the events file was created for a longer version of the recording, or if TR and onset times use different units.</p>"},{"location":"troubleshooting/labels/#related","title":"Related","text":"<ul> <li>Label readiness \u2014 per-subject label coverage</li> <li>Can train \u2014 binary label check</li> <li>EDA \u2014 signal statistics and label analysis</li> </ul>"},{"location":"troubleshooting/openneuro/","title":"OpenNeuro Troubleshooting","text":""},{"location":"troubleshooting/openneuro/#graphql-api-errors","title":"GraphQL API errors","text":"<p><code>APIError: Failed to fetch manifest</code></p> <p>The OpenNeuro GraphQL API is unreachable. Check:</p> <ol> <li>Network connectivity: <code>curl https://openneuro.org/crn/graphql</code></li> <li>OpenNeuro status: https://status.openneuro.org</li> </ol> <p>If the API is up and you still get errors, the dataset ID may not exist:</p> <pre><code>qortex inspect ds999999  # non-existent dataset \u2192 clear error message\n</code></pre> <p><code>APIError: Dataset not found</code></p> <p>The dataset ID does not exist in OpenNeuro. Check the ID at https://openneuro.org/datasets/{dataset_id}.</p> <p><code>APIError: Unauthorized</code></p> <p>The dataset is private or embargoed. Store an API token:</p> <pre><code>qortex login\n# Prompts for token from https://openneuro.org/profile/access-tokens\n</code></pre>"},{"location":"troubleshooting/openneuro/#rate-limiting","title":"Rate limiting","text":"<p>OpenNeuro's GraphQL endpoint has rate limits. If you are making many manifest requests in a loop, spread them out:</p> <pre><code>import time\nfrom qortex import Dataset\n\ndatasets = [\"ds000001\", \"ds000002\", ...]\nreports = []\nfor ds_id in datasets:\n    ds = Dataset(ds_id)\n    reports.append(ds.doctor())\n    time.sleep(1)  # 1 second between requests\n</code></pre>"},{"location":"troubleshooting/openneuro/#snapshot-not-found","title":"Snapshot not found","text":"<pre><code>SnapshotNotFoundError: Snapshot 1.0.0 not found for ds004130\n</code></pre> <p>Use <code>qortex metadata ds004130 --snapshots</code> to list available snapshot versions.</p>"},{"location":"troubleshooting/openneuro/#manifest-caching-issues","title":"Manifest caching issues","text":"<p>If you get stale manifest data, force a refresh:</p> <pre><code>ds = Dataset(\"ds004130\")\nmanifest = ds.manifest(force_refresh=True)\n</code></pre> <p>Or delete the cached manifest:</p> <pre><code>rm ~/.qortex/manifests/ds004130/1.2.0.json\n</code></pre>"},{"location":"troubleshooting/overlays/","title":"Overlay Troubleshooting","text":""},{"location":"troubleshooting/overlays/#affine-mismatch-error","title":"Affine mismatch error","text":"<pre><code>AffineMismatchError: Background and overlay have different affines.\nBackground: [[2.0, 0, 0, -90], ...], Overlay: [[1.0, 0, 0, -90], ...]\n</code></pre> <p>The overlay is in a different voxel space from the background. Qortex does not resample volumes automatically.</p> <p>Fix options:</p> <ol> <li>Resample before overlaying using nilearn or ANTs:</li> </ol> <pre><code>from nilearn.image import resample_to_img\nimport nibabel as nib\n\nbg = nib.load(\"sub-01_T1w.nii.gz\")\noverlay = nib.load(\"sub-01_brain_mask.nii.gz\")\noverlay_resampled = resample_to_img(overlay, bg, interpolation=\"nearest\")\noverlay_resampled.to_filename(\"sub-01_brain_mask_resampled.nii.gz\")\n</code></pre> <ol> <li>Check if the volumes are the same space but with floating-point rounding differences:</li> </ol> <pre><code>import numpy as np\nimport nibabel as nib\n\nbg = nib.load(\"sub-01_T1w.nii.gz\")\nov = nib.load(\"sub-01_brain_mask.nii.gz\")\nprint(np.allclose(bg.affine, ov.affine, atol=1e-3))  # True if nearly identical\n</code></pre> <p>If they are within floating-point tolerance, pass <code>check_affine=False</code>:</p> <pre><code>from qortex.visualize import overlay_mask\nresult = overlay_mask(bg_result, \"mask.nii.gz\", check_affine=False)\n</code></pre>"},{"location":"troubleshooting/overlays/#overlay-appears-misaligned-visually","title":"Overlay appears misaligned visually","text":"<p>If the overlay appears in the wrong position despite no affine error, the files may have different orientations (RAS vs. LAS). Qortex renders center slices in voxel coordinates, not world coordinates.</p> <p>Check the orientation codes:</p> <pre><code>import nibabel as nib\nprint(nib.aff2axcodes(nib.load(\"sub-01_T1w.nii.gz\").affine))\nprint(nib.aff2axcodes(nib.load(\"mask.nii.gz\").affine))\n</code></pre> <p>If they differ (e.g., <code>RAS</code> vs. <code>LAS</code>), reorient before overlaying.</p>"},{"location":"troubleshooting/overlays/#mask-shows-no-visible-overlay","title":"Mask shows no visible overlay","text":"<p>The mask may have all-zero values. Check:</p> <pre><code>import nibabel as nib\nimport numpy as np\nmask = nib.load(\"mask.nii.gz\")\nprint(np.unique(mask.get_fdata()))  # should include non-zero values\nprint(np.sum(mask.get_fdata() &gt; 0))  # number of non-zero voxels\n</code></pre> <p>Also check that the mask's center slice passes through non-zero voxels.</p>"},{"location":"troubleshooting/overlays/#stat-map-threshold-too-high","title":"Stat map threshold too high","text":"<p>If <code>overlay_stat</code> shows nothing, the threshold may be above all values:</p> <pre><code>import nibabel as nib\nimport numpy as np\nstat = nib.load(\"zstat.nii.gz\")\nprint(np.max(np.abs(stat.get_fdata())))  # maximum absolute value\n</code></pre> <p>Lower the threshold:</p> <pre><code>from qortex.visualize import overlay_stat\nresult = overlay_stat(bg, \"zstat.nii.gz\", threshold=1.5)  # lower threshold\n</code></pre>"},{"location":"troubleshooting/visualization/","title":"Visualization Troubleshooting","text":""},{"location":"troubleshooting/visualization/#importerror-when-calling-visualize-functions","title":"ImportError when calling visualize functions","text":"<pre><code>ImportError: nibabel is required for NIfTI visualization.\nInstall with: pip install \"qortex[mri]\"\n</code></pre> <p>Install the appropriate extra:</p> <pre><code>pip install \"qortex[mri]\"       # NIfTI, fMRI, MRI\npip install \"qortex[dwi]\"       # DWI with bval/bvec\npip install \"qortex[eeg]\"       # EEG/MEG time series\npip install \"qortex[visual]\"    # interactive Plotly output\npip install \"qortex[visual-all]\" # everything\n</code></pre>"},{"location":"troubleshooting/visualization/#blank-figures","title":"Blank figures","text":"<p>If <code>result.show()</code> produces a blank figure in Jupyter:</p> <ol> <li>Make sure <code>%matplotlib inline</code> or <code>%matplotlib widget</code> is set</li> <li>For Plotly figures, check that the Plotly renderer is configured:</li> </ol> <pre><code>import plotly.io as pio\npio.renderers.default = \"notebook\"  # or \"browser\"\n</code></pre>"},{"location":"troubleshooting/visualization/#memoryerror-during-fmri-visualization","title":"MemoryError during fMRI visualization","text":"<p>The <code>fmri_summary()</code> function uses Welford's algorithm to compute tSNR in a single pass with constant memory. However, loading a full 4D volume (e.g., for <code>lightbox()</code>) can require several GB.</p> <p>Check how much memory the volume needs:</p> <pre><code>viewer = VolumeViewer(\"sub-01_task-rest_bold.nii.gz\", modality=\"fmri\")\nprint(viewer.shape)           # (91, 109, 91, 200)\nprint(viewer.nbytes_3d)       # bytes for one 3D frame\nprint(viewer.nbytes_total)    # bytes for the full 4D volume\n</code></pre> <p>Use <code>fmri_summary()</code> instead of <code>lightbox()</code> for large files \u2014 it never loads the full volume.</p>"},{"location":"troubleshooting/visualization/#figure-shows-wrong-slice","title":"Figure shows wrong slice","text":"<p>The default center slice is <code>shape[axis] // 2</code>. For an asymmetric brain or a dataset in native space rather than MNI, the center may be in an unexpected location.</p> <p>Pass an explicit slice index:</p> <pre><code>viewer.ortho(z_idx=80)  # axial slice 80\n</code></pre>"},{"location":"troubleshooting/visualization/#nifti-header-error","title":"NIfTI header error","text":"<pre><code>InvalidNIfTIHeaderError: Could not read NIfTI header from ...\n</code></pre> <p>The file may be: - A Git LFS pointer (check with <code>content-status</code>) - Corrupted (check file size vs manifest) - In ANALYZE format (<code>.img</code>/<code>.hdr</code> pair) not supported</p>"},{"location":"troubleshooting/visualization/#related","title":"Related","text":"<ul> <li>Local viewer \u2014 visualize local files</li> <li>fMRI QC \u2014 fMRI-specific QC panels</li> </ul>"},{"location":"tutorials/","title":"Tutorials","text":"<p>Practical, end-to-end workflows using real neuroscience datasets. Each tutorial is self-contained and reproducible.  Install prerequisites once:</p> <pre><code>pip install 'qortex[tutorials]'\n</code></pre> <p>For tutorials that require MONAI (T08):</p> <pre><code>pip install 'qortex[tutorials,monai]'\n</code></pre>"},{"location":"tutorials/#loading-datasets","title":"Loading datasets","text":"<p>Qortex provides Keras-style dataset loaders for the most widely used open neuroscience datasets.  Every loader follows the same three-step pattern:</p> <pre><code>from qortex.datasets import eegbci        # import the dataset module\ncard = eegbci.describe()                  # inspect \u2014 no download\nbundle = eegbci.load_data(subjects=[1])   # download + load (cached)\n</code></pre> Function What it does <code>describe()</code> Returns a <code>DatasetCard</code> with metadata, license, and access instructions <code>load_data(**kwargs)</code> Downloads on first call, returns a typed Bundle <code>bundle.info()</code> Prints a summary of the loaded data <code>bundle.run_qc()</code> Runs signal / image QC via <code>qortex.neuroclassic</code> <p>Bundle types by modality</p> Modality Bundle type Key method EEG / PSG <code>EEGBundle</code> <code>.to_windows(window_s, bandpass)</code> \u2192 <code>(X, y)</code> Structural MRI <code>MRIBundle</code> <code>.load_images()</code> \u2192 list of arrays Task fMRI <code>FMRIBundle</code> <code>.load_events()</code> \u2192 event tables Segmentation <code>SegmentationBundle</code> <code>.load_pair(i)</code> \u2192 <code>(image, mask)</code> <p>Catalogue API</p> <pre><code>import qortex.datasets as qd\n\nqd.list_available()           # list all DatasetCards\nqd.describe(\"eegbci\")         # single DatasetCard\nprint(qd.summary())           # compact table\nqd.load_dataset(\"sleep_edf\", subjects=[0, 1, 2])\n</code></pre>"},{"location":"tutorials/#tutorial-roadmap","title":"Tutorial roadmap","text":"ID Title Dataset Task Difficulty T01 EEG motor imagery classification EEGBCI Classification Beginner T02 EEG connectivity and graph features EEGBCI Connectivity Beginner T03 EEG sleep-stage classification Sleep-EDF Classification Intermediate T04 EEG seizure event detection CHB-MIT Binary classification Intermediate T05 MRI dementia research baseline OASIS-1 Classification + confounds Intermediate T06 MRI age regression / sex QC IXI Regression + classification Intermediate T07 fMRI event and design readiness ds000001 Validation only Beginner T08 Brain tumour segmentation baseline MSD Brain Segmentation Advanced"},{"location":"tutorials/#validation-gates","title":"Validation gates","text":"<p>Every tutorial enforces dataset-specific validation before any model training. These gates prevent common mistakes \u2014 leakage, annotation mismatch, class imbalance \u2014 and produce a machine-readable artifact contract.</p> <p>Shared gates (all tutorials)</p> <ul> <li>File existence and loadability check</li> <li>Sampling rate / voxel size verification</li> <li>Subject-level leakage-safe split via <code>qortex.neuroclassic.assign_leakage_safe_splits</code></li> </ul> <p>Per-tutorial gates \u2014 see individual tutorial pages.</p>"},{"location":"tutorials/#data-licensing-summary","title":"Data licensing summary","text":"Dataset License Registration EEGBCI ODbL v1.0 (PhysioNet) None Sleep-EDF ODbL v1.0 (PhysioNet) None CHB-MIT ODbL v1.0 (PhysioNet) None \u2014 research only OASIS-1 CC BY-NC-SA 3.0 Required (sites.wustl.edu) IXI CC BY-SA 3.0 None ds000001 PDDL 1.0 None MSD Brain CC BY-SA 4.0 None <p>Always review the original license before redistributing derived datasets or trained models.</p>"},{"location":"tutorials/t01-eeg-motor-imagery/","title":"T01 \u2014 EEG Motor Imagery Classification","text":"<p>Dataset: PhysioNet EEGBCI (109 subjects, 64 ch, 160 Hz) Task: Left vs right hand motor imagery (binary) \u2014 or 3-class with rest First model: Bandpower + Logistic Regression / SVM Later model: Braindecode EEGNet Difficulty: Beginner</p>"},{"location":"tutorials/t01-eeg-motor-imagery/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn]'\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-1-load-data","title":"Step 1 \u2014 Load data","text":"<pre><code>from qortex.datasets import eegbci\n\ncard = eegbci.describe()\nprint(card)\n\nbundle = eegbci.load_data(\n    subjects=[1, 2, 3],\n    runs=[4, 8, 12],    # left/right fist imagery runs\n    preload=True,\n)\nbundle.info()\n# EEGBundle: PhysioNet EEG Motor Movement/Imagery Dataset\n#   Subjects     : [1, 2, 3]\n#   Runs         : [4, 8, 12]\n#   Channels     : 64\n#   Sampling Hz  : 160.0\n#   Label map    : {0: 'rest', 1: 'left_fist_imagery', 2: 'right_fist_imagery'}\n</code></pre> <p>Label mapping (from PhysioNet documentation)</p> <p>Runs 4, 8, 12 \u2014 Imagine opening/closing left or right fist:</p> Annotation Class Label T0 Rest 0 T1 Left fist imagery 1 T2 Right fist imagery 2"},{"location":"tutorials/t01-eeg-motor-imagery/#step-2-signal-qc","title":"Step 2 \u2014 Signal QC","text":"<p>Always check data quality before feature extraction.</p> <pre><code>qc_reports = bundle.run_qc(max_files=3)\nfor r in qc_reports:\n    print(f\"{r.scope}: flatline={r.n_flatline}, nan={r.n_nan}, \"\n          f\"clipped={r.n_clipped}, warnings={r.warnings}\")\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-3-validation-gates","title":"Step 3 \u2014 Validation gates","text":"<pre><code># Gate 1: sampling rate\nassert bundle.sfreq == 160.0, f\"Unexpected sfreq: {bundle.sfreq}\"\n\n# Gate 2: channel count\nassert bundle.n_channels == 64, f\"Expected 64 channels, got {bundle.n_channels}\"\n\n# Gate 3: at least one raw file loaded\nassert bundle.n_files &gt; 0, \"No EDF files loaded \u2014 check MNE data dir\"\nprint(f\"\u2713 Loaded {bundle.n_files} files across {len(bundle.subjects)} subjects\")\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-4-window-extraction","title":"Step 4 \u2014 Window extraction","text":"<pre><code>import numpy as np\n\nX, y = bundle.to_windows(\n    window_s=4.0,\n    bandpass=(8.0, 30.0),   # mu + beta band\n    event_driven=True,\n    tmin=0.5,               # discard 500 ms after cue onset\n)\nprint(f\"X shape: {X.shape}\")  # (n_epochs, 64, 640)\nprint(f\"y distribution: { {k: int((y==k).sum()) for k in np.unique(y)} }\")\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-5-leakage-safe-split","title":"Step 5 \u2014 Leakage-safe split","text":"<pre><code>from qortex.neuroclassic import assign_leakage_safe_splits, SplitConstraints\n\n# Build a per-epoch metadata table with subject group column\nrows = [\n    {\"id\": f\"ep{i}\", \"subject\": bundle.subjects[i % len(bundle.subjects)]}\n    for i in range(len(y))\n]\nresult = assign_leakage_safe_splits(\n    rows,\n    id_column=\"id\",\n    constraints=SplitConstraints(\n        group_columns=[\"subject\"],      # never split a subject across train/test\n        train_fraction=0.7,\n        val_fraction=0.15,\n        test_fraction=0.15,\n    ),\n)\nprint(result.optimality_status, result.residual_imbalance)\n\ntrain_ids = {k for k, v in result.assignments.items() if v == \"train\"}\nval_ids   = {k for k, v in result.assignments.items() if v == \"val\"}\ntest_ids  = {k for k, v in result.assignments.items() if v == \"test\"}\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-6-feature-extraction-bandpower","title":"Step 6 \u2014 Feature extraction (bandpower)","text":"<pre><code>from scipy.signal import welch\n\ndef bandpower(epoch, sfreq, band):\n    \"\"\"Mean PSD in band [low, high] Hz for one epoch [n_ch, n_times].\"\"\"\n    f, psd = welch(epoch, sfreq, nperseg=min(256, epoch.shape[1]))\n    mask = (f &gt;= band[0]) &amp; (f &lt;= band[1])\n    return psd[:, mask].mean(axis=1)  # [n_ch]\n\nbands = {\"delta\": (1, 4), \"theta\": (4, 8), \"alpha\": (8, 13), \"beta\": (13, 30)}\n\ndef extract_features(X, sfreq=160.0):\n    feats = []\n    for epoch in X:\n        row = np.concatenate([bandpower(epoch, sfreq, b) for b in bands.values()])\n        feats.append(row)\n    return np.array(feats, dtype=np.float32)\n\nX_feats = extract_features(X)\nprint(f\"Feature matrix: {X_feats.shape}\")  # (n_epochs, 64*4)\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-7-train-and-evaluate","title":"Step 7 \u2014 Train and evaluate","text":"<pre><code>from sklearn.preprocessing import StandardScaler\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import classification_report\n\n# Map split IDs to indices\nid_list = [f\"ep{i}\" for i in range(len(y))]\ntr_idx = [i for i, id_ in enumerate(id_list) if id_ in train_ids]\nva_idx = [i for i, id_ in enumerate(id_list) if id_ in val_ids]\nte_idx = [i for i, id_ in enumerate(id_list) if id_ in test_ids]\n\nscaler = StandardScaler()\nX_tr = scaler.fit_transform(X_feats[tr_idx])\nX_te = scaler.transform(X_feats[te_idx])\ny_tr, y_te = y[tr_idx], y[te_idx]\n\nclf = LogisticRegression(max_iter=1000, C=1.0, solver=\"lbfgs\", multi_class=\"auto\")\nclf.fit(X_tr, y_tr)\n\ny_pred = clf.predict(X_te)\ntarget_names = [bundle.label_map[k] for k in sorted(bundle.label_map)]\nprint(classification_report(y_te, y_pred, target_names=target_names))\n</code></pre>"},{"location":"tutorials/t01-eeg-motor-imagery/#step-8-next-steps","title":"Step 8 \u2014 Next steps","text":"<ul> <li>Replace bandpower with Common Spatial Patterns (CSP) from MNE or scikit-learn:   <pre><code>from mne.decoding import CSP\ncsp = CSP(n_components=8, reg=None, log=True)\nX_csp = csp.fit_transform(X[tr_idx], y[tr_idx])\n</code></pre></li> <li>Scale up to all 109 subjects with more runs.</li> <li>Try Braindecode EEGNet (requires <code>pip install 'qortex[braindecode]'</code>):   <pre><code>from braindecode.models import EEGNetv4\n</code></pre></li> </ul>"},{"location":"tutorials/t01-eeg-motor-imagery/#validation-summary","title":"Validation summary","text":"Gate Check Status EDF loadability MNE reads all EDF files without error Enforced in Step 2 Sampling rate 160.0 Hz Enforced in Step 3 Channel count 64 Enforced in Step 3 Annotation mapping T0/T1/T2 per-run mapping applied <code>eegbci.load_data()</code> Subject-safe split No subject spans train and test Step 5 Bandpass filter 8\u201330 Hz applied before feature extraction Step 4"},{"location":"tutorials/t02-eeg-connectivity/","title":"T02 \u2014 EEG Connectivity and Graph Features","text":"<p>Dataset: PhysioNet EEGBCI (same data as T01) Task: Eyes-open vs eyes-closed resting state; optional motor-imagery condition comparison First model: Connectivity graph metrics + Logistic Regression / Random Forest Later model: Graph kernel or shallow MLP after graph API stabilises Difficulty: Beginner</p>"},{"location":"tutorials/t02-eeg-connectivity/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn]'\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-1-load-resting-state-baseline","title":"Step 1 \u2014 Load resting-state baseline","text":"<pre><code>from qortex.datasets import eegbci\n\n# Runs 1 = eyes open, 2 = eyes closed (baseline, no task events)\nbundle = eegbci.load_data(\n    subjects=[1, 2, 3, 4, 5],\n    runs=[1, 2],\n    preload=True,\n)\nbundle.info()\n# label_map: {1: 'eyes_open', 2: 'eyes_closed'}\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-2-extract-epochs-sliding-windows","title":"Step 2 \u2014 Extract epochs (sliding windows)","text":"<pre><code>import numpy as np\n\nX, y = bundle.to_windows(\n    window_s=4.0,\n    bandpass=(8.0, 13.0),   # alpha band\n    event_driven=False,      # sliding windows for resting state\n    overlap=0.5,\n)\nprint(f\"X: {X.shape}, y: {y.shape}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-3-compute-pearson-connectivity-matrix","title":"Step 3 \u2014 Compute Pearson connectivity matrix","text":"<pre><code>from qortex.neuroclassic import compute_pearson_connectivity, ConnectivitySpec\n\nspec = ConnectivitySpec(\n    method=\"pearson\",\n    threshold_type=\"absolute\",\n    threshold_value=0.3,\n)\n\n# Average connectivity across all training epochs\nX_mean = X.mean(axis=0)                     # [64, 640]\nconn = compute_pearson_connectivity(\n    X_mean[np.newaxis, :, :],               # [1, 64, 640]\n    spec=spec,\n)\nprint(f\"Connectivity matrix: {conn.matrix.shape}\")  # (64, 64)\nprint(f\"Density: {conn.density:.3f}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-4-compute-graph-metrics","title":"Step 4 \u2014 Compute graph metrics","text":"<pre><code>from qortex.neuroclassic import compute_graph_metrics\n\ngraph_report = compute_graph_metrics(conn)\n\nprint(f\"Clustering coefficient : {graph_report.clustering_coefficient:.4f}\")\nprint(f\"Mean path length       : {graph_report.mean_path_length}\")\nprint(f\"Small-world \u03c3          : {graph_report.small_world_sigma}\")\nprint(f\"Modularity Q           : {graph_report.modularity}\")\nprint(f\"Communities            : {set(graph_report.community_assignments)}\")\nprint(f\"Betweenness (top 5)    : {sorted(graph_report.betweenness_centrality, reverse=True)[:5]}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-5-build-per-epoch-feature-vectors","title":"Step 5 \u2014 Build per-epoch feature vectors","text":"<pre><code>def epoch_graph_features(epoch, spec, ch_names):\n    \"\"\"Return graph metric feature vector for one epoch.\"\"\"\n    conn = compute_pearson_connectivity(\n        epoch[np.newaxis, :, :],\n        spec=spec,\n    )\n    gr = compute_graph_metrics(conn)\n    bc = gr.betweenness_centrality or [0.0] * len(ch_names)\n    comm = gr.community_assignments or [0] * len(ch_names)\n    feats = [\n        gr.clustering_coefficient or 0.0,\n        gr.mean_path_length or 0.0,\n        gr.small_world_sigma or 0.0,\n        gr.modularity or 0.0,\n        conn.density,\n        float(max(set(comm), key=comm.count)),   # dominant community\n        float(max(bc)),                           # hub centrality\n    ]\n    return np.array(feats, dtype=np.float32)\n\nch_names = bundle.channel_names\nX_graph = np.array([epoch_graph_features(ep, spec, ch_names) for ep in X])\nprint(f\"Graph feature matrix: {X_graph.shape}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-6-classify-eyes-open-vs-eyes-closed","title":"Step 6 \u2014 Classify eyes-open vs eyes-closed","text":"<pre><code>from sklearn.ensemble import RandomForestClassifier\nfrom sklearn.model_selection import StratifiedKFold, cross_val_score\nfrom sklearn.preprocessing import StandardScaler\nimport numpy as np\n\n# Only use epochs that have valid labels (1=eyes_open, 2=eyes_closed)\nmask = y &gt; 0\nX_valid = X_graph[mask]\ny_valid = y[mask]\n\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X_valid)\n\ncv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)\nscores = cross_val_score(\n    RandomForestClassifier(n_estimators=100, random_state=42),\n    X_scaled, y_valid, cv=cv, scoring=\"f1_macro\",\n)\nprint(f\"CV macro-F1: {scores.mean():.3f} \u00b1 {scores.std():.3f}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-7-condition-comparison-plot-alpha-power","title":"Step 7 \u2014 Condition comparison plot (alpha power)","text":"<pre><code># Compare connectivity density between conditions\nfrom qortex.neuroclassic import compute_pearson_connectivity, ConnectivitySpec\n\ndensity_by_condition = {}\nfor label, name in bundle.label_map.items():\n    mask = y == label\n    if mask.sum() == 0:\n        continue\n    X_cond = X[mask].mean(axis=0)\n    c = compute_pearson_connectivity(X_cond[np.newaxis], spec=spec)\n    density_by_condition[name] = c.density\n    print(f\"{name}: density={c.density:.3f}\")\n</code></pre>"},{"location":"tutorials/t02-eeg-connectivity/#step-8-next-steps","title":"Step 8 \u2014 Next steps","text":"<ul> <li>Use full sensor-level adjacency from <code>graph_report.betweenness_centrality</code> to rank hub electrodes.</li> <li>Run on motor imagery runs (4, 8, 12) to compare task vs baseline graph topology.</li> <li>Feed graph features into a graph neural network with PyTorch Geometric.</li> </ul>"},{"location":"tutorials/t02-eeg-connectivity/#validation-summary","title":"Validation summary","text":"Gate Check Channel sets match All files have 64 channels (enforced in <code>eegbci.load_data</code>) Band explicitly set 8\u201313 Hz alpha passed to <code>to_windows()</code> Threshold explicit Absolute 0.3 passed to <code>ConnectivitySpec</code> Graph construction logged <code>GraphMetricReport</code> carries all computed values Condition labels Run 1 = eyes_open, run 2 = eyes_closed (set in <code>label_map</code>)"},{"location":"tutorials/t03-eeg-sleep-staging/","title":"T03 \u2014 EEG Sleep-Stage Classification","text":"<p>Dataset: Sleep-EDF Expanded (197 PSG recordings) Task: 5-class sleep staging: Wake / N1 / N2 / N3 / REM First model: PSD bandpower + Random Forest Later model: 1D CNN or Braindecode sleep model Difficulty: Intermediate</p>"},{"location":"tutorials/t03-eeg-sleep-staging/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn]'\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#step-1-load-data","title":"Step 1 \u2014 Load data","text":"<pre><code>from qortex.datasets import sleep_edf\n\ncard = sleep_edf.describe()\nprint(card)\n\nbundle = sleep_edf.load_data(\n    subjects=[0, 1, 2, 3],\n    recording=\"SC\",          # Cassette recordings (sleep at home)\n    crop_wake_mins=30,       # trim leading/trailing wake to reduce imbalance\n    preload=True,\n)\nbundle.info()\n</code></pre> <p>Label mapping (AASM-aligned)</p> Annotation Hypnogram code Class Label Wake <code>W</code> 0 Wake Stage 1 <code>1</code> 1 N1 Stage 2 <code>2</code> 2 N2 Stage 3 / 4 <code>3</code> / <code>4</code> 3 N3 REM <code>R</code> 4 REM Movement <code>M</code> \u2014 excluded Unknown <code>?</code> \u2014 excluded"},{"location":"tutorials/t03-eeg-sleep-staging/#step-2-validation-gates","title":"Step 2 \u2014 Validation gates","text":"<pre><code>for raw in bundle.raws:\n    assert raw.info[\"sfreq\"] == 100.0, f\"Unexpected sfreq: {raw.info['sfreq']}\"\n    n_ann = len(raw.annotations)\n    assert n_ann &gt; 0, f\"No annotations in {raw.filenames[0]}\"\n    # Check no excluded annotations leaked through\n    bad = [a for a in raw.annotations.description if a in {\"M\", \"?\", \"Sleep stage M\", \"Sleep stage ?\"}]\n    assert not bad, f\"Excluded annotations present: {bad}\"\n\nprint(f\"\u2713 {len(bundle.raws)} PSG files validated\")\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#step-3-extract-30-second-epochs","title":"Step 3 \u2014 Extract 30-second epochs","text":"<p>Standard sleep staging uses 30-second non-overlapping windows.</p> <pre><code>import numpy as np\n\nX, y = bundle.to_windows(\n    window_s=30.0,\n    event_driven=False,       # sliding, no overlap\n    overlap=0.0,\n)\nprint(f\"X: {X.shape}\")        # (n_epochs, n_channels, 3000)\n\n# Class distribution\nunique, counts = np.unique(y, return_counts=True)\nfor u, c in zip(unique, counts):\n    name = bundle.label_map.get(int(u), str(u))\n    print(f\"  {name}: {c} ({100*c/len(y):.1f}%)\")\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#step-4-feature-extraction","title":"Step 4 \u2014 Feature extraction","text":"<pre><code>from scipy.signal import welch\n\nBANDS = {\n    \"delta\": (0.5, 4.0),\n    \"theta\": (4.0, 8.0),\n    \"alpha\": (8.0, 13.0),\n    \"sigma\": (12.0, 16.0),   # sleep spindles\n    \"beta\":  (16.0, 30.0),\n}\n\ndef extract_sleep_features(X, sfreq=100.0):\n    \"\"\"Bandpower + statistical features per epoch.\"\"\"\n    features = []\n    for epoch in X:\n        row = []\n        for ch in epoch:\n            f, psd = welch(ch, sfreq, nperseg=min(512, len(ch)))\n            for lo, hi in BANDS.values():\n                mask = (f &gt;= lo) &amp; (f &lt;= hi)\n                row.append(float(psd[mask].mean()) if mask.any() else 0.0)\n            # Time-domain features\n            row.extend([float(ch.mean()), float(ch.std()), float(np.abs(ch).max())])\n        features.append(row)\n    return np.array(features, dtype=np.float32)\n\nX_feats = extract_sleep_features(X, sfreq=bundle.sfreq)\nprint(f\"Feature matrix: {X_feats.shape}\")\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#step-5-subject-held-out-split","title":"Step 5 \u2014 Subject-held-out split","text":"<p>Sleep staging must use subject-level splits to prevent leakage.</p> <pre><code>from qortex.neuroclassic import assign_leakage_safe_splits, SplitConstraints\n\n# Assign epoch-to-subject (approximation: epochs from file i \u2192 subject i//files_per_subject)\nn_epochs = len(y)\nfiles_per_subject = max(1, len(bundle.raws) // len(bundle.subjects))\nepoch_subjects = [bundle.subjects[min(i // (n_epochs // max(len(bundle.subjects), 1)),\n                                     len(bundle.subjects)-1)]\n                  for i in range(n_epochs)]\n\nrows = [{\"id\": str(i), \"subject\": str(s)} for i, s in enumerate(epoch_subjects)]\nresult = assign_leakage_safe_splits(\n    rows,\n    id_column=\"id\",\n    constraints=SplitConstraints(group_columns=[\"subject\"]),\n)\nprint(result.optimality_status)\n\ntr_idx = [int(k) for k, v in result.assignments.items() if v == \"train\"]\nte_idx = [int(k) for k, v in result.assignments.items() if v == \"test\"]\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#step-6-train-and-evaluate","title":"Step 6 \u2014 Train and evaluate","text":"<pre><code>from sklearn.ensemble import GradientBoostingClassifier\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.metrics import classification_report, cohen_kappa_score\n\nscaler = StandardScaler()\nX_tr = scaler.fit_transform(X_feats[tr_idx])\nX_te = scaler.transform(X_feats[te_idx])\ny_tr, y_te = y[tr_idx], y[te_idx]\n\nclf = GradientBoostingClassifier(n_estimators=200, max_depth=5, random_state=42)\nclf.fit(X_tr, y_tr)\n\ny_pred = clf.predict(X_te)\ntarget_names = [bundle.label_map[k] for k in sorted(bundle.label_map)]\nprint(classification_report(y_te, y_pred, target_names=target_names))\nprint(f\"Cohen \u03ba: {cohen_kappa_score(y_te, y_pred):.3f}\")\n</code></pre>"},{"location":"tutorials/t03-eeg-sleep-staging/#validation-summary","title":"Validation summary","text":"Gate Check PSG + hypnogram pair Both files exist per subject Annotation coverage <code>n_ann &gt; 0</code> for every recording 30 s alignment <code>window_s=30.0, overlap=0.0</code> Excluded annotations M and ? filtered in <code>sleep_edf.load_data()</code> Subject-held-out split <code>group_columns=[\"subject\"]</code> in split optimizer Class distribution printed Step 3"},{"location":"tutorials/t04-eeg-seizure-detection/","title":"T04 \u2014 EEG Seizure Event Detection","text":"<p>Dataset: CHB-MIT Scalp EEG Seizure Database (23 cases, 42.6 GB) Task: Binary window classification \u2014 seizure vs non-seizure First model: Sliding-window features + Random Forest / XGBoost Later model: 1D CNN / temporal CNN Difficulty: Intermediate Research only \u2014 not for clinical use.</p>"},{"location":"tutorials/t04-eeg-seizure-detection/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn]'\n# Optionally for gradient boosting:\npip install xgboost\n</code></pre> <p>Download: CHB-MIT must be downloaded manually from PhysioNet. The full dataset is ~42.6 GB.  Start with <code>chb01</code> only (~2.3 GB).</p> <pre><code>https://physionet.org/content/chbmit/1.0.0/\n</code></pre> <p>Download <code>chb01/</code> and place it at <code>/data/chbmit/chb01/</code>.</p>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-1-load-data","title":"Step 1 \u2014 Load data","text":"<pre><code>from pathlib import Path\nfrom qortex.datasets import chbmit\n\ncard = chbmit.describe()\nprint(card.access_instructions)\n\nbundle = chbmit.load_data(\n    cases=[\"chb01\"],\n    seizure_files_only=True,    # only EDF files with seizures\n    local_root=Path(\"/data/chbmit\"),\n    preload=True,\n)\nbundle.info()\n# EEGBundle: CHB-MIT Scalp EEG Seizure Database\n#   Files    : &lt;n seizure files in chb01&gt;\n#   Channels : 23\n#   Sfreq    : 256.0 Hz\n</code></pre>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-2-validation-gates","title":"Step 2 \u2014 Validation gates","text":"<pre><code>assert bundle.sfreq == 256.0, f\"Unexpected sfreq: {bundle.sfreq}\"\nassert bundle.n_files &gt; 0, \"No EDF files found in chb01\"\n\nseizure_map = bundle.metadata.get(\"seizure_map\", {})\ntotal_seizures = sum(len(v) for v in seizure_map.values())\nprint(f\"\u2713 {bundle.n_files} seizure-containing files, {total_seizures} seizure intervals\")\n\n# Confirm seizure intervals are non-zero duration\nfor fname, intervals in seizure_map.items():\n    for iv in intervals:\n        assert iv.duration_sec &gt; 0, f\"Zero-duration seizure in {fname}\"\n</code></pre>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-3-label-windows","title":"Step 3 \u2014 Label windows","text":"<pre><code>import numpy as np\nfrom qortex.datasets.chbmit import label_windows_for_file\n\nwindow_s = 5.0\nstep_s   = 1.0            # aggressive overlap to capture transitions\n\nall_windows: list[np.ndarray] = []\nall_labels: list[int] = []\n\nfor i, (raw, path) in enumerate(zip(bundle.raws, bundle.local_paths)):\n    data = raw.get_data().astype(np.float32)   # [23, n_times]\n    n_samples = data.shape[1]\n    fname = path.name\n\n    windows, labels = label_windows_for_file(\n        fname,\n        n_samples=n_samples,\n        sfreq=bundle.sfreq,\n        window_s=window_s,\n        step_s=step_s,\n        seizure_map=seizure_map,\n    )\n    for (start, end), label in zip(windows, labels):\n        all_windows.append(data[:, start:end])\n        all_labels.append(label)\n\nX = np.stack(all_windows, axis=0)   # [n_windows, 23, 1280]\ny = np.array(all_labels, dtype=np.int64)\n\nn_seizure = int(y.sum())\nn_total   = len(y)\nprint(f\"Total windows   : {n_total}\")\nprint(f\"Seizure windows : {n_seizure} ({100*n_seizure/n_total:.2f}%)\")\nprint(f\"Class ratio     : 1 : {(n_total-n_seizure)//max(n_seizure,1)}\")\n</code></pre> <p>Expect severe imbalance (~1 : 50 or worse). Handle it explicitly.</p>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-4-feature-extraction","title":"Step 4 \u2014 Feature extraction","text":"<pre><code>from scipy.signal import welch\n\nBANDS = {\n    \"delta\": (0.5, 4),\n    \"theta\": (4, 8),\n    \"alpha\": (8, 13),\n    \"beta\":  (13, 30),\n    \"gamma\": (30, 80),\n    \"hfo\":   (80, 120),   # high-frequency oscillations\n}\n\ndef extract_seizure_features(X, sfreq=256.0):\n    features = []\n    for epoch in X:\n        row = []\n        for ch in epoch:\n            f, psd = welch(ch, sfreq, nperseg=min(512, len(ch)))\n            for lo, hi in BANDS.values():\n                mask = (f &gt;= lo) &amp; (f &lt;= hi)\n                row.append(float(psd[mask].mean()) if mask.any() else 0.0)\n            row.extend([float(ch.mean()), float(ch.std()), float(np.abs(ch).max())])\n        features.append(row)\n    return np.array(features, dtype=np.float32)\n\nX_feats = extract_seizure_features(X, sfreq=bundle.sfreq)\nprint(f\"Feature matrix: {X_feats.shape}\")\n</code></pre>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-5-file-level-split-no-same-file-across-traintest","title":"Step 5 \u2014 File-level split (no same-file across train/test)","text":"<pre><code>from qortex.neuroclassic import assign_leakage_safe_splits, SplitConstraints\n\n# Build rows with file_id group\nfile_ids = []\nfor i, path in enumerate(bundle.local_paths):\n    n = len(all_windows) // len(bundle.local_paths)  # approx equal windows per file\n    file_ids.extend([path.stem] * n)\nfile_ids = file_ids[:len(y)]\n\nrows = [{\"id\": str(i), \"file\": fid} for i, fid in enumerate(file_ids)]\nresult = assign_leakage_safe_splits(\n    rows, id_column=\"id\",\n    constraints=SplitConstraints(group_columns=[\"file\"]),\n)\n\ntr_idx = [int(k) for k, v in result.assignments.items() if v == \"train\"]\nte_idx = [int(k) for k, v in result.assignments.items() if v == \"test\"]\n</code></pre>"},{"location":"tutorials/t04-eeg-seizure-detection/#step-6-train-with-class-weighting","title":"Step 6 \u2014 Train with class weighting","text":"<pre><code>from sklearn.ensemble import RandomForestClassifier\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.metrics import classification_report, confusion_matrix\n\nscaler = StandardScaler()\nX_tr = scaler.fit_transform(X_feats[tr_idx])\nX_te = scaler.transform(X_feats[te_idx])\ny_tr, y_te = y[tr_idx], y[te_idx]\n\nclf = RandomForestClassifier(\n    n_estimators=300,\n    class_weight=\"balanced\",   # handles severe imbalance\n    random_state=42,\n    n_jobs=-1,\n)\nclf.fit(X_tr, y_tr)\n\ny_pred = clf.predict(X_te)\nprint(classification_report(y_te, y_pred, target_names=[\"non_seizure\", \"seizure\"]))\n\n# False positive rate per hour (clinical metric)\ntn, fp, fn, tp = confusion_matrix(y_te, y_pred).ravel()\ntest_hours = (len(te_idx) * window_s) / 3600.0\nfpr_per_hour = fp / max(test_hours, 1e-6)\nprint(f\"False positives per hour: {fpr_per_hour:.2f}\")\n</code></pre>"},{"location":"tutorials/t04-eeg-seizure-detection/#validation-summary","title":"Validation summary","text":"Gate Check EDF loadability MNE reads all EDF files Seizure interval parse <code>chb01-summary.txt</code> parsed before windowing No cross-file leakage <code>group_columns=[\"file\"]</code> in split optimizer Class imbalance Printed and <code>class_weight=\"balanced\"</code> used FPR/hour reported Clinical-relevant metric in Step 6 Research-only disclaimer Dataset card + comment in code"},{"location":"tutorials/t05-mri-dementia-baseline/","title":"T05 \u2014 MRI Dementia Research Baseline","text":"<p>Dataset: OASIS-1 (416 subjects, T1 MRI + clinical CSV) Task: CDR=0 vs CDR&gt;0 (research label; not a clinical diagnostic tool) First model: Logistic Regression / SVM on clinical + image QC features Later model: Subject-level 3D CNN (only after leakage/confound enforcement) Difficulty: Intermediate Not for clinical diagnosis. Research purposes only.</p>"},{"location":"tutorials/t05-mri-dementia-baseline/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn,mri]'\n</code></pre> <p>Registration required: Register at https://sites.wustl.edu/oasisbrains/home/oasis-1/ Download the T1 archives and <code>oasis_cross-sectional.csv</code>. Extract to <code>/data/oasis1/</code>.</p>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-1-load-data","title":"Step 1 \u2014 Load data","text":"<pre><code>from pathlib import Path\nfrom qortex.datasets import oasis1\n\ncard = oasis1.describe()\nprint(card.access_instructions)\n\nbundle = oasis1.load_data(\n    local_root=Path(\"/data/oasis1\"),\n    cdr_binary=True,              # CDR=0 \u2192 0; CDR&gt;0 \u2192 1\n    exclude_missing_cdr=True,\n)\nbundle.info()\n# label_map: {0: 'no_dementia', 1: 'dementia'}\n</code></pre>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-2-confound-report","title":"Step 2 \u2014 Confound report","text":"<p>Age, sex, and MMSE are covariates \u2014 not hidden leakage. Run the confound report to understand their association with CDR.</p> <pre><code>import numpy as np\nfrom qortex.neuroclassic import compute_statistical_diagnostics\n\n# Build a flat row table for the statistical diagnostics module\nsubjects = bundle.subjects\nlabels = bundle.labels  # CDR binary\n\nrows = []\nfor sid in subjects:\n    meta = bundle.metadata.get(sid, {})\n    rows.append({\n        \"subject\": sid,\n        \"cdr_binary\": meta.get(\"cdr_binary\"),\n        \"Age\": meta.get(\"Age\"),\n        \"M/F\": meta.get(\"M/F\"),\n        \"MMSE\": meta.get(\"MMSE\"),\n        \"nWBV\": meta.get(\"nWBV\"),   # normalized whole brain volume\n        \"eTIV\": meta.get(\"eTIV\"),\n    })\n\ndiag = compute_statistical_diagnostics(\n    rows,\n    target_column=\"cdr_binary\",\n    covariate_columns=[\"Age\", \"M/F\", \"MMSE\", \"nWBV\", \"eTIV\"],\n)\nfor assoc in diag.confound_associations:\n    print(f\"{assoc.variable:12s}: stat={assoc.statistic:.3f}, p={assoc.p_value:.4f}\")\n</code></pre> <p>Expected: Age and nWBV will show significant association with CDR. Document this \u2014 do not remove these variables silently.</p>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-3-subject-level-split","title":"Step 3 \u2014 Subject-level split","text":"<pre><code>from qortex.neuroclassic import assign_leakage_safe_splits, SplitConstraints\n\nrows_split = [\n    {\"id\": sid, \"cdr\": str(bundle.metadata[sid].get(\"cdr_binary\", -1))}\n    for sid in bundle.subjects\n]\nresult = assign_leakage_safe_splits(\n    rows_split,\n    id_column=\"id\",\n    constraints=SplitConstraints(\n        stratify_column=\"cdr\",   # balance classes across splits\n        train_fraction=0.7,\n        val_fraction=0.15,\n        test_fraction=0.15,\n    ),\n)\nprint(result.optimality_status)\nprint(result.class_distribution)\n\ntrain_subs = {k for k, v in result.assignments.items() if v == \"train\"}\ntest_subs  = {k for k, v in result.assignments.items() if v == \"test\"}\n</code></pre>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-4-image-qc","title":"Step 4 \u2014 Image QC","text":"<pre><code># Load NIfTI images for QC (first 10 training subjects)\ntrain_paths = [p for s, p in zip(bundle.subjects, bundle.local_paths) if s in train_subs]\nbundle_sub = bundle   # reuse the same bundle, just check a subset\n\nqc_reports = bundle.run_qc(max_subjects=10)\nfor rpt in qc_reports:\n    flag = \"\u26a0 outlier\" if rpt.is_outlier else \"\u2713\"\n    print(f\"{rpt.scope}: SNR={rpt.snr:.1f}, CNR={rpt.cnr:.1f} {flag}\")\n</code></pre>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-5-build-tabular-feature-matrix","title":"Step 5 \u2014 Build tabular feature matrix","text":"<p>This tutorial uses clinical + image-QC-derived features only. No voxel-level features \u2014 that requires a 3D CNN pipeline.</p> <pre><code>import numpy as np\n\ndef subject_features(sid, meta, qc_map):\n    \"\"\"Tabular features from clinical CSV + image QC.\"\"\"\n    row = []\n    # Clinical covariates (mean-impute missing)\n    row.append(float(meta.get(\"Age\", 70.0)))\n    row.append(1.0 if str(meta.get(\"M/F\", \"M\")) == \"F\" else 0.0)\n    row.append(float(meta.get(\"MMSE\", 27.0)))\n    row.append(float(meta.get(\"nWBV\", 0.73)))\n    row.append(float(meta.get(\"eTIV\", 1500.0)))\n    # Image QC features (if available)\n    qc = qc_map.get(sid)\n    if qc:\n        row.extend([qc.snr, qc.cnr, float(qc.is_outlier)])\n    else:\n        row.extend([0.0, 0.0, 0.0])\n    return row\n\nqc_map = {}  # sid \u2192 ImageQualityReport (populated by run_qc above)\nfor rpt in qc_reports:\n    qc_map[rpt.scope] = rpt\n\nall_subs = bundle.subjects\nX_all = np.array([subject_features(s, bundle.metadata.get(s, {}), qc_map)\n                  for s in all_subs], dtype=np.float32)\ny_all = bundle.labels\n\ntr_idx = [i for i, s in enumerate(all_subs) if s in train_subs]\nte_idx = [i for i, s in enumerate(all_subs) if s in test_subs]\n</code></pre>"},{"location":"tutorials/t05-mri-dementia-baseline/#step-6-train-and-evaluate","title":"Step 6 \u2014 Train and evaluate","text":"<pre><code>from sklearn.linear_model import LogisticRegression\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.metrics import classification_report, roc_auc_score\n\nscaler = StandardScaler()\nX_tr = scaler.fit_transform(X_all[tr_idx])\nX_te = scaler.transform(X_all[te_idx])\ny_tr, y_te = y_all[tr_idx], y_all[te_idx]\n\nclf = LogisticRegression(C=1.0, max_iter=1000, class_weight=\"balanced\", random_state=42)\nclf.fit(X_tr, y_tr)\n\ny_pred = clf.predict(X_te)\ny_prob = clf.predict_proba(X_te)[:, 1]\n\nprint(classification_report(y_te, y_pred, target_names=[\"no_dementia\", \"dementia\"]))\nprint(f\"AUROC: {roc_auc_score(y_te, y_prob):.3f}\")\nprint()\nprint(\"\u26a0 These results reflect a research baseline model, not a clinical tool.\")\nprint(\"  CDR is a research label; this model does not diagnose Alzheimer's disease.\")\n</code></pre>"},{"location":"tutorials/t05-mri-dementia-baseline/#validation-summary","title":"Validation summary","text":"Gate Check T1 file found <code>_find_nifti()</code> in <code>oasis1.load_data()</code> Clinical CSV join <code>oasis_cross-sectional.csv</code> parsed before loading CDR binary label CDR=0 \u2192 0; CDR&gt;0 \u2192 1; missing CDR excluded Subject-level split <code>assign_leakage_safe_splits(group_columns=[...])</code> Confound report <code>compute_statistical_diagnostics()</code> in Step 2 Research disclaimer Printed in Step 6 and dataset card"},{"location":"tutorials/t06-mri-age-sex-qc/","title":"T06 \u2014 MRI Age Regression / Sex Classification + QC","text":"<p>Dataset: IXI (~600 healthy subjects, multimodal MRI) Task: Age regression (continuous) and sex classification (binary) First model: Ridge regression for age; Logistic Regression for sex Later model: 3D DenseNet Difficulty: Intermediate</p>"},{"location":"tutorials/t06-mri-age-sex-qc/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,sklearn,mri]'\npip install openpyxl   # for IXI.xls demographic spreadsheet\n</code></pre> <p>Download: No registration required. T1 images: https://brain-development.org/ixi-dataset/ Demographics: download <code>IXI.xls</code> from the same page. Extract T1 NIfTI files to <code>/data/ixi/IXI-T1/</code> and place <code>IXI.xls</code> in <code>/data/ixi/</code>.</p>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-1-inspect-and-load","title":"Step 1 \u2014 Inspect and load","text":"<pre><code>from pathlib import Path\nfrom qortex.datasets import ixi\n\ncard = ixi.describe()\nprint(card)\n\n# Age regression\nbundle_age = ixi.load_data(\n    local_root=Path(\"/data/ixi\"),\n    modalities=[\"T1\"],\n    task=\"age_regression\",\n    max_subjects=100,\n)\nbundle_age.info()\n# label_col = \"AGE\"\n\n# Sex classification\nbundle_sex = ixi.load_data(\n    local_root=Path(\"/data/ixi\"),\n    modalities=[\"T1\"],\n    task=\"sex_classification\",\n    max_subjects=100,\n)\nbundle_sex.info()\n# label_map = {0: 'male', 1: 'female'}\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-2-scanner-site-qc","title":"Step 2 \u2014 Scanner / site QC","text":"<p>IXI data comes from 3 hospitals with different scanner hardware. Site-related variance can inflate model performance.</p> <pre><code>import numpy as np\nfrom collections import Counter\n\n# Site distribution\nsites = [bundle_age.metadata.get(sid, {}).get(\"site\", \"unknown\")\n         for sid in bundle_age.subjects]\nsite_counts = Counter(sites)\nprint(\"Site distribution:\", dict(site_counts))\n\n# Age distribution per site\nfrom collections import defaultdict\nage_by_site = defaultdict(list)\nfor sid, site in zip(bundle_age.subjects, sites):\n    age = bundle_age.metadata.get(sid, {}).get(\"AGE\")\n    if age:\n        age_by_site[site].append(float(age))\n\nfor site, ages in age_by_site.items():\n    print(f\"{site}: n={len(ages)}, age={np.mean(ages):.1f}\u00b1{np.std(ages):.1f}\")\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-3-image-qc","title":"Step 3 \u2014 Image QC","text":"<pre><code># Load and QC first 10 images\nbundle_age.load_images(max_subjects=10)\nqc_reports = bundle_age.run_qc(max_subjects=10)\n\nfor rpt in qc_reports:\n    flag = \"\u26a0\" if rpt.is_outlier else \"\u2713\"\n    shape = rpt.image_shape if hasattr(rpt, 'image_shape') else \"?\"\n    vox   = rpt.voxel_sizes_mm if hasattr(rpt, 'voxel_sizes_mm') else \"?\"\n    print(f\"{flag} {rpt.scope}: shape={shape}, voxels={vox}\")\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-4-tabular-baseline-features","title":"Step 4 \u2014 Tabular baseline features","text":"<p>Instead of loading all 100 volumes into memory, use demographic features for a tabular baseline first.</p> <pre><code>def tabular_features(bundle, include_site=True):\n    \"\"\"Build feature matrix from demographics only.\"\"\"\n    X_rows, y_vals, subjects = [], [], []\n    for sid in bundle.subjects:\n        meta = bundle.metadata.get(sid, {})\n        age = meta.get(\"AGE\")\n        sex = meta.get(\"SEX_ID\")\n        site = meta.get(\"site\", \"unknown\")\n        if age is None or sex is None:\n            continue\n        row = [float(age), float(sex)]\n        if include_site:\n            row.append({\"Guys\": 0, \"HH\": 1, \"IOP\": 2}.get(site, -1))\n        X_rows.append(row)\n        label = bundle.labels[bundle.subjects.index(sid)] if bundle.labels is not None else 0\n        y_vals.append(float(label))\n        subjects.append(sid)\n    import numpy as np\n    return np.array(X_rows, dtype=np.float32), np.array(y_vals, dtype=np.float32), subjects\n\nX_age, y_age, subs_age = tabular_features(bundle_age, include_site=True)\nX_sex, y_sex, subs_sex = tabular_features(bundle_sex, include_site=True)\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-5-subject-level-split","title":"Step 5 \u2014 Subject-level split","text":"<pre><code>from qortex.neuroclassic import assign_leakage_safe_splits, SplitConstraints\n\nrows_age = [{\"id\": s} for s in subs_age]\nresult = assign_leakage_safe_splits(\n    rows_age, id_column=\"id\",\n    constraints=SplitConstraints(train_fraction=0.7, val_fraction=0.1, test_fraction=0.2),\n)\ntr_idx = [i for i, s in enumerate(subs_age) if result.assignments.get(s) == \"train\"]\nte_idx = [i for i, s in enumerate(subs_age) if result.assignments.get(s) == \"test\"]\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-6-age-regression","title":"Step 6 \u2014 Age regression","text":"<pre><code>from sklearn.linear_model import Ridge\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.metrics import mean_absolute_error, r2_score\nimport numpy as np\n\nscaler = StandardScaler()\nX_tr = scaler.fit_transform(X_age[tr_idx])\nX_te = scaler.transform(X_age[te_idx])\n\nreg = Ridge(alpha=1.0)\nreg.fit(X_tr, y_age[tr_idx])\ny_pred_age = reg.predict(X_te)\n\nprint(f\"Age MAE  : {mean_absolute_error(y_age[te_idx], y_pred_age):.2f} years\")\nprint(f\"Age R\u00b2   : {r2_score(y_age[te_idx], y_pred_age):.3f}\")\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#step-7-sex-classification","title":"Step 7 \u2014 Sex classification","text":"<pre><code>from sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import classification_report\n\nrows_sex = [{\"id\": s} for s in subs_sex]\nresult_sex = assign_leakage_safe_splits(\n    rows_sex, id_column=\"id\",\n    constraints=SplitConstraints(train_fraction=0.7, val_fraction=0.1, test_fraction=0.2),\n)\ntr_sex = [i for i, s in enumerate(subs_sex) if result_sex.assignments.get(s) == \"train\"]\nte_sex = [i for i, s in enumerate(subs_sex) if result_sex.assignments.get(s) == \"test\"]\n\nX_tr_s = scaler.fit_transform(X_sex[tr_sex])\nX_te_s = scaler.transform(X_sex[te_sex])\n\nclf = LogisticRegression(max_iter=500, random_state=42)\nclf.fit(X_tr_s, y_sex[tr_sex])\ny_pred_sex = clf.predict(X_te_s)\nprint(classification_report(y_sex[te_sex], y_pred_sex, target_names=[\"male\", \"female\"]))\n</code></pre>"},{"location":"tutorials/t06-mri-age-sex-qc/#validation-summary","title":"Validation summary","text":"Gate Check NIfTI loadability <code>bundle.load_images()</code> Demographic join <code>ixi.load_demographics()</code> parses IXI.xls/csv Scanner/site profile Step 2 \u2014 printed before training Voxel-size QC <code>run_qc()</code> reports voxel sizes and outlier flag Subject-level split <code>assign_leakage_safe_splits()</code> Age MAE + R\u00b2 Regression metrics in Step 6"},{"location":"tutorials/t07-fmri-design-readiness/","title":"T07 \u2014 fMRI Event and Design Readiness","text":"<p>Dataset: ds000001 \u2014 Balloon Analog Risk-taking Task (BART), 16 subjects Task: Event/design validation; no ML model required First model: Design matrix inspection + optional simple GLM diagnostic Difficulty: Beginner</p>"},{"location":"tutorials/t07-fmri-design-readiness/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials]'\n</code></pre> <p>Download: No registration required. Download from https://legacy.openfmri.org/dataset/ds000001/ (BIDS format, ~1.4 GB). Extract to <code>/data/ds000001/</code>.</p>"},{"location":"tutorials/t07-fmri-design-readiness/#step-1-load-and-inspect","title":"Step 1 \u2014 Load and inspect","text":"<pre><code>from pathlib import Path\nfrom qortex.datasets import ds000001\n\ncard = ds000001.describe()\nprint(card)\nprint(card.access_instructions)\n\nbundle = ds000001.load_data(\n    local_root=Path(\"/data/ds000001\"),\n    subjects=[\"01\", \"02\", \"03\", \"04\", \"05\"],\n)\nbundle.info()\n# FMRIBundle: ds000001 \u2014 Balloon Analog Risk-taking Task\n#   Task       : balloonanalogrisktask\n#   Subjects   : 5\n#   TR         : 2.0 s\n#   BOLD files : 5\n#   Event files: 5\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#step-2-validation-gates","title":"Step 2 \u2014 Validation gates","text":"<pre><code>from pathlib import Path\n\nmissing_bold = [p for p in bundle.bold_paths if not p.exists()]\nmissing_evts = [p for p in bundle.event_paths if not p.exists()]\n\nprint(f\"Missing BOLD files  : {len(missing_bold)}\")\nprint(f\"Missing event files : {len(missing_evts)}\")\nassert not missing_bold, f\"BOLD files not found: {missing_bold}\"\nassert not missing_evts, f\"Event files not found: {missing_evts}\"\nprint(\"\u2713 All BOLD and event files present\")\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#step-3-parse-and-audit-event-files","title":"Step 3 \u2014 Parse and audit event files","text":"<pre><code>events = bundle.load_events()\n\nfor sub, (evt, path) in zip(bundle.subjects, zip(events, bundle.event_paths)):\n    rows = evt.get(\"rows\", [])\n    n = evt.get(\"n_events\", 0)\n    print(f\"sub-{sub}: {n} events in {Path(path).name}\")\n    if rows:\n        # Show first 3 events\n        for row in rows[:3]:\n            print(f\"  onset={row.get('onset','?')} dur={row.get('duration','?')} \"\n                  f\"trial_type={row.get('trial_type','?')}\")\n</code></pre> <p>Expected events in ds000001 (BART): - <code>trial_type</code> values: <code>inflate</code>, <code>cashout</code>, <code>explode</code>, <code>pump</code> - Check that onsets are within scan duration</p>"},{"location":"tutorials/t07-fmri-design-readiness/#step-4-nifti-header-inspection","title":"Step 4 \u2014 NIfTI header inspection","text":"<pre><code>try:\n    import nibabel as nib\nexcept ImportError:\n    print(\"Install nibabel: pip install 'qortex[mri]'\")\n    raise\n\nfor sub, bold_path in zip(bundle.subjects, bundle.bold_paths):\n    if not bold_path.exists():\n        continue\n    img = nib.load(str(bold_path))\n    shape = img.shape\n    pixdim = img.header.get_zooms()\n    print(f\"sub-{sub}: shape={shape}, voxel={pixdim[:3]}, TR={pixdim[3]:.2f}s\")\n    assert abs(float(pixdim[3]) - bundle.tr) &lt; 0.1, \\\n        f\"TR mismatch: header says {pixdim[3]:.2f}, expected {bundle.tr}\"\nprint(\"\u2713 TR consistent with header for all subjects\")\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#step-5-event-timing-audit","title":"Step 5 \u2014 Event timing audit","text":"<p>Check that all event onsets fall within the scan duration.</p> <pre><code>for sub, (evt, bold_path) in zip(bundle.subjects, zip(events, bundle.bold_paths)):\n    if not bold_path.exists():\n        continue\n    img = nib.load(str(bold_path))\n    n_vols = img.shape[3] if len(img.shape) == 4 else 1\n    scan_duration = n_vols * bundle.tr\n\n    rows = evt.get(\"rows\", [])\n    for row in rows:\n        onset = float(row.get(\"onset\", 0))\n        duration = float(row.get(\"duration\", 0))\n        if onset + duration &gt; scan_duration:\n            print(f\"\u26a0 sub-{sub}: event at {onset:.1f}s + {duration:.1f}s \"\n                  f\"exceeds scan duration {scan_duration:.1f}s\")\n\n    condition_counts = {}\n    for row in rows:\n        t = row.get(\"trial_type\", \"unknown\")\n        condition_counts[t] = condition_counts.get(t, 0) + 1\n    print(f\"sub-{sub}: {condition_counts}\")\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#step-6-readiness-report","title":"Step 6 \u2014 Readiness report","text":"<pre><code>print(\"\\n=== fMRI Design Readiness Report ===\")\nprint(f\"Dataset          : {bundle.card.full_name}\")\nprint(f\"Task             : {bundle.task}\")\nprint(f\"TR               : {bundle.tr} s\")\nprint(f\"Subjects checked : {len(bundle.subjects)}\")\nprint(f\"BOLD files       : {sum(p.exists() for p in bundle.bold_paths)} / {len(bundle.bold_paths)} present\")\nprint(f\"Event files      : {sum(p.exists() for p in bundle.event_paths)} / {len(bundle.event_paths)} present\")\nprint(f\"License          : {bundle.card.license}\")\nprint()\nprint(\"\u26a0 Revision 2.0.4 corrected event timing files.\")\nprint(\"  Always use the corrected revision \u2014 check dataset_description.json.\")\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#step-7-optional-simple-design-matrix-preview","title":"Step 7 \u2014 Optional: simple design matrix preview","text":"<pre><code>import numpy as np\n\n# Minimal HRF convolution check (no model fitting)\ntry:\n    from nilearn.glm.first_level import make_first_level_design_matrix  # type: ignore\n    import pandas as pd\n\n    sub = bundle.subjects[0]\n    evt_data = bundle.events[0]\n    rows = evt_data.get(\"rows\", [])\n\n    img = nib.load(str(bundle.bold_paths[0]))\n    n_vols = img.shape[3]\n    frame_times = np.arange(n_vols) * bundle.tr\n\n    events_df = pd.DataFrame({\n        \"onset\":      [float(r[\"onset\"]) for r in rows],\n        \"duration\":   [float(r[\"duration\"]) for r in rows],\n        \"trial_type\": [r.get(\"trial_type\", \"unknown\") for r in rows],\n    })\n\n    dm = make_first_level_design_matrix(frame_times, events_df, hrf_model=\"spm\")\n    print(f\"Design matrix shape: {dm.shape}\")\n    print(f\"Conditions: {list(dm.columns)}\")\nexcept ImportError:\n    print(\"nilearn not installed \u2014 skip design matrix preview\")\n</code></pre>"},{"location":"tutorials/t07-fmri-design-readiness/#validation-summary","title":"Validation summary","text":"Gate Check BIDS manifest <code>ds000001</code> directory with sub-XX layout BOLD file exists Step 2 \u2014 asserted for all subjects Events file exists Step 2 \u2014 asserted for all subjects Corrected revision Check <code>dataset_description.json</code> BIDSVersion Event onsets within scan Step 5 \u2014 onset + duration \u2264 scan_duration TR / volume metadata Step 4 \u2014 header TR matches bundle.tr"},{"location":"tutorials/t08-brain-tumour-segmentation/","title":"T08 \u2014 Brain Tumour Segmentation Baseline","text":"<p>Dataset: MSD Task01 Brain Tumour (484 training / 266 test volumes) Task: Multiclass segmentation \u2014 background / NCR+NET / edema / enhancing tumour First model: MONAI 3D SegResNet with Dice loss Later model: UNETR / SwinUNETR Difficulty: Advanced</p>"},{"location":"tutorials/t08-brain-tumour-segmentation/#prerequisites","title":"Prerequisites","text":"<pre><code>pip install 'qortex[tutorials,monai,mri]'\n# Full MONAI with GPU support:\npip install 'monai[all]'\n</code></pre> <p>GPU required for reasonable training time (~16 GB VRAM for full BraTS). CPU mode works for a quick sanity check on 5 cases.</p>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-1-load-data","title":"Step 1 \u2014 Load data","text":"<pre><code>from qortex.datasets import msd_brain\n\ncard = msd_brain.describe()\nprint(card)\n\nbundle = msd_brain.load_data(\n    split=\"train\",\n    max_cases=20,     # start with 20 cases\n    download=True,    # MONAI downloads and caches on first call\n)\nbundle.info()\n# SegmentationBundle: MSD Brain Tumour\n#   Split      : train\n#   Cases      : 20\n#   Modalities : ['FLAIR', 'T1w', 'T1gd', 'T2w']\n#   Label map  : {0: background, 1: NCR_NET, 2: edema, 3: enhancing_tumour}\n</code></pre> <p>Label map</p> Value Region 0 Background 1 NCR/NET \u2014 Necrotic core / non-enhancing tumour 2 Edema 3 Enhancing tumour"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-2-image-mask-pair-validation","title":"Step 2 \u2014 Image-mask pair validation","text":"<pre><code>import numpy as np\n\nfor i in range(min(5, bundle.n_cases)):\n    image, mask = bundle.load_pair(i)\n    # image: [4, x, y, z] (4 modalities stacked)\n    # mask:  [x, y, z]\n    assert image.ndim == 3 or (image.ndim == 4 and image.shape[0] == 4), \\\n        f\"Case {i}: unexpected image shape {image.shape}\"\n    assert mask.shape == image.shape[-3:] or mask.shape == image.shape[1:], \\\n        f\"Case {i}: mask shape {mask.shape} does not match image {image.shape}\"\n\n    # Label inventory\n    unique_vals = np.unique(mask).tolist()\n    label_names = [msd_brain.LABEL_MAP.get(int(v), f\"val_{v}\") for v in unique_vals]\n    print(f\"Case {bundle.case_ids[i]}: image={image.shape}, \"\n          f\"mask={mask.shape}, labels={label_names}\")\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-3-monai-data-pipeline","title":"Step 3 \u2014 MONAI data pipeline","text":"<pre><code>import torch\nfrom monai.transforms import (\n    Compose, LoadImaged, EnsureChannelFirstd, Orientationd,\n    Spacingd, ScaleIntensityRangePercentilesd, CropForegroundd,\n    RandCropByPosNegLabeld, RandFlipd, RandRotate90d, ToTensord,\n)\nfrom monai.data import CacheDataset, DataLoader\n\n# Build data dicts for MONAI\ndef bundle_to_data_dicts(bundle):\n    dicts = []\n    for i in range(bundle.n_cases):\n        img_paths = bundle.image_paths[i]\n        msk_path  = bundle.mask_paths[i] if i &lt; len(bundle.mask_paths) else None\n        d = {\"image\": [str(p) for p in img_paths]}\n        if msk_path and msk_path.exists():\n            d[\"label\"] = str(msk_path)\n        dicts.append(d)\n    return dicts\n\ntrain_dicts = bundle_to_data_dicts(bundle)\n\ntrain_transforms = Compose([\n    LoadImaged(keys=[\"image\", \"label\"]),\n    EnsureChannelFirstd(keys=[\"image\", \"label\"]),\n    Orientationd(keys=[\"image\", \"label\"], axcodes=\"RAS\"),\n    Spacingd(keys=[\"image\", \"label\"], pixdim=(1.0, 1.0, 1.0),\n             mode=(\"bilinear\", \"nearest\")),\n    ScaleIntensityRangePercentilesd(\n        keys=[\"image\"], lower=0.5, upper=99.5,\n        b_min=0.0, b_max=1.0, clip=True, channel_wise=True,\n    ),\n    CropForegroundd(keys=[\"image\", \"label\"], source_key=\"image\"),\n    RandCropByPosNegLabeld(\n        keys=[\"image\", \"label\"], label_key=\"label\",\n        spatial_size=(128, 128, 64), pos=1, neg=1, num_samples=2,\n    ),\n    RandFlipd(keys=[\"image\", \"label\"], prob=0.5, spatial_axis=0),\n    RandRotate90d(keys=[\"image\", \"label\"], prob=0.5, max_k=3),\n    ToTensord(keys=[\"image\", \"label\"]),\n])\n\ndataset = CacheDataset(data=train_dicts, transform=train_transforms, cache_rate=0.5)\nloader  = DataLoader(dataset, batch_size=2, shuffle=True, num_workers=4)\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-4-build-model","title":"Step 4 \u2014 Build model","text":"<pre><code>from monai.networks.nets import SegResNet\nfrom monai.losses import DiceLoss\nfrom monai.metrics import DiceMetric\n\nmodel = SegResNet(\n    spatial_dims=3,\n    in_channels=4,        # FLAIR, T1w, T1gd, T2w\n    out_channels=4,       # background + 3 tumour regions\n    init_filters=16,\n).to(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\nloss_fn = DiceLoss(\n    to_onehot_y=True,\n    softmax=True,\n    include_background=False,   # focus on tumour regions only\n)\noptimizer = torch.optim.Adam(model.parameters(), lr=1e-4, weight_decay=1e-5)\ndice_metric = DiceMetric(include_background=False, reduction=\"mean\")\n\ndevice = next(model.parameters()).device\nprint(f\"Model device: {device}\")\nprint(f\"Parameters: {sum(p.numel() for p in model.parameters()):,}\")\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-5-training-loop","title":"Step 5 \u2014 Training loop","text":"<pre><code>from monai.inferers import sliding_window_inference\n\nMAX_EPOCHS = 50\nVAL_INTERVAL = 5\nbest_dice = -1\n\nfor epoch in range(1, MAX_EPOCHS + 1):\n    model.train()\n    epoch_loss = 0.0\n    for batch in loader:\n        x = batch[\"image\"].to(device)\n        y = batch[\"label\"].to(device)\n        optimizer.zero_grad()\n        pred = model(x)\n        loss = loss_fn(pred, y)\n        loss.backward()\n        optimizer.step()\n        epoch_loss += loss.item()\n\n    if epoch % 10 == 0:\n        print(f\"Epoch {epoch:3d} | loss: {epoch_loss / len(loader):.4f}\")\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-6-sliding-window-inference-and-dice-evaluation","title":"Step 6 \u2014 Sliding-window inference and Dice evaluation","text":"<pre><code>from monai.transforms import AsDiscrete\nfrom monai.data import decollate_batch\n\npost_pred  = AsDiscrete(argmax=True, to_onehot=4)\npost_label = AsDiscrete(to_onehot=4)\n\nmodel.eval()\nwith torch.no_grad():\n    for batch in loader:\n        x = batch[\"image\"].to(device)\n        y = batch[\"label\"].to(device)\n        pred = sliding_window_inference(\n            x, roi_size=(128, 128, 64), sw_batch_size=4, predictor=model,\n        )\n        preds_list  = [post_pred(p) for p in decollate_batch(pred)]\n        labels_list = [post_label(l) for l in decollate_batch(y)]\n        dice_metric(y_pred=preds_list, y=labels_list)\n    mean_dice = dice_metric.aggregate().item()\n    dice_metric.reset()\n\nprint(f\"Mean Dice (excl. background): {mean_dice:.4f}\")\nprint()\nregion_names = [msd_brain.LABEL_MAP[k] for k in [1, 2, 3]]\nprint(\"Regions measured:\", region_names)\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#step-7-patch-sanity-check","title":"Step 7 \u2014 Patch sanity check","text":"<pre><code># Verify patch extraction is sane\nsample = next(iter(loader))\nimg_patch  = sample[\"image\"]\nmask_patch = sample[\"label\"]\nprint(f\"Image patch shape : {img_patch.shape}\")   # (batch, 4, 128, 128, 64)\nprint(f\"Mask patch shape  : {mask_patch.shape}\")  # (batch, 1, 128, 128, 64)\n\n# Fraction of foreground in patches\nfrac_fg = (mask_patch &gt; 0).float().mean().item()\nprint(f\"Foreground fraction in patches: {frac_fg:.2%}\")\nassert frac_fg &gt; 0.01, \"Patches contain almost no foreground \u2014 check CropForeground\"\nprint(\"\u2713 Patch sanity check passed\")\n</code></pre>"},{"location":"tutorials/t08-brain-tumour-segmentation/#validation-summary","title":"Validation summary","text":"Gate Check Image-mask pair check Shapes match in Step 2 Label inventory All expected values {0,1,2,3} present (Step 2) Affine/orientation MONAI Orientationd ensures RAS+ Patch extraction Foreground fraction &gt; 1% (Step 7) Dice metric Computed excluding background (Step 6) No clinical claims Documented in card and code comments"},{"location":"visualization/","title":"Visualization","text":"<p>Qortex includes visualization tools for inspecting neuroimaging data before and after download. All viewers are designed around lazy loading: only the slices needed for the current view are read from disk.</p>"},{"location":"visualization/#design-principles","title":"Design principles","text":"<p>One center slice per NIfTI, not the whole volume. NIfTI files can be several hundred MB. Loading one at full resolution for a thumbnail is wasteful. Qortex reads only the center slice along each axis using nibabel's ArrayProxy.</p> <p>Plotly for interactive HTML, matplotlib fallback for static figures. The <code>visual</code> extra adds Plotly. Without it, static PNG output is available via matplotlib.</p> <p>No Pillow required for basic rendering. Colormaps (gray, hot, plasma, RdBu_r) are implemented as pure numpy LUTs.</p>"},{"location":"visualization/#viewers","title":"Viewers","text":"<p>Visual pipeline \u2014 How VisualAsset, VisualPlan, and VisualResult compose a rendering pass.</p> <p>Local viewer \u2014 <code>visualize()</code> on a local BIDS directory. Renders one figure per file type.</p> <p>Visualize OpenNeuro \u2014 <code>visualize-openneuro</code> CLI command. Fetches center slices from CDN without full download.</p> <p>Visual audit \u2014 <code>VisualAuditReport</code> coverage matrix, warnings, and action items.</p> <p>fMRI QC \u2014 <code>fmri_summary()</code> 6-panel QC figure with optional confound overlays.</p> <p>DWI QC \u2014 <code>dwi_summary()</code> 4-panel QC and gradient sphere.</p> <p>Overlays \u2014 Mask, labelmap, stat map, PET, and contour overlays on anatomical background.</p> <p>Segmentation comparison \u2014 <code>compare_masks()</code> for side-by-side label comparison.</p> <p>DICOM browser \u2014 <code>browse_dicom()</code> for series-level DICOM inspection.</p> <p>EEG viewer \u2014 <code>TimeSeriesViewer</code> butterfly, PSD, spectrogram, topomap, and ERP.</p> <p>Artifact visualization \u2014 <code>Artifact.visualize_sample()</code> for ML-format artifact inspection.</p> <p>Colormaps and windows \u2014 CT window presets and MRI window auto-detection.</p>"},{"location":"visualization/#requirements","title":"Requirements","text":"<pre><code>pip install \"qortex[visual-all]\"\n</code></pre> <p>For MRI/fMRI/DWI viewers only:</p> <pre><code>pip install \"qortex[mri,dwi]\"\n</code></pre> <p>For EEG viewers only:</p> <pre><code>pip install \"qortex[eeg]\"\n</code></pre> <p>Next \u2192 Convert \u2014 transform validated local data into training-ready ML formats.</p>"},{"location":"visualization/artifact-visualization/","title":"Artifact Visualization","text":"<p>Once a dataset has been converted to a Qortex artifact (Parquet, Zarr, HDF5, etc.), you can inspect individual samples and compare splits without loading the full dataset.</p>"},{"location":"visualization/artifact-visualization/#visualize-a-sample","title":"Visualize a sample","text":"<pre><code>from qortex import Artifact\n\nart = Artifact.open(\"artifacts/ds004130_parquet/\")\nart.visualize_sample(split=\"train\", index=0)\n</code></pre> <p>This opens a figure showing the first sample from the training split:</p> <ul> <li>For EEG: a butterfly plot of the window</li> <li>For fMRI: the extracted volume window (mean across time if 4D)</li> <li>For tabular: a bar chart of feature values</li> </ul> <p>Pass a subject ID instead of an integer index:</p> <pre><code>art.visualize_sample(split=\"train\", subject=\"01\", label=\"rest\")\n</code></pre>"},{"location":"visualization/artifact-visualization/#visual-audit-of-an-artifact","title":"Visual audit of an artifact","text":"<pre><code>audit = art.visual_audit()\naudit.show()\n</code></pre> <p>The artifact visual audit shows:</p> <ul> <li>Sample counts per split</li> <li>Class distribution per split</li> <li>Feature range summary (min, max, mean per feature)</li> <li>Any suspicious samples (all-zero windows, NaN features)</li> </ul>"},{"location":"visualization/artifact-visualization/#compare-splits","title":"Compare splits","text":"<pre><code>art.compare_splits()\n</code></pre> <p>Produces a side-by-side comparison of the train, val, and test splits:</p> <ul> <li>Class distribution histogram (should be similar across splits)</li> <li>Subject count per split</li> <li>Mean signal amplitude per split (should be similar)</li> <li>Feature correlation matrix (should match between train and val)</li> </ul> <p>Any major differences between splits suggest a data issue (e.g., non-stationary signal, inadvertent stratification).</p>"},{"location":"visualization/artifact-visualization/#cli","title":"CLI","text":"<pre><code>qortex artifact-visualize artifacts/ds004130_parquet/ --split train --index 0\nqortex artifact-visualize artifacts/ds004130_parquet/ --compare-splits\n</code></pre>"},{"location":"visualization/artifact-visualization/#inspecting-the-artifact-manifest","title":"Inspecting the artifact manifest","text":"<pre><code>print(art.manifest.n_samples)\nprint(art.manifest.splits)        # {\"train\": 1200, \"val\": 260, \"test\": 280}\nprint(art.manifest.feature_names) # list of feature names\nprint(art.manifest.label_classes) # [\"rest\", \"eyes-open\", \"task\"]\nprint(art.manifest.source_dataset) # \"ds004130@1.2.0\"\nprint(art.manifest.created_at)    # ISO timestamp\n</code></pre>"},{"location":"visualization/artifact-visualization/#related","title":"Related","text":"<ul> <li>Artifact overview \u2014 the Artifact class and its splits</li> <li>ML bridge \u2014 loading artifacts for training</li> <li>Compare splits \u2014 split quality checks</li> </ul>"},{"location":"visualization/colormaps-and-windows/","title":"Colormaps and Windows","text":"<p>Qortex includes a set of display window presets for CT and MRI, and four pure-numpy colormap LUTs.</p>"},{"location":"visualization/colormaps-and-windows/#ct-window-presets","title":"CT window presets","text":"<p>CT images use Hounsfield units (HU). Different anatomical structures are visible at different HU ranges. Qortex provides ten presets:</p> Preset Center (HU) Width (HU) Use <code>brain</code> 40 80 Standard brain CT <code>subdural</code> 75 215 Subdural hemorrhage <code>stroke</code> 32 8 Early ischemic stroke <code>bone</code> 400 1800 Skull and vertebrae <code>soft_tissue</code> 50 400 General soft tissue <code>lung</code> -600 1500 Lung parenchyma <code>liver</code> 60 160 Liver and abdominal <code>angio</code> 300 600 CT angiography <code>abdomen</code> 40 350 Abdomen general <code>pelvis</code> 55 400 Pelvis <p>Each preset is a <code>WindowPreset</code> dataclass with <code>center</code> and <code>width</code> fields. The display window maps <code>center - width/2</code> to the bottom of the colormap and <code>center + width/2</code> to the top.</p>"},{"location":"visualization/colormaps-and-windows/#applying-a-preset","title":"Applying a preset","text":"<pre><code>from qortex.visualize._colors import CT_PRESETS, apply_window\n\npreset = CT_PRESETS[\"brain\"]\nnormalized = apply_window(voxel_data, center=preset.center, width=preset.width)\n# normalized is float32 in [0.0, 1.0]\n</code></pre>"},{"location":"visualization/colormaps-and-windows/#auto-windowing-for-mri","title":"Auto-windowing for MRI","text":"<p>For MRI, the appropriate window depends on the image content. <code>auto_window()</code> computes window center and width from the 2nd and 98th percentiles of non-zero voxels:</p> <pre><code>from qortex.visualize._colors import auto_window\n\ncenter, width = auto_window(voxel_data)\nnormalized = apply_window(voxel_data, center=center, width=width)\n</code></pre>"},{"location":"visualization/colormaps-and-windows/#colormap-luts","title":"Colormap LUTs","text":"<p>Qortex includes four colormap lookup tables as pure numpy arrays. These work without Pillow or matplotlib.</p> Colormap Shape Use <code>gray</code> (256, 3) float32 Standard grayscale <code>hot</code> (256, 3) float32 PET and temperature maps <code>plasma</code> (256, 3) float32 Sequential with high contrast <code>RdBu_r</code> (256, 3) float32 Diverging, for stat maps <pre><code>from qortex.visualize._colors import get_lut\n\nlut = get_lut(\"hot\")             # shape (256, 3), values in [0, 1]\nrgb = lut[(normalized * 255).astype(int)]  # apply to normalized voxel data\n</code></pre>"},{"location":"visualization/colormaps-and-windows/#using-presets-in-viewers","title":"Using presets in viewers","text":"<p>Viewers accept window presets by name:</p> <pre><code>from qortex.visualize.volume import VolumeViewer\n\n# CT with brain window\nviewer = VolumeViewer(\"sub-01_ct.nii.gz\", modality=\"ct\", window=\"brain\")\nviewer.ortho().show()\n\n# CT with custom window\nviewer = VolumeViewer(\"sub-01_ct.nii.gz\", modality=\"ct\")\nviewer.ortho(window_center=40, window_width=80).show()\n</code></pre> <p>For statistical maps, the colormap and threshold are set on the overlay call:</p> <pre><code>from qortex.visualize import overlay_stat\n\nresult = overlay_stat(\n    bg,\n    stat_path=\"zstat.nii.gz\",\n    colormap=\"RdBu_r\",\n    threshold=2.3,\n    vmax=8.0,\n)\n</code></pre>"},{"location":"visualization/colormaps-and-windows/#mri-presets","title":"MRI presets","text":"<p>In addition to CT, Qortex has presets for common MRI modalities:</p> <pre><code>from qortex.visualize._colors import MR_PRESETS, FMRI_PRESETS, PET_PRESETS\n\nmr_preset   = MR_PRESETS[\"T1w\"]\nfmri_preset = FMRI_PRESETS[\"bold\"]\npet_preset  = PET_PRESETS[\"fdg\"]\n</code></pre> <p>These are applied automatically when the modality is detected from the BIDS suffix.</p>"},{"location":"visualization/dicom-browser/","title":"DICOM Browser","text":"<p><code>browse_dicom()</code> renders a series-level overview of a DICOM directory. It groups files by series, extracts metadata from DICOM headers, and shows one thumbnail per series.</p>"},{"location":"visualization/dicom-browser/#python","title":"Python","text":"<pre><code>from qortex.visualize import browse_dicom\n\nresult = browse_dicom(\"dicom/sub-01/\")\nresult.show()\n</code></pre> <p>The output shows one thumbnail per series with:</p> <ul> <li>Series description</li> <li>Study date</li> <li>Modality (MR, CT, PET, etc.)</li> <li>Number of slices</li> <li>Voxel dimensions</li> <li>Center axial slice</li> </ul>"},{"location":"visualization/dicom-browser/#cli","title":"CLI","text":"<pre><code>qortex dicom-browser dicom/sub-01/ --output dicom_overview.html\n</code></pre>"},{"location":"visualization/dicom-browser/#what-it-groups-on","title":"What it groups on","text":"<p>DICOM files are grouped by <code>SeriesInstanceUID</code>. Within each series, files are sorted by <code>InstanceNumber</code>. The center slice is the file at index <code>n // 2</code>.</p>"},{"location":"visualization/dicom-browser/#supported-dicom-types","title":"Supported DICOM types","text":"<ul> <li>Standard DICOM (.dcm, no extension)</li> <li>Enhanced DICOM (multi-frame)</li> <li>DICOM-SEG (segmentation objects)</li> <li>SR (structured reports \u2014 shows metadata only, no image)</li> </ul>"},{"location":"visualization/dicom-browser/#requirements","title":"Requirements","text":"<pre><code>pip install \"qortex[dicom]\"\n</code></pre> <p>The <code>dicom</code> extra pulls in pydicom. Without it, <code>browse_dicom()</code> raises an <code>ImportError</code>.</p>"},{"location":"visualization/dicom-browser/#limitations","title":"Limitations","text":"<ul> <li>DICOM-RT (radiation therapy dose and structure sets) is not supported.</li> <li>Compressed DICOM (JPEG or JPEG 2000 transfer syntax) requires the <code>pylibjpeg</code> package. Install it separately if needed.</li> <li>Multi-frame enhanced DICOM is supported but tested on a limited set of vendors.</li> </ul>"},{"location":"visualization/dicom-browser/#converting-dicom-to-nifti","title":"Converting DICOM to NIfTI","text":"<p>Qortex does not convert DICOM to NIfTI. For that, use dcm2niix before calling Qortex:</p> <pre><code>dcm2niix -o output/ -z y dicom/sub-01/\n</code></pre> <p>After conversion, use the standard NIfTI visualization tools.</p>"},{"location":"visualization/dwi-qc/","title":"DWI QC","text":"<p><code>dwi_summary()</code> produces a four-panel QC figure for a DWI acquisition. It shows b=0 volume, one high-b volume, b-value histogram, and gradient direction sphere.</p>"},{"location":"visualization/dwi-qc/#basic-usage","title":"Basic usage","text":"<pre><code>from qortex.visualize import dwi_summary\n\nresult = dwi_summary(\n    \"sub-01_dwi.nii.gz\",\n    bval_path=\"sub-01_dwi.bval\",\n    bvec_path=\"sub-01_dwi.bvec\",\n)\nresult.show()\n</code></pre> <p>Or through DWIViewer directly:</p> <pre><code>from qortex.visualize.dwi import DWIViewer\n\nviewer = DWIViewer(\n    \"sub-01_dwi.nii.gz\",\n    bval_path=\"sub-01_dwi.bval\",\n    bvec_path=\"sub-01_dwi.bvec\",\n)\nresult = viewer.dwi_summary()\nresult.show()\n</code></pre>"},{"location":"visualization/dwi-qc/#four-panel-layout","title":"Four-panel layout","text":"Position Panel What it shows Row 1, Left b=0 volume Mean of all b=0 (or low-b) volumes \u2014 center axial slice Row 1, Right High-b volume One volume from the highest b-value shell Row 2, Left b-value histogram Distribution of b-values, colored by shell Row 2, Right Gradient sphere 3D sphere with gradient directions plotted as points"},{"location":"visualization/dwi-qc/#individual-panels","title":"Individual panels","text":"<pre><code>viewer = DWIViewer(\"sub-01_dwi.nii.gz\", bval_path=\"...\", bvec_path=\"...\")\n\nb0_fig      = viewer.b0()            # b=0 center slice\nhigh_b_fig  = viewer.high_b()        # highest b-value slice\nhist_fig    = viewer.bval_histogram() # b-value distribution\nsphere_fig  = viewer.gradient_sphere() # gradient directions on sphere\n</code></pre>"},{"location":"visualization/dwi-qc/#contact-sheet","title":"Contact sheet","text":"<p><code>contact_sheet()</code> produces a grid of axial slices, one per gradient direction, arranged in acquisition order:</p> <pre><code>sheet_fig = viewer.contact_sheet(n_cols=10)\nsheet_fig.show()\n</code></pre> <p>This is useful for identifying dropped volumes (all-zero or corrupted slices) or gradient encoding errors.</p>"},{"location":"visualization/dwi-qc/#cli","title":"CLI","text":"<pre><code>qortex dwi-qc sub-01_dwi.nii.gz \\\n    --bval sub-01_dwi.bval \\\n    --bvec sub-01_dwi.bvec \\\n    --output figures/sub-01_dwi_qc.html\n</code></pre>"},{"location":"visualization/dwi-qc/#interpreting-the-panels","title":"Interpreting the panels","text":"<p>b=0 volume. Should show brain anatomy with T2-weighted contrast. Dark regions may indicate susceptibility artifacts (common near ear canals and prefrontal sinuses). Signal dropout here will affect all diffusion metrics derived from that voxel.</p> <p>High-b volume. At high b-values (\u2265 1000 s/mm\u00b2), signal drops dramatically. What you see is mostly noise plus diffusion-weighted contrast. The brain should still be roughly visible. An all-noise image suggests the high-b gradient was not applied correctly.</p> <p>b-value histogram. For single-shell data, you expect two peaks: one near b=0 and one at the shell value. For multi-shell data (HCP style), you expect three or more peaks. An unexpected peak at an intermediate value may indicate gradient non-linearity or a protocol error.</p> <p>Gradient sphere. Gradient directions should be approximately uniformly distributed on the hemisphere. Clustering in one region means poor angular coverage, which degrades reconstruction accuracy for fiber orientation distributions.</p>"},{"location":"visualization/dwi-qc/#limitations","title":"Limitations","text":"<ul> <li><code>gradient_sphere()</code> uses matplotlib's 3D projection, not Plotly. The interactive rotation is limited.</li> <li>DWIViewer requires the <code>dwi</code> extra: <code>pip install \"qortex[dwi]\"</code>.</li> <li>Dipy is used internally for bval/bvec parsing and shell detection. If dipy is not installed, bval/bvec are read with numpy and shell detection falls back to a simple threshold.</li> </ul>"},{"location":"visualization/eeg-viewer/","title":"EEG Viewer","text":"<p><code>TimeSeriesViewer</code> renders EEG, MEG, and iEEG recordings from local files. It uses MNE-Python for reading and requires the <code>eeg</code> extra.</p>"},{"location":"visualization/eeg-viewer/#setup","title":"Setup","text":"<pre><code>pip install \"qortex[eeg]\"\n</code></pre>"},{"location":"visualization/eeg-viewer/#python","title":"Python","text":"<pre><code>from qortex.visualize.timeseries import TimeSeriesViewer\n\nviewer = TimeSeriesViewer(\"sub-01_task-rest_eeg.set\")\n</code></pre> <p>MNE reads the channel layout and electrode positions from the <code>.set</code> file and its companion <code>*_channels.tsv</code> / <code>*_coordsystem.json</code>. Pass these explicitly if they are not auto-detected:</p> <pre><code>viewer = TimeSeriesViewer(\n    \"sub-01_task-rest_eeg.set\",\n    channels_path=\"sub-01_task-rest_channels.tsv\",\n    coordsystem_path=\"sub-01_task-rest_coordsystem.json\",\n)\n</code></pre>"},{"location":"visualization/eeg-viewer/#butterfly-plot","title":"Butterfly plot","text":"<p>All channels overlaid on the same time axis, color-coded by channel type:</p> <pre><code>fig = viewer.butterfly(t_start=0, t_stop=10)  # 0\u201310 seconds\nfig.show()\n</code></pre> <p>The butterfly plot is good for detecting channels with extreme artifacts \u2014 they appear as lines far outside the cluster of others.</p>"},{"location":"visualization/eeg-viewer/#power-spectral-density","title":"Power spectral density","text":"<pre><code>fig = viewer.psd(fmin=1, fmax=80, method=\"welch\")\nfig.show()\n</code></pre> <p>Uses Welch's method (scipy.signal). The plot shows PSD in dB/Hz for each channel, with the mean across channels highlighted. Look for the 1/f drop-off with alpha (8\u201313 Hz) and beta (13\u201330 Hz) peaks.</p>"},{"location":"visualization/eeg-viewer/#spectrogram","title":"Spectrogram","text":"<pre><code>fig = viewer.spectrogram(channel=\"Oz\", fmin=1, fmax=80)\nfig.show()\n</code></pre> <p>Uses STFT via scipy. Time is on the x-axis, frequency on the y-axis, power in decibels as color. One channel at a time.</p>"},{"location":"visualization/eeg-viewer/#topomap","title":"Topomap","text":"<pre><code>fig = viewer.topomap(freq_band=(8, 12))  # alpha band power\nfig.show()\n</code></pre> <p>Renders a 2D topographic map of power in a frequency band. Electrode positions are taken from the coordsystem JSON or the MNE standard montage. Interpolation uses IDW (inverse distance weighting) with unit-circle normalization.</p> <p>If no electrode positions are available, the topomap falls back to a summary-only output listing channels by power rank.</p>"},{"location":"visualization/eeg-viewer/#epoched-erp","title":"Epoched (ERP)","text":"<p>For event-related potential visualization:</p> <pre><code>fig = viewer.epoched(\n    events_path=\"sub-01_task-rest_events.tsv\",\n    event_id=\"stimulus\",    # trial_type value\n    tmin=-0.2,              # seconds before onset\n    tmax=0.8,               # seconds after onset\n)\nfig.show()\n</code></pre> <p>Shows the mean epoch per channel with a shaded standard error band. The time-zero line is the event onset.</p>"},{"location":"visualization/eeg-viewer/#dashboard","title":"Dashboard","text":"<p><code>dashboard()</code> launches a local Panel server with all views combined in one interactive interface:</p> <pre><code>viewer.dashboard()\n# opens http://localhost:5006\n</code></pre> <p>Requires the <code>dashboard</code> extra: <code>pip install \"qortex[dashboard]\"</code>.</p>"},{"location":"visualization/eeg-viewer/#cli","title":"CLI","text":"<pre><code>qortex visualize ds004130 \\\n    --subject 01 \\\n    --suffix eeg \\\n    --data-dir data/ds004130/\n</code></pre>"},{"location":"visualization/eeg-viewer/#supported-file-formats","title":"Supported file formats","text":"<p>MNE reads all standard EEG formats:</p> <ul> <li><code>.set</code> (EEGLAB)</li> <li><code>.fif</code> (MNE-Python, Elekta/Neuromag)</li> <li><code>.edf</code> (European Data Format)</li> <li><code>.cnt</code> (Neuroscan)</li> <li><code>.vhdr</code> / <code>.vmrk</code> / <code>.eeg</code> (BrainVision)</li> </ul> <p>BrainVision sets need all three files (<code>*.vhdr</code>, <code>*.vmrk</code>, <code>*.eeg</code>) in the same directory.</p>"},{"location":"visualization/eeg-viewer/#limitations","title":"Limitations","text":"<ul> <li><code>topomap()</code> with IDW interpolation requires at least 3 channels with known 3D positions.</li> <li>Very long recordings (&gt; 1 hour) are read in segments. <code>butterfly()</code> shows only the first <code>max_duration</code> seconds (default: 30).</li> <li>MEG is supported in terms of file reading, but <code>topomap()</code> for MEG sensor layouts uses the MNE default layout and may not match custom MEG configurations.</li> </ul>"},{"location":"visualization/fmri-qc/","title":"fMRI QC","text":"<p><code>fmri_summary()</code> produces a multi-panel QC figure for a single BOLD NIfTI file. It is designed to catch obvious acquisition failures and motion artifacts before investing time in preprocessing or training.</p>"},{"location":"visualization/fmri-qc/#basic-usage","title":"Basic usage","text":"<pre><code>from qortex.visualize import fmri_summary\n\nresult = fmri_summary(\"sub-01_task-rest_bold.nii.gz\")\nresult.show()\n</code></pre> <p>Or through VolumeViewer:</p> <pre><code>from qortex.visualize.volume import VolumeViewer\n\nviewer = VolumeViewer(\"sub-01_task-rest_bold.nii.gz\", modality=\"fmri\")\nresult = viewer.fmri_summary()\nresult.show()\n</code></pre>"},{"location":"visualization/fmri-qc/#six-panel-layout","title":"Six-panel layout","text":"<p>The summary produces six panels arranged in a 3\u00d72 grid:</p> Position Panel What it shows Row 1, Left Mean EPI Time-averaged volume \u2014 center axial slice Row 1, Right Middle frame One frame at timepoint N/2 \u2014 shows TR-level contrast Row 2, Left Temporal std Voxel-wise standard deviation across time Row 2, Right tSNR Temporal signal-to-noise ratio (mean / std), per voxel Row 3, Left Global signal Mean signal across all brain voxels, one value per TR Row 3, Right Framewise intensity Mean signal per frame \u2014 useful for detecting volume dropouts"},{"location":"visualization/fmri-qc/#how-tsnr-and-std-are-computed","title":"How tSNR and std are computed","text":"<p>tSNR and std are computed using Welford's online algorithm. This processes the 4D volume one 3D frame at a time, keeping only one frame in memory at a time. Peak memory = 3 \u00d7 one 3D frame (running mean, running M2, current frame).</p> <p>For a 91\u00d7109\u00d791 MNI-space BOLD at float32: one frame is ~3.6 MB, so peak memory during tSNR computation is ~11 MB regardless of the number of timepoints.</p>"},{"location":"visualization/fmri-qc/#with-events-overlay","title":"With events overlay","text":"<p>Pass <code>events_path</code> to overlay trial onset markers on the global signal and framewise intensity plots:</p> <pre><code>result = fmri_summary(\n    \"sub-01_task-rest_bold.nii.gz\",\n    events_path=\"sub-01_task-rest_events.tsv\",\n)\nresult.show()\n</code></pre> <p>Onset times are converted to TR units and drawn as vertical lines color-coded by <code>trial_type</code>.</p>"},{"location":"visualization/fmri-qc/#with-confounds-motion-qc","title":"With confounds (motion QC)","text":"<p>Pass <code>confounds_path</code> to add a third row of panels showing head motion metrics:</p> <pre><code>result = fmri_summary(\n    \"sub-01_task-rest_bold.nii.gz\",\n    events_path=\"sub-01_task-rest_events.tsv\",\n    confounds_path=\"sub-01_task-rest_desc-confounds_timeseries.tsv\",\n)\nresult.show()\n</code></pre> <p>The confounds row shows three panels:</p> <ul> <li>Framewise displacement \u2014 head motion in mm per TR (from <code>framewise_displacement</code> column)</li> <li>DVARS \u2014 signal change magnitude per TR (from <code>dvars</code> column)</li> <li>Std DVARS \u2014 standardized DVARS (from <code>std_dvars</code> column)</li> </ul> <p>A horizontal reference line at FD = 0.5 mm marks the common motion censoring threshold.</p> <p>The confounds file must be an fMRIPrep-format TSV with a header row. Qortex reads only the three columns above and ignores all others.</p>"},{"location":"visualization/fmri-qc/#cli","title":"CLI","text":"<pre><code>qortex fmri-qc sub-01_task-rest_bold.nii.gz\nqortex fmri-qc sub-01_task-rest_bold.nii.gz \\\n    --events sub-01_task-rest_events.tsv \\\n    --confounds sub-01_task-rest_desc-confounds_timeseries.tsv \\\n    --output figures/sub-01_fmri_qc.html\n</code></pre>"},{"location":"visualization/fmri-qc/#individual-panel-methods","title":"Individual panel methods","text":"<p>If you need individual panels rather than the combined figure:</p> <pre><code>viewer = VolumeViewer(\"sub-01_task-rest_bold.nii.gz\", modality=\"fmri\")\n\nmean_fig   = viewer.mean_epi_figure()\nstd_fig    = viewer.std_epi_figure()\ntsnr_fig   = viewer.tsnr_figure()\ngs_fig     = viewer.global_signal_timeseries()\nframe_fig  = viewer.framewise_preview()\n</code></pre> <p>Each returns a matplotlib Figure.</p>"},{"location":"visualization/fmri-qc/#interpreting-the-panels","title":"Interpreting the panels","text":"<p>Mean EPI. Look for signal dropout in frontal or temporal regions (dark voids where there should be brain tissue). Common near air\u2013tissue interfaces.</p> <p>Temporal std. High variance at brain edges indicates motion. High variance in deep structures may indicate pulsatility artifacts.</p> <p>tSNR. Typical cortical tSNR for a 3T resting-state acquisition is 50\u2013200. Values below 30 in cortex suggest poor shimming, strong motion, or thermal noise issues.</p> <p>Global signal. A sudden step change in global signal (large jump at one TR) indicates a within-scanner event (e.g., the subject sneezed). Slow drift suggests scanner instability or physiological noise.</p> <p>Framewise displacement. Volumes with FD &gt; 0.5 mm are typically censored before analysis. If more than 20% of volumes exceed this threshold, consider excluding the subject.</p>"},{"location":"visualization/fmri-qc/#related","title":"Related","text":"<ul> <li>DWI QC \u2014 equivalent panels for diffusion data</li> <li>Overlays \u2014 add brain mask or ROI overlays to the mean EPI</li> </ul>"},{"location":"visualization/local-viewer/","title":"Local Viewer","text":"<p><code>visualize()</code> renders figures for all supported files in a local path. It walks the directory, classifies each file by intent, and produces one figure per asset.</p>"},{"location":"visualization/local-viewer/#python","title":"Python","text":"<pre><code>from qortex.visualize import visualize\n\n# Visualize all files under a subject directory\nresults = visualize(\"data/ds004130/sub-01/\")\n\nfor r in results:\n    print(r.asset.path, r.intent)\n    r.show()\n</code></pre> <p>Or visualize a single file:</p> <pre><code>from qortex.visualize import visualize\n\nresult = visualize(\"data/ds004130/sub-01/anat/sub-01_T1w.nii.gz\")\nresult.show()\n</code></pre>"},{"location":"visualization/local-viewer/#from-the-dataset-api","title":"From the Dataset API","text":"<pre><code>ds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nresults = ds.visualize(subject=\"01\")\nfor r in results:\n    r.to_png(f\"figures/{r.asset.path.stem}.png\")\n</code></pre>"},{"location":"visualization/local-viewer/#cli","title":"CLI","text":"<pre><code># Visualize one subject's data and open in browser\nqortex visualize ds004130 --subject 01 --data-dir data/ds004130/\n\n# Save all figures to a directory\nqortex visualize ds004130 --subject 01 --data-dir data/ds004130/ --output figures/\n</code></pre>"},{"location":"visualization/local-viewer/#what-each-intent-renders","title":"What each intent renders","text":"Intent Figure type Anatomical 3-panel ortho (axial, coronal, sagittal) at center slices BOLD Mean EPI + temporal std + tSNR + global signal plot DWI b=0 volume + high-b volume + gradient sphere PET Ortho panels with PET colormap CT Ortho panels with brain window preset Fieldmap Ortho panels with diverging colormap Mask Overlay on center slice of the nearest anatomical Labelmap Overlay with discrete colormap Stat map Overlay with RdBu_r colormap, thresholded Raw signal Butterfly plot (EEG/MEG) Unknown Single-slice thumbnail, no colormap processing"},{"location":"visualization/local-viewer/#surface-files-gifticifti","title":"Surface files (GIFTI/CIFTI)","text":"<p>Surface files are detected by extension (<code>.surf.gii</code>, <code>.func.gii</code>, <code>.dscalar.nii</code>, <code>.dtseries.nii</code>). The intent is <code>INTENT_SURFACE</code>. GIFTI files are inspected as mesh/scalar/label assets and rendered as surface QC summaries with vertices, faces, bounds, labels, array roles, and value ranges. CIFTI files are inspected as dense matrices and rendered as axis-aware matrix summaries with sampled data ranges.</p> <p>This is a QC viewer, not a replacement for Connectome Workbench. Advanced interactions such as full multi-view workbench-style surface scenes and volume-to-surface projection remain future work.</p>"},{"location":"visualization/local-viewer/#saving-output","title":"Saving output","text":"<pre><code>for r in results:\n    r.to_html(f\"figures/{r.asset.path.stem}.html\")\n    r.to_png(f\"figures/{r.asset.path.stem}.png\")\n</code></pre> <p>Interactive HTML (Plotly) is produced for BOLD and EEG; static PNG (matplotlib) for anatomical and DWI.</p>"},{"location":"visualization/local-viewer/#related","title":"Related","text":"<ul> <li>Visual pipeline \u2014 how intent classification works</li> <li>fMRI QC \u2014 more detailed BOLD QC panels</li> <li>Visual audit \u2014 coverage view across all subjects</li> </ul>"},{"location":"visualization/overlays/","title":"Overlays","text":"<p>Qortex can overlay masks, labelmaps, statistical maps, PET tracers, contours, and edge maps on anatomical or functional backgrounds.</p>"},{"location":"visualization/overlays/#available-overlay-functions","title":"Available overlay functions","text":"<pre><code>from qortex.visualize import (\n    overlay_mask,\n    overlay_labelmap,\n    overlay_stat,\n    overlay_pet,\n    overlay_contour,\n    overlay_edges,\n)\n</code></pre> <p>All overlay functions take an existing <code>VisualResult</code> as the first argument and return a new <code>VisualResult</code> with the overlay applied.</p>"},{"location":"visualization/overlays/#mask-overlay","title":"Mask overlay","text":"<pre><code>from qortex.visualize import visualize, overlay_mask\n\nbg = visualize(\"sub-01_T1w.nii.gz\")\nresult = overlay_mask(\n    bg,\n    mask_path=\"sub-01_brain_mask.nii.gz\",\n    color=\"red\",\n    alpha=0.4,\n)\nresult.show()\n</code></pre> <p>Parameters:</p> Parameter Default Description <code>color</code> <code>\"red\"</code> Color for mask voxels (any matplotlib color) <code>alpha</code> <code>0.4</code> Transparency (0 = invisible, 1 = opaque)"},{"location":"visualization/overlays/#labelmap-overlay","title":"Labelmap overlay","text":"<pre><code>result = overlay_labelmap(\n    bg,\n    labelmap_path=\"sub-01_dseg.nii.gz\",\n    lut=None,   # auto-assign colors\n    alpha=0.6,\n)\nresult.show()\n</code></pre> <p>Each integer label gets a distinct color. By default, colors are assigned from a fixed palette that matches the FreeSurfer colormap for common parcellation indices. Pass a custom <code>lut</code> dict (<code>{integer_label: color}</code>) to override.</p>"},{"location":"visualization/overlays/#statistical-map-overlay","title":"Statistical map overlay","text":"<pre><code>result = overlay_stat(\n    bg,\n    stat_path=\"contrast_zstat.nii.gz\",\n    threshold=2.3,          # hide values below this absolute value\n    colormap=\"RdBu_r\",      # diverging colormap\n    vmax=None,              # auto from 99th percentile\n)\nresult.show()\n</code></pre> <p>Values below <code>threshold</code> are rendered transparent. The colormap center (white) is at zero for diverging maps.</p>"},{"location":"visualization/overlays/#pet-overlay","title":"PET overlay","text":"<pre><code>result = overlay_pet(\n    bg,\n    pet_path=\"sub-01_trc-FDG_pet.nii.gz\",\n    colormap=\"hot\",\n    alpha=0.7,\n)\nresult.show()\n</code></pre> <p>The <code>hot</code> colormap (black \u2192 red \u2192 yellow \u2192 white) is standard for PET visualization.</p>"},{"location":"visualization/overlays/#contour-overlay","title":"Contour overlay","text":"<pre><code>result = overlay_contour(\n    bg,\n    mask_path=\"sub-01_brain_mask.nii.gz\",\n    color=\"cyan\",\n    linewidth=1,\n)\nresult.show()\n</code></pre> <p>A contour draws only the boundary of the mask rather than filling it, which keeps the background anatomy visible.</p>"},{"location":"visualization/overlays/#edge-overlay","title":"Edge overlay","text":"<pre><code>result = overlay_edges(\n    bg,\n    edge_path=\"sub-01_T2w.nii.gz\",\n    color=\"yellow\",\n    alpha=0.8,\n)\nresult.show()\n</code></pre> <p><code>overlay_edges</code> computes a Sobel edge map from the input volume and overlays it. Useful for checking T1/T2 registration alignment.</p>"},{"location":"visualization/overlays/#chaining-overlays","title":"Chaining overlays","text":"<p>Multiple overlays can be chained:</p> <pre><code>result = visualize(\"sub-01_T1w.nii.gz\")\nresult = overlay_mask(result, \"sub-01_brain_mask.nii.gz\", color=\"blue\", alpha=0.2)\nresult = overlay_contour(result, \"sub-01_wm_mask.nii.gz\", color=\"white\")\nresult = overlay_stat(result, \"contrast_z.nii.gz\", threshold=3.0)\nresult.show()\n</code></pre>"},{"location":"visualization/overlays/#segmentation-comparison","title":"Segmentation comparison","text":"<p>For side-by-side comparison of two labelmaps (e.g., automatic vs. manual segmentation), use <code>compare_masks()</code>:</p> <pre><code>from qortex.visualize import compare_masks\n\nresult = compare_masks(\n    \"sub-01_T1w.nii.gz\",\n    mask_a=\"auto_seg.nii.gz\",\n    mask_b=\"manual_seg.nii.gz\",\n    labels={\"a\": \"FreeSurfer\", \"b\": \"Manual\"},\n)\nresult.show()\n</code></pre> <p>See Segmentation comparison for details.</p>"},{"location":"visualization/overlays/#limitations","title":"Limitations","text":"<ul> <li>All overlay volumes must be in the same voxel space as the background image. Qortex does not resample.</li> <li>Overlay rendering uses matplotlib. For interactive HTML output, overlays are rasterized and embedded in the Plotly figure as a background image.</li> </ul>"},{"location":"visualization/segmentation-comparison/","title":"Segmentation Comparison","text":"<p><code>compare_masks()</code> renders a side-by-side or overlay comparison of two binary or integer-label masks on the same anatomical background. Use it to check agreement between manual and automated segmentations.</p>"},{"location":"visualization/segmentation-comparison/#python","title":"Python","text":"<pre><code>from qortex.visualize import compare_masks\n\nresult = compare_masks(\n    background=\"sub-01_T1w.nii.gz\",\n    mask_a=\"auto_seg.nii.gz\",\n    mask_b=\"manual_seg.nii.gz\",\n    labels={\"a\": \"FreeSurfer\", \"b\": \"Manual\"},\n    mode=\"side-by-side\",   # or \"overlay\"\n)\nresult.show()\n</code></pre>"},{"location":"visualization/segmentation-comparison/#modes","title":"Modes","text":"<p><code>side-by-side</code> (default): Two sets of ortho panels are placed next to each other. The left panel shows mask A on the background; the right shows mask B.</p> <p><code>overlay</code>: Both masks are shown on the same background. Voxels where masks agree are green, where only A is present are blue, and where only B is present are red.</p>"},{"location":"visualization/segmentation-comparison/#cli","title":"CLI","text":"<pre><code>qortex compare-masks sub-01_T1w.nii.gz \\\n    --mask-a auto_seg.nii.gz \\\n    --mask-b manual_seg.nii.gz \\\n    --labels FreeSurfer Manual \\\n    --mode overlay \\\n    --output seg_comparison.html\n</code></pre>"},{"location":"visualization/segmentation-comparison/#dice-coefficient","title":"Dice coefficient","text":"<p>The comparison result includes a Dice similarity coefficient:</p> <pre><code>print(result.dice)           # float\nprint(result.jaccard)        # float\nprint(result.n_agree)        # voxel count where masks agree\nprint(result.n_only_a)       # voxels in A only\nprint(result.n_only_b)       # voxels in B only\n</code></pre> <p>For labelmaps with multiple integer labels, Dice is computed per label and returned as a dict:</p> <pre><code>print(result.dice_per_label)  # {1: 0.92, 2: 0.87, 3: 0.79, ...}\n</code></pre>"},{"location":"visualization/segmentation-comparison/#requirements","title":"Requirements","text":"<p><code>compare_masks</code> requires the <code>mri</code> extra: <code>pip install \"qortex[mri]\"</code>.</p> <p>Both masks must be in the same voxel space as the background. Qortex does not resample.</p>"},{"location":"visualization/segmentation-comparison/#related","title":"Related","text":"<ul> <li>Overlays \u2014 general overlay functions</li> <li>Visual audit \u2014 coverage-level inspection across all subjects</li> </ul>"},{"location":"visualization/visual-audit/","title":"Visual Audit","text":"<p><code>VisualAuditReport</code> is a coverage check across all subjects and file types in a dataset. It answers which files are present, which are missing, and how many of each type exist.</p>"},{"location":"visualization/visual-audit/#running-a-visual-audit","title":"Running a visual audit","text":"<pre><code>from qortex import Dataset\n\nds = Dataset(\"ds004130\", data_dir=\"data/ds004130/\")\nreport = ds.visual_audit()\nreport.show()  # opens browser with HTML report\n</code></pre> <p>Or directly:</p> <pre><code>from qortex.visualize._audit import run_visual_audit\n\nreport = run_visual_audit(\"data/ds004130/\")\nreport.show()\n</code></pre>"},{"location":"visualization/visual-audit/#from-the-cli","title":"From the CLI","text":"<pre><code>qortex visual-audit ds004130 --data-dir data/ds004130/ --output audit.html\n</code></pre> <p>Without <code>--data-dir</code>, runs in manifest-only mode (coverage matrix only, no thumbnails).</p>"},{"location":"visualization/visual-audit/#report-contents","title":"Report contents","text":""},{"location":"visualization/visual-audit/#coverage-matrix","title":"Coverage matrix","text":"<p>A table with rows = subjects, columns = file suffixes (T1w, bold, events, channels, etc.). Each cell is green (present), red (missing), or gray (not applicable).</p> <p>Access as a dict:</p> <pre><code>matrix = report.coverage_matrix()\n# {subject_id: {suffix: bool}}\nprint(matrix[\"sub-01\"][\"bold\"])   # True\nprint(matrix[\"sub-03\"][\"events\"]) # False\n</code></pre>"},{"location":"visualization/visual-audit/#per-suffix-counts","title":"Per-suffix counts","text":"<pre><code>for suffix, count in report.per_suffix_counts.items():\n    print(f\"{suffix}: {count}\")\n# bold: 88\n# events: 86\n# T1w: 88\n# channels: 88\n</code></pre>"},{"location":"visualization/visual-audit/#warning-summary","title":"Warning summary","text":"<pre><code>for warning in report.warning_summary():\n    print(warning)\n# MISSING: sub-23_task-rest_events.tsv (1 subject affected)\n# MISSING: sub-47_task-rest_events.tsv (1 subject affected)\n</code></pre>"},{"location":"visualization/visual-audit/#action-items","title":"Action items","text":"<p><code>action_items()</code> returns the highest-priority issues, sorted by number of subjects affected:</p> <pre><code>for item in report.action_items():\n    print(item.severity, item.message)\n</code></pre>"},{"location":"visualization/visual-audit/#missing-expected-files","title":"Missing expected files","text":"<p><code>missing_expected_files()</code> returns files that exist for most subjects but not all:</p> <pre><code>for path in report.missing_expected_files(threshold=0.9):\n    # path is missing for &gt; 10% of subjects\n    print(path)\n</code></pre>"},{"location":"visualization/visual-audit/#manifest-mode-no-download-required","title":"Manifest mode (no download required)","text":"<pre><code>ds = Dataset(\"ds004130\")\nreport = ds.visual_audit(mode=\"manifest\")\nreport.show()\n</code></pre> <p>In manifest mode, coverage is based on the remote manifest. No files are read from disk. Thumbnails are not included.</p>"},{"location":"visualization/visual-audit/#exporting-the-report","title":"Exporting the report","text":"<pre><code>report.to_html(\"audit.html\")     # full HTML with coverage matrix\nreport.to_json(\"audit.json\")     # machine-readable JSON\nreport.to_markdown(\"audit.md\")   # markdown table\n</code></pre> <p>The JSON export includes <code>visual_manifest_json()</code> \u2014 a nested dict of all files with their paths, sizes, entities, and coverage status.</p>"},{"location":"visualization/visual-audit/#interpreting-the-coverage-matrix","title":"Interpreting the coverage matrix","text":"<p>A missing file (red cell) is not always a problem. In datasets where some subjects did not complete all tasks, gaps in the coverage matrix are expected. Use <code>missing_expected_files(threshold=0.8)</code> to focus on files that are present for at least 80% of subjects but absent for some \u2014 those are the unexpected gaps.</p>"},{"location":"visualization/visual-audit/#related","title":"Related","text":"<ul> <li>First visual audit \u2014 quickstart walkthrough</li> <li>fMRI QC \u2014 per-file quality metrics beyond coverage</li> </ul>"},{"location":"visualization/visual-pipeline/","title":"Visual Pipeline","text":"<p>Qortex visualization is organized around three data classes: <code>VisualAsset</code>, <code>VisualPlan</code>, and <code>VisualResult</code>. Understanding their relationships explains how rendering is dispatched.</p>"},{"location":"visualization/visual-pipeline/#visualasset","title":"VisualAsset","text":"<p>A <code>VisualAsset</code> represents one file to be visualized. It combines a file path with an intent classification that tells the renderer what kind of figure to produce.</p> <pre><code>from qortex.visualize._asset import VisualAsset, INTENT_BOLD\n\nasset = VisualAsset(\n    path=Path(\"sub-01/func/sub-01_task-rest_bold.nii.gz\"),\n    intent=INTENT_BOLD,\n    subject=\"01\",\n    task=\"rest\",\n    metadata={\"TR\": 2.0},\n)\n</code></pre>"},{"location":"visualization/visual-pipeline/#intent-constants","title":"Intent constants","text":"Constant Meaning <code>INTENT_ANATOMICAL</code> Structural MRI (T1w, T2w) <code>INTENT_BOLD</code> fMRI BOLD series <code>INTENT_DWI</code> Diffusion-weighted imaging <code>INTENT_PET</code> PET tracer image <code>INTENT_CT</code> CT volume <code>INTENT_FIELDMAP</code> B0 or magnitude fieldmap <code>INTENT_MASK</code> Binary mask <code>INTENT_LABELMAP</code> Integer-valued segmentation label map <code>INTENT_STAT_MAP</code> Statistical map (z-score, t-stat) <code>INTENT_SURFACE</code> Surface geometry (GIFTI/CIFTI) <code>INTENT_SERIES_BROWSER</code> DICOM series <code>INTENT_RAW_SIGNAL</code> EEG/MEG time series <code>INTENT_UNKNOWN</code> Unrecognized file type <p>Intent is assigned automatically by <code>qortex.visualize.inspect()</code> based on suffix, extension, and sidecar JSON.</p>"},{"location":"visualization/visual-pipeline/#visualplan","title":"VisualPlan","text":"<p>A <code>VisualPlan</code> is a list of <code>VisualAsset</code> objects grouped by intent. The plan is produced by the <code>inspect()</code> function in <code>qortex.visualize</code>:</p> <pre><code>from qortex.visualize import inspect\n\nplan = inspect(\"data/ds004130/sub-01/\")\nfor asset in plan.assets:\n    print(asset.path, asset.intent)\n</code></pre> <p>The plan does not render anything \u2014 it only decides what to render and in what order.</p>"},{"location":"visualization/visual-pipeline/#visualresult","title":"VisualResult","text":"<p>A <code>VisualResult</code> wraps one rendered output. It has:</p> <pre><code>result.asset         # the VisualAsset that produced this result\nresult.figure        # matplotlib Figure or plotly Figure\nresult.intent        # same as asset.intent\nresult.warnings      # list of strings\nresult.show()        # display inline (Jupyter) or open browser\nresult.to_html(path) # save as interactive HTML (plotly)\nresult.to_png(path)  # save as PNG (matplotlib)\nresult.to_provenance_dict()  # dict with asset, params, qortex version\n</code></pre>"},{"location":"visualization/visual-pipeline/#high-level-entry-points","title":"High-level entry points","text":"<p>Most users do not interact with these classes directly. The high-level functions in <code>qortex.visualize</code> handle the full pipeline:</p> <pre><code>from qortex.visualize import visualize, fmri_summary, dwi_summary\n\n# Visualize all files in a directory\nresults = visualize(\"data/ds004130/sub-01/\")\n\n# Visualize a specific BOLD file\nresult = fmri_summary(\"sub-01_task-rest_bold.nii.gz\")\n\n# Visualize DWI\nresult = dwi_summary(\"sub-01_dwi.nii.gz\", bval_path=\"sub-01_dwi.bval\", bvec_path=\"sub-01_dwi.bvec\")\n</code></pre>"},{"location":"visualization/visual-pipeline/#adding-overlays","title":"Adding overlays","text":"<p>Overlays are attached to an existing <code>VisualResult</code> through the overlay functions:</p> <pre><code>from qortex.visualize import overlay_mask\n\nresult = visualize(\"sub-01_T1w.nii.gz\")\nresult = overlay_mask(result, mask_path=\"sub-01_brain_mask.nii.gz\", alpha=0.4, color=\"red\")\nresult.show()\n</code></pre>"},{"location":"visualization/visualize-openneuro/","title":"Visualize OpenNeuro","text":"<p>The <code>visualize-openneuro</code> CLI command renders center-slice thumbnails for an OpenNeuro dataset without downloading full files. It fetches only the bytes needed to render one slice from each NIfTI.</p>"},{"location":"visualization/visualize-openneuro/#how-it-works","title":"How it works","text":"<p>NIfTI files on the OpenNeuro CDN support HTTP range requests. Qortex reads the NIfTI header (first 352 bytes) to get voxel dimensions and data offset, then fetches only the bytes corresponding to the center axial slice. For a 256\u00d7256\u00d7176 T1w at float32, that is roughly 256 KB per slice instead of 30 MB for the full volume.</p> <p>For EEG and other non-NIfTI formats, full-file download is still required because the formats do not support range reads. The command skips non-NIfTI files unless <code>--include-all</code> is passed.</p>"},{"location":"visualization/visualize-openneuro/#cli","title":"CLI","text":"<pre><code>qortex visualize-openneuro ds004130\nqortex visualize-openneuro ds004130 --subjects 01 02 --suffixes T1w bold\nqortex visualize-openneuro ds004130 --output figures/ --format png\n</code></pre> <p>Options:</p> Option Default Description <code>--subjects</code> all Subjects to include <code>--suffixes</code> T1w, bold, dwi File suffixes to render <code>--snapshot</code> latest Snapshot version <code>--output</code> . Directory for output files <code>--format</code> html <code>html</code> or <code>png</code> <code>--include-all</code> False Include non-NIfTI files (downloads full file)"},{"location":"visualization/visualize-openneuro/#python","title":"Python","text":"<pre><code>from qortex.visualize import visualize\n\nresults = visualize(\"openneuro://ds004130/sub-01/anat/sub-01_T1w.nii.gz\")\nresults[0].show()\n</code></pre> <p>The <code>openneuro://</code> URI scheme triggers remote rendering. Pass a manifest FileRecord instead to avoid constructing the URI manually:</p> <pre><code>from qortex import Dataset\nfrom qortex.visualize import visualize\n\nds = Dataset(\"ds004130\")\nfiles = ds.files(subjects=[\"01\"], suffixes=[\"T1w\"])\nresults = [visualize(f) for f in files]\n</code></pre>"},{"location":"visualization/visualize-openneuro/#performance","title":"Performance","text":"<p>A center-slice thumbnail fetch takes 1\u20133 seconds per file on a typical broadband connection. The limiting factor is CDN latency, not bandwidth. Batch requests are parallelized up to 8 concurrent fetches.</p>"},{"location":"visualization/visualize-openneuro/#limitations","title":"Limitations","text":"<ul> <li>Range reads only work for NIfTI. DICOM, EEG .set/.fif, and CIFTI files require full downloads.</li> <li>The NIfTI must have a valid header at byte 0. Some older neuroimaging software writes malformed NIfTI headers. These files fail with <code>InvalidNIfTIHeaderError</code> and are skipped.</li> <li>Range reads bypass Qortex's local cache. Each call fetches from CDN. If you plan to inspect many slices repeatedly, download the files first.</li> </ul>"}]}